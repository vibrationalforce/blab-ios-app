build: 2026-07-14 v10.79.208 (Umbau Schritt 2a: Transpose gelöscht — Aufräumen vor dem sichtbaren Header-Umzug)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Weiter auf deine Ansage (Leiste auflösen). Schritt 2a: Transpose ist jetzt
  KOMPLETT gelöscht — wie in deiner Architektur festgelegt.

  WARUM sicher: Transpose war schon aus der Chip-Leiste raus und komplett
  unerreichbar (kein Chip, kein Weg es zu öffnen), also stand der Wert IMMER auf 0.
  Das Löschen ändert deshalb NICHTS am Klang oder an der Farbe des Visuals
  (currentToneHz bleibt bit-genau gleich — von beiden Reviewern bestätigt). Es
  räumt nur toten Code weg und bereitet den Composition-Bereich für den nächsten,
  SICHTBAREN Schritt vor.

  NÄCHSTER SCHRITT (2b — DAS siehst du dann): Tonart/Skala + Stimmung + Tempo
  wandern hoch in eine kompakte Kopfzeile, der Chip "Comp" verschwindet unten.
  BodyTempoField (der lebende Puls-Tempo-Wert) ist schon ein eigenes Leaf → der
  Umzug nach oben bleibt freeze-sicher (der 10-Hz-Read bleibt in sich, der Header
  churnt nicht). Danach 2c: Session-Name/Ort hoch, Chip "Session" weg.

  So löst sich die Leiste Chip für Chip auf und findet sich oben/pro-Instrument
  wieder — kontrolliert, ohne den Start zu riskieren.

  Reviewer: code PASS (alle 3 Menü-Switches exhaustiv, verhaltensgleich, Klammer-
  Balance 741/741) + ui-state PASS (keine erreichbare Regression, Visual-Farbe
  unverändert, Sheet-Kette unberührt, 8 Chips sauber). Gates: Xcode + CI/CD.

  Stand Umbau: Schritt 1 (Headroom, v207) ✓ · Schritt 2a (Transpose weg, v208) ✓ ·
  als Nächstes 2b Header-up (sichtbar).
