build: 2026-06-21 v10.34.34 (MIDI-in live · plugin effect chain · MIDI thru)
branch: claude/piano-roll-clip-view-wozlie
includes: Ralph batch — three finished tasks. (1) External MIDI input now works at all (it was never started) AND plays a hosted AUv3 plugin: connect a MIDI keyboard and it drives the built-in voice and any loaded plugin; "use plugin instead" gates the built-in off for external MIDI too. (2) Multiple insert effects per channel — tap effects to build an ordered chain on the hosted instrument (instrument → fx → fx → master), each with its own Open/Remove; the chain re-links cleanly when you remove one. (3) MIDI in→out thru: route MIDI In → MIDI Out in the patchbay and your controller echoes straight to your DAW while also playing Echoel. Held for a dedicated device-tested cycle: master-bus FX (surgery on the master output path). Deferred (large): RTMP/SRT broadcast + video. Verified: Xcode Compile Check + CI green.
