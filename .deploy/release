build: 2026-07-15 v10.79.227 (Tempo wandert nach oben in die Transport-Leiste neben Play)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Antwort auf dein Feedback ("Das soll da oben hin") + deine Wahl "beide behalten":
  die eigene, breite Tempo-Zeile ist WEG — das Tempo sitzt jetzt kompakt OBEN in der
  Transport-Leiste, direkt neben Play. Der Puls-Monitor bleibt wie gewünscht in der
  Marken-Kopfzeile (deshalb "beide": Puls oben-links, Tempo in der Transport-Zeile).

  WAS neu:
  · Kompaktes Tempo-Feld (weiter 4 Nachkommastellen + Schloss, nur ohne die Wörter
    „Tempo"/„BPM", damit es neben Play passt) — folgt unverriegelt dem Puls, verriegelt
    exakt editierbar wie bisher.
  · Die vier Chrome-Türen (Master · Export · Live · Learn) sind in EIN „•••"-Menü in der
    Transport-Leiste zusammengefasst — so wird der Platz fürs Tempo frei, OHNE die
    Marken-Kopfzeile anzufassen. Alle vier Türen bleiben erreichbar (voll umkehrbar).
  · Die separate Tempo-Zeile entfällt → mehr vertikaler Platz für die Timeline.

  GERÄTE-CHECK NÖTIG (bitte Screenshot): auf sehr schmalen iPhones (SE/mini) könnte die
  Transport-Zeile eng werden — sag mir, ob alles sauber passt oder ob ich Tempo/Türen
  noch kompakter machen soll. Falls du das „•••"-Menü NICHT magst, stelle ich die vier
  Türen mit einem Wort zurück.

  Reviewer ui-state CONFIRMED: Freeze-Regel gewahrt (Tempo-Feld ist ein eigenes Leaf,
  liest den 10-Hz-Puls nur in SEINEM Body — die Transport-Leiste/Menüs bleiben ruhig),
  keine Sheet-Ketten-Änderung (natives Menu), Environment erreicht das Feld. Ein vom
  Reviewer gefundener Kompilier-Fehler (Argument-Reihenfolge boxWidth/onCommit) wurde
  VOR dem Deploy behoben.

  Reihenfolge deiner heutigen Rückmeldungen: v225 Spur-Kopf-Fit · v226 Split/Join am
  Clip · v227 Tempo hoch. Noch offen: Sample/MIDI per Lang-Druck einfügen · Mehrfach-
  auswahl „Combine".
