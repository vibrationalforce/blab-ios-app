build: 2026-06-21 v10.34.24 (Patchbay — see and connect every channel)
branch: claude/piano-roll-clip-view-wozlie
includes: The universal router is now visible and controllable. A new Routing patchbay (Tools ▸ Routing) lists each source — your body (bio) and the music — with every destination it can reach: MIDI/MPE in & out, OSC, ADM-OSC (spatial), Art-Net & sACN (light), audio. Tap to connect; compatible types link directly or through a converter (pitch→colour, bio→MIDI CC). "Smart patch" wires every sensible connection at once, and each endpoint shows an honest live/soon status so nothing is faked. Connections persist. This sits on the typed Signal Router foundation and the first live adapter (music & body already drive light + spatial). Builds on the workstation home (Arrange · Clips · Compose) and the live Music→Colour visual. Routing edges are opt-in. Verified: Xcode Compile Check + CI green.
