build: 2026-07-02 v10.79.18 (Audit P0.2 — DrumSynth Cross-Thread-Race behoben)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notesT: Stabiler Code (Audit-Fund P0.2, HIGH). DrumSynthVoice hat bisher den Modal-Bank
  auf dem MAIN-Thread angeschlagen/konfiguriert, während der Audio-Thread dieselben
  Mode-Arrays las/schrieb — eine echte Cross-Thread-Race (gleiche Klasse wie die
  absichtlich abgeschaltete DDSP-Convolution-Race). Fix: fire()/configure() melden jetzt
  nur noch lock-frei einen Request (atomarer Zähler + Skalar-Felder, das SamplerVoice-
  Muster); der Audio-Thread wendet Anschlag UND Konfiguration in render() an — beide
  Modal-Bank-Operationen sind in-place/alloc-frei, laufen jetzt nur noch auf dem Audio-
  Thread und können den Render nicht mehr zerreißen. Kein hörbarer „glitchy Hit" mehr
  bei dichten Synth-Drum-Stellen / Live-Pad-Edits.
  TEST 10.79.18: Synth-Drums / Beat-Genres (DubTechno/Trap) — saubere, konsistente
  Schläge auch bei dichten Patterns? (Betrifft nur Synth-Pads, nicht die Melodie.)
