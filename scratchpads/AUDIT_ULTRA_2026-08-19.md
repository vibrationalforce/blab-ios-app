# ULTRA-AUDIT 2026-08-19 — Ergebnisse (Founder: ultraechoel Audit des gesamten Projekts)

Workflow `ultraechoel-audit` (24 Agenten: 3 MIDI-Worker + Lead, 8 Domain-Finder, 12 adversariale
Verifikationen — 0 widerlegt). Jede Zeile ist am zitierten Ort RE-GEMESSEN; Verifizierer waren auf
refuted=true als Default geprompted. Schwester-Dokument: Ultraaccessible-Funde = Zeilen 18-20 + Lows
in PLAN_GUI_UX_A11Y_GUIDE.md.

## A — MIDI-EXPORT (geht nicht), gerankte Ursachen

### CRITICAL — RANK 1 — Enabled tile, tap does literally nothing: empty roll with hasComposed=true (opened take whose notes are empty)
- Ort: `Sources/Echoelmusic/Studio/EchoelStudioView.swift:10174`
- Beleg: VERIFIED against source. exportMIDI() guards `!arrangedNotes.isEmpty` and returns with only a breadcrumb (:10174-10181; breadcrumb 'MIDI export skipped: no notes in the roll' at :10179 — the guard's own comment says 'Silent return = a control that looks like it worked'). The tile enables on `hasComposed` alone (:2113 `enabled: !isExporting && hasComposed`, :2118 `.disabled(isExporting || !hasComposed)`). `open(_:)` calls `pianoRoll.load(p.notes)` at :10411 and sets `hasComposed = true` UNCONDITIONALLY at :10492. `Project.notes` can decode empty: Core/Project.swift:403-404 (`decodeIfPresent([LossyDecoded<Note>].self) ?? []` then `compactMap(\.value)`). `hasComposed` is never written false after init — only writers are :511 (decl, false), :9580 (generate), :10492 (open), :10526 (importMIDI, itself unreachable: `midiImportPresented` :735 has no true-setter, confirmed by the code's own note at :10531). Self-propagation confirmed: the Save button is `.disabled(!hasComposed)` (:2130) and `currentProject()` saves `notes: pianoRoll.notes` (:10235), so an empty take can be saved under a real name and re-opened. The repo documents this exact trap at :10262-10267 for the autosave path (whose guard :10288 adds `!pianoRoll.notes.isEmpty` — the export tile never got the same second condition). REJECTED sub-claim from worker 1: 'the composer can return all-empty bars' — no evidence found that BioComposer.compose returns zero notes; unproven, do not act on it.
- Bedeutung: This is the only surviving failure mode where an enabled-looking control produces zero visible effect — the literal reading of 'Midi Export geht nicht'. Diag-log confirmation: the line 'MIDI export skipped: no notes in the roll' (written by :10179) in echoel_diag.log names this branch uniquely. On device it presents as: tap the bright pianokeys tile, nothing happens at all, forever, on that opened take.
- Fix: Ralph-sized: add the second guard condition to the tile's predicate — `.disabled(isExporting || !hasComposed || pianoRoll.notes.isEmpty)` and the matching `enabled:` (mirror of autosaveTake's two-condition guard at :10288). `pianoRoll.notes` is event-rate here (only changes on load/generate), but if the freeze law is a concern, the cheaper equivalent is `hasComposed = !p.notes.isEmpty` at :10492 in open(). No new presentation modifier needed.

### CRITICAL — RANK 2 — After relaunch the tile is disabled and eats the tap: hasComposed is @State, nothing auto-restores the autosave
- Ort: `Sources/Echoelmusic/Studio/EchoelStudioView.swift:511`
- Beleg: VERIFIED. `@State private var hasComposed = false` (:511) — false on every launch. The autosave slot exists (autosaveTake :10287-10296, fixed `Project.autosaveSlotID` Core/Project.swift:82) but the only reader is the Open sheet's list split (:8359-8360) — grep confirms no launch-time open of the slot. Until the founder goes through Open, the pianokeys tile renders dim (:2113) and `.disabled` (:2118) swallows the tap with no feedback. The first-run explainer at :1986-1993 renders only while `!hasComposed` and is the sole on-screen hint. Second leg verified: `isExporting = exporter.status == .capturing || .rendering` (:8283-8285) and `exporter.finishAttempt()` runs only after `await exporter.exportWav(...)` returns (:10079-10090), so a hung WAV export pins the MIDI tile disabled for the session — mechanism confirmed, actual hang not demonstrated.
- Bedeutung: For a founder who composed yesterday, relaunched (build 2523→2524 update forces a relaunch), and tapped the piano keys, this is the most probable literal reproduction. Diag-log signature: NO 'MIDI export' line of any kind (skipped/success/failed) around the tap timestamp — the tap never reached exportMIDI(). Distinguish from Rank 1 by the absence of the 'skipped' breadcrumb.
- Fix: Ralph-sized: make the disabled reason visible — the busyStatusLabel pattern already exists for the keep-last button (:10813-10822); give the MIDI tile the same accessibility/label treatment, or breadcrumb the swallowed state. Larger (separate slice): auto-open the autosave slot at launch, setting hasComposed only when its notes are non-empty.

### WARN — RANK 3 — Export 'succeeds' but delivers 1 bar of an 8-bar take after open: Project persists only the sounding bar and load() clears the arrangement
- Ort: `Sources/Echoelmusic/Studio/EchoelStudioView.swift:10411`
- Beleg: VERIFIED end to end. `currentProject()` saves `notes: pianoRoll.notes` — the single sounding bar (:10235). `open(_:)` calls `pianoRoll.load(p.notes)` (:10411); `load()` sets `arrangementBars = []` (PianoRollView.swift:615-618). `arrangementForExport()` then takes the fallback `guard arrangementBars.count > 1 else { return (notes, 1) }` (PianoRollView.swift:326-327). The raw bars ARE restored into `lastRawTake = p.rebakeSource` (:10435) but reach the roll only via a Mix-fader move → rebalanceTake → `pianoRoll.rebakeArrangement` (:7660); the code's own comment at :7646-7647 says 'Persisting the raw bars into Project is the real answer and is its own task'. Additionally the loaded single bar is the IntroAttenuation-softened bar 0 (PianoRollView.swift loadArrangement: `notes = IntroAttenuation.apply(bars[0], isFirstBar: true)`). Fresh-compose path is complete: generate() → loadArrangement (:9472) fills arrangementBars.
- Bedeutung: A DAW-bound founder opening a saved 8-bar take gets a 1-bar clip with a softened lead — plausibly phrased as 'Export geht nicht' even though a share sheet appeared. Diag-log signature: 'MIDI export: N bytes, M notes, 1 bars' (:10199) — the '1 bars' on a take the founder knows is 8 bars names this mode uniquely.
- Fix: Ralph-sized: in open(), when `p.rebakeSource` yields usable multi-bar rawTake bars, install them via `pianoRoll.rebakeArrangement(mixGlued bars, playing: false)` — the exact machinery rebalanceTake already uses at :7653-7660, invoked once at open instead of waiting for a fader move. Alternatively extend arrangementForExport to fall back to lastRawTake.

### WARN — RANK 4 — Share sheet silently dropped when set during another sheet's dismissal; self-heals on second tap
- Ort: `Sources/Echoelmusic/Studio/EchoelStudioView.swift:1437`
- Beleg: VERIFIED code shape (the race itself is UIKit behavior, not measurable statically). `.sheet(item: $share)` at :1437 sits in a 14-modifier chain (:1436-1474+: showOpen :1436, share :1437, diagnostics :1438, showAllFX :1439, showInput :1463, showRouting :1464, showLearn :1465, fileImporter :1467, fullScreenCover showVisual :1475...). The plausible window: tap a row in the Open sheet (open() runs, showOpen dismissal animates) → tap pianokeys within the animation → UIKit refuses the new presentation, `share` stays non-nil, nothing appears. Recovery confirmed: `ExportedFile.id = UUID()` per construction (:10544-10547), so a second tap creates a new identity and re-presents. Three writers land in the one `share` slot (:3216 onShare, :10082/:10096 WAV paths, :10193 MIDI) — the comment at :2116-2117 documents the shared-slot hazard.
- Bedeutung: Matches an intermittent one-tap 'geht nicht' that a retap would have fixed. Diag-log signature: 'MIDI export: N bytes, M notes, K bars' present with plausible counts (and K matching the take) while the founder saw nothing — success in the log plus no sheet on screen is this mode.
- Fix: None urgent (fresh UUID self-heals on retap). Ask the founder: did a second tap work? Hardening, if wanted later: consolidate the chain into the single .sheet(item:) enum the black-screen law already prescribes — not a Ralph slice.

### LOW — EXONERATED — the byte-level exporter is clean for the live call shape; NaN-tempo trap is latent but UNREACHABLE from this button
- Ort: `Sources/Echoelmusic/Sequencer/MIDIFileExporter.swift:151`
- Beleg: VERIFIED: header ntrks=3 matches 3 appended MTrk chunks (:177-186); serializeTrack sorts before delta computation with off-before-on tie-break (:381), melodyEvents clamps onTick >= 0 (:283) and pitch/velocity to legal ranges (:285-286), EOT anchors via `vlq(Swift.max(0, endTick - lastTick))` (:391-392); keySignatureMeta table (:248-257) hand-checked correct for all 12 pitch classes and the relative-major minor mapping. REJECTED as a root-cause candidate: worker 2's NaN trap at tempoMeta (:227 `Swift.min(Swift.max(tempo, 1), 1000)` does pass NaN, and :233 `UInt32(NaN)` would trap) is real code but unreachable from the live call: the input is `beatPlayer.pattern.tempo` (:10184), which is `private(set)` (PatternEngine.swift:68) and every writer stores a NaN-safe clamped value — setTempo clamps via `clamped(to:)` at PatternEngine.swift:297 before `tempo = clamped` (:306), and the glide paths (:341/:348/:351, :374-381) only store/ease between already-clamped values. No NaN producer exists for this argument.
- Bedeutung: Stops the wrong fix: a session that 'hardens the exporter' fixes a layer that is not failing. The tempoMeta NaN-safe clamp is worth one line of hygiene someday (it matches the CLAUDE.md max/min argument-order law and Note.swift:143-146 guards the same class for velocity), but it cannot explain the founder's report.
- Fix: Optional hygiene, not this cycle: `let clampedTempo = tempo.clamped(to: 1...1000)` in tempoMeta (Core/FloatingPointClamp.swift helper). Do not touch the exporter for this bug report.

### LOW — RULED OUT — lane/timeline state gap and drums-empty; plus a stale tiling comment at the call site worth one line of cleanup
- Ort: `Sources/Echoelmusic/Studio/EchoelStudioView.swift:10150`
- Beleg: VERIFIED worker 3's exclusions: `TimelineRegionPlayer.isPlaying = true` has exactly one writer (:293 inside play(document:)), and `git grep '\.play(document:' -- Sources` returns only the prose note at PatternEngine.swift:409 ('lost its last production caller') — WorkspaceView's transport calls `player.pattern.play(cause: .transportButton)` (:941). Nothing can play lanes, so the audible melody is 100% the primary roll on the fresh-compose path; arrangementForExport covers everything heard. Drums track empty by design: exportCombined called with `steps: [], accents: []` (:10183), justified at length :10155-10173 — matches the founder's 'keine Drums' decision, not a defect. CONFIRMED stale comment: the exportMIDI doc block still claims 'the 1-bar drum grid is tiled to the same N bars' (:10148-10153) directly contradicting the call ten lines down — the class of self-contradicting prose the repo's own history (#166/#167 corrections) warns about.
- Bedeutung: Closes the enumeration. The single diag pull disambiguates ALL surviving modes: 'MIDI export skipped: no notes in the roll' = Rank 1; NO midi line at all = Rank 2 (tile disabled / tap never arrived); '... 1 bars' on a known multi-bar take = Rank 3; success line with plausible counts but no sheet seen = Rank 4; 'MIDI export failed: ...' = disk write error (rare; temporaryDirectory + sanitized filename). Request the founder's echoel_diag.log BEFORE any code change — the three breadcrumbs at :10179/:10199/:10201 were installed precisely to answer this report.
- Fix: Delete the tiling sentence in the :10148-10153 doc block (one-line docs fix, can ride along with the Rank-1 slice). Separately note worker 2's verified test gaps for a future cycle: no test asserts key-signature sf/mi BYTES (only presence via containsMeta(0x59)), no EOT-tick assertion, no shuffled-input sort test — and all three MIDI export suites live in Tests/EchoelmusicTests, which no blocking gate runs (#208); the only blocking MIDI guard is Tests/CISmoke/RequestedDrumVelocityIsTheEmittedByteTests.swift.

## B — Projekt-Audit, 12 bestätigte Funde (alle adversarial verifiziert)

### B1 · CRITICAL — RetroCapture route-change reinstall relabels 30 s of old-rate ring history — pre-roll capture spanning a hardware-rate switch is written pitch-shifted
- Ort: `Sources/Echoelmusic/Audio/RetroCapture.swift:132`
- Beleg: install(on:) sets `captureSampleRate = format.sampleRate` (RetroCapture.swift:132) but never resets `ringWriteFrame` nor clears `ring` — the reinstall body (lines 122–188) only re-captures pointers and re-installs the tap. The configuration-change watchdog re-installs on every route change precisely because the hardware rate moves (AudioEngine.swift:653–660: "the rPPG camera activating mid-session drops it to 44.1 kHz. Re-install the RetroCapture tap … otherwise a retroactive capture/export would be pitch-shifted ('viel höher')"). But the ring still holds up to 30 s of frames captured at the OLD rate, and startRecording writes the ENTIRE pre-roll window at the CURRENT rate: the file format is built from `captureSampleRate` (RetroCapture.swift:241) and writePreRollToFile's frame math uses it for the whole window (RetroCapture.swift:295). `git grep` for any acknowledgment of mixed-rate ring content returns nothing — the gap is undocumented.
- Fix: In install(on:), when the newly read `format.sampleRate` differs from the previous `captureSampleRate`, reset `ringWriteFrame.pointee = 0` and zero the ring (or record a rate-boundary frame index and truncate the pre-roll window to post-switch frames). Trading wrong-rate history for correct audio matches the file's own "a control may be absent but may not lie" rule. Update the AudioEngine.swift:653 watchdog comment in the same commit — it currently over-claims.
- Verifikation: Confirmed by re-measurement. (1) RetroCapture.install(on:) (Sources/Echoelmusic/Audio/RetroCapture.swift:122-188) sets captureSampleRate from the new tap format at :132 but never resets ringWriteFrame nor zeroes ring — grep shows ring is initialized only in init (:91) and ringWriteFrame written only at init (:94) and by the tap's continuous advance (:155). (2) The config-change watchdog re-installs the tap exactly when the hardware rate switches (AudioEngine.swift:653-660, naming the rPPG-camera 48→44.1 kHz drop; the #612 comment at :666-672 independently confirms real 44.1↔48 switches). (3) A

### B2 · WARN — AudioOutputGuard coverage invariant says "All seven" render blocks are wired — six exist; the count survived the #167 deletion its own text records
- Ort: `Sources/Echoelmusic/DSP/AudioOutputGuard.swift:38`
- Beleg: AudioOutputGuard.swift:38: "All seven of the app's `AVAudioSourceNode` render blocks are wired." Measured: `git grep -n AVAudioSourceNodeRenderBlock -- Sources` returns exactly SIX sites — MetronomeVoice.swift:142, SessionEngine.swift:272, SamplerVoice.swift:192, BioReactiveSynthVoice.swift:562, PolySynthVoice.swift:980, SubBassVoice.swift:335. The seventh was DrumSynthVoice, whose deletion the SAME doc records parenthetically at lines 47–48 ("⛔ `DrumSynthVoice` shared this form and was deleted with #167") — the headline count was never updated.
- Fix: Change "All seven" to "All six" (or drop the count per the repo's own AutoMixChain precedent: "a number here would only go stale again" — AutoMixChain.swift:24-25) in the same commit as any future render-block change.
- Verifikation: Confirmed by independent re-measurement. AudioOutputGuard.swift:38 states "All seven of the app's AVAudioSourceNode render blocks are wired," but `git grep -n AVAudioSourceNodeRenderBlock -- Sources` returns exactly six sites (MetronomeVoice.swift:142, SessionEngine.swift:272, SamplerVoice.swift:192, BioReactiveSynthVoice.swift:562, PolySynthVoice.swift:980, SubBassVoice.swift:335), and a cross-check on `AVAudioSourceNode(` construction sites finds no seventh block under any other spelling. The doc's own coverage list (lines 41-48) names exactly six voices and parenthetically records the seven

### B3 · CRITICAL — "Simulation" bio source permanently deaf after any real frame — defer check reads uncleared latestBio with no freshness bound
- Ort: `Sources/Echoelmusic/Bio/BioSimulator.swift:123`
- Beleg: BioSimulator.start's loop (BioSimulator.swift:116-131) gates every publish on `if let latest = bus.latestBio, latest.source != .fallback { sleep(2); continue }` — raw `latestBio`, no age check. `latestBio` has exactly ONE writer (`self?.latestBio = frame`, EngineBus.swift:693) and NO clearer anywhere (`grep 'latestBio = ' EngineBus.swift` → one hit; no clearBio/resetBio in Sources). So after ANY non-fallback frame ever lands — one camera lock, one BLE packet, or a single HealthKit wrist frame (started at app level via `healthBio.startIfAlreadyAuthorized`, EchoelmusicApp.swift:1224) — the stale frame sits in `latestBio` forever and the simulator defers on every iteration, publishing NOTHING. The reachable path: pill/bioPanel → `selectBioSource("sim")` → `startBioSource()` case `.sim` → `demoSource.start(publishing: bus)` (EchoelStudioView.swift:8826-8828). Hot-swapping camera→Simulation mid-take, or picking Simulation any time after a camera take (or ever, for a Health-authorized Watch user), yields a source that never produces a frame; the instrument silently falls back to neutral defaults.
- Fix: In the defer check, replace `bus.latestBio` with a freshness-bounded read (`bus.usableBio()` — EngineBus.swift:586-592 — or `freshBio(maxAge:)`), so the simulator yields only to a source that is actually LIVE and takes over once the real frame expires (6 s camera/BLE, 90 s wrist).
- Verifikation: Re-measured every claim: (1) BioSimulator.swift:123 gates each publish on raw `bus.latestBio` with `source != .fallback` and no age check, while the purpose-built freshness helpers `freshBio(maxAge:)` (EngineBus.swift:572) and `usableBio()` (EngineBus.swift:586) go unused. (2) `latestBio` is `private(set)` (EngineBus.swift:561) with exactly one writer (EngineBus.swift:693) and no clearer — BioStripView.swift:755 even documents "EngineBus NEVER clears" it; `stopBioSource()` (EchoelStudioView.swift:8834-8842) stops publishers but clears nothing. (3) The deadlock is permanent by construction: onl

### B4 · WARN — BLE strap: 1 Hz loop re-stamps a frozen latestHR with fresh receipt time — defeats the exact frozen-strap guard EngineBus claims, and the sensor-contact flag bits are ignored
- Ort: `Sources/Echoelmusic/Bio/PolarH10BioPublisher.swift:202`
- Beleg: The publish loop fires every second (`Task.sleep(for: .seconds(1))`, PolarH10BioPublisher.swift:~223) gated ONLY on `RRIntervalHygiene.isPlausibleBPM(self.latestHR)` (line 166), and stamps `timestamp: CFAbsoluteTimeGetCurrent()` at PUBLISH time (line 202) — not at notification receipt. `latestHR` is written only by `didUpdateValueFor` (line 459) and reset only by `didDisconnectPeripheral` (line 406). There is no notification-age check anywhere in the file. So if HR notifications stop while the connection lives (electrodes drying, RF loss before CoreBluetooth's supervision timeout, a strap that stops notifying), the loop republishes the same frozen HR every second with an always-fresh stamp — and `usableBio()`'s 6 s `.ble` window (EngineBus.swift:405) can NEVER expire it, because it measures publish recency, not measurement recency. EngineBus.swift:567-569 explicitly claims the freshness guard "stops a frozen reading — a dropped BLE strap … from being treated as a live body signal"; for this source that is only true after a clean disconnect callback. Additionally `parseHRMeasurement` (lines 270-300) reads flags bits 0/3/4 and ignores bits 1-2 — the SIG Sensor Contact Status — so a strap reporting "contact not detected" alongside a garbage/held HR passes the plausibility gate.
- Fix: Record a `lastNotificationAt` in `didUpdateValueFor`; in the publish loop, skip publishing when `CFAbsoluteTimeGetCurrent() - lastNotificationAt` exceeds a few seconds (mirroring the camera's inboundRateEMA close). Optionally parse flags bits 1-2 and treat "contact supported + not detected" as not-plausible.
- Verifikation: Re-measured every claim. (1) The 1 Hz publish loop (PolarH10BioPublisher.swift:160-218) stamps CFAbsoluteTimeGetCurrent() at PUBLISH time (line 202) gated only on isPlausibleBPM(latestHR) (line 166); latestHR is written only in didUpdateValueFor (line 459) and reset only in stop() (line 148) and didDisconnectPeripheral (line 406); no notification-age check exists in the file. So if notifications stall while the connection lives, a frozen HR is republished every second with a fresh receipt stamp, and EngineBus's 6 s .ble freshness window (EngineBus.swift:405, usableBio at 586-592) can never exp

### B5 · WARN — Lying lifecycle claims: "Stop EVERY bio publisher" and "Only ONE source feeds the bus at a time" — HealthKit is a second, unstoppable feeder with zero stop() callers
- Ort: `Sources/Echoelmusic/Studio/EchoelStudioView.swift:8848`
- Beleg: `selectBioSource`'s doc claims "Only ONE source feeds the bus at a time" (EchoelStudioView.swift:8848) and `stopBioSource`'s claims "Stop EVERY bio publisher (idempotent) so no source keeps feeding the bus after Stop or a source switch" (8832-8833). Measured: `stopBioSource()` stops camera/polar/demo only (8834-8842); `HealthKitBioPublisher` is started at APP level (`healthBio.startIfAlreadyAuthorized` EchoelmusicApp.swift:1224, `healthBio.start` on first bio use :1253) and `git grep 'healthBio.stop'` over Sources returns ZERO callers — it publishes for the rest of the run, alongside whatever the pill selected and after ■ Stop. BioEventPublisher.swift:49-56 already states this exact contradiction in writing ("`stopBioSource` stops camera/strap/demo but NOT `HealthKitBioPublisher` … keeps publishing alongside whatever the user picked"), and BreathHold.swift:315-323 carries a MEASURED casualty of the interleave (one wrist frame between camera frames drove breath-hold weight 1.0 → 0.0 in a single sample, #447). Interleaved wrist frames (coherence hard-coded 0, HealthKitBioPublisher.swift:138) also pass `usableBio()`'s 90 s window and are applied by timestamp-deduping consumers (e.g. ModulationEngine.tick, ModulationEngine.swift:243-253) between camera frames.
- Fix: Correct both doc comments to name HealthKit as the deliberate app-level second feeder (or gate `healthBio` publishing while a live pill source runs). Do not silently reword — cite BioEventPublisher.swift:49-56 as the measured statement.
- Verifikation: Every claim re-measured and confirmed. (1) The two doc comments say exactly what the finding quotes, with no HealthKit caveat anywhere in their bodies: `stopBioSource`'s doc reads "Stop EVERY bio publisher (idempotent) so no source keeps feeding the bus after Stop or a source switch" (Sources/Echoelmusic/Studio/EchoelStudioView.swift:8832-8833), and its body stops only cameraRPPG/polarH10/demoSource (8835-8841); `selectBioSource`'s doc reads "Only ONE source feeds the bus at a time" (8848). (2) HealthKit is a genuine second feeder of the SAME bus: `HealthKitBioPublisher.publishIfFresh` calls `

### B6 · WARN — FaceExpressionBioPublisher: a 10 Hz fresh-stamp publisher whose wiring would break the ~1 Hz apply-rate law unguarded — and four surfaces claim a FeatureFlag gates it that nothing reads
- Ort: `Sources/Echoelmusic/Bio/FaceExpressionBioPublisher.swift:131`
- Beleg: The publish loop sleeps 100 ms ("// 10 Hz", FaceExpressionBioPublisher.swift:131) and every tick publishes a frame stamped `timestamp: now` (lines 142-151) — fresh timestamps, so timestamp-deduping consumers would apply at 10 Hz, not ~1 Hz. The blocking guard `Tests/CISmoke/BioApplyRateIsTheDedupedRateTests.swift` states "every wired publisher emits at ~1 Hz" (line 12) but pins ONLY the camera's `tick % 10` cadence — it contains no mention of faceCam, so wiring this publisher would silently invalidate every ~1 Hz-derived time constant (`EchoelDDSP.smoothCoeff` 0.6065 ≈ e^-0.5 at EchoelDDSP.swift:1843, `ModulationEngine.nominalFramePeriod = 1.0` at ModulationEngine.swift:134) with the guard staying green. Separately, its header claims it sits "behind `FeatureFlags.cameraExpression`, default OFF" (lines 22-23) — but `start(publishing:)` (lines 97-109) guards only `!isPublishing, Self.isSupported` and never reads the flag; FeatureFlags.swift:14-16 measures `cameraExpression` among the ELEVEN flags with "ZERO deciding readers". OSCSender.swift:270, ADMOSCSender.swift:265 and Tests/CISmoke/ADMOSCAbsenceTests.swift:24 all rest their "no .faceCam frame reaches egress" arguments partly on that non-gating flag; the only real gate is zero instantiation sites (`git grep 'FaceExpressionBioPublisher('` → 0 in production).
- Fix: Either make `start()` actually consult `FeatureFlags.cameraExpression` (or delete the flag claim from the four surfaces), and add a faceCam cadence assertion (or a rate-derivation note) to BioApplyRateIsTheDedupedRateTests so wiring a 10 Hz publisher goes red with instructions instead of green.
- Verifikation: Every mechanical claim re-measures true: the loop is 10 Hz with fresh `timestamp: now` stamps (FaceExpressionBioPublisher.swift:131,142-151), start() never reads FeatureFlags.cameraExpression (:97-109), the rate guard pins only the camera cadence with no faceCam assertion (BioApplyRateIsTheDedupedRateTests.swift:46-87), and the four cited surfaces carry the 'sits behind the flag' wording. So the finding is not refuted. But it is overstated on three counts, which drops it from WARN to LOW. (1) The flag-gates-nothing fact is already measured, documented and GUARDED: FeatureFlags.swift:14-16 name

### B7 · WARN — Generate labels a user-locked tempo as .flowServo in the T1 tempo-source log
- Ort: `Sources/Echoelmusic/Studio/EchoelStudioView.swift:9527`
- Beleg: generate() has ONE clock call for all of its tempo branches: `beatPlayer.pattern.glideTempo(to: tempo, source: .flowServo)` (EchoelStudioView.swift:9527). But the branch at :9373 sets `tempo = lockedBPM.clamped(to: Transport.minTempo...Transport.maxTempo)` — the USER'S locked number (the code comment at ~9525 even says "lockBPM already resolved `tempo` to the locked value above"). PatternEngine.glideTempo writes `lastTempoSource = source` unconditionally as its first line (PatternEngine.swift:333), even for a same-value no-op glide, and the transport-play breadcrumb prints it (`tempoSource=`, PatternEngine.swift:492-494). The `.user` case's own doc says a lock/locked-field edit is `.user` ("every path where a human named the number", PatternEngine.swift:459-461). Measured: only 7 `source:` call sites exist in Sources/; this is the one that serves both the locked and the body branch.
- Fix: Split the label at the call site: pass `source: lockBPM ? .user : .flowServo` (the lockBPM read is already in scope at :9373), or hoist a local `tempoSource` variable set per branch. One line; no behavior change to the clock itself.
- Verifikation: Every claim re-measured and confirmed. generate() has exactly one clock call (EchoelStudioView.swift:9527, `glideTempo(to: tempo, source: .flowServo)`) serving all three tempo branches; the lockBPM branch resolves `tempo` to the user's locked number (:9373), and the comment at :9525-9526 confirms locked takes flow through this call without documenting the source label as deliberate. PatternEngine.glideTempo stamps `lastTempoSource = source` unconditionally as its first line (:333), even on the same-value no-op path, and the play breadcrumb prints it (:492-494); generate() glides before play(ca

### B8 · WARN — AutomationPoint decode drops the whole keyframe on an unknown curve case — the exact #543 trap, unfixed, under a comment claiming the opposite
- Ort: `Sources/Echoelmusic/Sequencer/AutomationLane.swift:72`
- Beleg: `curve = try c.decodeIfPresent(AutomationCurve.self, forKey: .curve) ?? .linear` (AutomationLane.swift:72). AutomationCurve is a String-raw enum (line 15-20); synthesized RawRepresentable decoding THROWS dataCorrupted on an unknown raw value — decodeIfPresent absorbs only a missing key/null. The throw escapes AutomationPoint.init, LossyPoint (:103-106) yields nil, and the WHOLE keyframe is silently dropped — while the comment six lines above (:66-70) claims "every field tolerates absence via a sensible default … degrades THIS field, never throws". This is character-for-character the mechanism #543 fixed one file away for TimelineLane.kind (Timeline.swift:146-176, which spends a paragraph explaining why bare decodeIfPresent on an enum is NOT enough and shows the `(try? c.decode(...)) ?? default` house pattern).
- Fix: `curve = (try? c.decode(AutomationCurve.self, forKey: .curve)) ?? .linear` — the exact TimelineLane.kind/#543 form. Consider the same for tick/value (bare decodeIfPresent throws on a wrong-typed value and drops the point), though that needs corruption rather than a rename to trigger.
- Verifikation: Verified against code, not comments. (1) Mechanism confirmed: AutomationCurve is a String-raw enum (AutomationLane.swift:15-20); synthesized decoding throws dataCorrupted on an unknown raw value, and decodeIfPresent at AutomationLane.swift:72 absorbs only missing/null. The throw escapes AutomationPoint.init(from:), LossyPoint (AutomationLane.swift:105) try?-swallows it to nil, and compactMap (:115) silently drops the whole keyframe. (2) Not deliberate: the comment at AutomationLane.swift:64-68 claims "degrades THIS field, never throws" — the code refutes its own law comment (the #425 class), w

### B9 · WARN — Async export completion can drive a second modal while another sheet is up (two-modals law, unguarded path)
- Ort: `Sources/Echoelmusic/Studio/EchoelStudioView.swift:10083`
- Beleg: `exportWav()` sets `share = ExportedFile(url:)` (EchoelStudioView.swift:10083) and `keepLastLoop()` does the same (:10097) when the seconds-long real-time loop record finishes (the export button's own hint at :2105 says 'Records one loop and exports a WAV'). During that window the sheet doors stay live: `showOpen = true` (:2186), `showLiveColabo = true` (:2195), `showLearn = true` (:2206) carry no `isExporting` gate — only the export tiles themselves are disabled (:2118 `.disabled(isExporting || !hasComposed)`). All these bindings sit on the SAME body chain (`.sheet(item: $share)` at :1437, `.sheet(isPresented: $showLearn)` at :1465). No `share` assignment checks whether another presentation binding is active.
- Fix: Guard the two async `share =` assignments (defer or drop when any other chain binding is presented), or disable the three quick-door sheet buttons while `isExporting` — the same gate the MIDI tile already carries for the shared `share` slot (:2117-2118's comment documents exactly this slot-collision concern).
- Verifikation: Every evidence line re-measured and confirmed. (1) The two async completions really write the share modal binding unconditionally: EchoelStudioView.swift:10082 (exportWav) and :10096 (keepLastLoop) set `share = ExportedFile(url:)` after `await exporter.exportWav/exportRecentLoop(...)`; LoopExporter.swift:242-263 shows the capture is real-time (`await waitForCapture(seconds: seconds + tailSeconds)`, up to 8.5 min per its own :260-262 comment), so a long window exists in which the user can open another sheet. (2) The sheet doors carry no isExporting gate: showOpen (:2186, disabled only on empty 

### B10 · WARN — Immersive header monitor shows the dead 'sparkles' tile during live BLE-strap and Simulation sessions
- Ort: `Sources/Echoelmusic/Studio/WorkspaceView.swift:676`
- Beleg: WorkspaceView.topBar constructs `ImmersiveMonitorMini(active: cameraRPPG.isRunning)` (WorkspaceView.swift:676). `startBioSource()` (EchoelStudioView.swift:8804-8829) shows `.ble` starts only `polarH10.start(publishing:)` and `.sim` only `demoSource.start(publishing:)` — the camera never starts, so `cameraRPPG.isRunning` stays false for the whole session. `ImmersiveMonitorMini.body` (HeaderMonitors.swift:~395-400) renders the animated visual-colour tile only when `active`; otherwise the static grey sparkles placeholder. #616 made ble/sim first-class choosers (BioSourceOption.swift:40), so this path is now front-door reachable.
- Fix: Gate on `bus.instrumentRunning` (EngineBus.swift:598-601 documents it as exactly this low-frequency chrome mirror) instead of `cameraRPPG.isRunning`; both are start/stop-rate reads, so the freeze law is untouched.
- Verifikation: Every cited claim re-measured and holds. WorkspaceView.swift:676 gates ImmersiveMonitorMini on cameraRPPG.isRunning; startBioSource (EchoelStudioView.swift:8804-8829) starts only polarH10 for .ble and only demoSource for .sim, and stopBioSource (8834-8842) stops the camera on every switch, so isRunning stays false for whole BLE/sim sessions. HeaderMonitors.swift:398-415 renders the grey sparkles placeholder (and VoiceOver 'Idle', line 427) when active is false. The file header (HeaderMonitors.swift:8-10) states the tile renders 'while a bio session is live', and startBiofeedback (EchoelStudioV

### B11 · WARN — Wall-clock freshness predicates latch: bio rows keep claiming a live body after the source stalls or stops
- Ort: `Sources/Echoelmusic/Studio/EchoelStudioView.swift:11020`
- Beleg: `AutoModeRow.body` computes `let bioRunning = bus.usableBio() != nil` (:11020) for `.disabled(!bioRunning)` (:11030); `BreathVoiceRow.body` computes `bus.usableBio()?.hasMeasuredBreath` (:10977); `PulseMonitorMiniLive.body` computes `bus.freshBio()` (HeaderMonitors.swift:246) for its 'locked' accent and BPM. All three predicates are pure wall-clock checks against `latestBio.timestamp` (EngineBus.swift:572-592). But a SwiftUI body re-evaluates only when an OBSERVED property mutates — and when a publisher stalls or Stop runs (`stopBioSource()`, :8870-8878), `latestBio` simply stops being written. No timer/TimelineView exists in these leaves, so the 5-6 s expiry can never trigger a re-render: the last live evaluation (toggle enabled, 'your inhale opens it', locked accent + last BPM) persists indefinitely.
- Fix: Have these leaves also read `bus.instrumentRunning` (flips observably at Stop and covers the session-end case), and for the true stall case drive the freshness re-check from a slow `TimelineView(.periodic(...))` or have the publishers write an explicit stalled marker instead of merely going silent.
- Verifikation: Confirmed by re-measurement. The three predicates are pure wall-clock checks (EngineBus.swift:572-592) over latestBio, which is only ever written by publish(bio:) (EngineBus.swift:690-694) and never cleared; AutoModeRow (EchoelStudioView.swift:11038/:11048), BreathVoiceRow (:10995), and PulseMonitorMiniLive (HeaderMonitors.swift:246) read them in SwiftUI bodies with no TimelineView/timer, so when a source stalls or stopBioSource() runs (:8852-8860 — touches only publisher state these leaves don't observe), the last live evaluation latches indefinitely. Decisively, the repo's own "#503 lesson" 

### B12 · CRITICAL — Mid-take resize/rotation feeds wrong-size frames into a writer fixed at first-frame dimensions — distorted or lost take
- Ort: `Sources/Echoelmusic/Video/VideoRecorder.swift:158`
- Beleg: The writer is built lazily from the FIRST ingested frame's dimensions (VideoRecorder.swift:158-162, setUpWriterLocked width/height at :186-218) and `ingest` has NO dimension guard afterwards (:172-181 checks only readiness and monotonic PTS). Meanwhile a mid-take size change is explicitly reachable: `sizeWideEnoughForARunningTake` (FloatingVisualWindow.swift:1171-1175) only forbids small/medium while busy, and cycleSize's own comment says "Cycling therefore runs large ↔ fullscreen until the take ends" (FloatingVisualWindow.swift:1206-1207) — the size button sits in the same 44pt row as REC. On the flip, MetalBioView's settled-size management re-allocates the drawable at the new size (MetalBioView.swift:777-779), `VisualRecorder.ensureResources` happily rebuilds its pixel-buffer pool for the new dimensions (VisualRecorder.swift:184-197), and every subsequent frame is appended at the new size. Device rotation in fullscreen recording is a second route (landscape is supported; the aspect inverts).
- Fix: In `VideoRecorder.ingest`, compare the incoming buffer's dimensions to the writer's configured size and drop mismatched frames (keeping the take intact at the original size), or letterbox via the adaptor pool; alternatively have `VisualRecorder.capture` skip frames whose source size differs from the first captured size. Pin with a CISmoke source-scan guard like RecordingKeepsItsPictureTests.
- Verifikation: CONFIRMED by independent re-measurement of every link in the chain.

1) Writer fixed at first-frame size, no dimension guard afterwards: VideoRecorder.swift:158-162 builds the writer lazily from the first ingested buffer's CVPixelBufferGetWidth/Height; setUpWriterLocked (:186-218) bakes those into AVVideoWidthKey/HeightKey and the adaptor attributes. The only per-frame guards in ingest are `armed` (:156), `isReadyForMoreMediaData` (:174) and monotonic PTS (:176) — no comparison of incoming buffer dimensions to the configured size before `adaptor.append` (:178).

2) The mismatched-size frame so

## C — Lows (registriert, je eine Zeile)

- EchoelDDSP.init accepts a sampleRate parameter but filter/filterLFO/entrainment are pinned at literal 48000 in property initializers — a non-48k construction silently detunes them — `Sources/Echoelmusic/DSP/EchoelDDSP.swift:327`
- FX render paths still clamp bio-writable parameters with the NaN-propagating min(max(…)) idiom the house rule bans — one non-finite write silences dry+wet or poisons flanger delay state — `Sources/Echoelmusic/DSP/EchoelReverb.swift:90`
- BioEventPublisher staleGap rationale misattributes the frame rate ("normal 10 Hz frames") — and for HealthKit the reset fires between NORMAL frames, clearing the detector routinely — `Sources/Echoelmusic/Bio/BioEventPublisher.swift:88`
- The tempo-invariant guard file itself teaches THREE sources while the shipped enum has FOUR — `Tests/CISmoke/TempoInvariantTests.swift:11`
- TapTempo clamps to 40–240 under a comment claiming 'the app's musical range', which is 30–300 — `Sources/Echoelmusic/Sequencer/TapTempo.swift:18`
- GuideOverlay reopens on the terminal 'Done' card after a completed tour — `Sources/Echoelmusic/Studio/GuideOverlay.swift:105`
- Census verified — no drift: chain 14 / file-wide 16, no unsanctioned Slider/Stepper, no new high-frequency root-body reads since #616 — `Sources/Echoelmusic/Studio/EchoelStudioView.swift:1436`
- VideoMuxAlignment.loopAligned — founder-asked loop-exact video cut is pure math with zero production callers, and its doc claims a fallback caller that does not exist — `Sources/Echoelmusic/Video/VideoMuxAlignment.swift:46`
- Quick stop→re-record tap silently drops the new take: startRecording no-ops during .finishing but toggleRecording still sets recordStart — `Sources/Echoelmusic/Studio/FloatingVisualWindow.swift:1256`
- Bio egress rate ceiling governs only one of the two bio egress paths — ADM-OSC keeps waking at 20 Hz on a thermally stressed device — `Sources/Echoelmusic/Sync/ADMOSCSender.swift:108`
- ADM scene streaming (S3) is a doorless capability: streamsScene's only setter UI is in never-instantiated ImmersiveStageView, while attachScene is wired at app start — `Sources/Echoelmusic/Sync/ADMOSCSender.swift:70`
- CLAUDE.md identity line count has drifted again: '362 Swift under Sources/' vs 366 measured — `CLAUDE.md:74`
- Guard-needle sample (15 CISmoke guards, every quoted needle re-grepped in Sources): NO green-for-dead-reason guard found; all count-pins verified live — `Tests/CISmoke/TheTempoDestinationHasNoRouteTests.swift:236`
- Sources file count stale: CLAUDE.md and LEDGER_COUNTS say 362 (Stand 2026-08-14), measured 366 — `CLAUDE.md:74`
- Three '(Stand 2026-08-14)' line-number citations in CLAUDE.md already point at the wrong lines — `CLAUDE.md:24`

## D — Abarbeitung (PM-Rang)

1. **#622 (dieser Takt): MIDI Rank 1** — open() setzt hasComposed = !p.notes.isEmpty (ehrliche Kachel, Explainer kehrt zurück, Leer-Take-Propagation endet).
2. **#623: MIDI Rank 3** — open() installiert rebakeSource via rebakeArrangement (8-Takte-Export nach Open statt 1 weichgezeichneter Takt).
3. **B3 (CRITICAL, Simulation taub)** + **B1 (CRITICAL, RetroCapture-Ring)** + **B12 (CRITICAL, VideoRecorder-Resize)** — je eine Scheibe; B3 zuerst (Simulation ist sichtbare Tür seit #616).
4. MIDI Rank 2 (Feedback für disabled-Kachel / Autosave-Restore eigene Frage) · Rank 4 (Retap heilt; Founder-Frage im Delta).
5. WARNs B2/B4-B11 als Ralph-Backlog; Lows bei Gelegenheit (CLAUDE.md-Zähler 362→366 beim nächsten Ledger-Update).