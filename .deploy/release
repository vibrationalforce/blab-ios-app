build: 2026-06-30 v10.76.55 (Pulse: reject hard-press motion — gate 0.25→0.20)
branch: claude/piano-roll-clip-view-wozlie
notesT: First long log from the FIXED build (rate held ~15, no fps collapse) — and it locked
  strong: ~63 bpm steady for ~60 s at high periodicity (acf 0.8+). The fps fix (10.76.50) +
  steadier-readout (10.76.51) both landed and work.
  One clear remaining defect: at the end you pressed harder (amp 0.22–0.33, R≈0.6) and it
  briefly published WRONG rates (bpm 70→56) because the motion-reject gate (0.25) let those
  through. The log now shows real clean pulses top out at amp ≈0.16, so I tightened the motion
  gate 0.25→0.20 — it cleanly rejects hard-press/re-grip windows (0.22+) while keeping every
  real pulse (≤0.16). Pure change + unit tests.
  Test 10.76.55: hold a gentle, steady contact → it should lock near your resting rate and HOLD;
  if you press hard or re-grip, it should drop to "finding…" rather than flash a wrong number.
