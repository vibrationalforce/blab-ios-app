build: 2026-07-20 v10.79.311 (Adaptive Spurköpfe — "MIDI 1" nicht mehr abgeschnitten)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: HEAD bf1e002 — beide Gates grün (Xcode Compile Check + CI/CD Pipeline),
  Pflicht-Reviewer 0 Defekte (die eine MEDIUM columnWidth-Desync vorab gefixt).

  ADAPTIVE SPURKÖPFE (dein Screenshot — der Spurkopf war links abgeschnitten):
  • "MIDI 1" wird nicht mehr abgeschnitten. Der Kopf ist jetzt LINKS verankert
    (.leading) statt zentriert — jeder Rest-Überhang fällt nach RECHTS unter die
    Spaltenkante, nie über den linken Bildschirmrand raus (das war die eigentliche
    Ursache: der zentrierte Überlauf schob den ganzen Kopf nach links, bis das
    Label wegklippte).
  • Adaptive Breite: 184 pt auf dem iPad (Regular width), 162 pt auf dem iPhone —
    passt auf jede Bildschirmgröße, vom Team so entworfen.
  • Der Türname skaliert (minimumScaleFactor 0.85) und die Mix-Leiste ist auf
    xLarge Dynamic Type gedeckelt, damit ein langer Name / große Systemschrift den
    Kopf nicht sprengt.

  WEITER (still, bis Founder-Antwort):
  • EchoelBodyVibe-Oberfläche erste Slice ist GEBAUT (BioStrip + Genre/Mood/
    Variation pro Bio-Spur, "Read pulse" scharfstellen) und im Pflicht-Review —
    liefert "Genre auch in EchoelBodyVibe" und schaltet die restliche Leisten-
    Auflösung frei. Kommt im nächsten Deploy nach Review + grünen Gates.
  • AUv3: warte auf deinen Neustart-+-Reinstall-Test (self-probe OK = In-App-Fix
    greift; FAILED = Null-Risiko-Appex-Profil-Diagnose in CI, dann Portal).
