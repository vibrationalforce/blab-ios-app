build: 2026-06-28 v10.76.42 (rPPG: lean harder on the steady pulse fundamental)
branch: claude/piano-roll-clip-view-wozlie
notesT: Builds on 10.76.41. Further smooths the camera heart-rate. Your last log: the
  pulse fundamental (autocorrelation) sat rock-steady at 53–58 bpm for the whole read, but
  the shown number still wandered 51–87. The 10.76.39 "trust the fundamental" pull was too
  gentle in the MODERATE-confidence band (acf ≈0.5–0.7) that real fingertip contact mostly
  lives in — so peak-counting noise still leaked through. This build strengthens that pull:
  once there's a usable periodicity it now DOMINATES the noisy discrete count (it still
  contributes, and the existing octave/EMA guards remain), so the displayed BPM should track
  the steady fundamental instead of jumping. Pure helper change, unit-tested, camera-analysis
  path only (NOT audio/render/launch). Also confirms 10.76.41: NO menu-freeze in the log,
  app launched clean.
  Test: 10.76.42 → Well/Start with a finger on the lens: the BPM should hold steady (±a few)
  around your real resting rate instead of swinging 20–40. Still launches straight to Studio,
  and the Tonart/Genre menus stay selectable while playing.
