build: 2026-07-15 v10.79.248 (Audio-Spuren KLINGEN im Arrangement — Heilungs-Welle 1)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Zweiter verifizierter CRITICAL aus deinem Ultracode-Mandat geheilt: Audio-
  Regionen auf der Timeline waren beim Song-Playback KOMPLETT STUMM — der
  getestete AudioLanePlayer war nie mit der Engine verdrahtet (genau wie der
  Video-Player vor v246: Koordinator gebaut, Tür fehlte).

  NEU:
  • Audio-Clips (importierte Loops, Splits, getrimmte Stücke) SPIELEN jetzt im
    Arrangement — ab exakt der richtigen Datei-Position (Trim-Offset ehrlich,
    Split-Hälften klingen verschieden), bis zur Clip-Kante, mit Spur-Level.
  • Auch REINE Audio-Arrangements (ohne MIDI-Spur) spielen — die alte Play-
    Bedingung hat sie stumm geschaltet.
  • Streaming statt RAM: lange Clips werden aus der Datei gestreamt (kein
    70-MB-Puffer gegen das 200-MB-Limit).
  • Song-Loop: Audio startet am Wrap phasenrichtig neu (Reviewer-Fund — vorher
    lief es aus dem Takt und verstummte ab Runde 2).
  • Kein Mix-Aussetzer mitten im Song: alle Spuren werden VOR dem Start
    vorgewärmt (der Engine-Attach pausiert sonst kurz alles — Reviewer-Fund).
  • Interruption-fest: Anruf während der Wiedergabe crasht nicht mehr in
    node.play() (Guard, verstummt ehrlich bis zum nächsten Clip-Beginn).

  EHRLICH (dokumentiert, eigene Zyklen): Live-Mixer-Bewegung MITTEN in einer
  Region greift erst an der nächsten Clip-Grenze (H4 Pan/Gain-Fan-out als
  Nächstes) · Editor-Fades/Warp werden auf der Timeline noch nicht übernommen
  (A5) · Sekundär-MIDI-Spuren falten Mehrtakt-Clips noch (M1c).

  SICHER: audio-thread-reviewer — "zero work added to the render thread";
  alle 4 Funde (2 HIGH, 1 Crash-Guard, 1 Node-Leak) gefixt; +2 Koordinator-
  Tests (Vorwärmen ohne Klang, Detach beim Spur-Löschen). Kette v247 grün
  ausstehend — Gates laufen.

  Heilungs-Stand Welle 1: M1 MIDI-Takte ✓ (v247) · A1 Audio-Klang ✓ (dieser
  Build) · als Nächstes H3 Sample-Race · H4 Pan/Gain · H5 AU-Routing.

  Reihenfolge: v246 Video-Monitor · v247 MIDI-Takte · v248 Audio-Klang.
