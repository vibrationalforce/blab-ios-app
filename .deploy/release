build: 2026-07-20 v10.79.322 (AUv3-Hilfe: der wirksame Warm-Durchlauf steht jetzt drin)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: HEAD (AUv3BrowserView Hilfetext) — Copy-only in der zugeklappten Hilfe, kein Verhaltensrisiko.

  Zu deinem wiederkehrenden AUv3-Scan (0 Fremd-Plugins, -3000): ich habe PROAKTIV
  die Build-Seite geprüft — die eigene AUv3 IST korrekt in die App eingebettet
  (App-Dependency in project.yml), Manifest sitzt unter NSExtensionAttributes,
  Version 10001, Bundle com.echoelmusic.app.auv3. Also KEIN Build-/Einbettungs-Bug;
  der -3000 ist die kalte Geräte-Registry (diesem App-Prozess werden nur Apple-
  Einheiten geliefert). Das ist die Ursache, die nur der Warm-Durchlauf löst.

  DAMIT DU (und jeder Nutzer im selben Zustand) den echten Weg im App findet:
  Der zugeklappte Aufklapper "Not seeing a plugin you installed?" nennt jetzt die
  WIRKSAME Sequenz statt generischer Schritte:
  1. GarageBand (oder anderen AU-Host) einmal öffnen, Plugin-Liste ansehen →
     das weckt iOS' geteilte Registry → zurück, Rescan tippen.
  2. Immer noch nichts? iPhone neu starten, Echoel neu öffnen, Rescan — iOS
     registriert frisch installierte Plugins oft erst nach einem Neustart.

  (Hauptbild bleibt ruhig/professionell — die Schritte stehen bewusst nur im
  Aufklapper, niemandem ins Gesicht gedrückt.)
