build: 2026-06-22 v10.44.0 (Live humanization + consonance convergence — coherence core complete)
branch: claude/piano-roll-clip-view-wozlie
notes: Completes Phase 1 (science-grounded coherence core) and extends Phase 2 (sound). (1) LIVE HUMANIZATION — the `humanize` mood now varies LIVE note velocities (seeded, ±up to 18%), not only at MIDI export, so repeated notes breathe instead of machine-gun uniform. (2) CONSONANCE CONVERGENCE — as HRV coherence rises the melody strays less from chord tones (effective tension → 40% at full coherence), so a settled body yields more consonant, resolved music. Together with v10.43.0 (two-clock tempo entrainment, default unison) and v10.42.0 (density servo), the full validated loop is now wired: as coherence rises the music becomes SPARSER + CALMER-TEMPO + MORE CONSONANT, and sounds FULLER (unison) + more ALIVE (humanization). Grounded in a peer-reviewed HRV-coherence brief; Solfeggio/"healing-frequency" pseudoscience explicitly excluded. Carries v10.41.0 (real-FFT oscillating spectral rings, non-standard-tuning guard). Compile + CI tests green.
