build: 2026-06-20 v10.34.11 (design consistency + Submit-to-community emails the preset)
branch: claude/piano-roll-clip-view-wozlie
includes: (1) "Submit to community" no longer dead-ends in the GitHub app — it now opens a pre-addressed email to the curator with the preset JSON embedded (works on any device with Mail, no GitHub account). Wired for FX presets, sounds (patches) and moods. (2) App-wide design consistency pass toward the "Sound & texture" panel look — clear, accessible, professional: the Sound editor (PatchEditorView) gets Atkinson Hyperlegible type + the same 1px bordered sections as the inline panel; FX, Piano-Roll, Sample browser, Bio strip, Bio-metric info, the mood/patch preset bars and Onboarding all moved off raw system fonts onto EchoelTheme.font (Atkinson, Dynamic-Type + pinch-zoom aware) and off hardcoded white/grey onto EchoelTheme color tokens. SF-Symbol icon metrics, the live-signal green accent and the monospaced diagnostics are intentionally preserved; no layout or logic changes. Verified: ci.yml (build+test, all platforms) + xcode-compile-check (AUv3 included) green on 6d4b5c4. Plus everything from v10.34.3–v10.34.10.
