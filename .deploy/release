build: 2026-06-28 v10.76.46 (rPPG coaching: say "hold still" when it's movement)
branch: claude/piano-roll-clip-view-wozlie
notesT: Builds on 10.76.45. Small but important companion to the motion-reject fix. When the
  finger is moving / pressing unevenly (large signal swings), the analyzer now correctly
  refuses to lock onto a wrong rate — but the on-screen guidance only said a generic
  "finding your pulse…". It now says "Hold still — keep your finger steady" in exactly that
  case, so a shaky/heavy contact gets actionable help instead of seeming stuck. (Your last
  log was still a pre-10.76.45 build and showed exactly this: amplitude 0.34–0.74 = movement,
  BPM climbing 99→115 — that won't false-lock anymore, and now you'll be told to hold still.)
  One line in the pure coaching text, reusing the reviewed motion helper. Camera path only.
  Test: 10.76.46 → Start, finger on lens but move/press a bit: the pulse readout should say
  "Hold still — keep your finger steady" and NOT jump to a wrong number; hold steady & light →
  it locks onto your real rate. Light, steady contact is what it wants.
