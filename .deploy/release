build: 2026-06-30 v10.76.54 (Visuals overhaul · step 2 — one control set, identical everywhere)
branch: claude/piano-roll-clip-view-wozlie
notesT: "Complete and easy to understand/control" pass on the immersive visuals.
  • DECISIONS (you said decide): colour stays the physically-grounded model — the heard tone's
    overtone series rendered as drifting colour-clouds (that IS harmony-as-light, and it works);
    "design meets physics" comes from CRISP physical STRUCTURE per look (the lever I can verify
    is right), not a blind colour re-plumbing. Rings was the only un-physical "haze" look →
    already fixed to real interference (step 1, 10.76.53). Chladni/Plasma/Water/Prism are already
    real wave physics and looked right in your video, so I left the good ones alone.
  • EASY TO CONTROL (this build): the visual controls were DUPLICATED — the inline Visual panel
    and the fullscreen VJ overlay each had their own copy of the six adjust fields + preset strip,
    so they could drift apart and were confusing. Now there's ONE definition each (visualPresetRow
    + visualAdjustFields), used in both places — the controls are IDENTICAL whether you adjust them
    in Compose or live over the fullscreen visual. Less to learn, nothing can desync.
  No behavior change beyond consistency; reviewed (build) before ship. Test: open the Visual panel
  AND the fullscreen visual — same Look strip, same Preset row, same Intensity/Detail/Motion/
  Spread/Hue/Saturation, behaving identically in both. Rings should read as crisp interference.
