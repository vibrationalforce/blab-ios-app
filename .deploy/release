build: 2026-06-19 v10.34.3 (FX params unified on EchoelValueField; Xcode strict-concurrency fix)
branch: claude/piano-roll-clip-view-wozlie
includes: Effects panel parameters now use the app-wide EchoelValueField control (label + number + unit, vertical-fader drag / tap-to-type) — no raw sliders anywhere; set as the permanent app-wide parameter-row standard (CLAUDE.md). Plus the full preset/FX/community package from v10.34.2 (click-free attack across all characters; full FX panel incl. Saturation/Harmonizer/Reverb; FX presets save/recall + 22 curated + search + favorites/recents; community Submit->triage->loader; Sound/patch parity; Drum Samples with preview-before-assign). Build fix: AudioEngine meter-poll timer reads its nonisolated(unsafe) meter pointers via self instead of capturing non-Sendable local copies (resolves Xcode 26.5 "sending pointer risks data races"). Verified: ci.yml (SPM) green + new xcode-compile-check (XcodeGen + xcodebuild, AUv3 included) green through c189555.
