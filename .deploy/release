build: 2026-07-05 v10.79.88 (Puls zeigt schneller: starke Periodizität allein reicht als Beweis)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notesT: Dein Log (Build 2192) war eigentlich GUT — der Puls wurde erfasst,
  hat sich gesettelt und den Takt geseedet (51 bpm). Aber eine Stelle hat
  mich gestört: ~10 Sekunden lang war dein Puls eindeutig ECHT (starke
  Periodizität, stabile 56 bpm), trotzdem stand die App auf "acquiring",
  weil der Display-Filter Periodizität UND einen zweiten Konfidenz-Wert
  verlangte — und der hing hinterher.
  FIX: Starke Periodizität allein (der stärkere Beweis) reicht jetzt, um den
  Puls zu zeigen und den Takt zu seeden. Der alte Junk-Fall (hohe Konfidenz,
  aber KEINE echte Periodizität — die Ursache für falsche Werte) wird weiter
  abgewiesen. Konkret: der Puls sollte jetzt ein paar Sekunden FRÜHER
  erscheinen und ruhiger stehen.
  (Der Sound- und Visual-Umbau aus .87 — Drone-Fläche + spielbares
   Multi-Touch-Visual — ist unverändert drin.)
  TEST 10.79.88:
  A) Start, Finger ruhig auflegen: erscheint der Puls jetzt schneller?
  B) Drone-Fläche: trägt sie? Vollbild-Visual mit mehreren Fingern spielen:
     kohärent + Wasser-Ringe?
  Log bitte wieder mitschicken.
