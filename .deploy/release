build: 2026-06-28 v10.76.40 (Remove planetary tones + Cousto sound→colour)
branch: claude/piano-roll-clip-view-wozlie
notesT: Builds on 10.76.39. Founder call: drop the planetary-tone picker AND the Hans
  Cousto "sound → colour" octave mapping — the colour we had before is better.
  • Compose ▸ Composition: the "Planet tone" row is gone (Kammerton / tone-system rows
    stay). Removed PlanetTone + its picker/tuning logic + tests.
  • Immersive visual: the tone→colour now uses the PHYSICAL light mapping again — the tone
    is transposed up whole octaves into the visible band and converted to RGB via the CIE
    colour-matching functions (real spectral colour), instead of Cousto's 12-colour wheel
    convention. The Prism look now disperses by true light wavelength (a real prism).
    Removed coustoColour from the shader, ColorOctave.swift, and the parked Farboktave
    menu (VisualMenuView) + tests. Updated THIRD_PARTY_NOTICES (Cousto attribution dropped).
  Pure removal/revert to the prior colour path — no new sheets, no structural change; the
  shader is runtime-compiled so even a shader issue can't touch launch. Off-paths only.
  Test: 10.76.40 → still launches straight to Studio. Compose ▸ Composition has no "Planet
  tone" row. Well ▸ immersive visual: colours look like the natural-light palette from
  before the Cousto change (warm spectral light), not the 12-step wheel.
