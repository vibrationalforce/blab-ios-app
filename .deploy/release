build: 2026-06-26 v10.76.33 (Diagnose: which UI branch at launch)
branch: claude/piano-roll-clip-view-wozlie
notesT: Builds on 10.76.32. Pure diagnostics: two shared logs stopped right after
  "init done" with no startup steps, which means the app may be showing a NON-studio
  screen (Safe-Mode recovery or Onboarding) or hanging after init — not the studio
  whose startup task writes those steps. This build breadcrumbs the UI branch taken:
  "ui branch: STUDIO — startup task running" / "ui branch: SAFE MODE recovery screen"
  / "ui branch: ONBOARDING (not yet completed)". The next diagnostics log will name
  the screen, so we can tell a harmless early snapshot from a real stuck state.
  Test: 10.76.33 → Diagnostics → the log now includes a "ui branch:" line.
