build: 2026-07-15 v10.79.252 (Timeline falten killt keine Live-Session mehr + Stop-Quellen im Log)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Zweiter Welle-2-Punkt geheilt (H7) + Diagnose-Verbesserung aus deinem
  v249-Log.

  H7 — FOLD/UNFOLD IST JETZT EIN REINER LAYOUT-TOGGLE:
  • Timeline ein-/ausklappen zerstörte die View-Identität der Instrument-Zone —
    SwiftUI riss sie ab und baute sie neu, und ihr onDisappear feuerte
    stopEverything(): ein Klapp-Tap beendete eine LAUFENDE Session (Bio,
    Kamera, Transport) und warf offene Dropdowns/Zustand weg.
  • Fix (chirurgisch, 1 Datei): EIN EchoelStudioView an EINER stabilen
    Struktur-Position; nur der Timeline-Block ist konditional, nur Frame-WERTE
    flexen. Bonus mitgeheilt: das Falten re-triggerte bisher auch die
    onAppear-Effekte und überschrieb live editierte Patches — vorbei.
  • ui-state-Review: CORRECT (Identität, Layout-Äquivalenz, Lifecycle alle
    verifiziert; Keep-Awake bleibt bei gefalteter Timeline korrekt AN, weil
    die Session ja weiterläuft). Offene Kosmetik, bewusst: die Fold-Animation
    snappt; der Timeline-Zoom reset beim Falten (sag Bescheid, wenn's stört).

  AUS DEINEM v249-LOG (Triage beantwortet im Chat):
  • Jede stopEverything-Zeile nennt jetzt ihre QUELLE (user-stop / intent-stop /
    transport-stopped / unmount) — deine zwei "Stopps ohne Auslöser" sind ab
    diesem Build eindeutig zuordenbar.
  • Erinnerung AU-Test: dein Gerät hat 0 Third-Party-AUv3 — die 4 Apple-
    Instrumente im Plugins-Browser gehen durch denselben Assign-Flow (v250).

  Gates c41d938 → 67d248d → 35affb2 alle grün.

  Welle-2-Rest: H10 MIDI-Eingangs-Flut (als Nächstes) · H9 AU-FX pro Spur +
  musicalContext · H11-13.

  Reihenfolge: v249 Live-Mixer+Sample-Race · v250 AU-Instrumente ·
  v251 Update-feste Medien · v252 Fold-Fix + Stop-Quellen.
