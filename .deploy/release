build: 2026-06-21 v10.34.18 (EFX matches the app — interface cleanup begins)
branch: claude/piano-roll-clip-view-wozlie
includes: First step of the interface optimization. The EFX (effects) screen no longer looks like a stock iOS settings list — it now wears the Echoel design: black background, themed navigation bar, bio-green accent on search and Done, dark system controls. Every effect and preset action works exactly as before; this is a look-and-feel pass so EFX fits the rest of the app. Next steps (queued): a shared panel component + full bespoke-panel EFX, then the big one — a persistent workspace bar that puts the tools on the front instead of hidden behind a dropdown, FL/Ableton-clear, with only working tools shown. Verified: Xcode Compile Check + CI green on 7792b62 (auto-merged to main).
