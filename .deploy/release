build: 2026-07-14 v10.79.201 (Ort manuell eingeben — dein eigener Städtename, auch ohne GPS)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Kleiner, gewünschter Baustein aus deinem Umbau (Punkt c): den Ort im
  Session-Namen kannst du jetzt SELBST eintippen.

  WAS NEU IST:
  · Neben dem Orts-Schalter gibt es jetzt ein Textfeld "Ortsname". Tippst du dort
    z.B. "Hamburg" oder "Studio Hamburg" ein, steht das im Session-/Export-Namen —
    egal ob GPS an, aus oder blockiert ist. (Founder 07-14: "auch manuell eingeben
    wenn man Lust hat oder der Standort nicht funktioniert".)
  · Dein getippter Name gewinnt IMMER gegen den automatisch erkannten Ort. Leeren
    (x-Knopf) → wieder automatisch.
  · Freitext ist dateisicher: der Name wird beim Speichern/Export automatisch
    bereinigt (z.B. "Studio Hamburg" → im Dateinamen sauber).

  Test-first: 2 neue Tests (manueller Override + Freitext-Sicherheit, Linux-grün).
  Reviewer: ui-state + concurrency je PASS(0). Gates grün: Xcode + CI/CD.

  Ebenfalls schon auf dem Gerät (aus 200): die STILLE ist gelöst + abgesichert —
  falls MIDI 1 stumm ist, siehst du oben den amber Hinweis + "Ton an"-Knopf.
  Sofort auch ohne Update: bei MIDI 1 das orange "M" austippen → Ton zurück.

  DEIN GROSSER UMBAU (Genre/Variation/Mood pro Spur, Sound&Texture/Mix/FX in
  EchoelSynth, Weather optional pro Instrument, Key/Tempo + Session nach oben)
  wartet auf EIN Wort von dir: kommt nach dem Unmute von MIDI 1 der Ton zurück?
  Sobald du das bestätigst, lege ich mit Modul 1 los.
