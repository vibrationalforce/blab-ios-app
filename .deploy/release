build: v10.79.326 — #77 Melodie-Autopilot AUS + #79 Rhythmus-Vielfalt ALLE 18 Genres
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: FIX für "letzter Build ist noch 10.79.325 (2433)" — meine Session-Builds kamen
  nicht oben an, WEIL ich den Versionsstring vergaß.

  URSACHE (jetzt behoben): testflight.yml parst `v[0-9]+\.[0-9]+\.[0-9]+` aus DIESER
  Datei und setzt MARKETING_VERSION daraus. Meine bisherigen Session-Releases begannen mit
  "build: 2026-07-21C…" — OHNE v10.79.xxx-String → DEPLOY_VER leer → MARKETING_VERSION
  fiel auf den project.yml-Default 10.79.66 zurück. Die Builds 2436 (#77) und 2439 (#79)
  wurden also als v10.79.66 hochgeladen — NIEDRIGER als der 10.79.325 auf dem Gerät des
  Founders → TestFlight sortierte sie UNTER 2433, sie wirkten älter. (Beide sind in App
  Store Connect state=VALID, nur unter der falschen, zu niedrigen Version einsortiert.)

  FIX: (1) diese Datei trägt jetzt `v10.79.326` (höher als 325) → der Build erscheint als
  neueste Version oben. (2) project.yml-Default MARKETING_VERSION auf 10.79.326 gehoben,
  damit ein künftig vergessener Versionsstring nie wieder auf ein zu niedriges 10.79.66
  zurückfällt.

  INHALT dieses Builds (der komplette Session-Stand, den der Founder testen soll):
  · #77: keine Auto-Lead-Melodie mehr in irgendeinem Genre (leadDensity=0 überall) +
    Bar-0-Intro-Dämpfung; Melodien spielt der Nutzer selbst auf dem warmen Genre-Timbre.
  · #79: rhythmische Vielfalt über ALLE 18 melodischen Genres (GenreFlavor-Overlay auf
    fourOnFloor/backbeat/offbeat/halfTime) — Genres desselben Archetyps klingen jetzt
    unterscheidbar; deterministisch, Signaturen erhalten, Distinctness per Test abgesichert.
    Bio-Orientierung des Rhythmus (energy/coherence → Beat-Dichte) existiert weiterhin.

  BITTE: TestFlight öffnen → v10.79.326 (Build 244x) installieren → hören ob die 18 Genres
  angenehm rhythmisch verschieden klingen (Klang-Feintuning der Flavor-Werte ist bewusst
  noch offen, wartet auf dieses Ohr-Feedback).

  Gates: Xcode Compile Check + Echoelmusic CI/CD Pipeline zuletzt grün auf 0c816b3; dieser
  Deploy ändert nur project.yml (Versions-Literal) + diese Datei.
