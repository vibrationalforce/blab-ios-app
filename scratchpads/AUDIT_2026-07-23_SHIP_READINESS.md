# Ship-Readiness Audit — 2026-07-23 (ultra-agentic, adversarially verified)

Run wf_05c4e3fe-994 · 26 agents · 7 dimensions · 18 raw findings → **16 confirmed** (adversarial refute pass).

Method: 7 dimension finders (audio-thread · render-safety · concurrency · bio-correctness · auv3 · determinism · clip-export) → each finding a skeptic had to construct a concrete failure or mark false-positive → only confirmed survive.


## Synthesis

**Summary:** Honest read: the shipping v1.0 MAIN app has no confirmed build-breaker and no confirmed crash — the codebase is solid. The single critical/high finding is one defect counted twice (audio-thread COW malloc of padScratch/texScratch on every AUv3 render callback, EchoelmusicAudioUnit.swift:476). It is real code that fires whenever any host loads the plugin, but the AUv3 target is marked deferred/not-active for v10, so it does NOT break today's shipped experience — which is why shipBlockers is effectively empty for v1.0. It IS, however, the highest-severity confirmed fix and it lands squarely on the AUv3-hardening workstream just shipped (v10.79.341), making it the natural next Ralph point. The most user-impactful issue in the LIVE shipping app is medium, not critical: SynthPatch (SynthPatch.swift:56) has no defensive decoder, so upgrading from a build that saved patches before the 2026-07-11 timbre fields silently discards the user's entire patch library (PatchStore ?? []) or collapses a whole project to an empty document (TimelineStore try? load) — a one-line-per-field decodeIfPresent guard fixes it. The remaining mediums are live-bio quality drift (camera ÷200 vs BLE/HealthKit ÷100 HRV scale; HealthKit fabricating RMSSD from averaged HR and discarding real SDNN; the 90 s freshness gate measured from sample time dropping valid at-rest Watch readings) and a clip-export A/V bug (VideoMuxer keeps video head but audio tail past 30 s → desync + lost footage). Lows are defensive/robustness gaps. Recommendation: take the AUv3 COW malloc as the next Ralph cycle while AUv3 hardening is hot, then the SynthPatch decoder guard as the top shipped-app data-integrity fix.


**topNextPoint (done → 4d896e5):** Fix the AUv3 audio-thread malloc in Sources/EchoelmusicAUv3/EchoelmusicAudioUnit.swift:476 — `var pad = padRef; pad[i] = 0` fires copy-on-write (~16 KB pad + ~16 KB tex) on every render callback because padScratch/texScratch stay multiply-referenced (self + captured padRef). Replace the COW `[Float]` zero-fill with a uniquely-owned buffer the render block mutates in place (raw UnsafeMutableBufferPointer captured as a pointer, or vDSP_vclr on a truly-unique scratch), matching the app-side DrumSynthVoice/PolySynthVoice withUnsafeMutableBufferPointer pattern. One file, contained, and it directly continues the AUv3-hardening cycle that v10.79.341 just shipped.


## Confirmed findings (ranked) — status


### [critical] AUv3 render block heap-allocates every callback via COW on pre-allocated scratch
- **file:** `Sources/EchoelmusicAUv3/EchoelmusicAudioUnit.swift:476` · dim: audio-thread · status: **FIXED 4d896e5 (RenderScratch sole-ref COW fix)**
- **what:** internalRenderBlock captures `let padRef = self.padScratch` / `let texRef = self.texScratch` (comment lines 47-49 and 415-416), then inside the block does `var pad = padRef` and `var tex = texRef` (lines 476-477) and immediately mutates them with `pad[i] = 0; tex[i] = 0` (line 478) and `synthRef.render(buffer: &pad, ...)`. Because `self.padScratch` AND the captured `padRef` both hold live references to the same Array storage, `pad` is a THIRD reference — the buffer is never uniquely referenced when the first `pad[i] = 0` write happens, so Swift's copy-on-write fires a full heap allocation + me
- **failure:** Load the Echoel AUv3 instrument in Logic Pro / AUM / GarageBand at a 64- or 128-frame buffer. Every render callback allocates ~16 KB (pad) + ~16 KB (tex) on the real-time audio thread; under memory pressure or at small buffer sizes the allocator lock stalls the callback past its deadline, producing audible dropouts/pops and, in the host, xruns — exactly the class of glitch the pre-allocated scratc

### [high] Audio-thread heap allocation via COW on padScratch/texScratch every render block
- **file:** `Sources/EchoelmusicAUv3/EchoelmusicAudioUnit.swift:476` · dim: auv3 · status: **FIXED 4d896e5 (RenderScratch sole-ref COW fix)**
- **what:** The render block captures the scratch arrays by value (`let padRef = self.padScratch` / `let texRef = self.texScratch`, lines 415-416) and then does `var pad = padRef` / `var tex = texRef` (lines 476-477) and mutates them (`pad[i] = 0`, then `synthRef.render(buffer: &pad, ...)`). The inline comment on line 475-476 claims 'captured by value — COW safe since we own them', but this is WRONG: `self.padScratch`/`self.texScratch` remain live stored properties for the AU's lifetime, so the backing buffer's reference count is always >= 2 (self + captured `padRef`), plus 3 with the local `pad`. Swift's
- **failure:** On every audio render callback (e.g. 128-frame block at 48 kHz => ~375 callbacks/sec), `var pad = padRef; pad[0] = 0` finds the buffer shared with self.padScratch (refcount > 1) and mallocs a new 4096-Float array on the real-time thread, risking priority-inversion / dropouts under load — the exact class of violation the DSP-isolation and audio-thread rules forbid.

### [medium] RMSSD→hrvNormalized normalization scale diverges by source (÷200 camera vs ÷100 BLE/HealthKit)
- **file:** `Sources/Echoelmusic/Bio/CameraRPPGBioPublisher.swift:716` · dim: bio-correctness · status: **OPEN**
- **what:** The camera path normalizes RMSSD as `analyzer.rmssd / 200.0` (line 716) while PolarH10BioPublisher.swift:166 uses `rmssd / 100.0` and EchoelBioEngine.swift:119/418 use `rmssdNormalizationMax = 100.0`. HRVMetrics.swift exists precisely so every source computes the SAME metric the SAME way, but the [0..1] normalization that lands in `BioSampleFrame.hrvNormalized` silently diverges. That normalized field is not cosmetic: it drives DDSP brightness/reverb/harmonic modulation (`hrvVariability` in applyBioReactive), the UI HRV readout, and the ADM-OSC elevation mapping (ADMOSCSender.admMessages: `hvr
- **failure:** A resting user with a true RMSSD of ~60 ms yields hrvNormalized 0.6 on a BLE strap but 0.3 on the camera for the identical physiology; switching bio source mid-session halves the HRV-driven brightness/elevation with no real change in the body — the 'same body, different sound depending on sensor' inconsistency.

### [medium] HealthKit hrvNormalized driven by RMSSD fabricated from AVERAGED HR, discarding the real SDNN measurement
- **file:** `Sources/Echoelmusic/Bio/EchoelBioEngine.swift:418` · dim: bio-correctness · status: **OPEN**
- **what:** processHeartRateSamples reconstructs a fake RR interval per HR sample as `60000 / max(bpm,40)` (line 334) from AVERAGED HealthKit HR samples that arrive minutes apart — these are not beat-to-beat intervals. Once ≥5 accumulate, calculateRMSSD (line 402) computes RMSSD over them and overwrites `snapshot.hrvNormalized` (line 418). Meanwhile processHRVSamples has the genuine HealthKit SDNN but only uses it while `rrIntervals.count < 5` (line 372). So after the first few beats the real SDNN measurement is permanently discarded in favor of an RMSSD whose 'successive differences' are between HR readi
- **failure:** Apple Watch user at rest: after 5 HR syncs the HRV shown/used collapses toward ~0 (successive averaged-HR-derived RR differences are tiny), so the HRV-driven timbre flatlines and the strip shows a near-zero HRV even though the Watch's real SDNN was valid and available.

### [medium] HealthKit freshness gate compares against sample measurement time, not receipt time — valid wrist readings dropped as stale
- **file:** `Sources/Echoelmusic/Bio/EchoelBioEngine.swift:341` · dim: bio-correctness · status: **OPEN**
- **what:** processHeartRateSamples sets `snapshot.timestamp = latestSample.startDate` (lines 335/341), i.e. the sample's original MEASUREMENT time. HealthKitBioPublisher.swift:99 forwards that as the BioSampleFrame timestamp, and EngineBus.usableBio()/freshBio() compare it against the source freshness window (90 s for .healthKit/.watch). Because HealthKit/Watch batches and syncs HR sporadically, a freshly-synced sample can already carry a startDate several minutes in the past, so `age = now - startDate` exceeds 90 s and the frame is rejected as stale the instant it arrives — the exact opposite of the 90 
- **failure:** Resting user, Watch writes HR every ~5 min and delivers a sample whose startDate is 3 min old; usableBio()/freshBio() see age 180 s > 90 s and return nil, so the composer/visual treat the wrist as disconnected and the HR path intermittently goes dead despite live, valid data.

### [medium] Mono-voice note-priority bug: releasing an older held key silences a newer held note
- **file:** `Sources/EchoelmusicAUv3/EchoelmusicAudioUnit.swift:450` · dim: auv3 · status: **OPEN**
- **what:** The AU is monophonic but keeps NO held-note stack. `EchoelMIDIDecode.action` returns a bare `.noteOff` (EchoelMIDIDecode.swift line 45-47) that discards the note number, and the render walk (lines 450-451) calls `synthRef.noteOff()` on ANY note-off, unconditionally putting the envelope into `.release` (EchoelDDSP.noteOff, line 806-810). So the classic legato/overlap sequence — press A (noteOn A), press B while still holding A (noteOn B, voice now sounds B), release A — decodes the A note-off as `.noteOff` and releases the voice even though B is still held, silencing the newer note. The `isNote
- **failure:** Host plays overlapping notes A then B, releases A first (routine legato phrasing). The A note-off silences the voice mid-note-B, producing an audible gap/dropout instead of B continuing — the instrument stops sounding while a key is still held.

### [medium] fullState / value-provider reads live synth-derived state, so coherence/hrv/breathPhase/baseFrequency do not round-trip
- **file:** `Sources/EchoelmusicAUv3/EchoelmusicAudioUnit.swift:330` · dim: auv3 · status: **OPEN**
- **what:** fullState.get stores `p.value` per identifier (line 330). But reading `coherenceParam.value` invokes `implementorValueProvider` (lines 235-247), which for .coherence returns `synthRef.harmonicity`, for .hrv returns `synthRef.brightness`, for .breathPhase returns `synthRef.amplitude`, and for .baseFrequency returns `synthRef.frequency`. Those synth fields are continuously overwritten by `applyBioReactive` (EchoelDDSP.swift: harmonicity line 1420, brightness 1368, amplitude 1407) and `frequency` is overwritten by every MIDI noteOn (line 778). So saving a preset during playback captures the momen
- **failure:** User sets coherence=0.5 and base frequency=220 in the host, biofeedback/MIDI runs, then saves a user preset. The stored 'coherence' is the live modulated harmonicity (e.g. 0.44) and 'baseFrequency' is the last MIDI note (e.g. 330 Hz). Reloading the preset restores different values than were set — state does not round-trip, and host automation displays the wrong parameter readback while playing.

### [medium] SynthPatch has no defensive decoder — non-optional timbreProfile/timbreBlend added after ship break old-document loading
- **file:** `Sources/Echoelmusic/DSP/SynthPatch.swift:56` · dim: determinism · status: **OPEN**
- **what:** SynthPatch is `Codable` with a fully SYNTHESIZED decoder (no custom `init(from:)`, no `CodingKeys`). Its later-added acoustic-timbre-transfer fields `timbreProfile: String` (line 56) and `timbreBlend: Float` (line 57) are NON-optional, so synthesized decoding emits `try container.decode(String.self, forKey: .timbreProfile)` — which THROWS when the key is absent. This directly violates the codebase's own stated backward-compat convention: the neighbouring fields added in the same later era were deliberately made Optional for exactly this reason (`unisonVoices`/`unisonDetuneCents` line 50: "Opti
- **failure:** A user saved synth patches (or a project with a per-track patch) in a build that had SynthPatch persistence but predated the timbre-transfer fields. On upgrade the stored JSON lacks `timbreProfile`/`timbreBlend`. Library load: JSONDecoder().decode([SynthPatch]) throws → PatchStore falls back to `?? []` → the user's entire saved-patch library disappears with no error. Project load: TimelineLane.pat

### [medium] Recordings >30s: muxer keeps the video HEAD but the audio TAIL → wrong audio + lost video, contradicting its own 'last 30s' comment
- **file:** `Sources/Echoelmusic/Video/VideoMuxer.swift:41` · dim: clip-export · status: **OPEN**
- **what:** VisualRecorder.stop() (line 61) calls captureRecentMixAudio(seconds: duration); captureRecent caps frames at ringCapacity (RetroCapture.swift:298, 30s@48k / ~32.6s@44.1k), so for a >30s recording audioURL holds only the LAST ~30s of the mix. VideoMuxer.mux then does duration=CMTimeMinimum(vDur,aDur)=~30s and inserts BOTH tracks at .zero over range [0,30] (lines 41-43). That pairs the FIRST 30s of video with the LAST 30s of audio and discards the video tail — a ~30s A/V desync plus content loss. VisualRecorder.swift:59's comment ('longer videos get their last 30 s') is simply wrong about what t
- **failure:** User records a 50s visual take. audioURL = mix seconds 20–50. Video = 0–50s. Muxer emits a 30s clip = video 0–30 over audio 20–50: the picture and sound are 20s out of phase and the final 20s of the performance are gone, with no warning.

### [low] CameraCapture.onFrame closure is read on captureQueue while reassigned on the main actor (ref-counted data race)
- **file:** `Sources/Echoelmusic/Video/CameraCapture.swift:547` · dim: concurrency · status: **OPEN**
- **what:** `onFrame` is a `nonisolated(unsafe) var onFrame: ((CVPixelBuffer) -> Void)?` — a reference-counted closure. It is read (and retained) on the capture queue inside `captureOutput(...)` via `onFrame?(pixelBuffer)` (line 547) while it is assigned on the main actor: set in `CameraRPPGBioPublisher.start` and cleared with `capture.onFrame = nil` in `CameraRPPGBioPublisher.stop`. The file's own comment only justifies the atomic-width `lastFrameTime` Double race as benign; the closure race is unaddressed. Unlike a Double, concurrent read+reassignment of a heap-allocated closure is a true data race (ARC
- **failure:** User stops the rPPG source while a frame is in flight. `CameraRPPGBioPublisher.stop()` calls `capture.stop()` (which only *async*-dispatches `session.stopRunning()` to sessionQueue) and then immediately executes `capture.onFrame = nil` on the main actor. Before the async stopRunning takes effect, the capture session delivers one more buffer on captureQueue; `captureOutput` reads/retains `onFrame` 

### [low] SPSCQueue drop-oldest path mutates `head` from the producer, racing the consumer's non-atomic head store
- **file:** `Sources/Echoelmusic/Core/SPSCQueue.swift:153` · dim: concurrency · status: **OPEN**
- **what:** In `enqueue`, the queue-full branch advances `head` from the PRODUCER thread via `OSAtomicCompareAndSwap64Barrier` (drop-oldest). But the CONSUMER's `dequeue` publishes `head` with a plain non-atomic store: `head.pointee = (currentHead + 1) & mask` (line 226). A CAS on one side does not synchronize with a plain store on the other, so under queue-full the producer's head advance and the consumer's head advance can interleave and one can clobber the other. This deviates from the strict single-owner-of-`head` invariant the class documents ("Exactly ONE consumer thread may ... dequeue"). Because `
- **failure:** A host streams a very dense MPE burst that fills the 128-slot controllerEvents queue. The main-actor producer (MIDIBusPublisher) hits the full branch and CAS-advances `head` in the same window the audio-thread consumer (BioReactiveSynthVoice render) executes its plain `head.pointee = ...` store. The two head updates interleave, causing one MIDI event to be silently dropped or a stale event to be d

### [low] BLE RR intervals fed to time-domain HRV (RMSSD/SDNN/pNN50) without physiological filtering — artifact beats spike HRV
- **file:** `Sources/Echoelmusic/Bio/PolarH10BioPublisher.swift:418` · dim: bio-correctness · status: **OPEN**
- **what:** didUpdateValueFor appends raw parsed RR intervals (lines 418-422) with no range/ectopy filter, then startPublishing (line 157-173) feeds the whole window to HRVMetrics.rmssd/sdnn/pnn50, which also do not filter (HRVMetrics.swift). HRVCoherence.tachogram DOES drop intervals outside 250–2000 ms (HRVCoherence.swift:204), so the spectral coherence is protected — but the time-domain RMSSD/SDNN/pNN50 that populate hrvRMSSDms/hrvSDNNms/hrvPNN50 (and hrvNormalized) are not. A single dropped or ectopic beat produces an RR roughly 2× normal, whose squared successive difference dominates the RMSSD sum.
- **failure:** One missed beat on a Polar strap yields an RR of ~1600 ms next to ~800 ms neighbours; RMSSD jumps by tens of ms for that window, hrvNormalized and the OSC /heart/rmssd value spike, and the HRV-driven brightness/reverb lurches on a pure sensor artifact rather than physiology.

### [low] BeatPlayer.trigger draws non-seeded Float.random per sequenced hit — drum-velocity is not reproducible across playback/export
- **file:** `Sources/Echoelmusic/Sequencer/BeatPlayer.swift:254` · dim: determinism · status: **OPEN**
- **what:** `let base = respectMix ? gain * (1 - Float.random(in: 0...Self.humanizeDepth)) : gain` uses the global non-seeded `Float.random` on every sequenced drum trigger. This is deliberate live-humanize (the inline comment justifies it, and it runs on the pattern timer, not the audio thread — the audio-thread rule is respected). It is NOT a break of the persisted composition (BioComposer's `drumSteps`/`drumAccents` grid remains fully seeded and deterministic). Flagged only because it means the drum layer's per-hit velocity differs run-to-run, so a WAV captured from live playback of 'the same take' is 
- **failure:** Export the same generated take to WAV twice: the melodic notes are identical (seeded) but the drum-hit velocities differ each render because Float.random is re-rolled per trigger, so the two WAVs are not byte-identical.

### [low] Non-finite (inf/NaN) samples silently poison LUFS gain → the whole shared clip is crushed −12 dB or written with NaN, no user signal
- **file:** `Sources/Echoelmusic/Audio/SingleExport.swift:197` · dim: clip-export · status: **OPEN**
- **what:** measureLUFS accumulates sumOfSquares via vDSP_measqv with no isFinite scrub (lines 189-197). One inf sample → sumOfSquares=inf → rmsOverall=inf → the guard `rmsOverall > 0.000001` (line 198) passes → dBFS = 20*log10f(inf) = inf → returns inf → normalizeGain = targetLUFS − inf = −inf → clamped to −12 dB (line 93): the entire loop ships 12 dB too quiet. One NaN sample → sumOfSquares=NaN → rmsOverall=NaN → the same guard is FALSE → returns −60 → +12 dB boost applied to still-NaN audio (renderWithGain vDSP_vsmul at line 283 also has no scrub, so NaN/inf passes straight into the exported WAV/AAC). 
- **failure:** A hosted master-bus effect emits a single inf on one frame during the captured window. The exported 'shareable' WAV is normalized to −12 dB (near-inaudible) yet the app reports export success and hands the user the file.

### [low] VisualRecorder.stop() returns nil on writer/setup failure and the caller shows nothing — record→stop with no clip and no error
- **file:** `Sources/Echoelmusic/Video/VisualRecorder.swift:63` · dim: clip-export · status: **OPEN**
- **what:** If the AVAssetWriter setup throws, VideoRecorder.ingest sets recordState=.error and disarms (VideoRecorder.swift:164-168); stopRecording() then hits `guard recordState == .recording else { return nil }` (line 90) and returns nil, discarding the .error message. VisualRecorder.stop() `guard let videoURL else { return nil }` (line 63) propagates nil, and the call site `if let url = await visualRecorder.stop() { visualShare = ... }` (EchoelStudioView.swift:766) does nothing on nil — no alert, no toast. Same silent path if no Metal frame was ever captured (writer never built → stopRecording returns
- **failure:** Disk is full (or Documents/Videos unwritable). First frame's setUpWriterLocked throws, recordState=.error. User taps stop: stop() returns nil, the record button flips back to idle, no clip appears, no error is shown — the failure is invisible.

### [low] Muxed-clip A/V tail drift: audio window is sampled AFTER finishWriting completes, offsetting it from the video's last frame; audioURL CAF also leaks
- **file:** `Sources/Echoelmusic/Video/VisualRecorder.swift:61` · dim: clip-export · status: **OPEN**
- **what:** stop() awaits video.stopRecording() (which awaits AVAssetWriter.finishWriting, typically 50–200ms) BEFORE reading recordedSeconds() and calling captureRecentMixAudio at line 61. captureRecent grabs the ring window ending 'now' — i.e. after the finalize latency — so the audio tail ends later than the video's last frame by that latency, a small but real lip-sync offset the 'ends ≈ the video end' comment understates. Separately, the temporary audioURL CAF (Documents/Recordings) is never deleted after mux (only the silent videoURL is removed at line 72), so every visual recording leaves an orphan 
- **failure:** On a device where finishWriting takes ~150ms, every muxed clip has audio shifted ~150ms late relative to picture, and the Recordings folder accumulates one undeleted echoel_*.caf per capture.