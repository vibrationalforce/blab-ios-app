build: 2026-07-12 v10.79.182 (Start erzwingt kein Visual-Fenster mehr · Visual-Zustand steht jetzt im Log)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notes: Deine Anweisung umgesetzt:
  · START = NUR MUSIK: das Visual-Fenster geht beim Biofeedback nicht
    mehr automatisch auf (vorher zwang Start es in Vollbild). Der
    Monitor oben rechts bleibt der lebendige Hinweis — du öffnest das
    Fenster, wann DU willst.
  · LOG-RÄTSEL GELÖST: die visual:-Diagnosezeile ging bisher in einen
    anderen Kanal als dein kopierbares Log — darum fehlte sie IMMER,
    auch bei offenem Fenster. Ab diesem Build steht sie in jedem Log
    (alle ~5 s, wenn das Fenster offen ist): ob Noten ankommen,
    welcher Ton die Farbe treibt, welche Detail-Stufe rendert.
  · Dein 2286-Log BESTÄTIGT den v180-Fix: während der iOS-Pausen
    keine blinden Belichtungs-Locks mehr, und nach dem Resume saubere
    Wieder-Erfassung bis Konfidenz 0,87 bei Puls 58. Sehr gute Läufe
    insgesamt (0,90 · 0,87 · 0,79).
  · In v181 (vorheriger Build): F ist wieder eine Farbe (Purpur),
    Akkorde färben das Visual — bitte dort einmal F im Raster und
    einen Akkord im offenen Visual anschauen.
  Als Nächstes (dein Vorschlag, angenommen): EchoelVideo- und
  EchoelLux-Monitore neben dem BioSynth-Monitor im Header.
  Gates: Quick Test + Xcode Compile Check + CI/CD auf 561d1ab.
  NEEDS-FOUNDER-VERIFY (Start ohne Auto-Visual, visual:-Zeilen im Log).
