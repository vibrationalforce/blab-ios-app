build: 2026-06-30 v10.76.51 (Steadier pulse readout — trust autocorrelation earlier)
branch: claude/piano-roll-clip-view-wozlie
notesT: From three device pulse logs: the engine locks cleanly with good contact (one read
  held 50–51 bpm at conf 0.95; another hit acf 0.83 at 64 bpm). The visible wander (bpm
  jumping 62→106→72) tracked weak contact where discrete peak-counting gets noisy — but the
  autocorrelation estimate stayed steady (50–65) the whole time, and was already reliable down
  to acf≈0.4, a band the smoother previously ignored (it only trusted autocorrelation at ≥0.5).
  FIX: lower that trust gate 0.5→0.4 so the steady fundamental anchors the readout from the
  moment it's reliable, instead of letting the noisy peak-count lead on moderate signals. Pure
  change + unit tests; the octave/EMA guards upstream still protect against a wrong harmonic.
  NOTE: one log still showed the camera frame rate collapsing (rate→6.6) — that's the fps fix
  already in 10.76.50, which this device hadn't received yet. Test 10.76.51 once installed: a
  resting pulse readout should hold steadier (fewer big jumps) on a normal fingertip contact.
