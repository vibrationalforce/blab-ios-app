build: 2026-06-21 v10.34.28 (AUv3 host — your loaded plugin plays the song)
branch: claude/piano-roll-clip-view-wozlie
includes: A hosted AUv3 instrument now plays your whole composition, not just the preview keyboard. Load a plugin instrument in Tools ▸ Plugins, hit play, and the piano roll / arrangement drives it over host-MIDI from the same clock as Echoel's own voices — in lock-step with the song. It layers with Echoel's built-in voice for now (a "use the plugin instead" toggle is a quick follow-up). Stays a no-op until you load a plugin; clean note-offs on stop. Builds on this week's AUv3 discovery + load steps. Still to come: hosting effects in a channel insert, showing a plugin's own interface, saving plugin state, and routing external MIDI-in into the hosted instrument. Verified: Xcode Compile Check + CI green.
