build: 2026-06-19 v10.34.5 (Sound & texture library bar — preset/community parity)
branch: claude/piano-roll-clip-view-wozlie
includes: the inline Sound & texture panel now has the same library bar as Mood and FX — one load menu (genre default · your saved + factory sounds with favorites first · community) plus save-as / favorite / save / delete / submit, wired to the shared PatchStore so favorites/recents match the deep Sound Editor. Completes the preset/community pattern across Mood + Sound & texture (ROADMAP #8). Plus everything from v10.34.4 (saveable Moods with 8 curated factory moods + community loop) and v10.34.3 (FX params unified on EchoelValueField, app-wide parameter-row standard, AudioEngine strict-concurrency fix). Verified: ci.yml (SPM) green + xcode-compile-check (AUv3 included) green through 8c985c8.
