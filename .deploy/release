build: 2026-07-15 v10.79.237 (Playhead läuft jetzt flüssig — zwischen den 16teln interpoliert)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Letzter Punkt aus deinem Timeline-Video: "läuft auch etwas unflüssig."

  URSACHE: der Transport zählt nur in 16tel-Schritten, also sprang der Playhead pro
  Sechzehntel weiter — bei ruhigem Tempo als Ruckeln sichtbar.

  FIX: der Playhead gleitet jetzt mit 60 fps ZWISCHEN den Schritten. Er interpoliert vom
  Moment, in dem der aktuelle 16tel-Schritt begann, sanft zum nächsten — die Bewegung
  ist durchgehend flüssig statt gestuft. Beim Loop-Zurückspringen SNAPPT er sauber auf
  den Anfang (kein Rückwärts-Wischen). Beim Stoppen/Ziehen zeigt er exakt die Position,
  ohne Glide. Steht er still, kostet die Animation nichts (pausiert).

  SICHER: ui-state-reviewer CONFIRMED — nur das Playhead-Leaf rendert mit 60 fps neu
  (es ist ein Overlay NEBEN dem Raster, kein Vorfahr der Clips/Menüs → Freeze-Regel
  gewahrt; der gleitende Pfad liest nicht mal den 16tel-Zähler, nur den Zeit-Anker),
  Loop-Wrap snappt, kein Vorwärts/Rückwärts-Sprung im Normallauf, idle 0 fps. Gates
  v234–v236 grün.

  DAMIT ist dein Timeline-Video komplett: Playhead greifbar (v232) · ziehbar mit Snap
  (v230) · Lücke am Griff zu (v235) · erneut schneidbar (v235) · Clip vorne trimmbar
  (v236) · Playhead flüssig (v237). Plus separat: ein Play-Knopf (v234), Bio auf den
  Pill (v233).

  Reihenfolge: v235 Playhead-Lücke + Schnitt · v236 Clip vorne trimmbar · v237 Playhead
  flüssig.
