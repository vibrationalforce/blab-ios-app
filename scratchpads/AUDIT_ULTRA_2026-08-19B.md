# ULTRA-AUDIT 2026-08-19B — 5 Teams, 15 Agenten (2,89 M Subagent-Token, 696 Werkzeugaufrufe)

Erzeugt vom `Workflow`-Lauf `wf_6e71cf3c-711`. Jeder Worker war auf lesende Inspektion
festgenagelt, jeder Lead hat verworfen, was er nicht selbst nachgegreppt hat.

**10 CRITICAL · 28 WARN.** Verworfen von den Leads: 21 Worker-Befunde.

⚠️ ERLEDIGT SEIT DEM AUDIT: die drei roten Wächter (Guard-Integrity #0, Audio-Monitoring #0)
und die #625-Brotkrume (Audio-Monitoring #1) sind mit **#631** repariert. Beim Abarbeiten
eines Eintrags GEGEN DEN BAUM prüfen — dieses Dokument altert wie jedes andere.


---

## Team Session-Claims

**Nächste Scheibe laut Lead:** Sweep the "Demo is unlabelled" claim to its second and third homes — one law: a registration is only closed when it is retracted at EVERY site that states it. Three files, no behaviour change: (1) Sources/Echoelmusic/Bio/BioSimulator.swift — extend the existing #629b ⛔ block upward over :31-34 and :53-58, replacing the "no user-facing Demo string" property with the three render sites (BioStripView:712, HeaderMonitors:168, LiveColaboView:439) and narrowing #462 to the OSC/ADM/Art-Net/sACN half only; (2) Sources/Echoelmusic/EchoelmusicApp.swift:177-183 — the same retraction, verbatim, since that comment predicts this exact "look for the SECOND place" failure and then commits it; (3) Tests/CISmoke/TheDemoSourceIsMarkedWhereItRendersTests.swift — add one needle asserting that no file under Sources/ carries the phrase "no user-facing" alongside "Demo", so the third recurrence turns the blocking bundle red instead of aging silently. It closes both CRITICALs at once, touches zero executable code, and installs the guard the two prior retractions each lacked.


### SESS-0 · CRITICAL — Two source files still assert "there is no user-facing \"Demo\" string anywhere in Sources/" — this session shipped three render sites, and the sentence quotes the grep that refutes it

- **Beleg:** Sources/Echoelmusic/Bio/BioSimulator.swift:31-34 reads: "There is no user-facing \"Demo\" string anywhere in `Sources/` — every hit is a comment. … `git grep -nw Demo -- Sources` is the check; the count is not worth restating, the property is." The identical property claim sits a second time at Sources/Echoelmusic/EchoelmusicApp.swift:177-179 ("It is NOT labeled: there is no user-facing \"Demo\" string in `Sources/`"), inside a comment that itself names "exactly the 'look for the SECOND place' failure". Running the cited check — `git grep -n 'Text("Demo")' -- Sources` → Sources/Echoelmusic/Studio/BioStripView.swift:712, Sources/Echoelmusic/Studio/HeaderMonitors.swift:168, Sources/Echoelmusic/Studio/LiveColaboView.swift:439. All three were added inside this session (2aa621f #627, e1adee1 #627b, 963f9f6 #629). The declarations are real, not dead: BioStripView.swift:709-720 `demoTag`, reached from `sourceControl` at :649-650; HeaderMonitors.swift:167 `if synthetic {`; LiveColaboView.swift:438 `if synthetic == true {`. The one retraction this session landed in BioSimulator covers a DIFFERENT sentence fifteen lines lower (:44-50, "⛔ #629b: this sentence went on 'on the header pill … with no source marker at all' — FALSE since #627/#627b"), and `git show --stat 5c2680c` lists four files, none of them EchoelmusicApp.swift.
- **Warum es zählt:** A next session reading either header concludes the demo source is unlabelled on screen and either rebuilds #627 or files it as an open defect. Worse, the prose instructs the reader to run a grep, and the grep now prints the opposite of the prose — the EchoelModalBank failure mode CLAUDE.md records ("ein Vermerk, der ein `grep` ZITIERT, altert schneller als einer, der eine Tatsache behauptet"). This is the same class as the CRITICAL #629b itself booked one commit earlier, reproduced one file over.
- **Billigster Fix:** Extend the existing #629b ⛔ block upward to cover BioSimulator.swift:31-34, and write the twin retraction at EchoelmusicApp.swift:177-183, both naming the three sites (BioStripView:712, HeaderMonitors:168, LiveColaboView:439) and the slices that added them.

### SESS-1 · CRITICAL — #462 is registered in two source files as one open founder question — "a Demo label on the strip AND a synthetic marker on the OSC egress" — but the strip half shipped in #627; only the wire half is open

- **Beleg:** Sources/Echoelmusic/Bio/BioSimulator.swift:53-58: "So a demo session shows a confident heart rate and \"HRV 50 ms\" beside a source cell reading \"No signal\" — which reads as ABSENCE, not as SYNTHETIC. … Whether to add a Demo label on the strip and a synthetic marker on the OSC egress is a founder question (#462)". Same conjunction at Sources/Echoelmusic/EchoelmusicApp.swift:180-183. Against the tree: BioStripView.swift `sourceControl` branches `else if isSynthetic { demoTag }` at :649-650, which sits AHEAD of `noSignalTag` at :667 — a synthetic frame can no longer reach the "No signal" cell (`isSynthetic` declared :846). The OSC half is genuinely open and was verified: Sources/Echoelmusic/Sync/BioEgressPolicy.swift:54 `case .ble, .cameraPPG, .faceCam, .fallback:` → `return true`, and `git grep -n 'source' Sources/Echoelmusic/Sync/OSCSender.swift` shows no source-bearing address. The correct split already exists two files away, in this session's own guard head: Tests/CISmoke/TheDemoSourceIsMarkedWhereItRendersTests.swift:57-61 ("OSC / ADM-OSC / Art-Net / sACN — STILL OPEN"), and in scratchpads/SESSION_LOG.md:10573 ("OSC/ADM/Art-Net/sACN-Quelle (#462, offen)"). The commit body of 5c2680c states the split explicitly ("die OSC/ADM/Art-Net/sACN-Hälfte bleibt WAHR und offen und ist deshalb abgespalten") — but performed it only in the guard head.
- **Warum es zählt:** A conjunctive registration that is half-closed reads as wholly open, and these two files are where a session lands when it greps `#462`. The log, the guard and the source now disagree about what #462 covers; the source is the copy that is read first and the one that says the strip label is unbuilt.
- **Billigster Fix:** Narrow both sentences to the wire in the same edit pass as the finding above: mark the strip/pill half CLOSED (#627, pointing at BioStripView.swift:709-720 and HeaderMonitors.swift:167-172) and keep #462 aimed only at BioEgressPolicy.swift:54 plus the OSCSender address set.

### SESS-2 · WARN — A blocking-bundle guard head books #336 as "still open" — it is fixed in ModulationEngine and has its own nine-assertion guard

- **Beleg:** Tests/CISmoke/BioApplyRateIsTheDedupedRateTests.swift:8: "… und #336 (`ModulationEngine`'s `dt`, still open). None of them was a coding mistake". Measured closed: Sources/Echoelmusic/Core/ModulationEngine.swift:100-108 carries "⛔ #336 — WHAT USED TO BE HERE WAS A CONSTANT `tickSeconds: Float = 0.1` … ⭐ THE FIX IS #337'S SHAPE AT ONE SITE: measure `dt` from the two frames themselves", backed by the stored property `lastSmoothedFrameTimestamp` at :98. Dedicated guard: Tests/CISmoke/SmoothingStepsTheFrameGapNotThePollRateTests.swift, `grep -c "func test"` → 9. Not touched in this session (`git log --oneline dadcf65..HEAD -- Tests/CISmoke/BioApplyRateIsTheDedupedRateTests.swift` → empty), so it is pre-existing debt, not a claim this session made.
- **Warum es zählt:** Reporting a closed item as open is this repo's named worst defect, and this instance sits in the bundle that blocks merges. It also weakens its two neighbours by association: the line's job is to name the three defects the "10 Hz" figure caused, and one of the three is wrong. Contained, though — the ModulationEngine site itself carries a loud ⛔ block, so a session that follows the pointer finds the fix immediately.
- **Billigster Fix:** One line: "#336 (`ModulationEngine`'s `dt`, closed — `lastSmoothedFrameTimestamp`, guarded by `SmoothingStepsTheFrameGapNotThePollRateTests`)".

### SESS-3 · WARN — #623b's commit body and guard head grade a parent-RED assertion as FORWARD — the self-refuting sentence #625b retracted one commit later and deferred here

- **Beleg:** Commit 149ca63 (#623b) body line 54: "Transkription beidseitig: die neue Bedingung ist die einzige FORWARD-Behauptung, rot auf dem Elternbaum, grün hier". In the tree: Tests/CISmoke/AnOpenedTakeExportsItsWholeArrangementTests.swift:38 — "claims 4-6 are the FORWARD half and are red on the parent". Tests/CISmoke/CLAUDE.md:110-111 defines the term the other way: "How many are FORWARD guards — they drive a symbol this same commit creates and could never have been red." Commit bae1672 (#625b) retracts the identical wording in its own guard (Tests/CISmoke/MonitoringCannotStrandTheEngineStoppedTests.swift:40-42, "a sentence that refutes itself") and defers the rest at body line 63: "Dieselbe Fehlbenennung steht in den Köpfen von #622 und #623 und wird dort bei der nächsten Berührung korrigiert". `git log --oneline dadcf65..HEAD -- Tests/CISmoke/AnOpenedTakeExportsItsWholeArrangementTests.swift` → 149ca63, 9bff687 only; bae1672 is later in the session and did not touch it, so the deferral is unredeemed at HEAD.
- **Warum es zählt:** §3 exists so a header states what a guard actually proved. Calling parent-red assertions FORWARD tells the next reader they could never have failed — i.e. that they prove nothing about the defect — which removes the only evidence that #623's install-order fix is guarded. Mitigating: the debt is explicitly registered by #625b rather than hidden.
- **Billigster Fix:** At AnOpenedTakeExportsItsWholeArrangementTests.swift:38 change "the FORWARD half" to "the REGRESSION half" and paste the ⛔ retraction #625b already wrote at MonitoringCannotStrandTheEngineStoppedTests.swift:40-42; #622's head carries the twin.

### SESS-4 · WARN — V1-plan commit: "EchoelFXChain wird an genau ZWEI Stellen konstruiert" — there are four

- **Beleg:** Commit 5230587 body line 6: "BEFUND: EchoelFXChain wird an genau ZWEI Stellen konstruiert (die zwei Synthesestimmen); in AudioEngine null Treffer." Measured: `git grep -n "EchoelFXChain(" -- Sources` → Sources/Echoelmusic/Studio/FXCuratedLibrary.swift:31, Sources/Echoelmusic/Studio/FXCuratedLibrary.swift:106, Sources/Echoelmusic/Tools/BioReactiveSynthVoice.swift:154, Sources/Echoelmusic/Tools/PolySynthVoice.swift:333 — FOUR. The load-bearing half is TRUE: `git grep -c "EchoelFXChain" -- Sources/Echoelmusic/Audio` → no files, the mic chain is raw. The two extra sites are non-audio (preset capture in FXCuratedLibrary). The plan file uses the safer verb; the commit message hardened it into a falsifiable count.
- **Warum es zählt:** This is the inventory line of a plan whose own mitigation is "eine eigene FXChain-Instanz für die Stimme" — a decision that depends on knowing every chain instance. A session executing V1a runs the grep, gets 4 against a stated 2, and must re-derive the whole survey to learn which half was wrong.
- **Billigster Fix:** Restate wherever the plan is next quoted: "two RENDERING sites (the two synth voices); two further non-audio sites in FXCuratedLibrary that capture presets; zero in AudioEngine."

### SESS-5 · WARN — #628's diagnosis says "only three exits return false" — on the build the founder screenshotted there were two; the third is created by #628 itself

- **Beleg:** Commit e3c9d50 body: "`engageInputMonitoring` hat `setInputMonitoring(true)` erreicht, und das gab `false` zurück. Genau drei Ausgänge tun das: ein werfender Claim, ein 0-Hz-Eingangsformat, ein werfender Neustart." The same sentence was shipped into the source at Sources/Echoelmusic/Audio/AudioEngine.swift:1705 ("Only three exits do that: a throwing claim, a 0 Hz input format, and a throwing restart"). On the parent tree, `setInputMonitoring` begins at line 1653 and `git show e3c9d50^:Sources/Echoelmusic/Audio/AudioEngine.swift | sed -n '1650,1830p' | grep -n "return false\|catch"` yields the claim `catch` at relative :41 with NO return (log only), and `return false` only at relative :76 (format guard) and :129 (restart catch). The commit's own FIX list item 2 says so: "Der werfende Claim KEHRT ZURÜCK, statt nur zu loggen".
- **Warum es zählt:** The enumeration is the evidence that narrows the founder's screenshot to the format read, and it is a claim about the shipped build, where it is wrong — and it contradicts the fix list three paragraphs below it in the same message. A session triaging an older diag log would hunt for a claim-return exit that did not exist in that build.
- **Billigster Fix:** At AudioEngine.swift:1705: "two exits returned false on the shipped build (the 0 Hz format guard and the throwing restart); #628 adds the third by making the claim return."

### SESS-6 · WARN — #628's new source comment says the restart is "unconditional on `wasRunning`" — the code gates on it, and the commit body says the opposite

- **Beleg:** Sources/Echoelmusic/Audio/AudioEngine.swift:1721 (added by e3c9d50): "pausing a moment earlier changes no state the rest of the branch reads, and the restart at the end is unconditional on `wasRunning`." The restart at Sources/Echoelmusic/Audio/AudioEngine.swift:1815-1817 is `if wasRunning { armTimingInstrument(); do { try masterEngine.start() } … }`. The German original states it correctly: "der Neustart am Ende hängt unverändert an `wasRunning`" — the English rendering inverted "unverändert" into "unconditional".
- **Warum es zählt:** That sentence is the entire risk argument for shipping an unmeasured hypothesis ("costs nothing if wrong"). Read at face value it says the restart cannot be skipped — which is precisely the belief that produced #625's bug, where `if wasRunning` latched false and left the engine stopped. An auditor of this branch would skip the one line that matters.
- **Billigster Fix:** Reword AudioEngine.swift:1721 to "the restart at the end still gates on `wasRunning`, unchanged by this slice".

### SESS-7 · WARN — "B1" names two different open items in two registries, and B12 / V1a are defined in neither board a session is told to read

- **Beleg:** scratchpads/SESSION_LOG.md:10524, :10545, :10555, :10563, :10573 all carry bare "B1" / "B12" / "V1a" in the Offen lines (defined once at :10524 as "B1 (RetroCapture) und B12 (VideoRecorder)"). Their definitions live in scratchpads/AUDIT_ULTRA_2026-08-19.md:48 ("B1 · CRITICAL — RetroCapture route-change reinstall relabels 30 s of old-rate ring history") and :114 ("B12 · CRITICAL — Mid-take resize/rotation feeds wrong-size frames into a writer fixed at first-frame dimensions") — a file committed in ac9ffd7, BEFORE this session (`git log --oneline dadcf65..HEAD -- scratchpads/AUDIT_ULTRA_2026-08-19.md` → empty). scratchpads/BAUSTELLEN_BOARD.md:129 defines a DIFFERENT B1: "| B1 | **a) Externe AUv3 fehlen in der Liste** |", and `grep -n "RetroCapture\|VideoRecorder\|B12\|V1a" scratchpads/BAUSTELLEN_BOARD.md` → empty.
- **Warum es zählt:** The `baustellen` skill drives off BAUSTELLEN_BOARD.md. A session resolving the log's "B1" against that board lands on the AUv3 scan item and works the wrong CRITICAL, while two live CRITICALs have their only definition in a dated audit file no session-start instruction names.
- **Billigster Fix:** Write the namespace once in the Offen line ("AUDIT_ULTRA B1 / B12"), or promote the three items to BAUSTELLEN_BOARD.md rows with non-colliding ids.

### SESS-8 · WARN — The rolling device-probe list decays: thirteen registered probes (2513–2525) left the Offen list without any entry recording an answer

- **Beleg:** Each was registered with an explicit probe list: scratchpads/SESSION_LOG.md:10382 (2521), :10392 (2522), :10419 (2523), :10436 (2524), :10453 (2524 again), :10491 (2525). The carry-over tail shrinks monotonically: :10436 lists "offen: 2523 · 2522 · 2521 · 2519 · 2518"; :10491 lists "Offen: 2524er-Proben · 2523 · 2522"; :10524 lists "Geräteprobe 2526" only; the final Offen line at :10573 carries "Geräteproben 2527/2526". `grep -n "251[2-9]\|252[0-5]" scratchpads/SESSION_LOG.md | grep -i "erledigt\|abgehakt\|bestätigt\|geprüft"` returns nothing recording a founder answer for 2513–2525.
- **Warum es zählt:** Device probes are the only evidence path for changes CI cannot prove (audibility, tap targets, AX reflow). A FIFO that forgets is indistinguishable from a probe that passed, so shipped-but-unverified UI accumulates silently — and the Offen line is exactly what the next session plans from.
- **Billigster Fix:** Keep one append-only probe table (id · build · question · answer/date) instead of re-typing a decaying tail per entry; a probe leaves the list only when a line records the founder's answer.

### SESS-9 · NIT — #629b: "beide Wert-Zellen verzweigen schon darauf" (`bpm > 0`) — one does; the other branches on its own term

- **Beleg:** Commit 5c2680c body: "`bpm > 0` ist die hiesige Fassung von „ein Puls ist da\" (beide Wert-Zellen verzweigen schon darauf)", shipped verbatim into Sources/Echoelmusic/Studio/LiveColaboView.swift:418-419 ("both value cells already branch on it"). The cells: :449 `Text(bpm > 0 ? "… bpm" : "— bpm")` and :452-454 `Text(verbatim: coherence > 0 ? … : "coh —")`. The coherence cell branches on `coherence > 0`.
- **Warum es zählt:** The sentence is the justification for choosing `bpm > 0` as this row's "a pulse is here" predicate. A reader who checks it finds one of two and cannot tell whether the term or the prose is wrong. The expression at :429 is correct, so nothing behaves wrongly.
- **Billigster Fix:** One word at LiveColaboView.swift:418: "each value cell already branches on its own `> 0`."

### SESS-10 · NIT — SESSION_LOG Takt 42 says the #629 guard scans "zwei Ansichts-Aufrufstellen" — it was three when written and is five now

- **Beleg:** scratchpads/SESSION_LOG.md:10562 (committed in 68f8070): "nur die zwei Ansichts-Aufrufstellen sind gescannt (gemessen 0/0/0 → 1/1/1, roh wie gestrippt)" — the parenthetical it carries is itself a triple. Today `grep -n "XCTAssertEqual(lines.filter" Tests/CISmoke/ThePeerSeesWhetherItIsABodyTests.swift` → :181, :185, :189, :208, :213 = five. Commit 5c2680c records the correction as a NIT ("„zwei Aufrufstellen\" waren drei Nadeln und sind jetzt fünf") and applied it to the guard header only; ebf5e69 adds ten lines to SESSION_LOG.md and does not touch :10562.
- **Warum es zählt:** SESSION_LOG is the second thing a session reads. A count that undersells the guard's source-scan half invites the next reader to add needles that already exist, or to trust a smaller scan surface than exists when judging what a refactor may move. Self-disclosing (the parenthetical contradicts the word), hence NIT.
- **Billigster Fix:** Amend :10562 to "fünf Ansichts-Nadeln (drei bei #629, zwei nachgetragen in #629b)" with the grep beside it.

### SESS-11 · NIT — The AlwaysOnBioPanelStrip founder question is recorded in the guard head and the log, but nowhere at the surface that hosts it

- **Beleg:** Registered at Tests/CISmoke/TheDemoSourceIsMarkedWhereItRendersTests.swift:40-46 ("`AlwaysOnBioRow` — DEFERRED … The Bio-panel mount is NOT covered by that argument … Open question for the founder, not a settled deferral") and in scratchpads/SESSION_LOG.md:10555, :10563, :10573 ("Founder-Frage `AlwaysOnBioPanelStrip`"). ⛔ Worker's "sole registration" framing is REJECTED — it is registered twice. What is true: `git grep -n "synthetic\|Demo\|fallback\|#627\|#629" -- Sources/Echoelmusic/Studio/AlwaysOnBioRow.swift` → ONE hit (:45, an unrelated freshness-window remark); the struct is declared at :169 and mounted at Sources/Echoelmusic/Studio/EchoelStudioView.swift:3139 with no note. Contrast the sibling decision, which DID land at its site: Sources/Echoelmusic/Studio/LiveColaboView.swift:412-428.
- **Warum es zählt:** A deliberate non-marking with no note at the surface reads, to the next honesty sweep, as an oversight — so it gets marked, silently overturning a founder-gated open question. Low severity because two registrations exist and both are current.
- **Billigster Fix:** Copy the two-sentence verdict from the guard head to AlwaysOnBioRow.swift beside the `AlwaysOnBioPanelStrip` declaration at :169.

### SESS-12 · NIT — AudioOutputGuard's coverage invariant says "All seven" render blocks — its own list names six, and six exist

- **Beleg:** Sources/Echoelmusic/DSP/AudioOutputGuard.swift:38: "All seven of the app's `AVAudioSourceNode` render blocks are wired." The bullet list immediately below (:41-48) names exactly six: PolySynthVoice, BioReactiveSynthVoice, SubBassVoice, MetronomeVoice, SessionEngine, SamplerVoice — and :47-48 records why: "(⛔ `DrumSynthVoice` shared this form and was deleted with #167, founder 2026-07-27.)" Measured: `git grep -l "AVAudioSourceNodeRenderBlock" -- Sources` → six files (MetronomeVoice, SessionEngine, SamplerVoice, BioReactiveSynthVoice, PolySynthVoice, SubBassVoice). Pre-existing, not this session's claim, and already registered as scratchpads/AUDIT_ULTRA_2026-08-19.md:54 (B2 · WARN).
- **Warum es zählt:** A coverage invariant that overstates its own denominator lets a session adding a seventh render block read "All seven" as already satisfied and skip wiring it into the silence-detection path. Graded NIT because the count is contradicted by its own adjacent list and the item is already on a board.
- **Billigster Fix:** Correct :38 to "All six" in the same commit as the next render-block change, or drop the count entirely and pin the set with one CISmoke assertion.

**Vom Lead verworfen (5):**
- ~~WORKER 2: ColabPayload.swift:69-73 claims a follow-up slice is "registered" with no ticket number and no second record — a defect of this session~~ — The observation is factually correct (`git grep -rn "wider slice" -- Sources Tests scratchpads` → one hit, ColabPayload.swift:71; no board row, no id), but the sentence PRE-DATES this session: `git show dadcf65:Sources/Echoelmusic/Sync/ColabPayload.swift | gre
- ~~WORKER 2: "Taxonomie-Schuld in den Köpfen von #622/#623" was registered once, never defined, never repeated, never closed — an unresolvable four-word phrase~~ — It IS defined, and the worker's own note calls the finding UNDECIDABLE. Commit bae1672 (#625b) body lines 59-63 states it in full: the #622/#623 guard heads label parent-red assertions FORWARD, and "wird dort bei der nächsten Berührung korrigiert". That is the
- ~~WORKER 2 graded the stale #336 registration and the AudioOutputGuard "All seven" count as CRITICAL~~ — Severity rejected, findings kept at WARN and NIT. Neither would make a future session plan wrongly for long: the #336 site (ModulationEngine.swift:100-108) carries a loud ⛔ block plus a nine-assertion guard, and AudioOutputGuard.swift:38's own list at :41-48 n
- ~~WORKER 2: the AlwaysOnBioPanelStrip founder question's "Sole registration" is Tests/CISmoke/TheDemoSourceIsMarkedWhereItRendersTests.swift:40-46~~ — "Sole" is false — scratchpads/SESSION_LOG.md:10555, :10563 and :10573 each carry "Founder-Frage `AlwaysOnBioPanelStrip`" in the Offen line, in three consecutive entries including the last one at HEAD. The surviving true half (no note at Sources/Echoelmusic/Stu
- ~~Both workers' "OK" entries (re-derived measurements of #623/#624/#625b/#626/#627/#627b/#629, and the #627b per-entry tick list)~~ — Not findings. Spot-checked and consistent with the tree (e.g. BioEgressPolicy.swift:54 admits `.fallback`; TheDemoSourceIsMarkedWhereItRendersTests.swift:34-39 and :51-56 do tick `OwnBioRow` and the peer payload as closed, matching LiveColaboView.swift:337/:38

---

## Team Guard-Integrity (Tests/CISmoke)

**Nächste Scheibe laut Lead:** Fix the one guard that is unconditionally RED: edit Tests/CISmoke/TheVoiceDoorFeedsTheCaptureTests.swift:92 to the prefix anchor already used at :70 — `XCTAssertTrue(beforeLeaf.contains("VoiceCaptureRow(controller: voiceCapture"), "the panel hands over the REFERENCE, not a read value — mount is soundPanel in EchoelStudioView.swift")` — one file, one line, and it carries one law: an anchor that pins a call site uses the PREFIX form (a signature change must re-anchor, not disappear), and no assertion in the blocking bundle ships without a failure message. It is the cheapest of the three CRITICALs, it is the only one that is failing *today* rather than latently, and it restores green→red signal to the 10.76.50 menu-freeze guard sitting two lines above it.


### GUAR-0 · CRITICAL — TheVoiceDoorFeedsTheCaptureTests:92 asserts an anchor that cannot match at HEAD — the assertion is unconditionally RED and carries no failure message

- **Beleg:** Tests/CISmoke/TheVoiceDoorFeedsTheCaptureTests.swift:92 — `XCTAssertTrue(beforeLeaf.contains("VoiceCaptureRow(controller: voiceCapture)"))`, a bare assert with no message (verified with `grep -n "" … | sed -n '88,96p'`).
The only mount: `git grep -nF "VoiceCaptureRow(controller:" -- Sources` → Sources/Echoelmusic/Studio/EchoelStudioView.swift:6680 `VoiceCaptureRow(controller: voiceCapture, patch: $currentPatch) {` — a comma where the needle demands `)`.
`git grep -cF "VoiceCaptureRow(controller: voiceCapture)" -- Sources` → no output (0 hits).
Not a scoping artefact: leaf decl is `git grep -n "private struct VoiceCaptureRow: View" -- Sources` → EchoelStudioView.swift:11195, so `beforeLeaf = studio[..<11195]` does span line 6680. `source()` at :119-131 applies only `SourceText.codeOnly` (comment stripping), and it is the COMPLIANT gate shape (dir gate at :122, thrown `DoorAnchorMissing` at :126). So the sole failure cause is the added `patch:` argument.
Provenance: `git log --oneline -- Tests/CISmoke/TheVoiceDoorFeedsTheCaptureTests.swift` → `5b50761` only; `git log --oneline -1 -S 'VoiceCaptureRow(controller: voiceCapture, patch:' -- Sources/Echoelmusic/Studio/EchoelStudioView.swift` → `c2e1a53` (#593c). The guard was not moved with its subject, and nothing in `dadcf65..HEAD` (HEAD = 55eae23) touched it.
The same file gets it right one test up, at :70 — `codeOccurrences(of: "VoiceCaptureRow(controller:", in: studio) == 1` — the prefix form, which is green.
- **Warum es zählt:** Tests/CISmoke is the BLOCKING bundle. A permanently red assertion there can never transition green→red, so the test it sits in (`testTheProgressReadsStayInTheLeaf`, whose :87-90 `XCTAssertFalse` is the real 10.76.41/50 menu-freeze law) produces no NEW signal when that law is actually broken. Because CI/CD reports `failure` on every push (#396), a permanently red test is indistinguishable from the noise — vacuity by saturation rather than by silence. And with no message string, whoever finally reads the log gets `XCTAssertTrue failed` with no pointer to the anchor or the call site.
- **Billigster Fix:** One line: change :92 to the prefix form already used at :70 — `XCTAssertTrue(beforeLeaf.contains("VoiceCaptureRow(controller: voiceCapture"), "the panel must hand over the reference, not a read value — mount is EchoelStudioView.swift's soundPanel")` — adding the missing failure message in the same edit.

### GUAR-1 · CRITICAL — 19 guards turn a MISSING ANCHOR into `XCTSkip` — a signature change silently retires every assertion downstream of it, and the drift mechanism has already fired once in this repo

- **Beleg:** Measured with a Python scan over all 308 files (`git ls-files 'Tests/CISmoke/*.swift' | wc -l` → 308; the workers' 307 is stale): 19 files throw `XCTSkip` within 6 lines of a `firstIndex`/`range(of:)`/`hasPrefix` anchor lookup — AFreshTakeStartsWithNoHeldFrame · BioNumbersGrowWithTheText · ChipLabelGrowsWithTheText · ChromeDynamicType · CoachingTextScales · DeletingAClip · DeletingAPreset · DiagnosticsTextScales · LockCueDoesNotShoveTheControls · MonitoringCannotStrandTheEngineStopped · OpeningAProjectRescuesTheLiveTake · ParameterRowStacksAtAccessibilitySizes · RandomizeIsUndoable · TapTargetFloor · TheAudioLanesHaveNoProducer · TheHeaderShowsTheLoop · TheReadyPageNamesTheFirstAct · TheStallRemedyReachesTheScreen · WeatherIsAMoodRubric.
Worst instance, Tests/CISmoke/OpeningAProjectRescuesTheLiveTakeTests.swift:271-288 — `window(_:from:)` throws `XCTSkip` when the declaration anchor is absent (:273) and again when the closing brace is not found (:283). Its own skip text reads "…should be rewritten with it, **not left to pass vacuously**" — which is precisely what a skip does in a bundle whose test names are not reliably in the log (#445).
Anchors are exact substrings: :111 `"private func open(_ p: Project)"`, :112 `"private func autosaveTake()"`; both resolve today (`git grep -n "private func open(_ p: Project)" -- Sources` → EchoelStudioView.swift:10423; `autosaveTake()` → :10332). All three assertions of `testOpeningRescuesTheLiveTakeFirst` (:126-137 the rescue call, :155-166 the `p.id != Project.autosaveSlotID` polarity, :184-190 the ordering before `style = openStyle`) hang off that window.
The file even argues at :117-121 that a missing call is "a real red, not an absent tree" — an argument that holds only while the window resolves.
The mechanism is not hypothetical: adding one parameter to a mount is exactly what `c2e1a53` did to `VoiceCaptureRow` (finding 1). There it produced a red; in these 19 it produces a green skip.
- **Warum es zählt:** Adding a parameter or renaming a member is ordinary work; deleting a whole file is rare. This is therefore the likeliest way a CISmoke guard evaporates. A session that changes `open(_ p: Project)` to `open(_ p: Project, animated: Bool)` sees a bundle with no new failure and ships the change believing the recovery-point law (one tap in the library list destroying the live take) is still defended.
- **Billigster Fix:** In `window(_:from:)` alone (3 lines, 1 file): replace both `throw XCTSkip(...)` with `XCTFail(<the same text>)` + `return []`. A missing declaration in a PRESENT tree is a re-anchor job, exactly as the message already says. The other 18 follow the same one-word substitution.

### GUAR-2 · CRITICAL — 50 guards gate their source read on a PER-FILE `fileExists` → `XCTSkip`, breaking the rule this bundle states verbatim about itself (#475): deleting the guarded file turns the guard into a green skip

- **Beleg:** The house rule, stated verbatim in the bundle — Tests/CISmoke/AHeldReadingSaysSoTests.swift:91-93: "Skips on the DIRECTORY, never on the individual file: a `fileExists` bracket around each read turns a deletion — the exact catastrophe this bundle stands against — into a green skip (#475)." Its compliant implementation is :94-103 (`XCTSkipUnless` on `root/Sources`).
Measured over all 308 files (Python: for each `fileExists` whose ±window contains `XCTSkip`, classify the probed path): **50 per-file-gated · 167 directory-gated · 91 with no such gate**. (The workers reported 55; my classifier resolves the probe expression rather than only the nearest string literal — the finding stands, the number is mine.)
Worst forms, read directly:
· Tests/CISmoke/FieldSoundSurvivesRelaunchTests.swift:179-190 — gates on the *argument* `path`, so ANY file it reads, missing, skips.
· Tests/CISmoke/CoachingTextScalesTests.swift:294-306 and DeletingAClipIsUndoableTests.swift:246-255 — gate on `Self.strip` / `Self.panel`, one specific file each.
· Tests/CISmoke/ThePeerSeesWhetherItIsABodyTests.swift:171-177 — `path = root/"Sources/Echoelmusic/Studio/LiveColaboView.swift"`, `guard fileExists else { throw XCTSkip("source tree not present at …") }`. This is the NEWEST guard in the bundle (`git log --oneline -- …ThePeerSees…` → 5c2680c #629b, 963f9f6 #629).
The skip message says "source tree not present" — a statement about the CHECKOUT — while the condition tests ONE file. The two diverge exactly when the guarded thing is deleted or renamed.
- **Warum es zählt:** For ThePeerSees, claims 1-6 and 8 are behavioural on `BioPeek` and stay green, so deleting or renaming `LiveColaboView.swift` silently retires the only assertions covering the visible "Demo" chip, the heart-tint realness gate and the VoiceOver prefix — the headline of #629/#629b, added one commit ago precisely because those were unguarded. This repo deletes view files routinely (#167, #473, #475), so the trigger is live, not theoretical. Nothing is skipping today; the defect is that the deletion which would matter is the one that goes unreported.
- **Billigster Fix:** Copy the two-step bracket already present in 167 sibling files: gate `XCTSkip` on `root.appendingPathComponent("Sources")` (the checkout question), then `throw` a plain `Error` — the `DoorAnchorMissing` type already used at TheVoiceDoorFeedsTheCaptureTests.swift:126 — when the specific relative path is absent while the tree is present. Mechanical, one helper per file; do the newest guards first.

### GUAR-3 · WARN — Stale file:line citations in at least seven guard headers — every one lands on unrelated code, and one of the copies sits in the always-loaded rule set

- **Beleg:** Each re-derived with the grep beside it:
1. Tests/CISmoke/TheShownNumberIsTheKeptNumberTests.swift:86-88 cites `EchoelFXView.swift:573/574/591/593/594/598` for six `field(…)` rows. Real: `git grep -n 'field("Threshold"|field("Ratio"|field("Attack"|field("Knee"|field("Make-up"|field("Ceiling"' Sources/Echoelmusic/Studio/EchoelFXView.swift` → 646, 647, 664, 666, 667, 671. Line 574 today is `intervalRow("Voice 1", $vm.harmInterval1)`.
2. Tests/CISmoke/PatchVibratoAnchorTests.swift:185-186 cites `PolySynthVoice.swift:680` and `BioReactiveSynthVoice.swift:399`. Real: `git grep -n "breathDepth: 0.5" -- Sources` → PolySynthVoice.swift:932, BioReactiveSynthVoice.swift:539. Both cited lines are a bare `}`. (The COUNT — two literal sites, no producer — still holds.)
3. Tests/CISmoke/SoundRowsCanReachTheShippedPatchesTests.swift:23 cites `SynthPatch.swift:424` for `noiseLevel` 0.006; `sed -n '424p'` → `noiseLevel: 0.02`. Real: `git grep -n "noiseLevel: 0.006" -- Sources` → SynthPatch.swift:549.
4. Same file :28-29 claims "three OTHER cutoff rows (`EchoelFXView:480`, `EchoelStudioView:2474` and `:2502`)". `git grep -n '"Cutoff"' -- Sources` → EchoelFXView.swift:531 and EchoelStudioView.swift:6791 — TWO rows, neither at a cited line.
5. Tests/CISmoke/TempoLockAlwaysAsksForARecomposeTests.swift:43 cites `EchoelmusicApp.swift:986`; that line is `polyVoice?.setTranspose(…)`. Real read: `git grep -n 'studio.lockBPM' Sources/Echoelmusic/EchoelmusicApp.swift` → 1121.
6. Tests/CISmoke/SourceText.swift:44 cites `EchoelStudioView.swift:3096` for the WeatherKit attribution literal. Real: `git grep -n 'developer.apple.com' Sources/Echoelmusic/Studio/EchoelStudioView.swift` → 4136.
7. Tests/CISmoke/CoachingTextScalesTests.swift:61 cites `EchoelTheme.swift:200`; real prose hit `git grep -n 'font(.system(size:' Sources/Echoelmusic/Studio/EchoelTheme.swift` → 243.
8. Tests/CISmoke/WebsitePagesAreFindableAndHonestTests.swift:440 cites `EchoelStudioView.swift:5201` for the "n of 16 offered" shape; that line is a `range: 1...Double(FieldAutoPlay.maxVoices)` argument. Real shapes: `git grep -nE "[0-9]+ of (the )?16 offered" -- Sources` → :6239, :6450, :6452, :9790.
Separately, a stale COUNT in two places at once: Tests/CISmoke/TheDSPLayerStaysFoundationOnlyTests.swift:4 and .claude/rules/swift-audio.md:93 both say "38 files"; `ls Sources/Echoelmusic/DSP/*.swift | wc -l` → 39 (`git ls-files` agrees). The 39th arrived with `89dc2bc` (#590); both prose sites were written by `9bccd9d` (#550) and neither was updated. The guard cannot catch it — :130 is `XCTAssertGreaterThan(files.count, 30, …)`. The rule's own sentence begins "Measure, do not quote", and it is in the always-loaded set every session pays for.
- **Warum es zählt:** Each of these is a PREMISE a guard leans on rather than asserts — "the shipped rows really use decimals: 1", "nothing writes this key", "exactly one line hides `//` in a literal". A reader who spot-checks one lands on unrelated code and cannot tell a moved line from an invented claim. Item 2 is the dangerous shape: the conclusion (`breathDepth` has no producer, matching CLAUDE.md's #496) is TRUE, so a correct claim loses its only evidence. Item 3 is worse still — the cited line holds a DIFFERENT value (0.02 where 0.006 is claimed), which reads as fabrication.
- **Billigster Fix:** Replace each number with the single-hit grep that regenerates it — the repo's own stated rule that a quoted phrase survives an insertion and a line number does not. For the DSP count, DELETE the literal from both sites rather than incrementing it; `.claude/rules/swift-audio.md` already prints the counting command one line above.

### GUAR-4 · WARN — Two guards count needles FILE-WIDE while their failure messages name a specific function — a writer relocated into another member keeps the count and the green

- **Beleg:** 1. Tests/CISmoke/AnOpenedEmptyTakeCannotArmTheExportTests.swift:61-71 asserts `lines.filter { $0.contains("hasComposed = !pianoRoll.notes.isEmpty") }.count == 2` over the whole 11k-line EchoelStudioView, with a message stating "there are TWO: open() (#622…) and rebalanceTake() (#622b…)". Same shape at :78-86 (`"hasComposed = true"` == 1, message names `generate()`), :88-92 (names `importMIDI()`), :98-113 (three more naming `exportMIDI()` and the autosave guard). No scope helper exists in the file: `grep -n "firstIndex\|window(\|span(" Tests/CISmoke/AnOpenedEmptyTakeCannotArmTheExportTests.swift` → no hits.
2. Tests/CISmoke/ThePeerSeesWhetherItIsABodyTests.swift:181-221 — five `lines.filter { … }.count == 1` over all of LiveColaboView.swift, with :208-212 saying "the visible \"Demo\" chip is gone from `bioLine`" and :181-184 saying "your OWN row". `grep -n "span(\|window(\|firstIndex" Tests/CISmoke/ThePeerSeesWhetherItIsABodyTests.swift` → no hits, so nothing ties the `if synthetic == true {` occurrence to `bioLine`.
The repo has already retracted this exact class one file over — Tests/CISmoke/TheDemoSourceIsMarkedWhereItRendersTests.swift:201-204: "#627b: the first version counted the SUBSTRING file-wide while its failure message warned about 'a separate UI flag that can drift from the bus' … The whole declaration is pinned instead." Both instances above were written after that retraction.
- **Warum es zählt:** `== 1` / `== 2` does catch deletion and duplication, so the blast radius is contained to RELOCATION — moving the write out of `open()` into another member, or the `if synthetic == true` block out of `bioLine`, keeps the count exact while the guarded behaviour stops happening. The messages assert a mapping the assertions never measure, which is the defect the bundle logged five commits earlier and then reproduced.
- **Billigster Fix:** Reuse a helper that already exists in the bundle: the brace-walk in `OpeningAProjectRescuesTheLiveTakeTests.window(_:from:)` (:271-290, once its skips are fixed) or the double-anchored `span(_:from:to:_:)` in TheDemoSourceIsMarkedWhereItRendersTests.swift:131-148. Resolve each writer to its enclosing declaration and assert one occurrence inside it.

### GUAR-5 · WARN — `span(_:from:to:_:)` documents "Both anchors must be unique" but only count-checks the FROM anchor — and its first caller passes a TO anchor matching six lines

- **Beleg:** Tests/CISmoke/TheDemoSourceIsMarkedWhereItRendersTests.swift:129-131 (doc): "Both anchors must be unique, so a rename fails LOUDLY here instead of silently selecting the wrong region."
Implementation :135-148: `let starts = lines.indices.filter { lines[$0].contains(from) }` → `XCTAssertEqual(starts.count, 1, …)`; then `guard let b = lines.indices.filter({ $0 > a && lines[$0].contains(to) }).first` — first hit after `a`, no count check on `to`.
Caller :232-240 — `span(lines, from: "struct PulseMonitorMini: View {", to: "var body: some View {", "PulseMonitorMini declaration")`. Measured: `git grep -n 'var body: some View {' -- Sources/Echoelmusic/Studio/HeaderMonitors.swift` → 26, 124, 260, 427, 538, 613 — SIX matches. The window lands correctly today only by position (`struct PulseMonitorMini` at :69, its body at :124).
- **Warum es zählt:** The doc comment is what the next session reads before trusting the window, and it over-states the mechanism by half — this helper is being copied across the bundle (#619b/#621b). Any line containing `var body: some View {` inserted between :69 and :124 — a nested helper `View` inside `PulseMonitorMini`, ordinary SwiftUI practice — silently truncates the window with no anchor assertion firing, and the claim-5 check for `var synthetic: Bool` then reports on a region that is not the declaration it names.
- **Billigster Fix:** Add the symmetric check — `XCTAssertEqual(lines[a...].filter { $0.contains(to) }.count, 1, …)` — or, if the callers cannot satisfy it, retract the second sentence of the doc at :130-131 to say the closing anchor is the FIRST hit after the opening one and may shrink the window.

### GUAR-6 · WARN — SectionHeadingIsOneTreatmentTests' inline-heading sweep bans a spelling that is extinct AND requires all three tokens on ONE line — every real 10 pt dim label in the file is written multi-line and evades it

- **Beleg:** Tests/CISmoke/SectionHeadingIsOneTreatmentTests.swift:167-180 — offenders are lines containing `Text(` AND `EchoelTheme.font(10, .medium)` AND `EchoelTheme.dim`, all three on the SAME line.
The banned literal is extinct in the app: `git grep -nF 'EchoelTheme.font(10, .medium)' -- Sources Tests` → one hit, the test's own needle at :169. Zero under `Sources/`.
The file's own header, :13-18, explains why the literal is irrelevant to rendering: "`EchoelTheme.font(_:_:)` maps only `.semibold/.bold/.heavy/.black` onto the Bold face … So `font(10, .medium)` never rendered medium — it rendered 10 pt REGULAR (#361)." Confirmed at Sources/Echoelmusic/Studio/EchoelTheme.swift:264-267 (`case .semibold, .bold, .heavy, .black: return "AtkinsonHyperlegible-Bold"` / `default: … Regular`) and :273 (`weight: Font.Weight = .regular`), so `EchoelTheme.font(10)` and `font(10, .medium)` are the same `Font`.
And the single-line shape is not how this file writes: all 8 sites of `grep -n 'EchoelTheme.font(10' Sources/Echoelmusic/Studio/EchoelStudioView.swift` (4138, 5662, 5827, 8096, 11049, 11121, 11170, 11255) put `.font(...)` and `.foregroundStyle(EchoelTheme.dim)` on SEPARATE lines. Six of them are 10 pt dim captions today; none can ever match the filter.
- **Warum es zählt:** Downgraded from the worker's CRITICAL: the sibling assertion at :138 (`XCTAssertFalse(joined.contains("Text(\"\(title)\").font(EchoelTheme.font(10"))`) does cover the seven NAMED headings with a prefix form, and the blast radius is heading cosmetics, not a user-visible falsehood. But `testNoInlineHeadingSurvivesInTheStudio` is one of eight tests in the file and it is inert: its message claims to catch "a heading spelled inline as 10 pt dim again", and the only spelling anyone would plausibly write — `EchoelTheme.font(10)` on its own line, which renders pixel-identically — sails past.
- **Billigster Fix:** Match the RENDERED weight class over a joined window rather than one literal: flag any `Text(` whose modifier chain carries `EchoelTheme.font(10` without one of the Bold-mapping weights AND `EchoelTheme.dim`, using the already-joined `joined` string the sibling test at :132-138 uses. Keep the message; it is already correct.

**Vom Lead verworfen (4):**
- ~~Worker B, NIT: `TheStallRemedyReachesTheScreenTests:282` names `HeaderMonitors.accessibilityText`, "no type of that name exists" — the message sends the reader ~~ — The member exists and is exactly what the guard reads. `git grep -n "accessibilityText\|accessibilityValue" Sources/Echoelmusic/Studio/HeaderMonitors.swift` → :208 `.accessibilityValue(accessibilityText)` and :211 `private var accessibilityText: String`. The q
- ~~Worker A: "55 of 307 CISmoke guards" gate on a per-file `fileExists`.~~ — Both numbers rejected as stated; the FINDING is verified with my own measurement and carried above. `git ls-files 'Tests/CISmoke/*.swift' | wc -l` → **308**, not 307 (both workers used 307). My classifier — which resolves the probed path expression instead of 
- ~~Worker B, OK: "Category (a): every guard referencing a deleted Sources file is a deliberate absence guard — no tautologies, no silent skips" (4 files enumerated~~ — Not a defect claim, so it cannot appear in a findings list; and it is a negative over 308 files that I did not re-derive independently within this pass. The four spot-cited absence guards (TheHeaderShowsTheLoopTests:203-205, SoundPanelPresetBarTests:123-125, T
- ~~Worker B, OK: "Category (d): the one genuinely duplicated invariant (`horizontalScrub` / `dxStep` across ConcertPitchDoesNotRideTheScrollTests and ScrollSweepCa~~ — Same reason — not a defect, and the exhaustiveness half ("the one") is a negative over all 308 files that I did not re-derive. The advice "do not deduplicate" is worth passing on as a caution, not as an audited finding.

---

## Team Audio-Monitoring

**Nächste Scheibe laut Lead:** Repair the two red counts in the blocking bundle, ONE law: "a category-changing exit in `setInputMonitoring` updates BOTH guard counts in the same commit." Two files, four edits, zero `Sources/` change — `Tests/CISmoke/MonitoringCannotStrandTheEngineStoppedTests.swift:216` 3→4 plus naming the #628 claim-failure exit in its message, and `Tests/CISmoke/RecordRouteOwnershipTests.swift:80` 3→4 plus adding "the failed session claim" to the enumeration at :79 and the message at :81-84. This is first not because it is the most interesting finding but because it is the prerequisite for every other one: six of the verified findings edit `setInputMonitoring`, and while these two guards fail on a correct tree, any of those commits lands inside a red that #396 makes indistinguishable from its own — which is exactly how #628 shipped an unnoticed fourth exit in the first place. The immediate follow-up slice (one file, one law) is the #625 breadcrumb at `AudioEngine.swift:1755`, since it is the founder-facing device evidence and it is currently a constant.


### AUDI-0 · CRITICAL — TWO guards in the blocking bundle are RED on HEAD — #628 added a fourth category-changing exit and moved neither count

- **Beleg:** Merged from both workers, re-measured on HEAD 55eae23 (their line numbers were ~8 low). Stripped-source count (mimicking `Tests/CISmoke/SourceText.swift` `codeOnly`) over `Sources/Echoelmusic/Audio/AudioEngine.swift`: `restoreEngineIfStranded(` = 4 — :1737 (claim-failure exit, added by #628), :1782 (format guard), :1940 (OFF), :2132 (declaration). `Tests/CISmoke/MonitoringCannotStrandTheEngineStoppedTests.swift:216` asserts `XCTAssertEqual(..., 3, ...)` with the message 'THREE occurrences are expected … the declaration plus TWO callers'. Same defect in the sibling: inside `setInputMonitoring`'s body (slice `func setInputMonitoring(` → `\n    }`), `releaseRecordRoute(.inputMonitoring)` = 4 (raw lines :1731, :1770, :1823, :1933), while `Tests/CISmoke/RecordRouteOwnershipTests.swift:80` asserts 3 with the comment at :79 'Three exits after the claim'. Provenance: `git log --oneline -3 -- Tests/CISmoke/MonitoringCannotStrandTheEngineStoppedTests.swift Tests/CISmoke/RecordRouteOwnershipTests.swift` → e3c9d50 (#628) is the last commit to touch both, and `git show e3c9d50 -- <both>` adds claims 5 and 6 and touches neither digit.
- **Warum es zählt:** MonitoringCannotStrandTheEngineStoppedTests claim 4's own failure message says 'A new exit that mutates the session category needs its own call and this count updated in the same commit' — #628 did the first half only. Because `Echoelmusic CI/CD Pipeline` reports `failure` on every push (#396) and the discriminator is `TEST EXECUTE FAILED` vs `TEST BUILD FAILED`, an assertion failure inside a passing build is exactly the class that is read as pre-existing noise. Consequence for planning: the two guards that exist to stop the next #625 currently cannot, and every remaining finding below touches `setInputMonitoring` — anything shipped there lands inside a red those guards cannot distinguish from their own.
- **Billigster Fix:** `MonitoringCannotStrandTheEngineStoppedTests.swift:216` 3→4 and name the claim-failure exit in its message; `RecordRouteOwnershipTests.swift:80` 3→4 and add 'the failed session claim' to the enumeration at :79 and the message at :81-84. Two files, four edits, no `Sources/` change.

### AUDI-1 · CRITICAL — The #625 breadcrumb is now a constant — #628's pause makes it fire on EVERY healthy toggle, and the comment above it asserts the opposite

- **Beleg:** `AudioEngine.swift:1723` `if wasRunning { masterEngine.pause() }` (moved above the claim by #628), then :1724 `claimRecordRoute`, then :1755-1757 `if wasRunning && !masterEngine.isRunning { log.audio("Input monitoring: the session claim stopped the engine (#625)", level: .warning) }`. `AVAudioEngine.pause()` clears `isRunning`, so whenever `wasRunning` is true the condition is unconditionally true. The comment at :1737-1738 states the opposite: 'It fires only when the claim actually changed the engine's running state, so it is silent on every healthy toggle and unmistakable on the broken one.' #628's own comment block at :1717-1722 says the hypothesis is unmeasured and 'The distinct log lines below are what will actually settle it on the device.' Gate status: `scratchpads/SESSION_LOG.md:10521` records the probe as pending ('Probe 1 ist der Kern (Musik läuft, Monitoring an — bleibt der Ton?)'); no later line records a result, and `scratchpads/PLAN_VOCAL_CHAIN_V1.md:90,92-93` makes that probe the gate for the whole vocal-chain slice.
- **Warum es zählt:** Two consecutive unmeasured hypotheses (#625, #628) shipped on this method, and the single breadcrumb designed to discriminate between them is now always-true. The next device log the founder sends will carry the #625 warning on every successful monitor arm, which is precisely the reading that sends the next session back to a mechanism that is not the cause — the same class as the #625b retraction at :1731-1734 (a log asserting something it cannot know). It also means the plan's gate can no longer be satisfied by the deployed builds 2526/2527.
- **Billigster Fix:** Capture the state between the pause and the claim (`let stoppedByUs = !masterEngine.isRunning` at :1724) and test `wasRunning && !stoppedByUs && !masterEngine.isRunning`; or delete the line and rely on `restoreEngineIfStranded`'s own :2129 'Audio engine was stopped by a session change' log, which is already emitted at every exit. Correct :1737-1738 in the same commit.

### AUDI-2 · CRITICAL — Plan §2 prescribes `processStereo` — an INTERNAL API whose own doc calls driving it directly a silent wrong-sound bug

- **Beleg:** `scratchpads/PLAN_VOCAL_CHAIN_V1.md:23` names `processStereo(_ inL: Float, _ inR: Float) -> (Float, Float)` as the per-sample entry point for the bridge. `Sources/Echoelmusic/DSP/EchoelFXChain.swift:433` declares `func processStereo(...)` with NO `public` — deliberately narrowed since #138 Slice 2. Its doc at :424-431: 'It does NOT advance the tone-filter glide — `processBuffer`/`processBufferMono` do that once per block before entering their loop. A caller that drove this directly would therefore get a filter frozen at whatever cutoff the glide last held, which is a silent wrong-sound bug rather than a crash … narrowing the access level is what keeps it that way.' Public entries: :479 `processBuffer(left:right:frameCount:)`, :496 `processBufferMono(_:frameCount:)`. Production callers use the block form: `Tools/BioReactiveSynthVoice.swift:618`, `Tools/PolySynthVoice.swift:1076`.
- **Warum es zählt:** V1b's instruction is literally 'EchoelFXChain in den Source-Render-Block'. An implementer following the plan writes a per-sample loop over `processStereo`, gets a frozen tone filter, and the failure has no crash and no log — it reads as a wrong-sounding voice and gets blamed on the new bridge, not on the call. The access level was narrowed precisely to prevent this, and the plan reintroduces it in prose the access level cannot police.
- **Billigster Fix:** Rewrite plan line 23 to name `processBufferMono` / `processBuffer` (public, block-form, glide-correct) and drop the per-sample framing. One line.

### AUDI-3 · CRITICAL — Plan never names `AudioOutputGuard`, whose stated invariant a new source-node render block must join — and the guard's own coverage sentence is already stale

- **Beleg:** `git grep -c "AudioOutputGuard" -- scratchpads/PLAN_VOCAL_CHAIN_V1.md` → exit 1, zero hits. `Sources/Echoelmusic/DSP/AudioOutputGuard.swift:37-48` — '── COVERAGE ── All seven of the app's `AVAudioSourceNode` render blocks are wired' — while the enumeration below it names SIX voices (`PolySynthVoice`, `BioReactiveSynthVoice`, `SubBassVoice`, `MetronomeVoice`, `SessionEngine`, `SamplerVoice`), the seventh (`DrumSynthVoice`) being marked deleted with #167 in the same block at :46-48. :52-54: 'THE INVARIANT IS EXACTLY THIS AND NO WIDER: every SOUNDING path of every render block of ours passes the samples IT WRITES through this guard.'
- **Warum es zählt:** V1a's source node writes samples into the hardware buffer list and is therefore inside the invariant. Unguarded, a non-finite produced by the FX chain or by a torn ring read reaches the master bus — the exact failure this file exists for, and one that pins the limiter's peak envelope at infinity and silences the master permanently (:30-33). The 'seven' also has to move in the same commit or the file over-claims a second time.
- **Billigster Fix:** Add to plan V1a: 'der neue Render-Block wird in `AudioOutputGuard` eingehängt (in-place-Form wie `SamplerVoice`), und die COVERAGE-Zeile `AudioOutputGuard.swift:38` wird im selben Commit nachgeführt — sie sagt heute "seven" und zählt sechs.'

### AUDI-4 · WARN — The `input → notchEQ` edge is never severed — both exits lower the session to `.playback` with the input node still in the graph, and the rollback comment claims the opposite

- **Beleg:** Built at `AudioEngine.swift:1799` `masterEngine.connect(input, to: notchEQ, format: inFmt)`. Restart-rollback disconnects only downstream: :1820 `disconnectNodeOutput(notchEQ)`, :1821 `voiceTunePitch`, :1822 `monitorMixer`, then :1823 `releaseRecordRoute` and :1830 `restartOrDegrade`. OFF path is identical: :1905/:1906/:1907, then :1933 `releaseRecordRoute` and :1935 `restoreEngineIfStranded`. No inverse exists anywhere: `git grep -c "disconnectNodeInput" -- Sources` → exit 1 (zero matches); `git grep -n "disconnectNodeOutput(input\|disconnectNodeOutput(masterEngine.inputNode" -- Sources` → exit 1 (zero matches). The comment at :1826-1829 states 'The monitor chain is disconnected again, so this start restores the exact pre-toggle graph' — measurably false: the input→notchEQ half survives both exits, and `releaseRecordRoute` → `AudioConfiguration.downgradeToPlaybackAfterRecording()` → `setCategory(.playback)` runs with it still connected.
- **Warum es zählt:** The graph asymmetry and the false comment are MEASURED. The failure mechanism the worker attached to them — that `masterEngine.start()` then throws because the input node is connected under a non-record category — is REASONED from AVAudioEngine semantics and is UNVERIFIED here (no toolchain, no device log showing it). Graded WARN on that split, not CRITICAL. What is certainly wrong today is the comment: it is the sentence a future session reads to rule the graph out as a cause, and it rules out something that is actually true.
- **Billigster Fix:** Add `masterEngine.disconnectNodeInput(notchEQ)` before `releaseRecordRoute` at both :1823 and :1933, and correct :1826-1829 in the same commit. Do NOT ship this before the guard-count repair — it changes `setInputMonitoring` while both of that method's guards are red.

### AUDI-5 · WARN — The OFF path mutates a RUNNING graph without pausing — asymmetric to the ON path #628 just taught to pause first, and it is the automatic path

- **Beleg:** `AudioEngine.swift:1898-1907`: the `else` branch does `removeTap` (:1901) and three `disconnectNodeOutput` calls (:1905-1907) with no `masterEngine.pause()`; the running state is not read until :1932 `let offWasRunning = masterEngine.isRunning`. Contrast the ON path: :1723 `if wasRunning { masterEngine.pause() }`, whose #628 justification at :1714-1716 is 'the order every sibling site in this file already uses (attach/detach all pause before they touch the graph)' — and the siblings do: :2149 (`attachSourceNode`), :2184, :2223.
- **Warum es zählt:** OFF is on the recovery hot path — `start()` :1160 → `rearmInputMonitoring` :2135 → `setInputMonitoring(false)` :2139 — so it runs on every app start where monitoring is on, and again on every route sample-rate change (:687). If #628's hypothesis about a running engine straddling a graph/session change is right, OFF is the remaining unhardened instance of exactly that pattern, and it is the one that runs without a user tap. `offWasRunning` at :1932 is correctly placed relative to the RELEASE but cannot observe a stop caused by the disconnects 25 lines earlier.
- **Billigster Fix:** Hoist the running-state read above :1899, pause if running, and keep `restoreEngineIfStranded` at :1935 with that earlier value — one capture covering both the graph mutation and the category change, structurally identical to the ON path.

### AUDI-6 · WARN — `rearmInputMonitoring` discards the ON result — an automatic re-arm that fails disarms monitoring with no log and no reachable explanation

- **Beleg:** `AudioEngine.swift:2135-2142`: `guard isInputMonitoring else { return }` … :2139 `_ = setInputMonitoring(false)` … :2141 `_ = setInputMonitoring(true)`. Both callers are automatic: :1160 `rearmInputMonitoring(reason: "engine start")` and :687 `rearmInputMonitoring(reason: "route sample-rate change")`. The refusal copy is view-local `@State` written ONLY inside the toggle closures — `Studio/AudioInputPickerView.swift:131` `monitorRefused = !engaged` and `Studio/EchoelStudioView.swift:3577` `micMonitorRefused = !engaged` — and both refusal blocks are gated on that flag (`AudioInputPickerView.swift:144`, `EchoelStudioView.swift:3600`).
- **Warum es zählt:** If the ON leg hits any of the three false exits (:1730, :1778, :1831), `isInputMonitoring` ends false, both Toggles flip themselves off, and no refusal copy can appear because nothing wrote the `@State`. The user sees monitoring silently disarm after an AirPods connect or a camera-triggered rate switch, with no explanation and no retry affordance — the picker's own #613b comment (`AudioInputPickerView.swift:62`) calls a snapped-back toggle with no explanation 'the exact defect class this chain fights'. This is a direct hit on the founder's 'für jede Art von Situation' half of the ask.
- **Billigster Fix:** `if !setInputMonitoring(true) { log.audio("Input monitoring re-arm failed (\(reason))", level: .error) }` at :2141 — one line, and it is the only exit in the whole monitoring surface that currently logs nothing at all.

### AUDI-7 · WARN — FeedbackGuard ducks the MIC from the MASTER meter — loud music alone satisfies its runaway signature, and the plan blesses it as correct

- **Beleg:** `AudioEngine.swift:2030` `let level = Swift.max(_rawMeterL.pointee, _rawMeterR.pointee)` → :2031 `monitorLevelHistory.append(level)` → :2034 `FeedbackGuard.gainReductionDB(rmsHistory:)` → `monitorMixer.outputVolume = base * factor`. `_rawMeter*` is fed from taps on the MASTER bus (`:873` and `:1089-1092` `masterMixer.installTap(onBus: 0, ...)`), so it carries music + monitor. The detector, `Studio/FeedbackGuard.swift:58-68`: `let rising = last > first * 1.10 && last > 0.001` / `guard rising, last > ceiling else { return 0 }` with `ceiling: Float = 0.85`, `maxReductionDB: Float = 12`. `scratchpads/PLAN_VOCAL_CHAIN_V1.md:53-57` considers only the tail-chasing case and concludes the guard 'bleibt damit korrekt'.
- **Warum es zählt:** A generative build-up that crosses 0.85 master RMS while rising ducks the singer by up to 12 dB with the mic contributing nothing — a live defect today, and exactly the 'optimise for every situation' half of the founder's ask. The plan's control-ORDER reasoning is right; its DETECTOR reasoning is absent, so V1b's first wet vocal preset (reverb tail = above ceiling and rising, on a meter that already holds the music) walks straight into it and will be blamed on the new bridge.
- **Billigster Fix:** One line in plan §4: 'Der Detektor liest den MASTER-Meter (`AudioEngine.swift:2030`), nicht das Mikro — Musik allein und eine Hallfahne erfüllen beide seine Signatur. V1b misst das mit eingeschaltetem Reverb, bevor die Tür kommt.' The engine-side repair (feed the detector from `monitorTapWindow` instead of the master meter) is its own slice.

### AUDI-8 · WARN — Plan §2 points at `SPSCQueue` without naming an element type, and the obvious instantiation allocates on the audio thread

- **Beleg:** `scratchpads/PLAN_VOCAL_CHAIN_V1.md:37`: 'Der Ring dagegen schon: `Core/SPSCQueue` ist der lock-freie Träger des EngineBus.' `Sources/Echoelmusic/Core/SPSCQueue.swift:35` is `public final class SPSCQueue<Element>` over `private var buffer: UnsafeMutablePointer<Element?>` (:45). Carrying audio needs Float blocks; `SPSCQueue<[Float]>` allocates an `Array` per render block, which `.claude/rules/swift-audio.md` bans outright ('Array.append, Array.init — Allocates'). The repo's only preallocated float ring is `Sources/Echoelmusic/Audio/MonitorTapWindow.swift`, and that one is `NSLock`-protected and legal only because a tap callback is not a render block (its :12-14 states exactly that discriminator).
- **Warum es zählt:** Line 37 reads as 'the ring is solved'. It is not: the implementer's two nearest precedents are both wrong for a render-to-render bridge — one boxes its element, the other takes a lock — and an `AVAudioSinkNode` receiver block IS a render block, so the `MonitorTapWindow` exemption does not carry. `AudioConfiguration.swift:644` additionally advertises `AudioUnfairLock` as the audio-thread lock, which actively invites the wrong move.
- **Billigster Fix:** Name the shape in V1a: a preallocated `[Float]` / `UnsafeMutablePointer<Float>` ring with atomic read/write indices — `MonitorTapWindow`'s storage with `SPSCQueue`'s index discipline, explicitly NOT `SPSCQueue<[Float]>` — and promote the lock ban from §7 into V1a's acceptance criteria with the tap-vs-render discriminator.

### AUDI-9 · WARN — V1a rewrites two connect lines pinned by hard-equality guards in the blocking bundle; the plan's price section counts only latency

- **Beleg:** `Tests/CISmoke/TheNotchIsSlewedAndMonitorOnlyTests.swift:127-133`: `XCTAssertEqual(codeOccurrences(of: "connect(input, to: notchEQ, format: inFmt)", in: engine), 1)` and `XCTAssertEqual(codeOccurrences(of: "connect(notchEQ, to: monitorMixer, format: inFmt)", in: engine), 2, "the #595 build path AND the #599 setVoiceTune off-branch — sibling guard TheVoiceTuneSnapsToTheSessionKeyTests pins the same fact; the two counts must move together")`. Plan line 31 inserts an `AVAudioSinkNode` between `[voiceTunePitch]` and `monitorMixer`, which necessarily rewrites those exact lines. The same guard's :118-124 records that this count already went red once for exactly this reason and 'the red hid under #396 the whole time'.
- **Warum es zählt:** Plan §3 (lines 40-49) prices only latency. Combined with the CRITICAL above — two guards on the same method already red — V1a as written ships red-into-red on a surface where CI's conclusion is uninformative by construction.
- **Billigster Fix:** Add to V1a's Wächter bullet: 'zwei bestehende Wächter ziehen mit — `TheNotchIsSlewedAndMonitorOnlyTests.testTheNotchSitsInsideTheMonitorChain` (harte Gleichheit auf beide connect-Zeilen) und `TheVoiceTuneSnapsToTheSessionKeyTests`; die Zählungen wandern im SELBEN Commit.'

### AUDI-10 · WARN — Plan line 12 says `EchoelFXChain` is constructed at exactly TWO sites — the grep it cites returns FOUR

- **Beleg:** `git grep -n "EchoelFXChain(" -- Sources` → `Studio/FXCuratedLibrary.swift:31` (`let chain = EchoelFXChain()` inside the curated-preset loop), `Studio/FXCuratedLibrary.swift:106` (`private static func make(...)`), `Tools/BioReactiveSynthVoice.swift:154`, `Tools/PolySynthVoice.swift:333`. The two extra ARE offline preset capture — `FXPreset.capture(from: chain, ...)` at `FXCuratedLibrary.swift:33-34` and :108, no audio path — so the plan's CONCLUSION (no chain in `AudioEngine.swift`; the mic path is raw) survives intact. Its MEASUREMENT does not.
- **Warum es zählt:** The plan opens with 'Alles unten ist gemessen (`git grep`, 2026-08-19), nicht erinnert' (line 6) and its very first measurement is one the cited command contradicts. This is the `EchoelModalBank` lesson in the repo's own words: a quoted recipe that prints 4 where the prose promises 2 is read as a contradiction, and it costs the next session the time to re-derive the whole section instead of trusting it.
- **Billigster Fix:** Rewrite line 12: 'an vier Stellen konstruiert, davon zwei im AUDIOPFAD (`BioReactiveSynthVoice.swift:154`, `PolySynthVoice.swift:333`); die zwei in `FXCuratedLibrary.swift:31,106` sind Offline-Preset-Erfassung. In `AudioEngine.swift`: null.'

### AUDI-11 · NIT — `@unknown default` in `engageInputMonitoring` returns false with zero log output — a fourth route to the founder's exact on-screen message

- **Beleg:** `AudioEngine.swift:1647-1648` `@unknown default:` / `return false`, with no `log.audio` call — unlike the two branches above it (:1641 the dialog-denial log, :1645 the previously-denied log). `micPermissionDenied` (:1629-1631) tests only `== .denied`, so an unknown case yields `false` and the picker renders the non-permission copy at `Studio/AudioInputPickerView.swift:184` 'Monitoring could not start — try again.' — the exact string in the founder's screenshot.
- **Warum es zählt:** #628's comment at :1709-1713 enumerates 'Only three exits do that: a throwing claim, a 0 Hz input format, and a throwing restart.' That enumeration is short by this one and by the `#else return false` at :1650. Vanishingly unlikely on today's iOS, but it is the only exit that would make a device log look as if the toggle were never pressed — which matters precisely because a device log is the current evidence.
- **Billigster Fix:** One `log.audio("Input monitoring: unknown record-permission state", level: .error)` before :1648, and correct the exit enumeration at :1709-1713.

### AUDI-12 · NIT — The configuration-change RUNNING branch reconnects nothing and zeroes the recovery counter — the gap is registered in the file, but not where the branch is

- **Beleg:** `AudioEngine.swift:671-694`: the `if self.masterEngine.isRunning` branch does `recoveryAttempts = 0` (:672), `retroCapture.install` (:673), `refreshRenderQuantum`, `armTimingInstrument`, conditionally `rearmInputMonitoring(reason: "route sample-rate change")` (:687), then `return` (:694). No `connect` call in the branch. The master chain is wired only in `setupMasterEngine()`, reachable only from the one-shot `prepareGraph()`. ⚠️ CORRECTION to the worker's framing: the gap IS written down — `restoreEngineIfStranded`'s doc at :2126-2131 states 'if the session change altered the HARDWARE FORMAT, a bare restart brings the engine back with the master chain still wired at the old format — running and silent. Nothing in this app reconnects it … That is a separate, bigger slice with its own Council; do not read this helper as covering it.'
- **Warum es zählt:** Downgraded from the worker's CRITICAL because the claim 'no mitigation and nobody wrote it down' is half false — a future session reading `restoreEngineIfStranded` is correctly warned. What survives is narrower and cosmetic: the RUNNING branch at :671 carries no pointer to that note, and its `recoveryAttempts = 0` at :672 disarms the recovery counter for a state it has not verified is healthy.
- **Billigster Fix:** One comment at :672 pointing at :2126-2131. The registry + `reconnectSources()` repair is the separate Council slice that note already names — do not fold it into a monitoring commit.

**Vom Lead verworfen (5):**
- ~~Worker B: plan line 99 ('Autotune und Pitch Shift existieren bereits und werden NICHT neu gebaut') over-claims a free pitch shift on the voice, which has a node~~ — The node measurement is correct — `git grep -n "voiceTunePitch" -- Sources` shows `.pitch` written only at `AudioEngine.swift:1808` (=0), :1969 (=0) and :2022 (the corrector), with no UI writer — but the over-claim reading is contradicted by the plan itself. `
- ~~Worker B: plan §7's Architect verdict cites a retracted rationale ('AUv3 kompiliert DSP/ isoliert', plan line 81).~~ — The retraction is real — `.claude/rules/swift-audio.md` says '⛔ A block of AUv3 patterns stood here for a target removed 2026-07-24' and 'Reason = hygiene + one-way dependency, not portability and not AUv3'. Not carried as a finding because the VERDICT it supp
- ~~Worker A: the input→notchEQ asymmetry is CRITICAL because `masterEngine.start()` throws when the input node is connected under `.playback`, which is the founder~~ — Downgraded, not rejected — retained as a WARN. The graph asymmetry and the false comment at :1826-1829 are measured; the throwing-start mechanism is inferred from AVAudioEngine semantics with no toolchain, no device log and no test. Reporting it as the confirm
- ~~Worker B (six 'OK' items): the mic path shape and its two wiring sites, the absence of `AVAudioSinkNode`, the six `AVAudioSourceNode` factories, the harmonizer/~~ — All re-checked and all correct — but they are premise confirmations, not defects, so they do not belong in a findings list. Two are worth carrying into the plan as notes rather than findings: the mic path is written TWICE (`AudioEngine.swift:1799-1812` build p
- ~~Both workers' file:line citations for AudioEngine.swift (Worker A ~8 low, Worker B ~5 low).~~ — Not a finding, but stated so the PM does not re-derive it: both workers measured a tree at or before `ebf5e69`, and `28b3805` (#630) inserted ~8 lines of retraction comment into `AudioEngine.swift`'s configuration-change observer. Every line number in the veri

---

## Team Bio-Honesty

**Nächste Scheibe laut Lead:** Add cross-process provenance to the App-Group bio payload and render it in the widget — 3 source files, one law ("a synthetic vital never renders as a measured body"), plus its CISmoke guard. (1) `Sources/Echoelmusic/Core/BioFeedbackManager.swift`: `public var synthetic: Bool?` beside `egressAllowed` (:55), a `.synthetic` CodingKey (:81-83), and `decodeIfPresent` at :98 — `Bool?` and NOT `BioSource`, because :113-121 records that this file compiles standalone into EchoelmusicWidgets/EchoelmusicWatch where `BioSource` does not exist; `nil` = older payload, renders unmarked, the exact tri-state semantics already ratified at `Sync/ColabPayload.swift:19-36` (#629). (2) `Sources/Echoelmusic/Core/BioFeedbackPublisher.swift:90`: `synthetic: frame.source == .fallback`, one line beside the existing `egressAllowed` mapping. (3) `Sources/EchoelmusicWidgets/EchoelBioWidget.swift:107` and `:126`: a dim `Text("Demo")` beside `Text("HR")` / `Text("Heart rate")` when `entry.vitals.synthetic == true`. Guard: `Tests/CISmoke/TheGlanceSaysWhetherItIsABodyTests.swift` — pin the mapping (`.fallback` → true, `.ble`/`.cameraPPG`/`.healthKit` → false), pin the `decodeIfPresent`-nil round-trip, and add the counterweight that nails shut the cheap wrong fix (dropping `.fallback` from the publisher entirely, which would blank the widget and assert "no session" — the worse of the two lies, per `ColabPayload.swift:26-31`). In the same commit, tick the surface onto the law's scope list at `Tests/CISmoke/TheDemoSourceIsMarkedWhereItRendersTests.swift:24-49`, which currently has zero occurrences of "widget" — a per-entry list only beats a count if entries get added when they are found. The Watch (`Sources/EchoelmusicWatch/EchoelWatchApp.swift:62-90`) inherits the field and is deliberately NOT touched: its target is not embedded (`project.yml:224`) and it has no transport, so marking it is free later and unverifiable now.


### BIO--0 · CRITICAL — The Home/Lock-Screen widget prints simulated HR/HRV/coherence as measured vitals — the payload type has no provenance field, and the law's own scope list never names it

- **Beleg:** PAYLOAD HAS NO SOURCE: `Sources/Echoelmusic/Core/BioFeedbackManager.swift:32-55` declares exactly seven fields (heartRateBPM/hrvNormalized/breathPhase/coherence/timestamp/breathRate/egressAllowed); CodingKeys at `:81-83` enumerate the same seven. PRODUCER IS UNGATED: `Sources/Echoelmusic/Core/BioFeedbackPublisher.swift:67-73` `guard isPublishing, let frame = bus?.usableBio(), frame.timestamp != lastPublishedTimestamp`, mapped at `:83-91` with no source term except `egressAllowed`. `.fallback` passes: `BioSimulator.swift:176` `bus.publish(bio: self.nextFrame())` with `source: .fallback` (`:203`) at 1 Hz, and `EngineBus.usableBio()` (`:586-592`) admits it inside the 5 s window (`:409`). LIVE AND SHIPPING: `EchoelmusicApp.swift:1105` `bioFeedback.start(publishingFrom: bus)` unconditional at launch, ticked at `:875`; `project.yml:212 - target: EchoelmusicWidgets` (embedded; the Watch line `:224` is commented out). RENDERER: `Sources/EchoelmusicWidgets/EchoelBioWidget.swift:67` `private var bpm: Int { Int(entry.vitals.heartRateBPM.rounded()) }` under `Text("HR")` (`:107`) / `Text("Heart rate")` (`:126`), HRV+Coh at `:74-81`, marketing line `:161` `.description("Live heart rate, HRV, and coherence from your session.")`. No freshness or source filter anywhere on the read path: `BioFeedbackManager.refreshFromSharedStore()` (`:178-188`) decodes and returns; `EchoelBioWidget.swift:47-50` re-serves the entry with `policy: .after(next)` (+5 min). `egressAllowed` cannot serve as the marker: it is `true` for `.fallback` (`Sync/BioEgressPolicy.swift:54`) and has ZERO production readers — `grep -rn "egressAllowed" Sources Tests` returns only the declaration/init/CodingKeys/decoder in BioFeedbackManager, the single write at BioFeedbackPublisher.swift:90, and test files. NOT ON THE LAW'S LIST: the scope enumeration at `Tests/CISmoke/TheDemoSourceIsMarkedWhereItRendersTests.swift:24-49` names header pill, BioStripView, BioMetricInfoView, OwnBioRow, AlwaysOnBioRow, BioModContributionRow + the two egress halves; `grep -in "widget" Tests/CISmoke/TheDemoSourceIsMarkedWhereItRendersTests.swift` → 0 hits in 383 lines. Same for `Sources/Echoelmusic/Bio/BioSimulator.swift:41-58`, which enumerates the affected surfaces and stops at the network egress.
- **Warum es zählt:** A glance at the Lock Screen reads "HR 74 bpm · HRV 41% · Coh 63%" with nothing that can say otherwise — and this is the ONLY bio surface that renders in a different process, so no in-app marker (#627/#627b/#629) can ever reach it. It also outlives the session: the timeline re-serves the last blob for 5 minutes and `refreshFromSharedStore` keeps returning it indefinitely afterwards, so a demo reading persists as a glanceable body measurement after the app is closed. Worse for planning: the law's own scope list is the artifact a future session will trust as the complete inventory, and it does not contain this surface. The identical unmarked render exists on the Watch (`Sources/EchoelmusicWatch/EchoelWatchApp.swift:62-90`), which is latent only because its target is not embedded and no transport exists.
- **Billigster Fix:** Mirror the #629 precedent rather than inventing a fourth spelling: add `public var synthetic: Bool?` to `BioVitals` (`BioFeedbackManager.swift:55`, beside `egressAllowed`), decoded `try c.decodeIfPresent(Bool.self, forKey: .synthetic)` at `:98` — `nil` = older payload, renders unmarked, exactly the semantics documented at `Sync/ColabPayload.swift:19-36` and `Studio/LiveColaboView.swift:393-396`. Set it at `BioFeedbackPublisher.swift:90` from `frame.source == .fallback`. ⚠️ IT MUST BE A `Bool`, NOT A `BioSource`: `BioFeedbackManager.swift:113-121` records that this file is compiled STANDALONE into EchoelmusicWidgets and EchoelmusicWatch, where `BioSource` does not exist (a previous attempt produced `cannot find 'BioSource' in scope`, twice). Then render `Text("Demo")` beside `Text("HR")` / `Text("Heart rate")` at `EchoelBioWidget.swift:107` and `:126` when `entry.vitals.synthetic == true`. The Watch inherits the field for free when its embed is solved.

### BIO--1 · WARN — SessionRecorder's header claims it counts "only real (or explicitly-labeled Demo)" frames — there is no such label and `ingest` has no source gate

- **Beleg:** `Sources/Echoelmusic/Core/SessionRecorder.swift:12-13` "Science-first: only real (or explicitly-labeled Demo) bio frames are counted." `grep -n "fallback\|synthetic\|Demo" Sources/Echoelmusic/Core/SessionRecorder.swift` returns ONLY that header line — no label exists in the file. `ingest` at `:174-181` folds `heartRateBPM`/`hrvNormalized`/`coherence` with no source test; it is fed the RAW snapshot at `:143` `if let frame = bus.latestBio { self.ingest(frame) }` (no freshness gate either). The persisted type carries no provenance: `BioSessionSummary` at `:22-43` is id/date/durationSeconds/avgHeartRate/avgHRV/avgCoherence/peakCoherence/sampleCount/name, written under `storageKey = "bioSessions.v1"` (`:125`). Rendered as history at `Sources/Echoelmusic/Studio/MeditationView.swift:221` `Text(verbatim: "coh " + EchoelDecimalText.string(s.avgCoherence, decimals: 2))` and folded into streak/trend at `:150`, `:181-182`. CANNOT FIRE TODAY: the only caller of `start`/`stop` is MeditationView (`:285`, `:295`), and `grep -rn "showMeditation" Sources` shows the flag declared at `EchoelStudioView.swift:753` and presented at `:1557` with NO `= true` setter anywhere (the file says so itself at `:775` and `:1718`).
- **Warum es zählt:** The false header is the load-bearing half. A dated, persisted, health-shaped record ("12 Aug, 14 min, coh 0.71") is the strongest implicit claim that a body was measured, and this header tells the next session the labeling problem is already solved here — so a re-door slice would ship the record unmarked while believing it was covered. Both halves of the sentence are untrue: nothing is source-filtered and no Demo label exists.
- **Billigster Fix:** Either gate (`guard frame.source != .fallback else { return }` at `SessionRecorder.swift:175`) or mark (count synthetic frames in `ingest` and surface a `Bool?` on `BioSessionSummary` decoded `decodeIfPresent`, rendered in the `MeditationView.swift:221` row). Pick one — but the header at `:12-13` must be struck in the same commit either way; leaving it is this repo's documented worst defect.

### BIO--2 · WARN — The Apple-Health whitelist — the health red line's one executable guarantee — is asserted only in the NON-blocking suite, and its CISmoke neighbour deliberately defers to it on a premise that is false

- **Beleg:** The invariant is intact and fail-safe: `Sources/Echoelmusic/Bio/HealthWritePolicy.swift:226-228` `isWritableSource` = `source == .ble || source == .cameraPPG`, a whitelist over a 7-case enum (`Core/EngineBus.swift:374-385`), checked at `:235` inside `shouldWrite`, which is the only path to `store.save` (`Bio/HealthKitWriter.swift`). But the assertion lives at `Tests/EchoelmusicTests/HealthWritePolicyTests.swift:28-39`, and `project.yml:355-359` shows the BLOCKING bundle `EchoelmusicTests` has `sources: - path: Tests/CISmoke`, while `Tests/EchoelmusicTests` builds only into `EchoelmusicFullTests` (`project.yml:388-389`), run by the non-blocking workflow. `grep -rn "isWritableSource" Tests/CISmoke` → three hits, ALL comments (`TheBreathEdgeReachesHealthTests.swift:39,41`, `TheDemoSourceIsMarkedWhereItRendersTests.swift:61`), zero assertions. AND the deference is explicit: `Tests/CISmoke/TheBreathEdgeReachesHealthTests.swift:43-45` — "These tests deliberately do not restate that gate; `HealthWritePolicyTests` covers it, and a second weaker copy of a live claim is its own defect (#416)." SECOND HALF: the deny-list at `HealthWritePolicyTests.swift:33` iterates `[.healthKit, .watch, .oura, .fallback]` — 4 of the 5 non-writable sources; `.faceCam = 6` (`EngineBus.swift:385`, "carries NO pulse") is absent, and `BioSource` is not `CaseIterable` (`:374`) so the loop cannot become exhaustive by construction.
- **Warum es zählt:** CLAUDE.md records that `Echoel Full Test Suite (non-blocking)` reported success on bc35248 while both real gates failed on 12 compile errors — so the rule that keeps synthetic, circular and pulse-free values out of a user's permanent Apple Health record is guarded by a suite that proves nothing. The #416 deference is correct reasoning on a wrong premise: it assumes `HealthWritePolicyTests` gates, and it does not. What actually protects users today is the whitelist FORM, not any executable check.
- **Billigster Fix:** Add the assertion to the blocking bundle and retire the false premise in the same commit: assert `isWritableSource` true for `.ble`/`.cameraPPG` and false for `.fallback`/`.healthKit`/`.watch`/`.oura`/`.faceCam` in `Tests/CISmoke/TheBreathEdgeReachesHealthTests.swift` (which already `@testable import Echoelmusic`), and correct its `:43-45` honest-limits bullet — it is no longer a "second weaker copy" once it is the only copy that gates. No new file, no new target.

### BIO--3 · WARN — The PERMANENT home of the five mandated safety warnings is English-only, while the transient onboarding copy IS localized to German

- **Beleg:** `Sources/Echoelmusic/Studio/LearnLibrary.swift:33-38` declares `title`/`summary`/`detail` as `String`; `Sources/Echoelmusic/Studio/LearnView.swift:140` renders `Text(entry.detail)` — the `StringProtocol` overload, which does NOT consult the catalog. The safety text at `LearnLibrary.swift:185-207` is therefore a literal English string. By contrast onboarding takes `LocalizedStringKey`: `Sources/Echoelmusic/Views/OnboardingView.swift:296` `private func safetyRow(_ symbol: String, _ text: LocalizedStringKey)`, fed the five warnings at `:187-191`, and the German values exist — `Sources/Echoelmusic/Resources/Localizable.xcstrings:274-287` "Not while driving or operating machinery." → "Nicht beim Führen von Fahrzeugen oder Maschinen." The app ships a full de-DE store listing (`fastlane/metadata/de-DE/description.txt:52`).
- **Warum es zählt:** `LearnLibrary.swift:172-180` records exactly why these entries were created: four of the five warnings lived ONLY in OnboardingView, which has no reset path, so no existing tester or second-launch reviewer could reach them. That failure is reintroduced one language over — a German user who taps through onboarding once can never re-read the mandated warnings in German. The same applies to the reachable BioScienceInfo/LightScienceInfo scope copy, also plain `String`.
- **Billigster Fix:** Change `LearnEntry.title/summary/detail` to `LocalizedStringKey` (`LearnLibrary.swift:36-38`) so `Text(entry.detail)` picks the localizing overload, and add the two `safety.*` values to `Localizable.xcstrings` in the SAME commit — the #618b precedent quoted at `OnboardingView.swift:160-163` is that shipping a key without its catalog entry silently renders English.

### BIO--4 · WARN — MIDI-out per-note expression is the one egress path that never consults BioEgressPolicy — dormant only because two flags have no writer

- **Beleg:** `Sources/Echoelmusic/Studio/PianoRollView.swift:1113` `guard midiOut?.expressionEnabled == true, let bio = bus?.usableBio() else { return nil }` → `:1124-1126` `MPEExpression.from(coherence: bio.coherenceForSound, breathDepth: breathSwell, hrvNormalized: bio.hrvForSound)` → `Sources/Echoelmusic/Audio/MIDIOutput.swift:142` `if mpeEnabled, expressionEnabled { … }` → pitch-bend / CC74 / channel pressure on the virtual source "Echoelmusic" (`:114`). `grep -rn "BioEgressPolicy\." Sources` returns 14 hits — OSCSender:166/205, ADMOSCSender:189, ArtNetSender:175, SACNSender:169, ColabPayload:136, ModulationEngine:306, BioFeedbackPublisher:90, PerformerSignature:245, plus 5 comments — and NOT ONE in `PianoRollView.swift`'s expression path or in `MIDIOutput.swift` (the single `BioEgressPolicy` mention in PianoRollView, `:1283`, is about the `isSounding` music-frame gate, an unrelated mechanism). DORMANT: `grep -rn "expressionEnabled\|mpeEnabled" Sources` shows declarations at `MIDIOutput.swift:50` and `:63`, both `= false`, and reads only — zero writers of `true` in `Sources/`.
- **Warum es zählt:** Two faults on one path. (a) `.fallback` coherence/HRV would drive a third-party DAW's per-note expression with nothing marking it synthetic. (b) Larger and outside this task's frame: because the policy is not consulted AT ALL, a `.healthKit`/`.watch`-sourced coherence would also reach a third-party host — the exact 5.1.3 shape `BioEgressPolicy` exists to prevent, on the one egress its own header does not enumerate. The day a routing surface adds that toggle, both ship together.
- **Billigster Fix:** One guard at `PianoRollView.swift:1113`: add `BioEgressPolicy.allowsEgress(bio.source)` — the same symbol every other sender already calls — which closes the 5.1.3 half. `.fallback` passes that policy by design (`BioEgressPolicy.swift:54`), so the synthetic half needs either `bio.source != .fallback` on top, or an explicit recorded decision to let the demo drive external gear (the #629 precedent argues for MARK, not silence — but MIDI CC has nowhere to carry a marker, which is why this is a decision, not a fix).

### BIO--5 · WARN — The founder's pastable diagnostic log reports `body=1` / `bio=1` for a purely synthetic session

- **Beleg:** Two breadcrumb writers, both source-blind. (1) `Sources/Echoelmusic/Views/MetalBioView.swift:798` `let bio = bus?.freshBio()` → `:901-903` `EchoelCrashLog.breadcrumb(String(format: "visual: bio=%d …", bio != nil ? 1 : 0, …))`. (2) `Sources/Echoelmusic/Studio/EchoelStudioView.swift:9023` `let frame = bus.usableBio()` → `:9811` `let bodyFlag: Int = frame == nil ? 0 : 1` → `:9816` `"genre=\(genreID), body=\(bodyFlag), …"` → `:9817` breadcrumb. Neither consults `BioSource`. That this stream is a reasoning surface is stated in-tree (`MetalBioView.swift:881-883`, `EchoelStudioView.swift:9687`), and it is shared in one tap (`Studio/SafeModeView.swift:62` `ShareLink(item: priorLog)`, `EchoelStudioView.swift:8292`). PARTIAL MITIGATION THAT EXISTS: `EchoelStudioView.swift:8867` `EchoelCrashLog.breadcrumb("bio simulation starting")` fires once at source start, so a reader who scrolls back far enough CAN correlate — the value lines cannot be read on their own.
- **Warum es zählt:** `body=` and `bio=` are the two fields a founder or a debugging session uses to answer "did a body actually reach the composer / the visual?". Answering "yes" for a simulated body sends the next investigation down the wrong branch. `Bio/PerformerSignature.swift:256-257` records that this is not hypothetical — a real 2026-08-05 founder device session ran under `bio simulation starting`.
- **Billigster Fix:** One interpolation per site, no new state: at `EchoelStudioView.swift:9811` `let bodyFlag: String = frame.map { $0.source == .fallback ? "1sim" : "1" } ?? "0"`; at `MetalBioView.swift:902` change `bio=%d` to `bio=%@` fed by the same three-way string. `body=1sim` is self-explaining in a log pasted with no source file beside it, which is the bar those two comment blocks set.

### BIO--6 · NIT — BreathGuideView and MeditationView each print a bare "Coherence 0.42" from any source — same three lines twice, both doorless

- **Beleg:** `Sources/Echoelmusic/Studio/BreathGuideView.swift:116-123` `let c = bus.freshBio()?.coherence ?? 0` → `Text(verbatim: c > 0 ? "Coherence " + … : "Coherence —")`, directly above `Text("Breathe with the circle. With a chest strap, watch coherence rise.")`. The file already knows how to ask: `:182` `guard let f = bus.freshBio(), f.source == .cameraPPG, …` correctly gates the ball-follow path. `Sources/Echoelmusic/Studio/MeditationView.swift:246-251` is the identical shape with no source test. BOTH DOORLESS: `grep -rn "BioSourceView(" Sources` → 0 (BreathGuideView's only mount is `BioSourceView.swift:61`); `showMeditation` has no `= true` setter (`EchoelStudioView.swift:753`, `:1557`).
- **Warum es zählt:** The adjacent sentence in BreathGuideView explicitly invites the number to be read as a physiological measurement that rises with practice. Latent only because both surfaces are unreachable — a re-door ships both. Listed separately from the SessionRecorder finding on purpose: that one is the PERSISTED average, this is the LIVE readout, and fixing one does not touch the other even though both live in MeditationView.swift.
- **Billigster Fix:** Reuse the shape already at `BreathGuideView.swift:182`: `let f = bus.freshBio(); let c = (f?.source == .fallback) ? 0 : (f?.coherence ?? 0)` renders "Coherence —" during simulation with zero new UI. One ternary each at `BreathGuideView.swift:116` and `MeditationView.swift:247`; do both in one slice.

### BIO--7 · NIT — The App Store submission checklist carries two stale claims on adjacent lines, both of which would misdirect a submission cycle

- **Beleg:** `docs/dev/APP_STORE_SUBMISSION_CHECKLIST.md:28` — "The `Framefile.json` caption \"Wellness\"/localized still needs brand-clean if used." `grep -ic wellness fastlane/Framefile.json` → **0**; the eight captions at `fastlane/Framefile.json:114-202` are Bio-Reactive/Generative/Visuals/Light/Sync/Export/Nearby/Sensors. And `:27` — "(NOT a MIDI export sheet — `exportMIDI()` has no caller, so that screen cannot be opened.)" `grep -rn "exportMIDI()" Sources` → `EchoelStudioView.swift:2111` `Button { exportMIDI() }`, i.e. it HAS a reachable caller (#188 restored the door).
- **Warum es zählt:** Prose outliving the code it describes, in the document a submission cycle reads first: one line sends someone to brand-clean a caption that does not exist, the next forbids a screenshot of a screen that is openable and that the shipped store description already claims. Neither is a compliance defect, both waste a submission cycle.
- **Billigster Fix:** Strike the `Wellness` clause at `:28` and correct the parenthetical at `:27` — two edits in one doc file, no code.

### BIO--8 · NIT — The one full 5-part safety block outside onboarding sits in a doorless view — harmless only because the feature it warns about is equally doorless

- **Beleg:** `Sources/Echoelmusic/Studio/BioSourceView.swift:175` carries the complete block ("Safety: not while driving or operating machinery; not under the influence of alcohol or drugs; coordinate any therapeutic use with your provider. For self-observation, not medical diagnosis. Max 3 Hz visual flash."). `grep -rn "BioSourceView(" Sources` → 0 instantiations. Symmetrically, the brainwave-entrainment feature it warns about is unreachable: `grep -rn "entrainmentEnabled" Sources` shows the ONLY writer is the Toggle at `BioSourceView.swift:151-152` inside that same doorless view, with the default `false` at `Tools/PolySynthVoice.swift:125`; the other three hits are reads (`PolySynthVoice.swift:953`, `EchoelStudioView.swift:5495`, `FloatingVisualWindow.swift:418`).
- **Warum es zählt:** No gap exists today — an unreachable warning for an unreachable feature, correctly co-located. It becomes a real compliance gap the moment anyone re-doors entrainment, because the warning does not travel with the toggle automatically and CLAUDE.md's "Brainwave Entrainment: NOT while operating vehicles" would then be unmet on a reachable surface.
- **Billigster Fix:** No change today. If entrainment is re-doored, mount the `BioSourceView.swift:175` text alongside the toggle in the same commit; a CISmoke guard asserting that any file writing `entrainmentEnabled = true` also contains the safety string would make it automatic.

**Vom Lead verworfen (2):**
- ~~WORKER 1, WARN: "`AlwaysOnBioRow` has TWO mounting points and the reachable one is the Bio panel — the deferral was presumably reasoned about as one surface."~~ — The two mounting points and the exact correction the worker offers are ALREADY recorded in-tree, in the artifact that defines the law's scope. `Tests/CISmoke/TheDemoSourceIsMarkedWhereItRendersTests.swift:40-46` reads: "`AlwaysOnBioRow` — DEFERRED, and the rec
- ~~WORKER 2, NIT: "Two of the three protected files have ZERO production consumers and BioEventGraph's heartbeat channel is fed a hardcoded 0 — a session reading '~~ — The measurements are correct — `grep -rn "HilbertSensorMapper" Sources` outside its own file → 0; `BioSignalDeconvolver` outside its own file → one DOC COMMENT (`Bio/BioEventGraph.swift:48`); `Bio/BioEventPublisher.swift:160` passes `cleanedHeart: 0, // raw wa

---

## Team UI-Shipblocker

**Nächste Scheibe laut Lead:** Guard the three `isSettled` writes so a tracked property on the rPPG publisher is written only on transition — one law, three files: (1) `Sources/Echoelmusic/Bio/CameraRPPGBioPublisher.swift:1248/:1250/:1254`, wrapped exactly like `recoveryState` at `:1170`; (2) `Sources/Echoelmusic/Studio/BioStripView.swift:80`, replacing "`isSettled` is low-frequency." with the honest split (the VALUE flips twice a take, the WRITE is per-tick); (3) one CISmoke guard asserting no unguarded `self.isSettled =` inside the publish tick, whose failure message names the `:464-470` law it enforces. Chosen over the cheaper `paused: reduceMotion` one-liner because this is the freeze-law family that has cost four device builds, it has an in-file precedent so the fix is unarguable, and it removes a false comment sitting at the exact read site that would otherwise authorise the next ancestor read. The `HeaderMonitors.swift:440` `paused: reduceMotion` line is the natural next slice after it.


### UI-S-0 · WARN — `isSettled` is written unconditionally on every 10 Hz tick, and the comment at its only body-scope read calls it "low-frequency"

- **Beleg:** `Sources/Echoelmusic/Bio/CameraRPPGBioPublisher.swift:1248` `self.isSettled = false`, `:1250` `self.isSettled = true`, `:1254` `self.isSettled = false` — three assignments, none equality-guarded, inside the publish tick (`activeTickSeconds` 0.1). The `:1250` branch is the costly one: once settled it fires on EVERY subsequent tick for the rest of the take. Declared TRACKED at `:355` `public private(set) var isSettled = false` (no `@ObservationIgnored`; class is `@Observable` at `:94`). The same file states the law at `:464-470` for `brightHintLatched`: "the `@Observable` macro calls `withMutation` on EVERY set regardless of equality: an unconditional assignment in the 10 Hz tick would ... re-create the menu-freeze this file has been fixed for twice." The guarded pattern is used ~80 lines earlier for `recoveryState` at `:1170` `if newRecovery != self.recoveryState { self.recoveryState = newRecovery }`. Body-scope reader: `Sources/Echoelmusic/Studio/BioStripView.swift:87` `.onChange(of: cameraRPPG.isSettled)` — the `of:` expression is evaluated during `body`, so the observation registers — and `BioStripView.swift:80` asserts verbatim "`isSettled` is low-frequency." Commands: `git grep -n "isSettled" -- Sources/Echoelmusic` (7 code sites); `git log -p dadcf65..HEAD -- Sources/Echoelmusic/Bio/CameraRPPGBioPublisher.swift | grep -E "^[+-].*isSettled"` → empty (pre-existing, not this session).
- **Warum es zählt:** Not live today: `BioStripView` is a leaf (`BioStripView.swift:15`, constructed at `EchoelStudioView.swift:3052`) and already churns from `fingerDetected` (:746) and `displayBPM` (:871), so the marginal cost is zero. It is a live 10 Hz invalidation source on the exact publisher the freeze law governs, and the prose at `BioStripView.swift:80` is the sentence a future session would cite to justify reading a cheap-looking settled/not-settled Bool from a header or HUD — inheriting the shipped 10.76.50 freeze. The value is low-frequency; the invalidation is not, and only the code says so.
- **Billigster Fix:** Wrap the three assignments the way `recoveryState` is at `CameraRPPGBioPublisher.swift:1170` — `if self.isSettled { self.isSettled = false }` / `if !self.isSettled { self.isSettled = true }` at :1248, :1250, :1254 — and correct `BioStripView.swift:80` to say the VALUE is low-frequency while the write is per-tick.

### UI-S-1 · WARN — Four transitively-high-frequency computed properties carry no freeze warning, while the one property that does is the one nobody is tempted to read

- **Beleg:** `CameraRPPGBioPublisher.swift:663` `rrWindowMs` carries an explicit block at `:653-658`: "⚠️ HIGH-FREQUENCY READ — the freeze law (10.76.50) ... must only ever be read inside a LEAF view." Four siblings carry none: `:344` `isLocked` (computes from `detectedBPM` + `confidence`, both TRACKED at `:110`/`:111` and written unguarded every tick at `:1191`/`:1192`); `:530` `acquisitionCue` → `placementCue`, which reads `analyzer.brightness`/`analyzer.redChannel` — TRACKED on the analyzer (`Sources/Echoelmusic/Video/CameraAnalyzer.swift:28`/`:30` under `@Observable` at `:22`, contrast `:157` `@ObservationIgnored ... lastFilteredAmplitude`) and written per analyzed frame at `CameraAnalyzer.swift:343-344`; `:518` `coachingHint` = `acquisitionCue.fullHint`; `:554` `cueWarrantsFullHintOnScreen`. Their docs actively read as safe — `:517` says "Pure derived state, read on the main actor by the UI." All current readers verified LEAF: `HeaderMonitors.swift:285`/`:300` sit inside `struct PulseMonitorMiniLive` (declared `:239`, next struct `:392`); `BioStripView.swift:234-235`; `PulseMeasurementView.swift:48`/`:53`. Command: `git grep -n "acquisitionCue\|coachingHint\|cueWarrantsFullHintOnScreen\|\.isLocked" -- Sources/Echoelmusic | grep -v CameraRPPGBioPublisher`.
- **Warum es zählt:** `coachingHint` is a `String` getter and `acquisitionCue` an enum getter — exactly the shape that invites the read the freeze law forbids, and exactly the 10.76.50 failure mode where every audit correctly cleared `EchoelStudioView` while the 10 Hz read sat one level up in a value that did not look live. The knowledge already exists in the repo but only in the header of a DOORLESS view: `PulseMeasurementView.swift:31` says "`coachingHint`, `isLocked` — is rewritten ~10 Hz while reading". A warning that lives only where nobody can reach is not a warning.
- **Billigster Fix:** Copy the `rrWindowMs` warning block (`CameraRPPGBioPublisher.swift:653-658`) onto `:344`, `:518`, `:530`, `:554`, each naming its transitive chain (`detectedBPM`/`confidence` for `isLocked`; `analyzer.brightness`/`redChannel` for the cue trio).

### UI-S-2 · WARN — `ImmersiveMonitorMini` is the repo's only `TimelineView(.animation)` without `paused:` — and under Reduce Motion its 20 Hz clock provably contributes nothing to the rendered pixel

- **Beleg:** `Sources/Echoelmusic/Studio/HeaderMonitors.swift:440` `TimelineView(.animation(minimumInterval: 1.0 / 20.0)) { tl in` — no `paused:`. `git grep -n "TimelineView(.animation" -- Sources/Echoelmusic` shows every other scheduled sibling passes it: `AnalysisScopeView.swift:65`, `AnalysisSpectrumView.swift:65`, `AnalysisWavefrontView.swift:73`, `SpectralDonutView.swift:31` (all `paused: reduceMotion`); `AnalysisSpectrumView.swift:186` passes `paused: false` deliberately. `reduceMotion` is already in scope at `HeaderMonitors.swift:398`. The proof that the clock is idle under Reduce Motion: `tl.date` feeds ONLY `phase` (`:490-491`), and `phase` feeds ONLY `pulse`, which at `:493` is `reduceMotion ? 0.5 : (0.5 - 0.5 * cos(...))` — a constant. The fix is safe and already modelled by its own sibling in the same file: `EchoelLuxMonitorMini` at `:544-547` branches `if reduceMotion` and drops the timer entirely, with the rationale "the colour then changes only when a new musical frame is published (via the observed `latestMusical`)" — and `latestMusical`/`latestBio` are indeed tracked (`Sources/Echoelmusic/Core/EngineBus.swift:613`/`:561`, neither `@ObservationIgnored`), so the tile still repaints on new frames when paused. Gate is `WorkspaceView.swift:676` `ImmersiveMonitorMini(active: cameraRPPG.isRunning)` — true for the whole biofeedback session. Not touched this session: `git log -p dadcf65..HEAD -- Sources/Echoelmusic/Studio/HeaderMonitors.swift | grep -E "^[+-].*(TimelineView|paused)"` → empty.
- **Warum es zählt:** Scoped honestly: this is NOT an observation leak — `ImmersiveMonitorMini` is a leaf struct (`HeaderMonitors.swift:392`), so the redraws do not reach the Picker-hosting ancestors, and there is no WCAG breach because the visible pulse is capped at `FlashGuard.maxPulseRateHz` (`:484`). What IS provable: the accessibility setting meant to reduce load does not reach the one always-on tile that runs for the entire session, and under it the 20 Hz redraw is pure waste. Whether that waste contributes to the reported "can't select while biofeedback runs" is UNVERIFIED — no profiler is available in this environment and I will not assert causation.
- **Billigster Fix:** `HeaderMonitors.swift:440` → `TimelineView(.animation(minimumInterval: 1.0 / 20.0, paused: reduceMotion))`. One argument, one line, precedent 100 lines below in the same file.

### UI-S-3 · WARN — CLAUDE.md and WorkspaceView prose claim two Analysis views are mounted; all four have zero construction sites, and the mount site itself says so

- **Beleg:** `git grep -c "AnalysisSpectrumView(\|AnalysisWavefrontView(\|AnalysisScopeView(\|AnalysisPoincareView(" -- Sources/Echoelmusic` → no output, exit 1 (zero matches). The tree states the truth at `Sources/Echoelmusic/Studio/EchoelStudioView.swift:4966-4969`: "`AnalysisWavefrontView` and `AnalysisSpectrumView` are now PARKED — files intact ... doorless ON PURPOSE, exactly like `AnalysisScopeView` and `AnalysisPoincareView` since 2026-08-02. All four analysis views are now in that state." Contradicted in two places: `CLAUDE.md:51` "Ihre zwei Geschwister derselben Epic sind sehr wohl montiert (`AnalysisSpectrumView` und `AnalysisWavefrontView`, beide in `EchoelStudioView`)", and `Sources/Echoelmusic/Studio/WorkspaceView.swift:402` "`AnalysisSpectrumView` in the Field panel is the same ring, the same bands" (present tense, arguing a live duplicate).
- **Warum es zählt:** Directly distorts this audit's own subject: those files carry 20/30 Hz `TimelineView` schedules (`AnalysisSpectrumView.swift:65`, `AnalysisWavefrontView.swift:73`, `AnalysisScopeView.swift:65`) and `AnalysisPoincareView.swift` reads the high-frequency `rrWindowMs`. A register saying they are mounted makes an auditor budget four live high-rate surfaces that cannot render — and makes the one real unpaused schedule look like one of five rather than the only one. CLAUDE.md is the always-loaded surface a session reads first; the correct statement is buried at line 4966 of an 11k-line file.
- **Billigster Fix:** Point `CLAUDE.md:51` and `WorkspaceView.swift:402` at `EchoelStudioView.swift:4966`, which is already the accurate statement in the tree — do not restate it a fourth time (#416).

### UI-S-4 · WARN — One stale pair ("SEVEN" indent-4 bodies, struct ":47") duplicated verbatim into both the guard's rationale and the always-loaded CLAUDE.md — measured 12 and :61

- **Beleg:** `Tests/CISmoke/ResetSoundClearsWhatTheLaunchLineReportsTests.swift:455-457`: "It took the first 4-indent `var body: some View` FILE-WIDE. There are SEVEN in this file (873, 9056, 9095, 9183, 9231, 9282, 9361); `EchoelStudioView`'s won only because the struct happens to start at :47." `CLAUDE.md:66` repeats it: "es gibt **sieben**; `EchoelStudioView` gewann nur, weil die `struct` bei :47 beginnt". Measured at f30eac0: `grep -c "^    var body: some View" Sources/Echoelmusic/Studio/EchoelStudioView.swift` → **12** (at 990, 10745, 10784, 10881, 10928, 10983, 11034, 11102, 11148, 11205, 11318, 11425 — none of the seven quoted figures is among them); `grep -n "^struct EchoelStudioView: View"` → **61**, not 47.
- **Warum es zählt:** The assertions themselves are unaffected — I replayed the anchor logic (`ResetSoundClearsWhatTheLaunchLineReportsTests.swift:481-495`) and it resolves struct=61, body=990, end=1643, sentinel `$showSavePatchAs` present, chain 14, file-wide 16. But the figure UNDERSTATES the exposure by five in the exact comment that justifies why the struct anchor exists, and CLAUDE.md carries it into every session's opening context sitting beside a correct 14/16, which lends it unearned credibility. A session trimming 'defensive' anchoring would reason from a premise that is wrong by 5.
- **Billigster Fix:** Replace the enumeration in both sites with the count-only claim plus its command (`grep -c "^    var body: some View"` → 12; struct at :61), or drop the list entirely — the argument for anchoring to the struct does not need it.

### UI-S-5 · NIT — HeaderMonitors' own rate ledger names 10 Hz for a 2.5 Hz tile and cites a 20 Hz sibling as the 2.5 Hz ceiling

- **Beleg:** `Sources/Echoelmusic/Studio/HeaderMonitors.swift:519` `/// 10 Hz TimelineView like the visual tile.` Wrong for both halves: this tile's schedule is `1.0 / FlashGuard.maxPulseRateHz` (`:559`) and `Sources/Echoelmusic/Studio/FlashGuard.swift:64` `public static let maxPulseRateHz: Double = 2.5`; the visual tile is `1.0 / 20.0` = 20 Hz (`:440`). A second cross-reference in the same block compounds it — `:550` calls 2.5 Hz "the app's MetalBioView/ImmersiveMonitorMini ceiling" while `ImmersiveMonitorMini` runs at 20 Hz.
- **Warum es zählt:** This is the file's own rate ledger, read by anyone triaging the menu-freeze. A doc line naming 10 Hz for a 20 Hz tile is plausibly how the unpaused 20 Hz schedule stayed invisible across prior audits: grep for high-rate surfaces, read "10 Hz", move on.
- **Billigster Fix:** `HeaderMonitors.swift:519` → "2.5 Hz TimelineView (FlashGuard-capped); the visual tile runs 20 Hz"; drop the `ImmersiveMonitorMini` name from `:550`.

### UI-S-6 · NIT — The `== 16` failure message sends a fixer to a comment that deliberately holds only the chain number

- **Beleg:** `Tests/CISmoke/ResetSoundClearsWhatTheLaunchLineReportsTests.swift:441-443`: "update this number, the `CHAIN LENGTH` comment in `EchoelStudioView`, and the two counts in `CLAUDE.md` in the same commit." But `Sources/Echoelmusic/Studio/EchoelStudioView.swift:768-778` states only the chain figure and opens with "CHAIN LENGTH — ONE number, kept here so nobody reads two: the body carries **14** presentation modifiers" — it never mentions 16.
- **Warum es zählt:** A file-wide-only change (adding or removing a NESTED modifier, e.g. a second `.sheet` inside `openSheet` at :8488) turns `== 16` red while the chain stays 14. Following the message sends the fixer to edit a comment that is still correct — and invites writing the file-wide number into the one site that deliberately holds one number, which is the defect that comment exists to prevent.
- **Billigster Fix:** Reword `:441-443` to "…update this number and the two counts in `CLAUDE.md`; update the `CHAIN LENGTH` comment only if the CHAIN assertion below also moved."

**Vom Lead verworfen (5):**
- ~~Worker 1, five OK items: chain = 14, file-wide = 16, three prose sites agree, the guard measures what it claims with a working sentinel, and no commit added a p~~ — All five independently re-measured at f30eac0 and all five are TRUE — but they are confirmations, not findings, and padding a findings list with clean results is the failure the brief names. Carried instead as the direct answer to the framing question: chain 1
- ~~Worker 2, OK item: no ancestor-body violation exists today; both root bodies are clean.~~ — True and independently re-measured — `awk 'NR>=990 && NR<=1643' EchoelStudioView.swift | sed 's|//.*||' | grep -c "cameraRPPG\.\|bus\."` → 0, and the WorkspaceView struct (94–741, close found by `awk 'NR>=94 && /^\}/'`) → 1 hit, `cameraRPPG.isRunning` at :676,
- ~~Worker 2, NIT: the bio-source `Menu` at EchoelStudioView.swift:3019 is hosted by the root body with zero structural insulation and should be named in the guard'~~ — Verified accurate (Menu at :3019 inside bioSourceRow, and dropdownContent's header at :2958-2961 does say it is evaluated in the ROOT body permanently) — but the worker's own cheapest_fix is "No code change", and the mechanism is already stated verbatim at the
- ~~Worker 2: the unpaused 20 Hz header TimelineView "is the SECOND documented cause of 'kann während Biofeedback nichts mehr auswählen' (10.76.48)."~~ — Mis-attribution. 10.76.48 is documented in CLAUDE.md as a per-frame `Task { @MainActor }` submission flood from `CameraRPPGBioPublisher.onFrame` at ~30 fps starving the executor — a different mechanism from a redraw schedule inside a leaf. The schedule finding
- ~~Worker 2: under Reduce Motion "a large share of that work provably repaints an identical value."~~ — Imprecise in both directions, so restated rather than accepted. Stronger than claimed on one axis: `tl.date` reaches the output ONLY through `phase` → `pulse`, and `pulse` is constant 0.5 at HeaderMonitors.swift:493, so the clock contributes exactly nothing. W
