build: 2026-06-24 v10.76.18 (DIAGNOSTIC: exact 10.76.9 code — the last build that launched)
branch: claude/piano-roll-clip-view-wozlie
notesT: After 4 builds that black-screened (10.76.14–.17), this build reverts the app code
  to EXACTLY 10.76.9 — the last version you confirmed launching (the "rendert bunt"
  build). Everything since (Bio→Visual routing, the colour-cloud shaders, the camera
  diagnostic, the sheet/cover refactors) is rolled back. This is a deliberate ground-truth
  test:
   • If 10.76.18 LAUNCHES → the problem is in the code added after 10.76.9, and I now know
     exactly which 7 files to bisect — I re-add them one at a time WITH a launch check each
     time, so we never go dark again.
   • If 10.76.18 STILL black-screens → the problem is NOT the app code (build pipeline,
     signing, or the install on the device), and I stop changing code and look there.
  PLEASE: delete the app first, then install 10.76.18 fresh from TestFlight (clears any
  stuck state). Tell me launches / black. If black, also send the crash log: Settings >
  Privacy & Security > Analysis & Improvements > Analytics Data > newest "Echoel…".
