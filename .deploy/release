build: 2026-07-18 v10.79.295 (Housekeeping — Sample-Namen + Colabo-Datenschutz)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Zwei Fixes, KEIN neuer Hörtest noetig — die v290-294-Hoertests bleiben offen
  (Akkord-Stempel, Audio-Clip-Launch, Chance, Per-Spur-Patch, Occurrence).

  (1) SAMPLE-/IMPORT-NAME: Wenn du eine Audio-Datei als Sample/Loop importierst,
  zeigt die Spur jetzt den ECHTEN Datei-Namen (z.B. "MyLoop") statt einer kryptischen
  UUID. Betrifft neue Importe; bestehende Refs laden unveraendert weiter.

  (2) DATENSCHUTZ-HAERTUNG (App Store 5.1.3): Live-Colabo (Puls an Geraete in der
  Naehe) streamt jetzt NUR noch Puls aus Echoels EIGENEN Messquellen — Kamera-rPPG,
  BLE-Brustgurt, Gesichtsausdruck, Demo. Werte aus Apple Health / Apple Watch / Ring
  bleiben aufm Geraet und verlassen es nie an fremde Peers (dieselbe Regel, die die
  OSC/ADM-OSC-Netz-Sender schon anwenden). Passt zu deinem Wunsch, dass Bio-Daten
  nicht ungefragt an Fremdgeraete gehen.

  Unter der Haube: security-agent hat den Egress-Pfad als vollstaendig+sicher
  bestaetigt (der Share-Loop ist der einzige Bio-Ausgang; der Projekt-Share traegt
  kein Bio). Beide Fixes: alle Gates gruen, Reviewer sauber.

  Weiter offen auf deiner Seite: iPhone-Neustart fuer AUv3-Register (Log "self-probe
  -3000"); die Hoertests v290 (Akkord), v291 (Audio-Clip-Launch), v292 (Chance), v293
  (Per-Spur-Synth-Patch: 2 Spuren 2 Klaenge?), v294 (Occurrence: Note auf 1:2, spielt
  sie nur jede 2. Runde?).
