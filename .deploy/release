build: 2026-06-23 v10.47.0 (Multidimensional color rings — swing + distance + elevation)
branch: claude/piano-roll-clip-view-wozlie
notes: The FFT color rings are now fully multidimensional, bound to the body and the spatial mapping (same azimuth/elevation/distance intuition as ADM-OSC): SWING (breath → horizontal azimuth, slow ≤0.13 Hz pendulum, amplitude follows loudness), DISTANCE (coherence scales the whole field — high coherence pulls it close/larger), ELEVATION (HRV squashes it into a taller/shorter ellipse = lift). Bounded + slow (no flashing; depth cues apply even under Reduce Motion, the pendulum sway respects it). Under the hood, the tested MPE 5D-expression core (body→Slide/Press/Glide + MIDI encoders) and the room-in-room convolution-reverb core are built + verified, staged for device-verified audio wiring (the founder-approved live sound stays untouched). Plan: scratchpads/PLAN_MULTIDIMENSIONAL_SOUND_2026-06-22.md. iOS compile gate green.
