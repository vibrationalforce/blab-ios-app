build: 2026-07-02 v10.79.5 (Pro pass II — music breathes · palette graded · filmic finish · rPPG)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notesT: Professionalization loop, build 2 — steer me on each and I keep going.
  MUSIC (M1): the take BREATHES now — whole texture rises/relaxes over the bar (downbeats
  lead + gentle swell) instead of a flat metronome. (Next: real intro/build/break across
  several bars — that's an arrangement rebuild, coming as its own careful step.)
  VISUAL (V1+V2): eased off "neon rainbow" toward a graded, natural-light palette (same
  scientific tone→colour, less saturated + warmer) AND added a gentle filmic contrast so the
  frame reads rich/graded, not flat. VJ Saturation can still push vivid.
  rPPG: carries both camera-pulse lock fixes (finger no longer flip-flops; a small movement
  no longer wipes a good lock) — Bio page, cover camera + flash, hold ~15-20 s.
  Test 10.79.5: (1) does the music phrase/breathe — more or less? (2) is the visual less neon
  / more premium — grade it further? (3) does the pulse lock and hold? Tell me per item.
