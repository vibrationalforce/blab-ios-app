build: 2026-06-21 v10.34.31 (AUv3 plugin UI + IA cleanup)
branch: claude/piano-roll-clip-view-wozlie
includes: Two things land here. (1) Open a hosted plugin's OWN interface — "Open" on a loaded instrument or insert effect in Tools ▸ Plugins presents the plugin's native UI in an Echoel-framed sheet (with an honest "no custom interface" fallback for plugins that ship none). (2) IA cleanup the founder flagged: Clips and Arrangement were appearing in BOTH the top Arrange · Clips · Compose surface picker AND the Compose tools bar, doing two different things under one name. The duplicates are removed from the Compose bar — Clips/Arrange now live only as top-level surfaces; the Compose bar holds just its own sub-editors (Piano Roll, Sound) plus tools (Drum Samples, Breathing, Audio In, Routing, Plugins, Visual). One name, one behaviour. Verified: full repo wiring audit (AUv3 host, Signal Router, music→light/spatial, MusicalFrame consumers all wired, no orphaned code) + Uncodixfy design audit (radii ≤12, no glow/glass, EchoelValueField everywhere) + Xcode Compile Check green.
