build: 2026-06-19 v10.34.6 (visual flash-safety centralized via BioVisualParams/FlashGuard)
branch: claude/piano-roll-clip-view-wozlie
includes: the immersive Metal bio-visual now sources its heartbeat pulse from the unit-tested BioVisualParams (→ FlashGuard), making WCAG-2.3.1 flash-safety a single source of truth instead of duplicated math in the shader. Visually identical (same hr/60 pulse mapping, same Reduce-Motion still frame, same 2.5 Hz defense-in-depth cap); this wires the BioVisualParams core into the live renderer and is the integration point for richer bio-visual patterns later. Plus everything from v10.34.5 (Sound & texture library bar), v10.34.4 (saveable Moods), v10.34.3 (FX param standard + AudioEngine concurrency fix). Verified: ci.yml (SPM) green + xcode-compile-check (AUv3 included) green through 4507e94.
