build: v10.79.339 — Instrument-Home + AUv3 wird spielbares Instrument (Founder: "arbeitest bis TestFlight erfolgreich")
branch: claude/echoelmusic-neustart-auv3-6ri2ek (tokenless deploy via .deploy/release bump)
notes: |
  Erster TestFlight-Ship des Vision-Umbaus + der AUv3-Neustart-Arbeit. Founder hob den
  TestFlight-Freeze explizit auf ("Du arbeitest bis TestFlight Build erfolgreich").

  ENTHALTEN:
  - Vision Step 1 (Instrument-Home): die App öffnet direkt ins lebendige Instrument —
    das bestehende FloatingVisualWindow FULLSCREEN (der eine MetalBioView + das
    spielbare TouchInstrumentView). Die DAW-Chrome bleibt DARUNTER montiert (kein
    Teardown, laufende Bio+Transport-Session überlebt), einen Tap entfernt. Hinter
    FeatureFlags.instrumentHome (DEFAULT-AN; Rückschalter set(.instrumentHome,false)).
    Gates grün (721661f: Xcode + CI).
  - Step 1b: eine würdige "Studio"-Tür am Vollbild-Visual, die ins volle tight-loops-DAW
    fällt (Timeline · Clips · Per-Track-Instrumente · Video-Spuren · WAV-Export) — die
    Produzenten-Ebene bleibt erhalten und erreichbar. ui-state-reviewer: 0 Findings.
  - B1 (AUv3): EchoelBodyVibe ist jetzt ein aumu-Musikinstrument (statt augn-Generator)
    mit MIDI-Note-Input im Render-Block (realtimeEventListHead -> EchoelMIDIDecode ->
    EchoelDDSP.noteOn/noteOff, audio-thread-safe, Reviewer CLEAN). Hosts routen jetzt
    MIDI an das Plugin. embed-verify-Gate erwartet aumu. Component-Version 10001->10002
    (erzwingt frische Registrierung).

  Beide Ship-Blocker-Gesetze verifiziert (kein Sheet-Wachstum an EchoelStudioView, kein
  10-Hz-@Observable-Read im Root/Ancestor). Kein Audio-Thread-alloc/lock, Rausch-Triad
  unberührt, kein Heilungs-Claim. Nächste Scheiben (Cold-Start-Choreografie Step 2,
  Startmodus) sind geplant + warten auf Founder-Wort.
