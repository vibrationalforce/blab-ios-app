build: 2026-07-19 v10.79.297 (AUv3-HOST-FIX — Inter-App-Audio-Entitlement)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: DER AUv3-FIX. Dein v296-Log war eindeutig: unser App-PROZESS sah 101
  Apple-Units, 0 Fremd-Plugins, nicht mal unsere eigene EchoelBodyVibe
  (self-probe -3000) — WÄHREND AUM auf demselben Gerät alles sah. Also nicht
  "nicht registriert", sondern: unsere App durfte die Fremd-Plugin-Liste gar
  nicht sehen. Der Discriminator (GarageBand öffnen → zurück → immer noch leer,
  dein "Nein") hat Timing/Warmup ausgeschlossen.

  URSACHE + FIX: uns fehlte das **Inter-App-Audio-Entitlement** — genau das Tor,
  das iOS prüft, bevor ein Host Fremd-AUv3 aufzählen darf. Jeder AU-Host (AUM,
  Loopy, AudioKit) hat es; wir nicht. Jetzt gesetzt (Host-App-Entitlement).

  BITTE AUF v297 TESTEN (der eigentliche Beweis):
  Spurkopf ▸ "Browse AUv3…" → erscheinen jetzt DEINE installierten Fremd-Plugins
  UND unser eigenes "Echoelmusic: EchoelBodyVibe" (nicht mehr nur die Apple-Units)?
  → Wenn ja: AUv3-Hosting ist LIVE — item 3 (sichtbare Instrument/FX-Belegung pro
  Spur) ist damit erlebbar, und "lässt sich nicht öffnen" sollte weg sein (Load
  zeigt jetzt zusätzlich den rohen OSStatus-Code, falls doch mal was hakt).

  REVERSIBEL: das IAA-Streaming-FEATURE ist abgekündigt, aber das ENTITLEMENT ist
  weiter der Discovery-Gate. Falls das Archiv es doch ablehnt (Provisioning),
  revertiere ich sofort — v296 bleibt unberührt, dieser Build lädt dann einfach
  nicht hoch. Erwartung: automatic signing + -allowProvisioningUpdates fügt die
  Capability der App-ID hinzu (gleicher Mechanismus wie HealthKit/CloudKit/Weather).

  Unter der Haube seit v296: Load-Fehler zeigt jetzt den echten domain#code
  (efe621b, reviewer clean, Gates grün-erwartet). rPPG hat in deinem v296-Log
  sauber gelockt (settled 64 bpm, conf 0.76) — Puls-Teil auf dem Gerät gesund.
