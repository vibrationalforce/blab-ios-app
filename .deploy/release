build: 2026-06-19 v10.34.7 (export the whole take as one multi-track MIDI file)
branch: claude/piano-roll-clip-view-wozlie
includes: "Export MIDI" now writes the COMPLETE arrangement as a single Standard MIDI File (Type 1) — a conductor/tempo track, the melody on channel 1 (real note durations + velocity) and the drums on channel 10 (GM percussion) — so the whole take opens together in any DAW instead of the beat being dropped. Pure value logic, unit-tested (SMF Type-1 header, three tracks, tempo on conductor, melody ch.1 + drum ch.10 events). Plus everything from v10.34.3–v10.34.6 (FX param standard + AudioEngine concurrency fix; saveable Moods; Sound & texture library bar; visual flash-safety via BioVisualParams/FlashGuard). Verified: ci.yml (SPM tests incl. MIDIFileExporterTests) green + xcode-compile-check (AUv3 included) green through 57851c8.
