build: 2026-06-21 v10.34.21 (Arrangement/Clips is the home — biofeedback becomes a tool)
branch: claude/piano-roll-clip-view-wozlie
includes: The app now opens like a professional workstation: a persistent surface switcher (Arrange · Clips · Compose) with the Arrangement/Clips timeline in the FOREGROUND, and the bio-generative instrument moved into a "Compose" surface — biofeedback is now one tool, not the whole screen. Clips are typed (Audio · MIDI · Video · Visual) so the main view can become a clip-typed timeline; today MIDI/pattern clips play, the other lanes are honest scaffolding (audio/video/visual engines are roadmap). Old clip libraries load unchanged (decode as MIDI). Switching surfaces never interrupts the live instrument — all surfaces stay mounted, so audio keeps running. Verified: Xcode Compile Check + CI green.
