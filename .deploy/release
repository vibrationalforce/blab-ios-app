build: 2026-07-14 v10.79.219 (Region an den Playhead verschieben — Verbsatz komplett)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Der Arrangement-Verbsatz ist jetzt vollständig: Schneiden (Split) · Fügen
  (Join) · Löschen (Delete) · und neu VERSCHIEBEN (Move).

  WO: Region lange drücken → Menü → „Move to playhead". Die Region springt mit
  ihrem Anfang genau an die aktuelle Playhead-Position.

  WARUM per Playhead statt Ziehen: freies Ziehen einer Region kollidiert mit dem
  seitlichen Scrollen der Timeline (das feine Gesten-Tuning dafür braucht ein
  Gerät und kommt später). Die Playhead-Geste ist sofort verlässlich — dasselbe
  Prinzip wie Split/Join, die auch am Playhead arbeiten: Playhead setzen, dann
  „Move to playhead". Regionen dürfen sich dabei überlappen (freies Arrangieren).

  WIE: die Store-Methode `moveRegion` gab es längst (klemmt den Start bei 0). Der
  Playhead-Stand wird NUR im Tastendruck gelesen (nie im Body) — das Menü-Freeze-
  Gesetz bleibt gewahrt, exakt wie bei Split/Join. Kein Sheet, keine Modal-Kette
  gewachsen.

  SICHER: Reviewer ui-state CONFIRMED CLEAN (Freeze-Gesetz gewahrt · Sheet-Kette
  unberührt · Tap-Zeit-Read + geklemmte Mutation + beobachtetes Neu-Rendern).
  Neuer Test: moveRegion setzt den Start-Tick und klemmt ein negatives Ziel auf 0.

  HINWEIS ZUR CI (kein Code-Problem): Der v218-TestFlight-Lauf scheiterte an einer
  FEHLENDEN Metal-Toolchain auf dem GitHub-Runner (Infra, nicht mein Code — v216/
  v217 archivierten identisch grün). Beide echten Code-Gates (Xcode Compile Check +
  CI/CD Pipeline) waren bei v218 GRÜN. TestFlight ist ohnehin eingefroren; falls der
  Metal-Fehler erneut auftritt, ist er persistent und ich ziehe den bekannten Fix
  (`xcodebuild -downloadComponent MetalToolchain` in testflight.yml) nach — nach
  deiner Freigabe, da CI-Config.

  Stand DAW-Ausbau: Automation ✓ · Clips Split/Join/Delete/Move ✓ · Audio-Import ✓ ·
  Video-Import ✓. Als Nächstes: das transport-synchrone Abspielen der Spuren
  (eigenes Player-Subsystem, geräteverifiziert; zuerst die reine Scheduling-Naht test-first).
