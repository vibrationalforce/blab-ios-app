build: 2026-06-23 v10.63.0 (Header layout polish — version centred under brand, monitors get the corners)
branch: claude/piano-roll-clip-view-wozlie
notes: Founder layout tweak on the new Header Dual-Monitor: the build version moves SMALL and CENTRED directly under "Echoelmusic", freeing both header corners entirely for the live monitors — LEFT the EKG pulse trace + BPM, RIGHT the immersive-visual monitor (now a touch larger, 54×32, since it owns the full right corner). Header height nudged to 50pt to seat the centred brand+version stack. Everything else unchanged: real EKG Canvas over the rPPG waveform, real MetalBioView immersive monitor (live only during a session), tap-to-expand full screen, full VoiceOver labelling, Uncodixfy-compliant. Cumulative with the v10.62 monitors, the v10.61 stop-hang stability fix, and the confirmed-working rPPG lock. iOS compile gate validates the SwiftUI + Metal.
