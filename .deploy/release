build: 2026-07-21 — WeatherKit Portal-Capability + AUv3-Provisioning-Refresh + Profi-Bereinigung
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Founder-Befehl "Bau" nach WeatherKit-Portal-Fix (Capability angehakt) + IAA-Klärung
  (Inter-App Audio war immer schon aktiviert — die IAA-Gate-Hypothese für AUv3 "0 third-party"
  ist damit widerlegt).

  WARUM DIESER BUILD: WeatherKit-Capability war im Portal NICHT angehakt (nur MusicKit/ShazamKit),
  obwohl `Echoelmusic.entitlements` `com.apple.developer.weatherkit` deklariert UND der Code das
  Feature real nutzt (Core/WeatherProvider.swift → WeatherService.shared.weather(for:), live über
  weatherEnabled). Founder hat die Capability jetzt im Portal angehakt. Ein neuer Archiv-Build mit
  `-allowProvisioningUpdates` regeneriert dabei ALLE Profile frisch (App + Widget + AUv3-Appex) —
  das behebt WeatherKit sicher UND ist der beste verbleibende Hebel für den offenen AUv3-„-3000"-
  Befund (embed/sign/provisioning/Deklaration waren bereits einzeln bestätigt korrekt; ein
  inkonsistentes/veraltetes Profil war die letzte plausible Restursache).

  Seit dem letzten Deploy (2433) auf dem Branch:
  · #77 — 3 verschwundene Bio→Sound-Mappings restauriert (HRV→Brightness, Atemphase→Amplituden-
    Swell, coherenceTrend→Spektral-Morph), alle 3 Pflicht-Reviewer PASS (Audio-Thread/DSP/Bio-Safety).
  · AUv3-Diagnose geschärft: On-Device-Scan liest jetzt die WIRKLICH eingebettete .appex und meldet
    im Report, ob die Ursache Build- oder Registrierungs-seitig ist.
  · Profi-Bereinigung (Founder: "No AUM/rescan guidance … stay professional") — jede
    Workaround-Anleitung (Fremd-App öffnen, Rescan, Neuinstallation, Geräte-Neustart) aus der
    Nutzer-Fläche entfernt; Lade-Fehler zeigen keinen rohen OSStatus-Jargon mehr.
  · #78 Test-Heal — mehrere Cluster gegen aktuelle Sources verifiziert und geheilt (A8-Harmonicity-
    Drift, EchoelDelay-Echo-Timing, EchoelDDSP-Defaults).

  Gates: Xcode Compile Check + Echoelmusic CI/CD Pipeline grün auf jedem Commit dieser Reihe.
