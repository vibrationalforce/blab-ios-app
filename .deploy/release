build: 2026-07-19 v10.79.298 (AUv3-HOST-FIX B — App-ID-Capability aktiv → Re-Archive)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: DER EIGENTLICHE AUv3-FIX-BUILD. v297 zeigte auf deinem Gerät exakt v296:
  "101 Audio Units — 0 third-party, own AUv3 not visible, self-probe FAILED -3000".
  Das Entitlement war im Code korrekt, wurde aber beim Signing GESTRIPPT, weil die
  "Inter-App Audio"-Capability nicht auf der App-ID aktiviert war (automatic signing
  kann diese abgekündigte Capability nicht selbst hinzufügen).

  DU HAST ES GEFIXT: im Developer-Portal Inter-App Audio auf com.echoelmusic.app
  angehakt + gespeichert (per Video bestätigt). Dieser Re-Archive lässt
  -allowProvisioningUpdates jetzt ein Profil MIT der Capability regenerieren →
  das inter-app-audio-Entitlement überlebt ins signierte Binary.

  BITTE AUF v298 TESTEN — der Beweis:
  Spurkopf ▸ "Browse AUv3…" → erscheinen jetzt deine installierten Fremd-Plugins
  UND "Echoelmusic: EchoelBodyVibe" (statt nur der 19 Apple-Units)? Und öffnet sich
  eins beim Antippen? Wenn ja: AUv3-Hosting ist LIVE — item 3 erlebbar, der
  #1-Blocker ist gelöst.

  Falls WIDER ERWARTEN v298 immer noch "0 third-party" zeigt: schick die
  self-probe-Log-Zeile. INSTANTIATE OK ⇒ Profil-Cache/Propagation (kurz warten +
  1× Re-Archive); FAILED ⇒ tiefer (dann Registrierungspfad). Aber der Fingerabdruck
  sagt: mit aktiver App-ID-Capability sollte v298 ziehen.

  Kein Code-Change gegenüber v297 — reiner Re-Archive gegen die jetzt-fähige App-ID.
