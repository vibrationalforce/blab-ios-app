build: 2026-06-21 v10.34.37 (Transport & mastering — metronome · tap tempo · R128 loudness)
branch: claude/piano-roll-clip-view-wozlie
includes: Production + performance staples this batch. METRONOME: a steady click (Composition panel) — self-driving so it stays rock-steady regardless of UI jitter and even when nothing is playing (practice click); accented downbeat, beats-per-bar + level controls; follows the live transport tempo and aligns its downbeat when a take starts. Launch-silent. TAP TEMPO: dial BPM by feel — tap in time and it sets + locks the tempo and steers the click. MASTER LOUDNESS: the master output's EBU R128 loudness — short-term + gated-integrated LUFS, max true-peak (dBTP) and loudness range (LU) — was computed every audio block but never shown; now a new "Master" panel surfaces it (with master volume + a Reset for a fresh measurement) and the streaming target (≈ −14 LUFS, ≤ −1 dBTP). The same readout appears in Tools ▸ Broadcast so you can check loudness before going live. All additive (no master-output-path change); audio-thread + concurrency reviewed; Xcode Compile Check green across every commit. Still honestly held for device verification: HaishinKit A/V streaming wiring and the master-bus AUv3 FX slot.
