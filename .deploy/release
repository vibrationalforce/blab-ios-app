build: 2026-07-02 v10.79.1 (Polish — one breath home, bigger tap targets, consistent theme)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notesT: A tidy-up pass on top of 10.79.0's unification.
  • Breathing lives only on the Bio page now (was also duplicated in Compose's Tools) —
    one body/breath home, no doubled path.
  • Bigger, easier taps: the project Share icon, Channel Rack Mute/Solo, the Browser
    favorite star, and the Arrangement controls are all full-size (≥44pt) now.
  • Consistent colours: a few stray off-theme greys/yellows now use the Echoel palette
    (Open-project subtitles, the Bio strip text, the FX favorite swipe).
  • Header pulse VoiceOver now reads correctly ("Opens the Bio page").
  Test 10.79.1: Compose Tools no longer lists Breathing (it's on Bio); Mute/Solo and the
  Browser star are comfortable to tap; nothing looks off-palette.
