build: 2026-07-04 v10.79.71 (FIX: echte Instrumente + Drums waren nie im App-Bundle — jetzt schon)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notesT: WICHTIG — dein Log 1783199053 hat den wahren Grund verraten, warum es immer noch
  nach dem alten Synth klingt: die 32 MB Soundfont (echte Instrumente) UND die Drum-WAVs
  waren nie ins App-Bundle kopiert. Der Xcode/TestFlight-Build (project.yml) hat nur
  Assets/Fonts gebündelt, NICHT den Instruments/Drums/Samples-Ordner. Ergebnis auf dem
  Gerät: Soundfont = nicht gefunden → alles fiel still zurück auf den additiven Synth
  (im Log sichtbar: "polyVoice.noteOn"). In den Tests (swift test) war die Soundfont da
  (anderer Bundle-Mechanismus), deshalb ist es nie aufgefallen.
  FIX: Instruments (Soundfont) + Drums + Samples werden jetzt korrekt ins App-Bundle
  gepackt. Ab diesem Build laden Lead/Harmonie WIRKLICH als echte Instrumente (Piano/
  Strings) statt Synth, und die Drums klingen nach echten Samples.
  (Enthält weiter: Drone wenn ruhig · Halten wenn eingerastet · weniger Verzerrung.)
  TEST 10.79.71: Start, Play. Klingt Lead/Harmonie jetzt nach ECHTEM Instrument (Piano/
  Strings) statt nach dem alten Synth? Und schick mir wieder ein Log — es sollte jetzt
  "SampledInstrument: loaded" beim Start zeigen (statt nur polyVoice).
