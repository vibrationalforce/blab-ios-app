build: 2026-07-15 v10.79.244 (Video aus der MEDIATHEK importieren — der Picker zeigt jetzt deine ganze Fotobibliothek)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Video-Reihe V2 aus deinem Auftrag: "Bei Video soll der volle Zugriff auf die
  Mediathek abgefragt werden."

  BEFUND: der bisherige Video-Import öffnete nur den DATEIEN-Picker (Files-App) — deine
  Fotobibliothek war gar nicht erreichbar. DAS war die eigentliche Sperre.

  NEU auf der Video-Import-Seite (Videospur → Import):
  • „Mediathek" (Primär-Knopf): öffnet den System-Foto-Picker mit deiner GANZEN
    Bibliothek (nur Videos gefiltert). Auswahl → Vorschau → „Add to timeline" wie
    gehabt. Der System-Picker läuft außerhalb der App — Apple-sauber, du musst dafür
    keinen Berechtigungs-Dialog wegklicken, und trotzdem ist ALLES sichtbar.
  • „Dateien" bleibt als zweiter Weg (für MP4/MOV aus iCloud Drive etc.).
  • Der Berechtigungstext für die Fotobibliothek ist jetzt hinterlegt (Info.plist,
    ehrlich formuliert: lesen nur bei deiner Auswahl, nichts wird hochgeladen) — der
    eigentliche Voll-Zugriff-Dialog kommt mit dem In-App-Medien-Browser (V3, Bilder),
    der ihn wirklich braucht. Kein Dialog auf Vorrat = kein Apple-Review-Risiko.

  SICHER: ui-state-reviewer CONFIRMED — Picker-API korrekt, Datei-Transfer nach dem
  dokumentierten Muster (Kopie in eigenes Temp, dann derselbe Vorschau/Import-Pfad wie
  Dateien), Plist wohlgeformt + nicht von project.yml überschreibbar, keine neue
  Sheet-Ebene, Linux-CI unberührt. Gates v243 grün.

  Baustellen-Stand (God-Mode): Clip-Game C1–C5 KOMPLETT ✓ · V2 Mediathek ✓ · als
  Nächstes V3 Bilder importieren · V4 Kamera-Capture in die Timeline · V5 Profi-Kamera
  (Kelvin) · C6 EchoelAI-TimelineToolCore.

  Reihenfolge: v242 Magnetkanten · v243 Überlapp-Regel · v244 Mediathek-Import.
