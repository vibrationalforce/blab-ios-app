build: 2026-07-13 v10.79.192 (AUv3: Generator-Instrumente sichtbar + Roh-Diagnose)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (deployed via deploy branch)
notes: Aus deinem v191-Gerätelog (2297): AUv3-Scan zeigt WIEDER nur Apple
  (2 Instrumente + 17 Effekte). Zwei Sachen dagegen:

  1) GENERATOR-TYP JETZT DABEI. Der Scan war schon breit (Wildcard + je Typ +
     ganze Registry), ABER der Host-Filter ließ nur MusicDevice/Effect zu — und
     VIELE Fremd-AUv3-INSTRUMENTE registrieren sich als "Generator", nicht als
     MusicDevice. Die fielen still raus. Generator ist jetzt als Instrument mit
     dabei → deine Generator-basierten Instrumente sollten erscheinen.
  2) ROH-DIAGNOSE INS LOG. Die Scan-Zeile zeigt jetzt zusätzlich, was das OS VOR
     unserem Filter zurückgab: "rawMakers: [...]" + "rawTypes: [...]". Damit ist
     der nächste Log eindeutig:
       · steht dein Hersteller in rawMakers, aber nicht in der Liste → unser
         Filter (ein noch fehlender Typ, s. rawTypes) — dann ergänze ich genau den.
       · steht auch roh nur Apple → iOS hat deine Fremd-AUv3-Extensions dem Host
         noch nicht registriert. Abhilfe am Gerät: die JEWEILIGE Hersteller-App
         einmal öffnen (das aktiviert ihre AUv3-Extension), Echoel zieht sie dann
         LIVE nach (der Registrierungs-Beobachter re-scannt automatisch).

  Schick mir den nächsten Log — die rawMakers/rawTypes-Zeile sagt uns sofort,
  welcher der beiden Fälle es ist.

  OFFEN, als Nächstes eigener Cycle: Bio liest im Log Puls noch nicht sicher aus
  (Finger erkannt, Belichtung gelockt, aber pk=0/bpm=0/conf=0 — die
  Periodizität/Autokorrelation kommt nicht über die Schwelle). Das ist der Kern
  und braucht saubere Signalarbeit + deine Finger-Technik, kein Blind-Tuning —
  nehme ich mir als eigenen Schritt vor.
