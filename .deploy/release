build: 2026-06-26 v10.76.27 (Bio→Visual reborn — body shapes the light, launch-safe)
branch: claude/piano-roll-clip-view-wozlie
notesT: Builds on 10.76.26 (Safe Mode). Brings back BIO→VISUAL — the body sculpts the
  immersive visual non-destructively: Coherence → Blend (one look morphs into another),
  Breath → Spread, Heart rate → Intensity, plus LFOs. Your base visual values stay
  editable; the live signal moves them around that base in real time.
  This is the feature that black-screened in 10.76.20 — rebuilt so it CAN'T do that
  again: (1) the editor lives in a lazy sheet (built only when you open it), never in
  the launch scroll — visualPanel just has a "Bio → Visual…" button; (2) the editor
  avoids all three confirmed crash patterns (no binding-ForEach over the @Observable
  array, no Menu, no .menu Pickers — it uses chip buttons + explicit per-field
  bindings); (3) it ships UNDER the 10.76.26 Safe-Mode net, so even a residual launch
  fault would self-heal into the recovery screen with a crash log, not a black void.
  Reviewed launch-safe (ui-state-reviewer GO). Pure cores restored with tests.
  Test: 10.76.27 launches normally? Then: open the immersive Visual → "Bio → Visual…"
  (in the Visual panel) → add e.g. Coherence → Blend, set Depth → watch the look morph
  with your coherence. Off by default (no routes = identical to before).
