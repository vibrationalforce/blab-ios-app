# ULTRASCAN DMMW 2026-07-22 — Produktions-Pfad (Quelle der Wahrheit, supersedet verstreute Pläne)

Multi-Agent (8 Bereichs-Auditoren + Synthese, aktueller Code file:line verifiziert; wf_21f50887).

## Ehrlicher Verdikt

The CORE product — a bio-reactive performance instrument where the body drives sound and image in real time, sitting on a tracks-centric DAW home — is genuinely close to production-ready and is NOT where the "no improvement" feeling comes from. Across 7 of 8 areas the prior audits' headline crash/leak/silence findings are verifiably FIXED (SPSC torn-head, DDSP Sendable header, freshness gate, LiveColabo egress leak, TimelineStore sync-write, mic teardown silence), and the shell is honest (no visible dead buttons). The founder's frustration maps to two real, specific things: (1) the generated SOUND converges to one warm-pad timbre because the drums-off/lead-off decision left near-identical genre patches as the only carrier — the instrument's central promise (body+genre changes what you HEAR) is muted by a data problem, not an engine problem; and (2) heavy effort went into the AUv3-hosting pillar which is fully built in Swift but completely non-functional on device (iOS serves the host 0 out-of-process components; own AUv3 fails -3000) — an optional pillar that isn't part of the core loop. Fix the sound distinctness, park AUv3/light-network/recording/video, and the core instrument is shippable.

## Produktionsreif HEUTE

 - Bio pipeline: three real sources (camera rPPG, Polar H10 BLE 0x180D, HealthKit) instantiated, wired, started on user action, single clean strap owner; freshness-gated modulation brain (ModulationEngine.tick uses usableBio() with correct per-source windows) — prior audit's #1 finding truly fixed.
 - bio->sound (BioReactiveSynthVoice, silent until armed), bio->FX (FXBioModulator, EchoelmusicApp:775), bio->visual (MetalBioView) are all live and honest.
 - Lock-free, audio-thread-correct synth engine: PolySynthVoice/SubBassVoice/EchoelDDSP with SPSC command queues and pre-allocated buffers; SPSC overflow-drop path now bounds-safe (masked CAS).
 - DAW/Timeline end-to-end for its shipped surface: MIDI + audio clips generate/import/edit/arrange and SOUND (AudioLanePlayer->TimelineAudioSink->master); deterministic additive-Codable persistence (no silent song loss), debounced non-hitching saves, full undo/redo, HONEST record model (no dead red buttons, no silent-take loss — only MIDI+bio lanes expose record-arm).
 - On-screen visual (MetalBioView) is flash-safe: pulseHz hard-capped <=3Hz, reduceMotion honored, single GPU path; FloatingVisualWindow adds no unsafe surface. ADM-OSC spatial egress wired with privacy gate.
 - Privacy/compliance: all 5 bio-egress paths gate through one BioEgressPolicy (HealthKit/Watch/Oura never leave device — 5.1.3 satisfied); camera usage string matches the only shipping camera use; no medical/healing/esoteric copy survives; aps-environment/CloudKit entitlements hard-gated OFF with zero runtime calls and no launch crash.
 - Honest shell: every reachable door lands on a working feature; FeatureFlags is a clean OFF-by-default switchboard; monetization genuinely absent (everything free); Broadcast/RTMP correctly has NO door while HaishinKit is unlinked.

## Top-Blocker

 - [Instrument sound quality] Generated sound converges to a single warm-pad timbre — the 'warm cluster' genres (classical/jazz/disco/oriental/rocksteady/klezmer) are perceptually identical because their SynthPatches share color:Pink/shape:Natural/cutoff 2600-3100/brightness 0.34-0.46, and with drums+lead intentionally off the held pad is the ONLY genre carrier.  (GenrePatches.swift:81-191 near-identical warm-cluster patches; applied via EchoelStudioView:1997/3780. Genre-anchor crossfade (BioComposer.swift:1405 anchor=min(1,coh/0.7)) collapses to a shared functional journey at genuinely-low measured coherence (k->0).)
 - [AUv3 hosting + own extension] AUv3-hosting pillar is fully built in Swift but NON-FUNCTIONAL on device: iOS serves the host process 0 out-of-process AudioComponents (third-party discovery returns only Apple built-ins) AND the own AUv3 (EchoelBodyVibe) fails to instantiate with -3000, an unresolved pluginkit/registry issue.  (SESSION_LOG.md:7927-7935 founder v309/310 self-probe FAILED NSOSStatusErrorDomain#-3000, Makers:[Apple]; engine itself is complete/wired (AUv3Host.swift, LaneAUInstrumentHost, EchoelmusicApp:844/891). Declaration is CI-proven correct.)
 - [Light renderer (EchoelLux)] Music-driven RGB colour on the Art-Net/sACN LIGHT output is not rate-limited (only the dimmer is slewed), leaving a residual WCAG flash gap when notes/chords change fast at high dimmer.  (ArtNetSender.swift:301-313 / SACNSender.swift:202 applyDimmer touches only the dimmer channel; RGB comes straight from MusicMediaMap.dmxChannels recomputed ~30Hz (ArtNetSender.swift:108). The app's own on-screen visual is fully capped and unaffected.)
 - [Concurrency / DSP] Latent COW cross-thread write on the bio-spectral path: applyBioReactive->updateSpectralEnvelope mutates the harmonicAmplitudes [Float] array from the @MainActor 10Hz poll while the audio render reads the same array — a small heap-copy crash chance and envelope glitch.  (EchoelDDSP.swift:1325-1329/644-669 write; render reads :943; driven from PolySynthVoice.applyLatestIfFresh:563. Note the patch-recall path was already moved onto the audio thread to kill exactly this race — the bio-spectral path was NOT.)

## Kürzester Produktions-Pfad

PRODUKT: The bio-reactive performance instrument: body (heart/breath/HRV) changes a genre-distinct generative pad+bass AND the immersive MetalBioView visual in real time, on a tracks-DAW home — everything else parked behind flags.

This is the north-star and it is already ~90% shipped: three real bio sources, a lock-free audio-thread-correct synth, a live flash-safe visual, an honest DAW timeline, and clean privacy/egress. The only thing standing between it and 'a real user feels it works and it delivers the promise' is that the generated SOUND doesn't audibly change per genre (the founder's exact complaint) and effort is leaking into AUv3/light/recording pillars that are broken or half-built on device. Scope-LOCK to the core, make the sound distinct, park the rest — that converts a diffuse '90% across five pillars' into a shippable 100% on ONE.

SCHRITTE:
  1. [S] Spread the warm-cluster genre patches in GenrePatches.swift — differentiate cutoff/brightness/spectralShape/attack/unison so classical/jazz/disco/oriental/rocksteady/klezmer are each audibly their own instrument. — Directly fixes the founder's 'everything sounds like classic' — pure data edit, no DSP change, immediately audible, highest leverage in the repo.
  2. [S] Floor the genre-anchor crossfade: BioComposer.swift:1405 anchor = max(0.34, min(1, coh/0.7)). — Guarantees genre root-motion never fully dissolves at low measured coherence (k>=1 for n>=2), so genre identity survives an aroused/finger-on state; one line, pure function, zero audio-thread risk.
  3. [M] Close the bio-spectral COW race: route updateSpectralEnvelope through the same SPSC/audio-thread drain PolySynthVoice already uses for notes+patch-recall (or double-buffer harmonicAmplitudes). — Removes the last real audio-thread-law violation / rare crash on the bio->PolySynth path, so bio-driven sound is crash-safe under sustained play.
  4. [S] Gate the AUv3 'plugins' lane door OFF (behind a flag or diagnostics state) and keep audioLaneRecording/video/face-expression/StoreKit flags OFF. — Removes the only reachable doors that lead to non-functional/half-built pillars, so a TestFlight user never taps into nothing — turns the shell fully honest for v1.
  5. [S] Resolve the light-output flash gap: either apply the ~15-line RGB slew-limit shared by ArtNet/SACN senders, or gate the Routing light output OFF for v1. — Lets the app honestly ship without a WCAG claim it can't back on the network light path; the on-screen visual is already safe.
  6. [M] Device-verify the full core loop on TestFlight: bio source locks -> armed sound changes with heart/breath -> genres sound distinct -> visual pulses <=3Hz -> no crash across a 5-minute performance and a background/foreground cycle. — This is the real production gate — the whole point is that a real user on device experiences the promise without crash/silence; everything above is dead until this passes on hardware.

## Parken / Gaten

 - AUv3 hosting (third-party + own EchoelBodyVibe): gate the lane 'plugins' door OFF (or behind a diagnostics-only state) until a device paste confirms thirdPartyCount>0 and own-AUv3 instantiates — stop investing Swift effort in an OS-registration problem; validate the shipped App-Group-removal fix via clean delete+power-cycle+reinstall, then set it aside for v1.1.
 - Audio-lane mic recording: keep FeatureFlags.audioLaneRecording OFF (already honestly hidden) — it is a missing feature, not a broken one; ship without it.
 - Video tracks capture/import: keep non-promising / doorless (already hidden) until a real capture path exists — do NOT surface a video record button before the recorder is wired.
 - Art-Net/sACN light output: either apply the ~15-line RGB slew fix now or gate the light output for v1 so no 'flash-safe' claim is made on an un-rate-limited path.
 - Face-expression / front-camera path (FaceExpressionBioPublisher): keep FeatureFlags.cameraExpression OFF and do NOT flip it without first broadening NSCameraUsageDescription (front-camera 5.1.1 risk) — currently dormant and safe.
 - StoreKit (EchoelStore/ProUnlockView), echoelAI, CloudKit/push announcements: keep all flag-gated OFF for v1.0 — everything free, honest, no App-Review placeholder-UI risk.
 - Dead code cleanup (non-blocking hygiene): delete or leave the unreachable toolsSection/openTool, orphaned spectral-donut fullscreen cover, and the dead bioFrames SPSC enqueue — zero user-facing dishonesty today, but re-door only MIDI-import via the transport ••• menu since its importer path is already complete.
 - Do NOT re-enable the drum beat (founder decision 2026-07-22; silentBeat is correct) and do NOT append another .sheet/.fullScreenCover to EchoelStudioView.body — consolidate to one .sheet(item:) enum before any new modal to avoid re-tripping the metadata black-screen ceiling.

## Bereichs-Gesundheit (one-liner je Auditor)

 - Bio pipeline & freshness/modulation brain: The freshness gate the prior audit flagged is genuinely fixed and bio→sound/FX/visual is live for the three real sources (rPPG/Polar/HealthKit); the face-expression path is honestly dormant (never instantiated, flag OFF) and the general tempo mod-matrix is wired but inert with no default routes/UI.
 - Instrument & generated-sound QUALITY (genre distinctness in what the user HEARS): The audible take is a genre-timbred sustained pad + bass (drums intentionally silent, no lead) with a solid lock-free synth engine and a wired genre-anchor crossfade — genres ARE structurally distinct at neutral/high coherence, but the "warm cluster" (classical/jazz/disco/oriental/rocksteady/klezmer) collapses toward one warm-pad sound because timbre is the only carrier left and those patches are near-identical.
 - DAW / Timeline (clips, arrangement, audio+MIDI lanes, record, persistence): The tracks-centric DAW is genuinely production-usable end-to-end for its shipped surface — MIDI + audio clips generate, import, edit, arrange, and SOUND, with deterministic persistence and no silent-data-loss lies — because audio/video RECORDING (the only broken-in-a-prior-audit path) is now honestly hidden behind captureImplemented rather than promised-and-empty; the real remaining gap is audio-lane recording and video tracks being missing features, not broken ones.
 - AUv3 hosting (3rd-party plugins per lane) + own AUv3 extension: The hosting engine is genuinely complete and wired end-to-end in code, but the feature does NOT work on device — the app's own diagnostics (founder tests v309/310, 2026-07-20) show iOS serves the host process ZERO out-of-process AudioComponents (0 third-party AND own AUv3 fails -3000), an unresolved pluginkit/registry issue that blocks the whole pillar.
 - Media renderers — Visual (MetalBioView), Light (EchoelLux Art-Net/sACN), Spatial (ADM-OSC), + AdaptiveQuality: Visual and spatial outputs are genuinely production-ready and flash-safe; the light output is safe on luminance (dimmer slewed) but has a real residual WCAG gap on music-driven RGB colour, and AdaptiveQuality still ships 5 of 7 knobs inert (2 now by documented design, 3 truly dead).
 - Shell/IA + built-but-switched-off honesty + FeatureFlags: The shipped shell is honest — every UNREACHABLE surface is truly hidden (dead code, not a visible dead button), so a TestFlight user never taps something that does nothing; the remaining issue is dead-code clutter plus two capabilities (spectrum-donut visual, MIDI import) that silently lost their only door when the Tools grid was removed.
 - Egress / Privacy / App-Store Compliance: This area is in good shape: bio egress is now consistently gated across all five network paths (the prior LiveColabo hole is fixed), usage strings match shipping use, no medical/healing claims survive, and the two "declared but inert" entitlements are hard-gated OFF — no hard App-Review blocker in this area today.
 - Cross-cutting concurrency & determinism laws (device-bought invariants): The two headline crash bugs the prior audits named (SPSC torn-head OOB, wrong DDSP Sendable header) are genuinely fixed and the 60 Hz meter props are correctly leaf-shielded today — the one real remaining latent hazard is the documented harmonicAmplitudes COW cross-thread write on the PolySynthVoice/AUv3 bio path, plus a sheet-chain that sits under but near the black-screen ceiling.