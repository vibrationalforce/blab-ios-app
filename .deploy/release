build: 2026-07-14 v10.79.211 (Baustelle Rang 1: Netzwerk-Ausgabe endlich adressierbar)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Erster abgearbeiteter Punkt aus der Baustellen-Ledger (No-Sleep-Modus). Bisher
  gingen OSC / ADM-OSC / sACN nur an ‚localhost‘ bzw. eine falsche fest verdrahtete IP —
  Resolume · TouchDesigner · MadMapper · Lichtknoten im LAN bekamen NICHTS, ohne Weg das
  zu ändern. Das ist behoben.

  WO: Routing (Patchbay) → neue Sektion „Netzwerk-Ausgabe". Pro Ausgang ein Feld für
  Ziel-Host/IP + Port (sACN/Art-Net zusätzlich Universe). Der Punkt zeigt, ob der Ausgang
  gerade sendet.

  WIE: Werte werden gespeichert (überleben Neustart) und wirken SOFORT, wenn der Ausgang
  läuft (der Socket verbindet sich neu). Trägt man nichts ein, bleibt alles exakt wie
  bisher (bit-identisch — die alten Standardwerte) — kein Risiko für bestehende Setups.

  SICHER: Müll-Eingabe crasht nicht (UDP scheitert still), Port 1…65535 geklemmt, keine
  Geheimnisse gespeichert (nur LAN-Adresse). Reviewer: concurrency PASS · ui-state PASS
  (Environment sauber, freeze-sicher, kein Card-in-Card) · security PASS (kein Crash,
  keine Regression, Privacy ok). Der didSet-Reconnect ist doppelt abgesichert (greift nur
  bei aktivem Ausgang). Gates: Xcode + CI/CD.

  Stand Ledger: 28 Baustellen verifiziert · Rang 1 (Netzwerk-Ziel) ✓ · als Nächstes
  Rang 2 (Vintage Tape/VHS-FX) bzw. die entscheidungsfreien Punkte der Reihe nach.
