build: 2026-06-30 v10.76.52 (Less take-time churn — Arrangement playhead off the hot path)
branch: claude/piano-roll-clip-view-wozlie
notesT: Proactive follow-up to the menu-freeze work (10.76.50): the workspace keeps ALL three
  surfaces (Arrange · Clips · Compose) mounted at once so switching never stops the audio. But
  the Arrangement surface read the transport song-position directly in its body, so even while
  HIDDEN behind Compose it rebuilt its whole view ~8×/s during every take (the position updates
  each 16th-note) — wasted main-thread work that adds to take-time/biofeedback jank. FIX: the
  live position label + the sweeping playhead now read the transport inside their OWN tiny leaf
  views, so only those 2 pt elements refresh; the rest of the (hidden) Arrangement view stays
  still. Same observation-isolation pattern as the 10.76.50 header fix. No visible change —
  purely less background churn while playing. (ClipView checked: already clean.)
  Build-checked. Nothing else changed.
