build: v10.79.341 — AUv3-Härtung: Bio-Pfad-COW-Race geschlossen + MIDI-Panic (kein isolierter Feature-Build)
branch: claude/echoelmusic-neustart-auv3-6ri2ek (tokenless deploy via .deploy/release bump)
notes: |
  Superset von v340 (Instrument-Home + Studio-Tür + First-Run-Einladung + AUv3-aumu).
  KEINE sichtbare App-Änderung ggü. v340 — nur zwei AUv3-Korrektheits-Fixes (Founder-
  Top-Priorität), konsolidiert zu EINEM Verify-Target statt getrennter Builds.

  NEU ggü. v340 (beide im AUv3 EchoelBodyVibe, beide Reviewer-CLEAN):
  - Bio-Pfad-COW-Race geschlossen: der Parameter-Observer rief applyBioReactive auf
    einem Control-Thread → updateSpectralEnvelope schrieb das harmonicAmplitudes-Array
    neben dem Render-Read (COW-Race + potenzielle Audio-Thread-Alloc). Fix: atomarer
    BioMirror (Observer schreibt), applyBioReactive läuft jetzt render-seitig
    (~10-Hz-gedrosselt) → Array ist single-owner → In-Place, kein Race/keine Alloc.
    audio-thread-reviewer: CLEAN.
  - MIDI-Panic: CC 120 (All Sound Off) + CC 123 (All Notes Off) → Note-Release, sonst
    hätte ein Host-„Stop"/Panic die Mono-Stimme stecken lassen. Alle anderen CCs bleiben
    ignoriert (Bio-Drone spielt weiter). Test-first, code-reviewer: CLEAN.

  Weiter zum Geräte-Verify (unverändert aus v340): App öffnet ins lebendige Vollbild-
  Instrument · First-Run-Finger-auf-Kamera-Einladung · Studio-Tür in die DAW · AUv3 als
  spielbares aumu-Instrument mit MIDI (jetzt inkl. Panic-Release). Alle Gates grün
  (2f61112: Xcode + CI). Rausch-Triad unberührt, kein Heilungs-Claim.

  Re-Trigger #1: der erste 341-Archive-Lauf (run 29973807672, build 2455) brach mit
  "The network connection was lost" während -allowProvisioningUpdates ab — ein
  transienter CI-Netzausfall (die "No profiles found"-Folgefehler sind die Konsequenz
  des Netzabrisses, keine Signing-Regression; v339/v340 archivierten mit identischem
  Provisioning). Alle Code-Gates auf 70d7e37 sind grün. Neuer Push = frischer
  Archive-Versuch mit neuer Build-Nummer, Version 10.79.341 unverändert.
