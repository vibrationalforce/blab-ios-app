build: 2026-06-26 v10.76.38 (Re-add 1/4: planetary-tone picker)
branch: claude/piano-roll-clip-view-wozlie
notesT: Builds on 10.76.37. Starts the careful re-add of the studio features that were
  reverted in 10.76.36 to fix the black screen — ONE at a time, each with a device
  check, so the launch can never silently break again. First and lowest-risk: the
  Cousto PLANETARY-TONE picker in Compose ▸ Composition (Sun…Pluto + Earth day/year).
  Pick a planet → the concert pitch A4 is set to play in tune with it (e.g. Earth-year
  → A4 ≈ 432 Hz). It's purely a Picker row + tuning logic (the same proven pattern as
  the Kammerton/Tone-system rows) — no new sheet, no structural change — so it can't
  reintroduce the metadata-overflow crash. Off by default.
  Test: 10.76.38 → does it still launch straight to the Studio? Compose ▸ Composition →
  "Planet tone" → e.g. "Earth — year" → Kammerton shows ≈432 Hz. If it launches clean,
  next re-add is the Licenses screen, then the visuals menu, then Bio→Visual.
