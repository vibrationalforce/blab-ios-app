build: 2026-06-23 v10.71.1 (EchoelFX bio-mod response curves + macro-morph)
branch: claude/piano-roll-clip-view-wozlie
notesF: Workstream 3/4 of the EchoelFX deepening — the performance MACRO. New pure FXPreset.morphed(to:amount:) (continuous params interpolate, enables/modes switch at midpoint; clamped; Linux-tested incl. chain round-trip). EchoelFX "Macro morph" section: pick any preset (yours + curated) as a target → snapshots the current sound as A → one fader blends A→target live (every parameter glides) for smooth on-stage transitions. FXViewModel.morph keeps the master gate as-is and reseeds the UI. Next: workstream 4 CI/design polish.
notesE: Founder — "Visuals müssen besser steuerbar sein: VJ, Installation, Performance Art Level." (1) Live VJ control overlay ON the fullscreen immersive visual: tap the canvas to hide/show a solid bottom panel with a one-tap scene strip + live Intensity/Detail/Motion/Spread/Hue/Saturation — perform the visual without leaving the projected view; status bar hidden for clean output. (2) Palette control: MetalBioView shader gains hue rotation (YIQ) + luma-preserving saturation, default neutral so the physically-correct tone→light colour is preserved (science-first) but a VJ can rotate/desaturate the palette live. Both wired into the Visual panel and the fullscreen overlay. Flash-safety (≤3 Hz) and adaptive-quality FPS/detail unchanged. Next visual: bio-reactive routing of visual params + external-display/mapping output.
notesD: Workstream 2/4 of the EchoelFX deepening — two pro stages added to the chain (now 13): EchoelBitcrush (digital lo-fi: bit-depth quantisation 1–16 bit + sample-rate reduction 1–64× via sample-and-hold, parallel wet/dry) and EchoelStereoWidener (M/S width 0=mono…1=unchanged…2=wide). Audio-thread-safe (final class, plain Float params, pure C-math, no alloc/locks), Foundation-only so the AUv3/DSP target compiles them. Signal flow: …saturation → BITCRUSH → harmonizer → mod → delay → reverb → WIDENER → comp → limiter. Fully wired: FXViewModel mirrors + EchoelFX UI sections (Bitcrush bits/downsample/mix, Stereo Width), FXPreset save/recall (lenient decode → old presets still load), and both are bio-modulation targets (.bitcrushMix, .stereoWidth) so the body can drive them too. New pure DSP tests (FXLoFiTests). Code-reviewed (compile-clean, arg-order verified). Next: macro-morph + deeper presets, then CI/design polish.
notesC: Founder — "EchoelFX optimieren, viel mehr möglich: konstant CI + raffiniertere Manipulation" (alle 4 Richtungen, Research-orientiert). Workstream 1, the most on-vision: the BODY now sculpts the FX chain live — the Echoel thesis applied to effects. New pure core FXModulation (in Core/, reuses ModSource so AUv3/DSP stays self-contained; Linux-unit-tested: bipolar/unipolar offset, range-clamp, LFO, Codable) + FXBioModulator (@MainActor @Observable, ~30 Hz control loop reading the bio snapshot + free LFOs, summing routes per target around the user's captured base value, auto-enabling the targeted stage; off the audio thread). EchoelFXView gains a "Bio-reactive" section: add routes mapping any carrier (coherence/breath/HR/HRV/motion/LFO) → any of 11 FX targets (filter cutoff/res, saturation, chorus/flanger/phaser mix, tremolo depth, delay mix/feedback, reverb mix/size) with depth + polarity, on the app-wide EchoelValueField vocabulary. Concurrency-reviewed (0 issues). Next workstreams: more FX algorithms (EQ/bitcrush/widener/formant/granular), macro-morph + deeper presets, CI/design polish.
notesB: Device log 2026-06-23 revealed the capture pipeline silently STALLED for ~68 s mid-session — the rPPG breadcrumb (read directly off the analyzer each 10 Hz tick) showed byte-identical raw R/brightness/amp/bpm for over a minute, i.e. the camera stopped delivering frames, with NO runtime error, then self-recovered. Root cause: CameraCapture had zero session-resilience. Added the canonical pattern: (1) AVCaptureSession runtime-error / wasInterrupted / interruptionEnded observers (Sendable-safe — only scalar info crosses the queue hop), restarting/resuming the session; (2) a frame-stall WATCHDOG (DispatchSource on sessionQueue) that, if running but no frame for >4 s, kicks stop/startRunning with a 6 s cooldown so it can never thrash. This catches the silent thermal/resource stall that fires no notification. Concurrency-reviewed (0 issues). The body-driven instrument no longer goes dead for a minute when iOS throttles the camera.
notesA: Founder — "analysiere die alte Version, was geht smoother / besser verknüpft." Frame+audio analysis of the v10.65 capture (PyAV): (1) master repeatedly slammed 0 dBFS (peaks −0.1/−0.2 → harsh, inter-sample clip-prone, "not smooth/homogeneous"); (2) immersive visual flickered to black for individual frames during a mode/screen switch (~63–67 s, luma→0.2); (3) the recompose/auto-evolution path is already SEAMLESS (boundary note-swap, no clock reset, sustained notes ring) — the recording's silence gaps were manual Stop/Start, not a bug. Fix shipped: a −1 dBFS true-peak safety trim on mainMixerNode AFTER the Apple PeakLimiter, so the master can never clip — smoother and more even level at a negligible 1 dB cost. Next slice: keep the MetalBioView alive across the multiscreen/capture toggle so the immersive visual never goes black on a mode switch.
notes0: Founder — "Komposition und Sound optimieren, soll homogener klingen." Composition side: each re-seed used to fully re-randomise the skeleton (progression rotation, register, ornament/octave-lift/syncopation placement, note count), so even with a steady body consecutive takes lurched (device log: 6→17 notes) and felt like different pieces. BioComposer now splits its RNG into a STRUCTURE stream and a DETAIL stream: the harmonic skeleton + register + density + WHERE ornaments/lifts/syncopation land are drawn from a body-stable structureSeed (bioSeed without the evolution nonce), while which pitches and fine velocity evolve from the existing seed. Result: the same body yields the SAME song breathing — cohesive/homogeneous — while the melody still evolves; when the body shifts, the structure evolves with it. Applies to the harmonic-production genres (the rich, lurching path) and to the continuous ~4-bar auto-evolution. Pure-core change, determinism + in-key + in-bar invariants preserved (CompositionStabilityTests / BioComposerTests unaffected). Sound/timbre blending is the next slice — evaluate whether the line still needs it.
notes3: Device log 2026-06-23 showed rPPG churn when the finger presses too hard / the torch washes out the lens: R≈0.98 (all channels clipped) failed the red-dominance finger test → lock dropped → exposure re-settled → BPM reset to 0 → harmonic re-acquire (bpm jumped 145→94→…). Root fix in CameraAnalyzer: a saturated frame (min(R,G,B) > 0.92) now counts as "finger present, signal temporarily bad" — the lock HOLDS, signalQuality decays, and the clipped sample is NOT fed into the pulse buffer. The rate stays put through a brief over-press instead of resetting, which also removes the harmonic re-seed (estimatedBPM no longer zeroes). One-file change; steady-finger behaviour unchanged (clean log holds bpm 52–58 @ conf 0.9+).
notes2: Founder Q — "iPhone mic + Bluetooth headphones for better live monitoring works too, right?" Yes: selecting the iPhone mic (low-latency, full-quality) while BT headphones carry the OUTPUT (A2DP) is the better of the BT options — but A2DP output still adds ~150–250 ms, so self-monitoring stays delayed (the latency is on the OUTPUT side). The Input sheet now reads the live output route (AudioInputManager.outputRouteName / outputIsHighLatency) and, when monitoring through a Bluetooth/AirPlay/CarPlay output, shows an honest amber note explaining the delay and pointing to wired/USB for delay-free monitoring. The per-input latency badge alone was misleading for this combo.
notes: Founder request — "Baue das System für Akku, CPU, GPU für Ressourcenschonungen ein. Adaptive Qualität und Formate" + "Visuals brauchen viel Aufmerksamkeit damit alles ruckelfrei funktioniert". New AdaptiveQuality (pure, Linux-unit-tested) maps device pressure — thermal state, Low Power Mode, battery level/charging, and the measured render FPS — to one app-wide QualitySettings (visual targetFPS, detail scale, reduce-motion, spectral-donut allowance, bio/OSC rates). Worst-active-constraint wins; reaches High only when charging AND cool; a sustained FPS shortfall (<75% of target) demotes one tier so the GPU is never oversubscribed. ResourceGovernor (@MainActor @Observable) reads ProcessInfo/UIDevice + thermal/power/battery notifications (Darwin-gated; Linux/sim treat as on-power/nominal) and publishes the live tier. MetalBioView now honours it: preferredFramesPerSecond, ring-density × detailScale, and reduce-motion all follow the governor, and each drawn frame feeds its timestamp back (main-thread MainActor.assumeIsolated) so a struggling device steps down BEFORE the OS throttles. Concurrency-reviewed (0 issues). Visuals stay ruckelfrei; battery/heat are conserved automatically with zero user action. Manual tier-pin UI + bio/OSC-rate consumers are the documented next adopters. iOS verifies on device; compile gate validates.
