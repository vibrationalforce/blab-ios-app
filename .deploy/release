build: 2026-07-02 v10.79.6 (Pro pass III — heartbeat throb + music breathes + graded/filmic palette)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notesT: Professionalization loop, build 3.
  VISUAL (V3): the immersive visual now GENTLY THROBS with your heartbeat — the central
  bloom pulses once per beat, so the body VISIBLY drives the picture (not a generic
  visualizer). Flash-safe (≤2.5 Hz, gentle swing). Plus the earlier grade: less neon, warmer,
  a filmic contrast so it reads premium — VJ Saturation still pushes vivid.
  MUSIC (M1): the take breathes over the bar (downbeats lead + swell), not a flat metronome.
  rPPG: both camera-pulse lock fixes (no finger flip-flop; a small movement won't wipe a lock).
  Test 10.79.6: (1) does the visual pulse WITH your heartbeat on the Bio/immersive view?
  (2) less neon / more premium? (3) does the music breathe? (4) does the pulse lock + hold?
  Next careful steps (need your device/ear, not blind): real multi-bar arrangement
  (intro/build/break), audio-graph mix glue, per-look anti-aliasing.
