build: 2026-07-03 v10.79.44 (Visuals: kein Doppel-Render/Black-Screen — nur EIN Visual aktiv)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notesT: Volle Autonomie („du entscheidest, grab all tasks") → ich arbeite den Visual-Strang ab.
  Ein Visual-Audit fand die Architektur solide (kein Audio-Thread-/NaN-Problem, Governor ok),
  aber EINEN echten Korrektheits-Bug:
  • BLACK-SCREEN-BUG behoben: Das Vollbild-Visual (Tools → Visual) mountete eine ZWEITE
    Render-Engine, während das schwebende Visual-Fenster darunter WEITER rendert. Zwei laufende
    Metal-Views hungern die GPU aus → das immersive Bild wird sekundenlang schwarz; außerdem nahm
    der Recorder doppelt auf. Jetzt: das schwebende Fenster blendet sich aus, solange das Vollbild
    offen ist, und kommt beim Schließen zurück → immer nur EINE Render-Engine aktiv (Projektions-
    Vollbild bleibt erhalten).
  • Kleiner freier Perf-Gewinn: Bildrate nur bei echtem Tier-Wechsel neu setzen statt 60×/s (spart
    über lange Installations-Läufe).
  Zurückgestellt (brauchen Geräte-Test): FPS-Pause bei Reduce-Motion, framebufferOnly-Feinschliff.
  Compile-Review grün, kein Audio-Thread/Bio-DSP berührt.
  NÄCHST im Strang: schwebendes Fenster „bearbeiten" + externe Displays (dormant+abgesichert).
  TEST 10.79.44: Vollbild-Visual öffnen/schließen (Tools → Visual) → bleibt es stabil (kein
  Schwarz mehr), und das schwebende Fenster ist danach wie vorher da?
