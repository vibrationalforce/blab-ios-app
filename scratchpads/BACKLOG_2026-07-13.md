# ECHOEL NIGHT BACKLOG — ranked, CI-safe-first (Deep Audit 2026-07-13, wf_97481e42)

Full audit JSON: task wlqguveo8.output. Architecture: see AUTONOMOUS_NIGHT_2026-07-13.md.
End-state pipeline: BODY → BIO → TIMELINE → VOICES → MIX/MASTER → OUT. One clock
(480-PPQ Transport), one home (ArrangeTimelineView), one spine (EngineBus).

## KEYSTONES (unblock the most)
1. rPPG signal-conditioning fix (B02 pure → B03 device) — unlocks EVERY bio feature.
2. LaneVoiceRack + multi-roll fan-out (B04 pure → B07/B08 device) — tracks=instruments.
3. Consolidate EchoelStudioView ~20 sheets → ONE .sheet(item:) (B01) — removes SIGSEGV
   ceiling blocking every new door. RISKY (launch path) → render-safety skill + care.
4. Region drag/move/resize gestures (B11) — viewer → real arranger. CI-safe.

## STATUS KEY: [ ]=todo [~]=in progress [x]=done(CI-green)  · CI-SAFE / DEVICE

### P1
- [ ] B01 CI-SAFE  Consolidate EchoelStudioView sheets → one .sheet(item:) enum. (render-safety)
- [~] B02 CI-SAFE  rPPG conditioning pure: linearDetrend + perfusionIsMotion + ACF prominence + tests.
- [~] B04 CI-SAFE  LaneVoiceRackPlan pure reducer (slot commands + overflow) + tests. deps: LaneVoicePool.
- [ ] B11 CI-SAFE  Region drag-move+resize gestures → moveRegion/resizeRegion + TimelineSnap. deps B01.

### P2 (pure cores mostly)
- [~] B12 CI-SAFE  WarpedClipPlan pure (tick→sample onset, stretch, frame count) + tests.
- [~] B14 CI-SAFE  AudioClipFactory pure seam (file → Clip(.audio)+region) + tests.
- [~] B17 CI-SAFE  RecordAnchor + RecordPlan + bio normalizer pure + tests.
- [~] B21 CI-SAFE  VideoResyncPolicy pure (deadband/nudge/hard-seek) + tests.
- [ ] B03 DEVICE   rPPG device wiring: BioSignalDeconvolver.ppg pre-stage + perfusion gate + flush. deps B02.
- [ ] B05 CI-SAFE  LaneVoicePool.reconcile churn/overflow tests. deps B04.
- [ ] B06 CI-SAFE  Per-lane MIDI content split [laneID: PianoRollModel]. deps B04.

### P3 (device — staged for TestFlight-on)
- [ ] B07 DEVICE  LaneVoiceRack: N pre-attached PolySynthVoice BEFORE start(). deps B04,B06.
- [ ] B08 DEVICE  TimelineRegionPlayer fan-out over slots. deps B07.

### P4
- [ ] B09 DEVICE  Per-lane mix/pan/mute/solo → slot voice. deps B08.
- [ ] B10 DEVICE  builtinInstrument → concrete voices (drums/sampler/bass). deps B08.
- [ ] B13 DEVICE  Audio-loop import door: fileImporter → App-Group copy → AudioClipFactory → addRegion. deps B01,B14.
- [ ] B15 DEVICE  AudioClipPlayer offline warp render + AVAudioTime scheduling. deps B12.

### P5
- [ ] B16 DEVICE  TimelineRegionPlayer drives audioLaneIDs via per-lane AudioClipPlayer. deps B13,B15.
- [ ] B18 DEVICE  TakeRecorder + bio capture (BioAutomationRecorder). deps B17.
- [~] B22 CI-SAFE  VideoExportPlan pure (regions → composition instructions) + tests.
- [ ] B26 CI-SAFE  ModulationMatrix DDSP bio keys + OSCSender.sendModulation.
- [ ] B27 CI-SAFE  Bio-modulation routing controls into a tracks-home leaf.

### P6
- [ ] B19 DEVICE  Audio-in take: parameterized RetroCapture tapping inputNode. deps B17,B18.
- [ ] B20 DEVICE  MIDI-in take: packet.timeStamp → Notes. deps B17,B18.
- [ ] B25 CI-SAFE  Drawable master automation surface (AutomationCanvasMath). deps B01.
- [ ] B28 CI-SAFE  EngineBus.publish(bio:) batched Sendable-queue drain.

### P7-P8
- [~] (B22 above) · [ ] B23 DEVICE VideoLanePlayer locked to audio host clock. deps B21.
- [ ] B24 DEVICE  Video trim UI + BioGrade.metal → flip ClipKind.video.isPlayable. deps B23,B22.
- [ ] B29 DEVICE  LightFixtureGroup + BioPhaser → Art-Net/sACN.
- [ ] B30 CI-SAFE  Gate dead EchoelStore StoreKit launch behind FeatureFlag OFF.

## NIGHT WAVE 1 (this cycle): pure cores as NEW self-contained files + tests —
## B02, B04, B12, B14, B17, B21, B22. Zero device risk, each unblocks a keystone.
