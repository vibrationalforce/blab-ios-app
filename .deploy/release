build: 2026-06-24 v10.76.21 (REPAIR: back to the launching code; Bio→Visual is the confirmed launch-breaker)
branch: claude/piano-roll-clip-view-wozlie
notesT: Clean repair. Proven on device: 10.76.19 launches (full session), 10.76.20 = 19 +
  the Bio→Visual feature → black screen. So the Bio→Visual feature is the exact launch-
  breaker, and (since AnyView panel-erasure did NOT help) it is a RUNTIME fault in that
  feature at launch, not a view-type/metadata issue. 10.76.21 = the exact launching code
  (Bio→Visual removed) PLUS the new, tested ColorOctave core (Cousto colour octave —
  present, not yet wired, zero launch risk). The app launches; nothing half-wired.
  TO FIX BIO→VISUAL IN ONE SHOT, I need the crash stack from the 10.76.20 run:
  Settings > Privacy & Security > Analytics & Improvements > Analytics Data > newest
  "Echoel..." entry > Share. With it I pinpoint the exact line; without it I must re-add
  Bio→Visual piece-by-piece over several builds. Next visible step after launch is
  confirmed: wire ColorOctave into the immersive visual (Cousto colours) — that path
  (MetalBioView shader) has never broken launch, so it is safe.
  Test: 10.76.21 launches?
