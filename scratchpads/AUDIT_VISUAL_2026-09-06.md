# AUDIT — Visual-Sektion (Ultradeepaudit, 2026-09-06, HEAD 4e43685e)

> Read-only Audit, 54 Agenten in 4 Teams (Surfaces · Colour · Grid+UX · History) mit je einem Lead als Qualitäts-Tor, danach 20 Defekte durch zwei unabhängige Widerleger: 17 bestätigt, 3 widerlegt. Die Entscheidungen des PM danach stehen in `PLAN_VISUAL_ONE_UNIT_2026-09-06.md`. Provenienz: Workflow wf_8122d992-162.


## CONFIRMED DEFECTS (survived two-lens refutation)

- [HIGH] The surface named 'Full screen' cannot be played and has no note grid  (EchoelStudioView.swift:1533)
  EVIDENCE: Color.clear.contentShape(Rectangle())
    .ignoresSafeArea()
    .onTapGesture { withAnimation(.easeInOut(duration: 0.15)) { showVisualControls.toggle() } }  — `git grep -n "TouchInstrumentView(" -- Sources` → only FloatingVisualWindow.swift:798
  CONSEQUENCE: A user who learned 'touch the image to play' from the window's hint opens Field → Full screen and a tap flips the control panel instead of sounding a note; `touchShowGrid` has 0 occurrences in EchoelStudioView.swift. This is the decisive argument for WHICH surface survives: the window's `.fullscreen` already renders instrument + grid edge-to-edge; the cover would need the whole play layer ported.
  PRECISION: Minor precision only: the second `TouchInstrumentView(` grep hit at Sequencer/FieldAutoPlay.swift:61 is a comment, so "only FloatingVisualWindow.swift:798" is correct as a count of mounts but the raw grep returns two lines. Also note the cover's tap-to-toggle is deliberately paired with an always-visible controls button (EchoelStudioView.swift:1548
- [HIGH] Floating REC + 'Full screen' = two capturing MetalBioViews feeding one VisualRecorder (GPU rule broken)  (FloatingVisualWindow.swift:708)
  EVIDENCE: if !isPresented && !mustKeepRenderingForRecording {  (:708) with `recorderIsRecording && !ExternalStageBridge.shared.isConnected` (:704); the cover's onChange only does `floatingVisualVisible = false` (EchoelStudioView.swift:1453) and mounts `MetalBioView(capturesVideo: true` (:1521)
  CONSEQUENCE: Sequence: start an MP4 take in the floating card → Field → Full screen. The window keeps its capturing renderer alive by design (#319), the cover mounts a second; MetalBioView.swift:752 `wantCapture = capturesVideo && (visualRecorder?.wantsFrameCapture ?? false)` is true in both, and VisualRecorder.capture(from:) (:219) guards only `video.recordState.isRecording || wantsStill` — no source identity. Both drawables reach one AVAssetWriter, the exact case the :1448-1451 comment claims the hide prev
  PRECISION: Minor: the `floatingVisualVisible = false` write is at EchoelStudioView.swift:1455 (line :1453 is `if isUp {`); the cited comment block is :1448-1452. Add as a consequence: VisualRecorder.ensureResources (:342) recreates the CVPixelBufferPool whenever the incoming width/height changes, so two capturing renderers at different sizes would also thrash
  PRECISION: Keep the finding; tighten scope: it applies only to the non-donut cover branch (EchoelStudioView.swift:1517 `if spectralDonuts` mounts SpectralDonutView, which does not capture) and only while no external stage is connected (:700). Add the second, weaker consequence that survives even if MTKView pauses off-window: the writer was set up from the flo
- [HIGH] Fullscreen toolbar sheds the only LABELLED way out on every portrait phone under 440 pt and the look slider on every phone — and fullscreen is the cold-launch state  (FloatingVisualLayout.swift:352)
  EVIDENCE: var shed: [(inout ChromeFit) -> Void] = [ { $0.lookSlider = false }, { $0.studioChip = false }, { $0.miniTransport = false }, { $0.gridToggle = false } ]  — re-derived from ChromeCost (:211-243): chip needs 40+56+83+84+28+28+28 + 8×8 + 20 = 431 pt, slider 529 pt
  CONSEQUENCE: WorkspaceView.swift seed (:332-334) writes `floatingSizeRaw = …fullscreen.rawValue` at every cold launch when `FeatureFlags.instrumentHome`, so the founder's phone (375-430 pt portrait) opens on a bar whose exits are two unlabeled glyphs (:1127, :1140) and both drop to `.small` (:1209, `.next` :362); the grid toggle is next in line. ChromeBudgetFitsTests.swift:193-212 records the same arithmetic as known (#1017) and pins only 'does not overflow'. Any merged control unit that lives in this bar in
  PRECISION: Two imprecisions in the consequence text: (1) "both drop to `.small`" is wrong for the xmark — FloatingVisualWindow.swift:1140 sets `isPresented = false` (hides the window; the persisted size stays fullscreen); only the resize glyph goes to `.small` via `.next` (:362, applied :1250). The cited `:1209` `.small` write is actually :1214 in `exitToStud
  PRECISION: Two precision nits, neither neutralising: (a) "both drop to `.small`" is half right — only the resize glyph does (`.next` :362 wraps fullscreen→small via cycleSize :1248); the xmark sets `isPresented = false` (:1140) and leaves the persisted size at fullscreen, so re-showing returns to fullscreen. `exitToStudio()` :1209-1214 (the `.small` writer ci
- [MEDIUM] Weather image mixers (Hue/Saturation/Glow/Movement) act only in the floating window — the cover and the beamer render the raw keys  (EchoelStudioView.swift:1525)
  EVIDENCE: hueShift: Float(visualHue), saturation: Float(visualSaturation),  (cover, :1525) — vs FloatingVisualWindow.swift:768 `hueShift: Float(wv.hue), saturation: Float(wv.saturation)` where `wv = weatheredVisuals()` (:594-610); `git grep -c WeatherMood -- Studio/ExternalDisplayScene.swift` → 0
  CONSEQUENCE: With weather on and any mixer > 0, the four value fields in the Field panel (:4210) stop having an effect the moment 'Full screen' opens or a projector connects — the lying-control class. Retiring the cover removes half of this for free; the beamer half stays and must be a conscious decision (weathered or raw picture on the external screen).
  PRECISION: Sharpen the precondition: the four image mixers default to 0.5 (Core/WeatherMood.swift:229 `defaultIntensity ... : 0.5`), so the divergence appears as soon as the Mood toggle is on and one fetch succeeded — no user needs to raise a mixer. Also note the toggle's own label (EchoelStudioView.swift:4086 "Weather shapes the music and the image") and hin
- [HIGH] Luminance floor 0.35 + per-channel clamp rotates the hue of D#/E/F by +16…+20° and F#/G by −6…−7° at defaults  (MetalBioView.swift:1887)
  EVIDENCE: `float lum = dot(col, float3(0.2126, 0.7152, 0.0722));` / `col *= (lum < 0.35) ? (0.35 / max(lum, 0.04)) : 1.0;` then echoelHue ends `return clamp(rgb, 0.0, 1.0);` (:1366)
  CONSEQUENCE: Re-executed at A4=440, coh 0, colourOn 1, sat 1.05: D# (0.241,0,1) → hue 254.5°→273.7° (+19.2°), E +19.7°, F +15.7°, F# −7.2°, G −6.2°; with the floor disabled the same chain moves hue ≤0.9°. The comment at :1885 calls the floor hue-PRESERVING; it is only until a channel clips, which for every dim violet/purple tone it does. This is exactly the per-channel-clip error the CIE code refuses at SpectralColor.swift:106-112. Fix = scale, then divide by max channel if >1 (ratio-preserving), and skip ec
  PRECISION: Line anchor: the `col *=` statement is at MetalBioView.swift:1888 (the `float lum` dot product is :1887). Re-executed magnitudes: D# +18.8°, E +19.2°, F +15.0°, F# −8.0°, G −6.8° (plus G# −3.4°, D −2.1° not listed in the finding); floor disabled → ≤0.3° for all twelve pitch classes.
  PRECISION: Two precision notes, neither refuting: (a) the anchor line 1887 is the `float lum = dot(col, …)` line; the multiply the finding quotes is :1888 (the guard needle at GlitterCannotBecomeAFlashTests.swift:234 quotes the :1888 string). (b) The proposed fix's second half — "skip echoelHue when hueShift == 0" — is insufficient on its own: the over-1 chan
- [HIGH] In donut mode 8 of the 9 VJ-overlay fields (Hue, Saturation, Intensity, Motion, Spread, Texture, Glitter, Structure) are inert — only Detail reaches the picture  (EchoelStudioView.swift:1518)
  EVIDENCE: `SpectralDonutView(reduceMotion: reduceMotion,` / `bandCount: max(8, Int(visualDetail))).ignoresSafeArea()`; SpectralDonutView.swift:24-25 declares only `reduceMotion` and `bandCount`
  CONSEQUENCE: The same cover renders `visualAdjustFields(spacing: 8)` (:6028) with all nine EchoelValueFields; the #1003 caption (:6369-6371) covers Detail only. A founder dragging Hue over a donut sees nothing change — a lying control, the class #1003 fixed for one row. Any single-control-unit design must feed the donut these parameters or hide the rows in donut mode.
  PRECISION: Count: 9 of the 10 VJ-overlay fields are inert on the donut (the 8 named plus "Energy" at EchoelStudioView.swift:6300, which only writes Intensity/Motion — :6213-6216); within the fine-tune grid alone the "8 of 9" figure is exact. Scope: inert on the on-device picture only — `ExternalDisplayScene` (ExternalDisplayScene.swift:144-148, :170-181) rend
  PRECISION: Two precisions, both strengthening rather than weakening the finding. (a) COUNT: the overlay carries TEN value fields, not nine — `Energy` (:6300) is a tenth, and it too is inert in donut mode: its setter writes only `visualIntensity` and `visualMotion` (:6214-6216), neither of which reaches `SpectralDonutView`. Honest phrasing: 9 of 10 overlay fie
- [MEDIUM] 'Filmic S-curve' is a per-channel affine x' = 1.06x − 0.03 — not hue-preserving, crushes any channel < 0.028, no uniform, no guard  (MetalBioView.swift:1903)
  EVIDENCE: `outCol = clamp((outCol - 0.5) * 1.06 + 0.5, 0.0, 1.0);` — comment :1901 'gentle filmic S-contrast … Cheap, no new uniforms'
  CONSEQUENCE: Re-executed: (0.10, 0, 0.50) → (0.076, 0, 0.50), R:B 0.20→0.15. Linear, not sigmoid; runs at every default with no door; the name is not the function. Small, but it is one more hue-changing per-channel op on the dark end the CMF already dims.
  PRECISION: Minor sharpening, not a refutation: the effect is strongest not at the finding's mid-tone example but at the ambient floor (energy≈0), where 0.06·col is entirely inside the 0.028 crush band for any channel < 0.47 — the "never a dead black" wash (:1893-1900) is partly undone and hue-distorted by this line, and the sRGB pixel format makes that visibl
- [MEDIUM] Intensity product exceeds 1 whenever music sounds and is applied to `col` before any clamp — loudness clips the dominant channel and shifts hue  (MetalBioView.swift:999)
  EVIDENCE: `intensity: lookIntensity * (1 + 0.45 * touchE + 0.30 * musicLevel)` / `* (1 + 0.5 * autoTerm),` feeding `col *= clamp(u.intensity, 0.0, 1.5);` (:1891)
  CONSEQUENCE: Every normalised spectral colour has one channel at exactly 1 (wavelengthToRGB `if (m > 1.0) c /= m;` :1397). A playing take pushes intensity to ≈1.3, a touch to ≈1.45; nothing clamps `col` between :1891 and the S-curve at :1903, so where `energy` is high the peak channel clips while the others keep growing — the finding-1 mechanism driven by loudness. The excitation should scale `energy` (the luminance mask, :1898), not `col`. Magnitude on device UNMEASURED.
  PRECISION: Two precision notes, neither refuting: (a) "whenever music sounds" holds when auto-attune is off or the body is unmeasured (autoTerm = 0); with `lookAutoAttuned` on and a low-coherence body autoTerm goes negative (BioVisualParams.swift:127 `2 * (coherence - 0.5)`, ×0.5 → down to ~0.925), so a quiet take with masterLevel below ≈0.3 can still sit at 
  PRECISION: Downgrade from defect/medium to design-note/low. Corrected facts: (1) the intensity product is not 'up to ≈1.3' but exactly 1.30× whenever ≥2 audible notes sound, because masterLevel is a velocity sum clamped to 1 (PianoRollView.swift:1353); (2) clipping is confined to pixels with energy > 1/intensity (≈0.77 at 1.30), i.e. the field's bright peaks;
- [MEDIUM] The Field caption overstates: 39.2 % of the octave is a purple-line interpolation, not a CIE rendering of the transposed frequency, and 'defaults' are not neutral after the physical colour  (EchoelStudioView.swift:5208)
  EVIDENCE: `Text("Colour defaults to the heard tone octave-transposed into visible light (its frequency doubled until it reaches the visible band, rendered via CIE 1931); …")`; SpectralColor.swift:216 `if t >= tRed && t <= tViolet {` (tRed 0.2854, tViolet 0.8931)
  CONSEQUENCE: Re-executed seam share = tRed + 1 − tViolet = 0.392. At A4=440 the pitch classes E, F, F#, G, G# all sit in the seam (t = 0.915/0.999/0.082/0.165/0.249). The one in-app sentence that states the colour claim is the same over-claim class CLAIMS.md polices for store text; it should say 'CIE 1931 across the visible span, closed over the CIE purple line' and not call the graded defaults physical.
  PRECISION: Minor tightening only: the caption's "its frequency doubled until it reaches the visible band" half is TRUE for every pitch (seam pitches land at 380–420 nm / 640–780 nm, inside the visible band); what is false is that the shown colour is the CIE rendering of THAT wavelength — in the seam it is a purple-line blend of the 640/420 nm endpoint colours
- [HIGH] Played-note light does not land on the touched cell: cloud x is a chromatic fraction above C, the grid is equal-width scale-degree columns with the ROOT at the left  (SpectralColor.swift:304)
  EVIDENCE: SpectralColor.swift:303-304 `x = the note's position WITHIN its octave (fraction of log2 above C) — C at the left, rising rightward: the fretboard grid's column order.` and :314-317 `let f = p - oct … let x = (f - 0.5) * 1.5`; grid: TouchInstrumentView.swift:813 `let pitch = key.degree(d, octave: bands[b])` with column `x: rect.minX + CGFloat(d) * cellW` (:817) where d=0 is the root (MusicalKey.sw
  CONSEQUENCE: Sharper than the worker's version: it is TWO mismatches, not one. (a) Root offset — in D minor the rightmost cell (C) blooms at x=−0.75, the far left. (b) Spacing — even in C the cloud sits at chromatic 12ths (x ∈ ±0.75 → 12.5 %…87.5 % of the picture) while a 7-degree grid's column centres sit at (d+0.5)/7 of the full play width; F (f=5/12) lands at 43.75 % where column 3's centre is 50 %. The two only coincide for no key at all. The founder's 2026-07-08 law ('colours appear WHERE the tone sound
  PRECISION: Title should say "tone CLOUD" rather than "played-note light": a second, coloured light does land on the touched cell — TouchInstrumentView.swift:1466-1489 spawnRing pushes a ripple into TouchRippleChannel at the actual touch point (x: p.x/w, y: 1 - p.y/h) in the note's SpectralColor, drawn by the same shader. What is misplaced is the persistent cl
  PRECISION: Scope precision: the mismatch is visible in `FloatingVisualWindow` at every `WindowSize` including its `.fullscreen` (the only `TouchInstrumentView(` construction site is FloatingVisualWindow.swift:798); the `showVisual` `.fullScreenCover` mounts no touch surface, so it has no grid to disagree with. The visible grid is default OFF (`touchShowGrid` 
- [HIGH] Grid labels cannot fit their cells at the small and medium floating steps (computed from the shipped layout constants)  (TouchInstrumentView.swift:849)
  EVIDENCE: :815 `let cellW = rect.width / CGFloat(n)`, :819 `let labelSize: CGFloat = min(12, max(9, cellH * 0.16))`, :849-851 `label.frame = CGRect(x: cellFrame.minX + 6, … width: cellFrame.width - 8, height: labelSize + 3)` on a `CATextLayer` with no truncation/size gate and cellFrame already `.insetBy(dx: 1.5, dy: 1.5)` (:822). Recomputed with FloatingVisualLayout.cardSize (smallStep 0.42 :59, minPictureA
  CONSEQUENCE: Label frame at the small step ≈ cellW − 3 − 8 ≈ 10-11 pt for a ~20 pt 'C♯4' at 10.5 pt; chromatic at small/medium is ≤ 9 pt; the 130-pt picture floor with 12 degrees gives a negative frame. CATextLayer renders into its own backing store, so the string is cut to fragments (the worker-2 'draws into the neighbouring cell' mechanism is not supported — see rejected). The grid is persisted (`touch.showGrid`), so a player who turned it on in fullscreen carries the fragments into every smaller step. No 
  PRECISION: Minor anchor drift only: the evidence cites :815 / :819 / :822 for cellW / labelSize / insetBy; at HEAD they are :811 / :814 / :825 (the CGRect starts :822, `.insetBy` is :825). :849-851 is exact. The "~20 pt for 'C♯4' at 10.5 pt" glyph width is UNMEASURED (no font metric run here) but is not load-bearing — at n=7 small the frame is 10.1 pt and at 
  PRECISION: Finding stands as written. Two refinements: (a) the medium step on a 375-pt phone (picture 231.7 pt) is slightly smaller than the finding's 390-pt example (241.6) — 7-degree label frame 22.1 pt there, i.e. medium is marginal for 7 degrees and fails for ≥8-degree scales, not only chromatic; (b) add to the consequence that `chromeFit` sheds the grid 
- [HIGH] The VJ overlay of the cover carries the exact 560-pt width cap the founder rejected in #1027, plus a 360-pt height cap, and two guards cite the cap as a premise  (EchoelStudioView.swift:6049)
  EVIDENCE: :6044 `.frame(maxHeight: 360)` and :6049 `.frame(maxWidth: 560)` on `visualVJOverlay`; decisions.csv:720 (#1027, founder verbatim) `Ich will adaptive Groesse also Bildschirmgroesse ausfuellend fuer alle Ansichten … Der 560-pt-Deckel zentrierte und liess Rand leer`. The anti-return guard only searches the deleted helper names: TwoControlsShareALineOnlyWhileTheyFitTests.swift:209 `for needle in ["re
  CONSEQUENCE: In landscape or on any wider device the stage overlay centres at 560 pt with empty margin either side — the look the founder reversed the same day. If the cover is retired (finding 1) both caps go with it and VisualFineTuneReflowsTests' header/claims must be rewritten in the same commit; if the cover survives a cycle, the cap must go now and that guard's arithmetic re-derived. Same-class literal outside this frame: GuideOverlay.swift:51 `.frame(maxWidth: 560)` (tour card) — report to the PM, not
  PRECISION: Minor precision only: the overlay's 560/360 caps predate #1025 (blame: graft 545b19ee, 2026-08-08) and were never touched by #1025/#1027 — they are an independent cap of the same value, which the founder's #1027 principle ("Bildschirmgröße ausfüllend für alle Ansichten") covers rather than a leftover of the removed `readableContentWidth` helper. Th
  PRECISION: Keep the finding; tighten three claims so the PM does not over-read it. (a) LINEAGE: the :6049 literal is NOT "the exact cap the founder rejected in #1027". `git blame` puts :6044/:6049 at the graft `545b19ee` (2026-08-08), a month before #1025; the #1025 body states the ceiling was "applied at exactly TWO sites" (WorkspaceView + EchoelSheetPanelMo
- [MEDIUM] Cover top bar: five glyph-only `.font(.title2)` buttons with no tap frame, including the only exit  (EchoelStudioView.swift:1552)
  EVIDENCE: :1553 `Image(systemName: "slider.horizontal.3").font(.title2)…` with no `.frame`/`contentShape`; same at :1557 (donut), :1575 (record), :1611 (`xmark.circle.fill`, the persistent Close) and StillShutterButton.swift:38 `.font(.title2)`. House floor: EchoelTheme.swift:215 `static let controlTapHeight: CGFloat = 44`; the window's toolbar buttons all carry `.frame(width: 28, height: 44).contentShape(R
  CONSEQUENCE: The cover's own comment says a persistent Close is 'the only guaranteed escape'; it is a ~22-pt glyph (SF Symbol at title2; hit rect UNMEASURED) over a canvas whose whole surface is the chrome-toggle tap target — a missed Close toggles the panel instead. Downgraded from the worker's HIGH because the whole bar disappears with the merge; it is HIGH only if the cover is kept.
  PRECISION: Line numbers drift by a few lines at HEAD 4e43685e: controls glyph `.font(.title2)` is :1552 (Image :1551), record :1576, and the persistent Close `xmark.circle.fill` is :1619-1620 (not :1611); `.padding()` on the HStack is :1624; the canvas tap-to-toggle is :1534-1536. Add: the guard's "recorded in VisualFineTuneReflowsTests' device-verify note" p
- [MEDIUM] Sharp-only naming tables spell flat keys wrong on the grid  (NoteNaming.swift:126)
  EVIDENCE: NoteNaming.swift:126-127 `private static let englishNames = ["C", "C♯", "D", "D♯", "E", "F", "F♯", "G", "G♯", "A", "A♯", "B"]` (German :128-129 same sharps + B/H); the grid passes the pitch class only: TouchInstrumentView.swift:844 `label.string = naming.name(pitchClass: pitch) + "\(pitch / 12 - 1)"`.
  CONSEQUENCE: F minor reads G♯ A♯ C♯ D♯ where a musician expects A♭ B♭ D♭ E♭ — pitch classes right, spelling wrong, on an in-key fretboard whose purpose is to read the key. Fix is a key-aware `name(pitchClass:in:)` preferring flats for flat-side tonics; Sargam already carries ♭ (:167-168) and `spokenName` already expands it (:221), so VoiceOver follows for free.
  PRECISION: None to the substance. Precision only: the caller passes an absolute MIDI note number (not "the pitch class only"); `name(pitchClass:)` folds it mod 12, so the effect is identical. Under the `.german` column pitch class 10 already reads "B" (German B = B♭), so the misspelling there is G♯/C♯/D♯ for A♭/D♭/E♭, three of four rather than four of four.
  PRECISION: Two precisions to the finding, not refutations: (a) the root picker (WorkspaceView.swift:1200-1201) itself offers only sharp names, so a flat-side tonic like A♭ can only be chosen as "G♯" — a key-aware `name(pitchClass:in:)` must also feed the picker/title (MusicalKey.swift:548) or the grid would say A♭ while the picker says G♯; the clean cases tod
- [LOW] Preset chips sit 2 pt under the in-panel floor their two neighbouring strips cite as law  (EchoelStudioView.swift:5713)
  EVIDENCE: :5713 `.padding(.horizontal, 12).frame(minHeight: 32)` (preset chip) vs :5665 `.padding(.horizontal, 11).frame(minHeight: 34)` (touch patch chip, "34 is the house in-panel floor") and :5843 (look chip, "Same law as `touchPatchChip` above (#617)").
  CONSEQUENCE: Five targets below the 34-pt floor; all three strips below the 44-pt `controlTapHeight`. Becomes moot if the strips turn into a Picker/wrapping row (finding above); otherwise a one-number fix.
  PRECISION: Line reference for the touch-patch chip is :5652 (comment :5648–5651), not :5665; everything else in the finding matches the source verbatim.
  PRECISION: None needed to the finding itself. One precision: the finding's "in-panel floor" phrase is the touch-patch comment's own wording (:5651), and the fix is literally one number at :5713 (32 → 34) plus, to match the two neighbours exactly, `.padding(.horizontal, 11)` instead of 12 — though the horizontal padding is cosmetic, not a tap-floor issue.
- [LOW] Stale comment in the overlay: dedicated dual-screen is claimed 'a separate cycle' while ExternalDisplayScene ships  (EchoelStudioView.swift:6032)
  EVIDENCE: :6029-6034 `(Dedicated dual-screen — device = controls, beamer = visual only — needs an Info.plist scene manifest + device check, a separate cycle.)`; but ExternalDisplayScene.swift:170 `MetalBioView(capturesVideo: false,` and Resources/iOS/Info.plist:38 `<key>UIWindowSceneSessionRoleExternalDisplayNonInteractive</key>`.
  CONSEQUENCE: The user-visible caption ('Project: mirror to a screen via AirPlay', :6035, the smallest type in the set at 10 pt) under-sells a shipped capability and its comment denies it. Goes with the cover; if the AirPlay hint is kept in the merged unit, it should name the external-display scene instead.
  PRECISION: Minor precision only: the AirPlay caption's 10 pt is the smallest size in the panel but not unique (one other `EchoelTheme.font(10` in :5068-6110). If the AirPlay hint is kept in the merged unit, the honest text is: mirroring via AirPlay/Screen Mirroring puts the whole UI on the screen; a dedicated beamer scene (device = controls, screen = visual o
  PRECISION: Two precisions for the merged-unit rewrite: (1) the "device check" half of the comment is NOT fully stale — slice 1 (launch with manifest) is device-logged per ExternalDisplayScene.swift:17-20, but the beamer picture itself has no device evidence in the repo (`grep -c extdisplay scratchpads/SESSION_LOG.md` → 0; the file header at :26-27 says "nothi
- [HIGH] Fresh install and every relaunch re-apply Vapor's hue rotation (0.82 turns) over the physical colour, and no Hue edit can clear it — the file contradicts its own doc comment  (EchoelStudioView.swift:976)
  EVIDENCE: `@AppStorage("visual.preset") private var visualPresetID = "vapor"` (:976); onAppear `if !visualPresetID.isEmpty, let p = VisualPreset.factory.first(…) { applyVisualPreset(p) }` (:1225-1228) → `if let h = p.hue { visualHue = Double(h) }` (:6090); `VisualPreset(id: "vapor" … hue: 0.82, saturation: 1.12)` (VisualPreset.swift:76); shader `col = echoelHue(col, u.hueShift);` AFTER the CIE mapping (Meta
  CONSEQUENCE: Founder-visible on ask (a): Hue 0 holds only until the next launch, then 0.82 comes back. Two founder decisions collide here — 2026-07-07 'mehr kitschige Vaporwave-Ästhetik' (Vapor default, SESSION_LOG:1791 era) vs. SpectralColor.swift:5-6 'in jeder Situation die physikalisch korrekt hochoktavierten Farbfrequenzen' (2026-07-27; provenance UNMEASURED — 0 hits in SESSION_LOG/decisions.csv/memory) vs. today's ask. Fix is one slice, founder-gated on WHICH default: Vapor without a palette (hue nil) o
  PRECISION: Two nuances, not refutations: (a) the consequence "Hue 0 holds only until the next launch" is true only while the Vapor chip stays selected — the user CAN make Hue 0 survive relaunch by tapping the active "Vapor" chip to deselect it (EchoelStudioView.swift:5702-5706 sets `visualPresetID = ""`, persisted), so a persistent physical palette is reachab
  PRECISION: Wording only: "no Hue edit can clear it" is accurate, but the finding should note the two non-Hue user paths that DO stop the relaunch re-apply — tapping the active Vapor chip deselects it (EchoelStudioView.swift:5702-5707 `visualPresetID = ""`) and any Intensity/Detail/Motion/Spread edit runs visualPresetDiverged() (:6138-6140). Neither is discove

## REFUTED (do NOT plan from these)

- ✗ The window's own .fullscreen has neither keep-awake nor a hidden status bar, and a different bleed geometry than the cover  (EchoelStudioView.swift:6066)
  WHY: One of the three halves is factually false and the other two are neutralised as CONSEQUENCES, so the finding does not stand as a MEDIUM defect. (1) STATUS BAR — false. `Resources/iOS/Info.plist:118-121` sets `<key>UIStatusBarHidden</key><true/>` and `<key>UIViewControllerBasedStatusBarAppearance</key><false/>`: the status bar is hidden app-wide, in every scene, floating-window fullscreen included. The `.statusBarHidden(true)` at `EchoelStudioView.swift:1626` is redundant with the plist, not the 
- ✗ Note grid adds 22 % white in gamma space to every tone and uses pow(1/2.2), while the field encodes true sRGB — same pitch class, two colours one finger-width apart; the Metal comment claiming a 'twin' names a function that does not exist  (SpectralColor.swift:293)
  WHY: The CODE facts hold, the stated CONSEQUENCE does not survive the compositing chain. Confirmed at HEAD 4e43685e: `Sources/Echoelmusic/Core/SpectralColor.swift:287-295` has `lift: Double = 0.22` and `func enc(_ c: Double) -> Double { l + (1 - l) * Foundation.pow(clamp01(c), 1.0 / 2.2) }`; the only consumer is `Studio/TouchInstrumentView.swift:1454`; `Views/MetalBioView.swift:421` names `SpectralColor.displayRGB`, which `git grep displayRGB -- Sources` finds nowhere else; `Tests/EchoelmusicTests/Sp
- ✗ Three writers persist a non-zero `visual.hue` (Vapor 0.82, Visual mood pad, Sound mood pad) and nothing writes 0 back; presets without a hue never restore it, and the colour swatch keeps showing the ungraded physical colour  (EchoelStudioView.swift:6090)
  WHY: The three non-zero writers, the nil-hue presets, the three reading mounts and the ungraded swatch are all confirmed at HEAD 4e43685e (EchoelStudioView.swift:6090 `if let h = p.hue { visualHue = Double(h) }`; VisualPreset.swift:75-77 `hue: 0.82, saturation: 1.12` only on Vapor; MoodPads.swift:132 `d.set(hue(forX: x), forKey: "visual.hue")`, :203 `visualHue = VisualMoodMap.hue(forX: x)`; readers at EchoelStudioView:1525, FloatingVisualWindow:598/604/609, ExternalDisplayScene:144; MusicColourRowVie
  WHY: The code description is accurate (EchoelStudioView.swift:6090 `if let h = p.hue { visualHue = Double(h) }`, Vapor `hue: 0.82, saturation: 1.12` at VisualPreset.swift:75-77, MoodPads.swift:132/:203 write hue), but every stated consequence is neutralised elsewhere in the source at HEAD 4e43685e:

1. "The physical default is lost app-wide" — the non-zero hue IS the shipped default, by founder decision. EchoelStudioView.swift:976 `@AppStorage("visual.preset") private var visualPresetID = "vapor"` wi

## LEAD REPORTS (verified non-defect findings + recommendation)

### Surfaces lead (this agent, adversarial re-read of every cite
FRAME: Which of the four visual surfaces (Field panel · FloatingVisualWindow incl. its .fullscreen size · the .fullScreenCover(isPresented: $showVisual) · ExternalDisplayScene) must merge into ONE, what each uniquely owns today, and what the merge costs in guards, GPU rule and the 14-modifier presentation chain.

- [HIGH] [duplication] Two fullscreens with disjoint control sets — neither is a superset of the other  (EchoelStudioView.swift:1509)
  Cover-only today: donut renderer (:1518, the single `SpectralDonutView(` site in Sources), tap-to-hide controls (:1533-1536, `showVisualControls`), StillShutterButton (:1607), VJ overlay panel (:6005), `.statusBarHidden(true)` (:1626), keep-awake (:6066). Window-only: the playable TouchInstrumentView (:798, the single site in Sources), note grid toggle (:1084), WAV record (:1094), loop readout (:1077), drag/snap, fir
- [HIGH] [design] Fresh install does not render the physical tone colour: two founder decisions collide (Vapor default 2026-07-07 vs 'physikalisch korrekte Farbtöne' 2026-09-06)  (EchoelStudioView.swift:976)
  Every cold launch rotates the palette toward magenta and lifts saturation while the caption under the Hue row says the opposite. This is NOT a plain defect to fix unilaterally: VisualPreset.swift:66-72 records the Vapor default as a founder ask ('mehr kitschige Vaporwave-Ästhetik … Brand fit'), and the shipped `visual.saturation` default is 1.05 (StudioDefaultKeys.swift:243) by a separate judgement (MetalBioView.swif
- [MEDIUM] [constraint] Guards that go red when the cover is retired — the list a merge commit must carry  (VisualFineTuneReflowsTests.swift:484)
  Also red: ResetSoundClearsWhatTheLaunchLineReportsTests file-wide `== 16` (:426; the chain `<= 14` at :535 stays green at 13 — removal is the SAFE direction), AStillIsOneFrameNotASecondPathTests raw `.sheet(` == 14 (:105 — ⛔ RETRACTED WHOLE #1050: "9 code + comments" was the measurement and nobody read it as the defect it was; a pin that counts its own documentation moves on prose and not on modifiers), TheStillSaysWhetherItWasSavedTests :97 and TheTakeSaysWhetherItWasWrittenTests :104 + the three-door list :124-131 (both pinned BY FILE to EchoelStudioView), T
- [MEDIUM] [constraint] Moving the still shutter and the donut toggle into the window's bar costs new shed ranks — the cover's own comment says so  (EchoelStudioView.swift:1592)
  The merge is not free on the window side. Cheapest honest shape: gate both like the Studio chip and slider — `if windowSize.isFullscreen, fit.…` (:1012, :1053) — so the small-card floor is untouched; the donut branch in `visualLayer` must stay its own leaf with `capturesVideo: false` semantics (the cover says 'no capture there yet', :1562), and TheMenuHostReadsNoHotStateTests already treats SpectralDonutView as a hot
- [MEDIUM] [duplication] One recorder, two share paths: the cover shares the raw temp URL, the window renames and holds while hidden  (EchoelStudioView.swift:1568)
  A take stopped in the cover is shared unrenamed; the same take stopped in the window is `Echoel_<date>_…mp4`. The nested sheet is NOT on the body chain (indent 12; ResetSoundClears… :469-470 says so), so dropping it is bookkeeping, not ceiling. After the merge only the window's two `heldWhileHidden` slots exist.
- [MEDIUM] [duplication] The look Slider is mounted three times (panel, overlay, window bar), is a raw SwiftUI Slider by founder override, and silently exits donut mode  (EchoelStudioView.swift:5789)
  Three writers of visual.style/styleB/blend on three surfaces; the ONE-control-unit ask collapses to one mount. The raw-Slider exemption is an explicit founder decision (FVW:1046-1050, 'langem slider … stufenlos') pinned by TheFXDoorNamesAControlThatExistsTests — the merged unit must keep a slider, not an EchoelValueField. The panel's scrub clears donuts while its readout is `showsDonutState: false` (:5174), i.e. an i
- [MEDIUM] [duplication] Look · Preset · Energy · Fine tune are one definition mounted in two surfaces; six guards are pinned to the overlay name  (EchoelStudioView.swift:6015)
  The 'compact menu' cannot be the 360-pt overlay pasted into the window (VisualFineTuneReflowsTests claim 5's own text: 'a third caller needs its own container spacing decided, not copied'). Retiring the overlay leaves the Field panel as the single control definition, which is what 'eine Einheit zum steuern' means in code.
- [MEDIUM] [design] The note grid (Ton-Gitter) exists only behind a shed-rank-4 toolbar glyph; the Field panel has no grid control  (FloatingVisualWindow.swift:1084)
  Founder ask (b) is already a real, physically-tinted feature, but it is unreachable from the panel the ask is about and can be shed by the chrome budget. A merged control unit should carry the grid toggle (and NoteNaming) next to Look/Preset. Colour work touches TWO encoders — SpectralColor.wavelengthToLinearRGB (Swift, :100) and its MSL twin `wavelengthToRGB` (MetalBioView.swift:1378) — with three consumers (Metal p
- [MEDIUM] [design] Detail is inert on a fresh install: its only Metal consumer is the Rings look, which the default slider sequence excludes  (MetalBioView.swift:1719)
  On a fresh install (Vapor sets detail 24) the 'Detail' row moves nothing on the shipped looks; its second consumer is the cover's donut `bandCount` (:1519). The caption :6372 exists to say so. A compact menu either hides Detail unless Rings/donuts is reachable or keeps paying a row plus a caption for it.
- [MEDIUM] [design] The Field panel is fifteen explanatory captions deep — the 'Menü kompakter' ask is largely a caption problem  (EchoelStudioView.swift:5325)
  Eight captions are visible on a fresh install before Fine tune, Arp or Weather open (up to ~19 then). Rendered height is UNMEASURED (no renderer here) — the worker's 1,300-1,600 pt figure is dropped. Which sentences may go is taste, i.e. the founder's.
- [LOW] [duplication] `visual.floating.size` is a bare literal with four writers in three files, two declared defaults and a third fallback  (FloatingVisualWindow.swift:347)
  The merge makes this key the ONE fullscreen switch (the 'Full screen' button :5114 becomes a size write instead of a cover flag), so it should enter StudioDefaultKeys in the same slice, the way `floatingVisualVisible` did (:260). Whether the direct `.small` write at :9115 can land under a running window-local WAV take is UNMEASURED (the worker's guard-claim half was not re-read by the lead).
- [LOW] [design] Two Uncodixfy vocabularies for one bar: cover uses `.title2` glyphs with no target frame, window uses 28×44 slots  (EchoelStudioView.swift:1548)
  The window's slots are the guarded ones (ChromeCost.iconButton :226, InfoSheetTextScalesTests); a merged bar inherits exactly one rule and it should be the window's.
- [LOW] [history] Stale prose the merge commit must sweep: 'showVisual has no true-writer', 'donut on a doorless path', 'dual-screen needs a separate cycle'  (EchoelStudioView.swift:6262)
  Resources/iOS/Info.plist:38-44 declares `UIWindowSceneSessionRoleExternalDisplayNonInteractive` → `ExternalDisplaySceneDelegate`, so the beamer scene IS mounted at runtime (resolves the workers' UNMEASURED) and the AirPlay hint's parenthetical is false user-facing copy inside the surface being redesigned. VisualEnergy.swift:33 ('on a doorless path') is stale the same way. #456: every home in the same commit.

RECOMMENDATION: Retire the `.fullScreenCover(isPresented: $showVisual)` and make FloatingVisualWindow's existing `.fullscreen` size the ONE fullscreen — the window is the only surface that can play, show the note grid, record WAV, keep rendering through a take and apply the weather mix, and the cover's four unique owners are all portable (donut branch as its own non-capturing leaf inside `visualLayer`; StillShutterButton and the donut toggle gated `if windowSize.isFullscreen` like the Studio chip so the small-card floor is untouched; tap-canvas-to-hide-chrome as a window-local @State; keep-awake re-keyed on `floatingVisualVisible && .fullscreen` in WorkspaceView). The 'Full screen' button in visualPanel becomes a `visual.floating.size = .fullscreen` write (no modifier), the chain drops 14 → 13 (the SAFE direction), `.onChange(of: showVisual)`, `visualShare`, `showVisualControls` and `visualVJOverlay` are deleted, and the Field panel becomes the single control definition ('eine Einheit'), reachable from fullscreen through the Studio chip. Cut it into Ralph slices, guards moved in the same commit each time: (1) port donut + still + hide-chrome + keep-awake into the window, re-point TheStillSays/TheTakeSays/TheDetailCaption/AutoMode host pins; (2) delete the cover and overlay, re-pin ResetSoundClears file-wide, AStillIsOneFrame raw count, VisualFineTuneReflows claims 5/6/7, sweep the stale prose (:6262, VisualEnergy.swift:33, the AirPlay parenthetical, CLAUDE.md Presentation bullet, LEDGER §D); (3) bar ranking — the labelled exit must survive on 375 pt and the grid toggle gets a door in the Field panel; (4) colour: pin Swift/MSL encoder parity with a test and decide the first-launch default ONLY after the founder answers question 1 — do not silently drop Vapor. Do NOT touch the beamer scene in this pass beyond deciding weathered-vs-raw. Everything above is compile/guard-verifiable except the double-capture race, the `.statusBarHidden` behaviour on a non-presented view, and the look of the merged fullscreen — those are device.

FOUNDER QUESTIONS RAISED:
  ? Erststart-Farbe: Am 2026-07-07 wurde 'Vapor' (Hue 0.82, Sättigung 1.12) als Brand-Default entschieden; heute lautet der Auftrag 'physikalisch korrekte Farbtöne'. Soll ein frischer Start die physikalische Tonfarbe zeigen (Hue 0, Sättigung 1.0 — auch der 1.05-Default fällt dann) und Vapor nur noch ein antippbares Preset sein?
  ? Fullscreen-Leiste im Hochformat (375–430 pt): Heute fallen 'Studio'-Chip und Look-Slider weg, es bleiben zwei unbeschriftete Glyphen. Was darf zuerst gehen — Transport-Anzeige, Gitter-Schalter, oder darf die Leiste im Fullscreen zweizeilig werden?
  ? Fullscreen-Geometrie: Statusleiste ausblenden und bis unter die Notch ziehen (wie der heutige Cover) oder die 44-pt-Leiste im sicheren Bereich lassen (wie das heutige Fenster)? Das ist ein Geräte-Blick, kein Code-Befund.
  ? Donuts: als Look ins Fenster übernehmen (dort nicht aufnehmbar, wie heute) oder ganz streichen? Ship-Gate 4 nennt sie als zweite Hälfte der Ausgabe.
  ? Beamer/externer Bildschirm: soll er das wettergemischte Bild zeigen (wie das Fenster) oder die Rohwerte (wie heute)?
  ? Detail-Reihe: ausblenden, solange die Slider-Sequenz keine 'Rings' enthält (auf einem frischen Gerät bewegt sie nichts), oder behalten?
  ? Welche der 15 Erklär-Untertitel dürfen weg — vor allem der fünfsätzige Self-play-Text und der dreisätzige Farb-Text? Das ist Geschmack und Sprache, nicht Messung.
  ? Nach dem Verschmelzen auf dem Gerät prüfen: (a) Floating-Aufnahme starten → Fullscreen → nur EIN Renderer und eine saubere MP4; (b) hält der Fullscreen den Bildschirm wach; (c) ist die Statusleiste im Fensterfullscreen wirklich weg.

### Colour lead over 3 workers (W1 Swift↔MSL twin parity, W2 pos
FRAME: Is the hue a tone renders PHYSICALLY correct from SpectralColor through the shader to the pixel, and which multipliers after the physical colour are non-neutral at defaults? Verdict: the PHYSICAL stage is correct and the two twins agree (28 lobe coefficients, 9 matrix entries, gamut method, 780 nm ceil-fold, seam tokens — read side by side at SpectralColor.swift:100-135/208-237 vs MetalBioView.swift:1374-1440; W1's float32 re-execution max |Δ| 5.7e-5, my double re-execution reproduces every colour). The pixel is NOT physical at defaults: eight stages sit between toneColour and the drawable, and the luminance floor + per-channel clamp rotate hue up to +20° on 5 of 12 pitch classes; the note grid adds 22 % white to every tone; the donut speaks a third language.

- [high] [design] The spectrum donut in the same fullscreen colours bands with a THIRD tone→colour language (log-stretch 30 Hz–16 kHz onto 700→400 nm): A4 is yellow-green on the donut, red on field and grid, and its bass/treble rings render near-black  (SpectralDonutView.swift:186)
  Re-executed: 440 Hz → 571.7 nm (1,0.876,0) on the donut vs 619.7 nm (1,0,0.041) via toneLinearRGB; 880 Hz → green vs red (not octave-equivalent). Band 0 (33.6 Hz → 694.6 nm) = linear (0.023,0.004,0) and band 27 (14.3 kHz → 405 nm) = (0.042,0,0.139): invisible, because visibleColor has none of the purple-line lift toneLinearRGB got for the 2026-07-12 'F fehlt' report. Contradicts SpectralColor.swift:5-11 ('ONE tone→co
- [medium] [constraint] Post-physical stages non-neutral at defaults with no user door: colour-truth gate to warm grey, warm tint 0.97 + warm lift 0.05 toward (1,0.92,0.80), and a coherence lift ×1.15+0.05 that is ACTIVE at the unmeasured 0.5 fallback  (MetalBioView.swift:1880)
  Re-executed: every non-floored pitch class loses 3–6 % chroma and drifts −0.3…−0.9° toward 3500 K; at colourOn < 1 (cloudGlow < 0.417, :1858) the tone is mixed with warm grey (0.60,0.56,0.50). With no body measured the frame gets ×1.075 and +0.025 achromatic white; idle breathes it 0.25…0.75. The gate and tint strengths carry founder quotes (2026-07-08, B9 'VISUALS GRAU', #578 'Bunter'); the warm POINT and the cohere
- [medium] [constraint] Default saturation 1.05 is an extrapolation that drives a channel negative for every saturated spectral hue — 1.0 is the only neutral value, and it is applied AFTER the floor/clip  (StudioDefaultKeys.swift:243)
  Re-executed: red640 → (1.039,−0.011,0.070), green530 → (−0.037,1.013,0.288), violet420 → (0.227,−0.005,0.830); the negatives are removed only by echoelHue's final clamp. Founder-quoted (#578 'Bunter') and guarded as a FLOOR ≥1.0 only (GlitterCannotBecomeAFlashTests:99), so 1.0 is allowed without touching the guard. If the warm tint goes to identity, 1.05 over-shoots the same request it compensated — the two must be r
- [medium] [design] Chord colours are averaged in RGB inside the shader (clouds and prism) while SpectralColor's law says chords mix in OKLab — same chord, two colours on one screen; W2's 'physically correct light superposition' reading is wrong  (MetalBioView.swift:1493)
  Resolved worker conflict: the shader does a NORMALISED weighted MEAN in RGB — neither additive light (that would be a sum) nor the OKLab average the header tile, MusicColourRowView and the DMX path use via physicalColor(forChord:) (:263-276). Where two note clouds overlap the field shows an RGB mean while the header monitor shows the OKLab mix of the same chord — the disagreement physicalColor was introduced 2026-07-
- [medium] [duplication] Three MetalBioView mounts disagree on the hue/saturation source: weather is mixed only in the floating window; the fullscreen cover and the beamer pass the raw key  (FloatingVisualWindow.swift:604)
  Neutral at defaults (`weather.enabled` false, StudioDefaultKeys.swift:461) — but with weather on, PiP, cover and projector show three palettes for one chord, breaking the 'every surface agrees' law the file cites. No CISmoke needle pins the cover's raw handoff (grep visualHue/weatheredVisuals in Tests/CISmoke → 0). Merging fullscreen into one unit is the moment to route all mounts through one source.
- [medium] [constraint] No test pins a single CIE coefficient, matrix entry, or Swift↔MSL parity beyond the two seam tokens; shaderSource is private so no parity claim can read it  (SpectralSeamTwinTests.swift:21)
  `git grep 3.2406\|599.8\|1.056 -- Tests` → 0. Today the 28 lobe coefficients and 9 matrix entries agree (read both sides); nothing goes red if one drifts — the seam token already drifted once (comment :1409-1414). Cheapest repair: `static let` (internal) + a CISmoke claim extracting the cieLobe/matrix literals from the string against the Swift constants (the pattern SpectralSeamTwinTests already uses), plus one physi
- [medium] [duplication] A second CPU fold `visibleWavelength(forToneHz:)` survives with zero production callers — the same naive nearest-555 fold whose MSL twin was deleted as a trap; the 'readout' it is kept for does not exist  (SpectralColor.swift:143)
  `git grep visibleWavelength( -- Sources` outside the file → 0; only tests call it. MetalBioView.swift:1400-1407 deleted the shader twin because 'it is the one a future edit reaches for by name'. If the merged control unit adds a wavelength readout it will print a number (nearest-555 fold) that does not match the colour beside it (780/2^t fold). Retire to test-only with a header, or wire a readout that reports the ren
- [medium] [design] Voice tint rotates hue ±0.05 turn (±18°) and scales saturation ×0.95…1.05 once a voice profile is applied — neutral only while none is captured  (MetalBioView.swift:851)
  Exactly 0/1 with `appliedVoiceProfile == nil` (:846-855), so byte-identical at defaults. With a captured voice every tone is off the physical circle with nothing on the surface saying so; `TheVoiceTintsTheVisualTests` pins the wiring, not neutrality. If 'physically correct' is the law, the voice belongs on a non-hue channel (texture/structure/spread).
- [low] [duplication] Three CPU display encoders, two curves: header tiles and the grid use pow(1/2.2), the drawable uses the true sRGB curve; the swatch and donut correctly hand linear to `Color(.sRGBLinear,…)` and need none  (HeaderMonitors.swift:547)
  ≤0.008 (≈2 LSB) — invisible, but the header tiles could use `Color(.sRGBLinear,…)` like the swatch, leaving displayComponents (UIColor has no linear initializer) as the one CPU encoder, and then align it to the sRGB curve.
- [low] [design] echoelHue at hueShift 0 is a 3-decimal YIQ round-trip on LINEAR values with a trailing clamp, run unconditionally — the clamp is where finding 1's clipping lands  (MetalBioView.swift:1350)
  Round-trip error ≤0.0015 < 1 LSB, so neutral to the eye; but skipping the rotation when u.hueShift == 0 (or replacing the clamp by a max-normalise) removes both this and the floor's hue rotation at defaults in one line. YIQ on linear values also means a non-zero VJ hue is not a uniform rotation — cosmetic.
- [low] [duplication] toneColour guards hz ≤ 0 as 1 Hz where toneLinearRGB returns neutral grey  (MetalBioView.swift:1423)
  The cloud path is CPU-guarded (`guard cloudHzSlot[k] > 0` :1122); the prism path takes `colorToneA = colorNoteFrom` (:1187) — whether `target.toneHz` can ever be ≤0 is UNMEASURED. Sub-perceptual today; a genuine twin divergence to close when the shader is next touched.
- [low] [constraint] Recorded mp4 pixel buffer carries no colour-primaries/transfer tags after the sRGB-encoded blit  (VisualRecorder.swift:239)
  The blit is a byte copy, so the drawable's single sRGB encode is neither lost nor repeated (no double-encode — verified all six shader pow() calls are on scalar fields). Untagged, players assume BT.709 transfer; shadow difference vs sRGB UNMEASURED on device, likely ≤2 LSB.
- [low] [constraint] SpectralColorTests is gated on AVFoundation+Accelerate for a Foundation-only type; its sibling explicitly says the opposite  (SpectralColorTests.swift:26)
  Harmless on macos-26 CI; a gate with no reason. Remove when the file is next edited.
- [low] [history] BioColorGradeParams header's own grep recipe now prints 1 where it promises 0, and its FlashGuard location note is wrong  (BioColorGradeParams.swift:13)
  Uncalled-in-Sources status holds (only the comment FlashGuard.swift:35, which now lives in Core/, not Studio/). The quoted-grep-ages trap CLAUDE.md names for EchoelModalBank; fix the header when the file is next touched. Not a colour-path consumer — MetalBioView uses its own BioUniforms.
- [low] [history] Stale 'piano roll rows' claims on the grid-tint helper and its consumer  (SpectralColor.swift:284)
  PianoRollView struct deleted #475; `displayComponents(forToneHz` has exactly one caller (TouchInstrumentView:1454). The line a 'Ton-Gitter' rework reads first names a second consumer that no longer exists.

RECOMMENDATION: The physical stage is right and provably twinned — leave SpectralColor/toneColour alone. Make 'physical' true at the PIXEL at defaults in one slice with one guard, in this order: (1) replace the floor at MetalBioView.swift:1887 with a ratio-preserving lift (scale, then divide by max channel if >1) and skip echoelHue when u.hueShift == 0 — moving the GlitterCannotBecomeAFlashTests:234 needle in the same commit; this alone removes the +20° rotations. (2) Make the grid the field's colour: displayComponents drops the 0.22 lift and encodes with the sRGB curve (or TouchInstrumentView builds its UIColor from the sRGB-encoded linear value), fix the :421 comment, and add a CISmoke claim that grid and field encode 640 nm identically. (3) Hide the eight inert rows in donut mode now; the donut's language is a founder call (question 2) — if he chooses one language, switch SpectralDonutView:186 to toneLinearRGB and move the two visibleColor tests in the same commit. (4) When fullscreen merges into one unit, route all three mounts through ONE hue/saturation source (weatheredVisuals) and give the unit a visible 'physical' state plus a reset that writes hue 0 / saturation 1.0. (5) Make shaderSource internal and add the coefficient/matrix parity claim so the twin cannot drift silently. Do NOT touch the warm tint, the 1.05 default, the S-curve or the coherence lift until the founder answers question 1 — and until then, scope every 'physikalisch korrekt' sentence (caption :5208, store text) to the tone stage, not the pixel.

FOUNDER QUESTIONS RAISED:
  ? Warm daylight white point (1.0, 0.92, 0.80), the −0.03 'filmic' offset and saturation 1.05: does 'physikalisch korrekte Darstellung der Farbtöne' mean the pixel should be the physical colour (tint off, saturation 1.0 = neutral, no S-curve), or should the field keep its warm graded character with 'physical' meaning only the tone→colour stage? This is a device-look decision — the two look measurably different and both have a founder quote behind them.
  ? Spectrum donut: keep its own spread language (fundamentals, partials and overtones each a DISTINCT colour, A4 yellow-green, bass core and treble rim near-black today) or the same octave-fold colour as field and grid (A4 red, partials of one note share a colour)? If it keeps its own, is an on-screen label of the difference acceptable in the merged control unit?
  ? Ton-Gitter: the 22 % white lift makes every grid cell pastel (max saturation 0.78) and was added so deep red/violet read on the dark field. With the lift removed the grid shows the field's saturated colour but deep violet cells get darker — please judge readability on device.
  ? Should a Vapor tap or a mood-pad drag (both persist a hue rotation with no way back) survive the 'physical' law — i.e. do you want a visible 'physical / reset' state in the one control unit, or should presets and pads stop writing hue at all?
  ? The 'never near-black' floor at 0.35 luminance: keep it (with the hue-preserving repair) or let dim violet/deep-red tones be dim, as the eye's sensitivity makes them?
  ? Does your device already hold a non-default visual.hue / visual.saturation in UserDefaults (from Vapor or the pad)? Only a device read tells — it decides whether the next build's default change is visible to you at all (StudioDefaultKeys.swift:239-243).

### Grid+UX lead (quality gate over the note-grid worker and the
FRAME: Is the touch note grid correct and legible, and where is the visual control set non-compact or non-adaptive against the Uncodixfy and adaptive-layout laws? Answer: the pitch↔cell LAW is correct and shared (TouchPitchMap.pitch and rebuildGrid use the same playRect and the same floor(x·n)/floor(y·3) law; every touch lands in key, pinned by TouchInstrumentTests). What is wrong is everything AROUND the law: the played-note light does not land on the touched cell in any key (and only approximately in C), labels cannot fit at the small/medium window steps, flat keys are spelled with sharps, the grid's only door is a shed-able icon on a surface that is off by default — and the app has TWO fullscreens with disjoint control sets, of which the one the founder reaches from the Field panel (`.fullScreenCover`) is the one WITHOUT the instrument, WITHOUT the grid, with sub-44-pt glyph buttons and with the 560-pt width cap the founder reversed in #1027.

- [high] [design] Two fullscreens, disjoint control sets, opposite tap meaning — the cover is the one WITHOUT the instrument and the grid  (EchoelStudioView.swift:1509)
  This IS the founder's 'nicht eine Einheit': from one Field panel two doors (:5076 window toggle, :5117 `showVisual = true`) open two full-screen pictures; the one labelled 'Full screen' cannot be played, shows no grid, and turns the touch a player has just been taught into a chrome toggle. The honest merge direction is INTO the window (it already owns instrument, grid, toolbar budget, WAV/MP4 record and the beamer ha
- [medium] [design] The note grid is off by default and its only door is the 4th-shed toolbar icon, absent exactly at the small step; the Field panel has no toggle  (StudioDefaultKeys.swift:320)
  A fresh install shows no grid, the panel that hosts every other visual control has no switch, and the icon vanishes at the size where a player looks for it — the founder asks for 'das Ton Gitter' because he does not see one. Correction to the worker: the default is NOT rationale-free — the founder's own verb was 'Griffbrett EINBLENDEN' (quoted at FloatingVisualWindow.swift:239-240), i.e. an overlay one can show; whet
- [medium] [design] Three horizontal chip strips nested in the panel host's vertical ScrollView (plus one in the overlay) for tiny named sets  (EchoelStudioView.swift:5692)
  Scroll-in-scroll on the panel the founder wants 'übersichtlicher', with indicators hidden so the count of options is invisible. Corrected count: 5 presets + 4 looks — these are NAMED choices with single-digit cardinality; the CLAUDE.md rule ('a parameter whose values have names is a Picker') plus a wrapping row removes all three nested scrollers and every off-screen chip without hiding a control. The patch strip's le
- [medium] [design] Caption density: 16 explanatory captions against 28 value rows, 5 headers and 3 pickers in the one Field panel  (EchoelStudioView.swift:5207)
  'Kompakter' is decided in this panel, not in the chrome: Look + Preset + Energy/Fine tune (9 fields) + colour + weather + Voice (7 fields, 2 pickers) + Self-play (up to 11 fields), most with a paragraph. Move that removes rather than hides: one caption per `groupHeader`, per-row explanations into the `accessibilityHint`s that already exist, and the disclosure form `visualAdjustFields` already uses (:6304 `showVisualF
- [low] [duplication] Look slider mounted three times; one mount's comment is stale since #747  (EchoelStudioView.swift:5792)
  The raw `Slider` is NOT a Uncodixfy violation (explicit founder ask overrides the EchoelValueField default) — only the triple mount is. Retiring the cover removes one mount for free and deletes the stale sentence's context; the exemption note should live once at `LookBlendMap`, not on one of three mounts.
- [low] [constraint] Fixed-pixel window chrome and two font/frame inconsistencies in the panel  (FloatingVisualWindow.swift:393)
  The 28-wide icon buttons are a DOCUMENTED geometric ceiling (FloatingVisualLayout.swift:216-225: 38 pt effective, floor 140 vs ≈147 pt small card, re-derived by `ChromeBudgetFitsTests`) — not to be nudged; they widen only with a small-card redesign. The one real defect is :5085 `height: 36` (clips at AX sizes, the #617 class) — and it is on the 'Show/Hide visual window' button that survives any merge.
- [low] [design] Grid labels bypass EchoelTheme; grid tint and Metal field use two transfer functions  (TouchInstrumentView.swift:847)
  Cosmetic but visible side by side: the one text a player reads on the instrument is in a different face and white than the toolbar beside it; a cell and its cloud for the same note differ slightly in lightness. Fills/borders/radius 6 are within Uncodixfy. The field-side encoding belongs to the colour lead; the grid side should call ONE shared encoder once that exists.
- [low] [design] Accessibility value hard-codes 'three octave rows'; two different things are called 'grid'  (TouchInstrumentView.swift:805)
  Derive the word from `octaveBands.count` in the same commit as any register change; when the Field panel gains the note-grid toggle, label it 'Fretboard'/'Griffbrett', not 'Grid', so the compact menu does not carry one word for two things.
- [low] [constraint] Constraint: the pitch↔cell law is correct and single-sourced — change drawing, labels, door and cloud placement only  (TouchInstrumentView.swift:1385)
  A rework must keep `TouchPitchMap.pitch` + `playRect` as the one source. Root is always column 0; `TouchQuantizer` is time-only. Nothing in the frame justifies touching the mapping.

RECOMMENDATION: Merge INTO the floating window, never into the cover, and fix the grid in CI-testable slices BEFORE the one device-only slice. Concretely: (1) Core first, zero UI risk — make `SpectralColor.notePosition` key- and grid-aware (x = the degree's column centre (d+0.5)/n over the play width, y = the octave band row; rewrite SpectralColorTests:176-183 accordingly) and add a key-aware `NoteNaming.name(pitchClass:in:)` that spells flat keys with ♭ — both pure, both pinnable in the blocking bundle. (2) Grid door + legibility — a 'Fretboard' toggle in the Field panel (the second reader `StudioDefaultKeys.touchShowGrid`'s own comment anticipates), the toolbar icon never shed in fullscreen (width is unbounded there), and a pure threshold rule (labels off below ~30 pt cellW, whole grid off below the toolbar's shed point) with the accessibility string derived from `octaveBands.count`; default stays whatever the founder answers below, CISmoke pin edited with it. (3) The one unit — retire `.fullScreenCover(isPresented: $showVisual)` and its `visualVJOverlay` (both 560/360 caps go with it), move donut toggle, still shutter and a collapsible Preset/Energy drawer (opacity-only, Picker/wrapping row for the 5 presets and 4 looks, one caption per header) into `FloatingVisualWindow`'s existing `.fullscreen` state, turn the panel's 'Full screen' into `floatingSizeRaw = .fullscreen`, delete `showVisual`/`showVisualControls`/`floatingWasVisible` and the `.onChange(of: showVisual)` GPU hack, fix :5085 `height:`→`minHeight:`. Guards to move in that commit: ResetSoundClearsWhatTheLaunchLineReportsTests:431 (16→14 file-wide, equality), the 4/6/6/3 CISmoke files listed in finding 1, TheTakeSaysWhetherItWasWrittenTests:123-127 (three doors→two), VisualFineTuneReflowsTests (cap premise). Slice 3 is NEEDS-FOUNDER-VERIFY on device (launch, 13-modifier chain, grid look at every step); slices 1-2 are not.

FOUNDER QUESTIONS RAISED:
  ? Gitter-Default: Soll das Griffbrett im Vollbild IMMER an sein (und nur in den kleinen Fenstern automatisch verschwinden), oder bleibt es wie 2026-07-08 gesagt zum 'Einblenden' (Schalter, Standard aus)?
  ? 'An der richtigen Stelle': Soll das Licht einer gespielten Note auf GENAU dem berührten Gitterfeld aufleuchten (spalten- und tonartbezogen, Grundton links), oder in absoluter Tonhöhen-Lage (C immer links, in jeder Tonart dieselbe Spalte)? Heute ist es das Zweite, und es passt in keiner Tonart aufs Gitter.
  ? Schreibweise: In F-Moll/Es-Dur sollen die Felder A♭/B♭/D♭ statt G♯/A♯/C♯ zeigen — einverstanden? Und im deutschen Modus B (nicht 'A♯') für die Halbstufe unter H?
  ? Die EINE Einheit: Braucht das Vollbild weiterhin einen 'sauberen Projektions-Zustand' ohne jede Bedienleiste (Tippen = Leiste weg), obwohl der Beamer inzwischen seine eigene zweite Szene hat — oder reicht: Telefon zeigt immer die Leiste, Beamer immer das reine Bild?
  ? Donut-Ansicht (Spektrum-Ringe): Sie ist ein Canvas, nicht Metal — nicht antippbar spielbar, nicht als Video aufnehmbar, kein Gitter. Bleibt sie als Look innerhalb der einen Einheit, oder fällt sie beim Zusammenlegen weg?
  ? Gerätebild: Bei der kleinen Fenstergröße sind die Felder ~21 pt breit (chromatisch ~12 pt) — sollen dort nur die Beschriftungen verschwinden (Felder bleiben farbig), oder das ganze Gitter?
  ? Standbild-Taste und Preset/Energy im Vollbild: als aufklappbare Schublade unter der Leiste (eine Geste mehr, Bild bleibt frei) oder fest sichtbar (immer erreichbar, aber weniger Bild)?

### History LEAD (founder decisions · dead-ends · guard blast ra
FRAME: What the founder has already decided about visuals, which dead-ends are recorded, and which CISmoke guards go red under the likely merge (cover → floating window). No guard was executed (no toolchain); verdicts are needle-vs-source, not runs.

- [high] [history] The founder already decided the floating window IS the fullscreen — four times, all in force; the .fullScreenCover is the leftover, not the target  (WorkspaceView.swift:333)
  Direction of the merge is not open: the window is the 'eine Einheit'. The cover got its door only because it was built and doorless (#747, decisions.csv:399, ship-gate 4). Today's ask is the founder's SECOND request for one visualiser; the plan's own finding still stands — the renderer is already one (MetalBioView), what is duplicated is mount sites and control surfaces.
- [high] [design] Two fullscreens with DIFFERENT capabilities: the cover has donuts + VJ panel + still + REC/close but NO play surface; the window has touch surface + grid + look slider + recorders but NO adjust fields, presets, donuts or still  (EchoelStudioView.swift:1509)
  The founder's 'Ton Gitter' is unreachable from the cover fullscreen and the fine-tune/preset controls are unreachable from the window fullscreen — the player must leave one surface to use the other. The merge must move three things INTO the window (donut branch in `visualLayer`, a VJ/adjust panel, the still) and then retire the cover; HARNESS_LEDGER.md:362 C1 ('Löschen = Feature-Verlust') binds the order: home first,
- [high] [constraint] Retiring the cover reddens nine guards by needle; all nine are re-anchor-not-delete, and one freed chain slot is the prize  (VisualFineTuneReflowsTests.swift:352)
  Same-commit re-anchors (#364/#456): move `hasDoor` to the window's `spectralDonuts` writer, drop the studio entries from the three-file lists, set ResetSound to 14, re-point the `visualAdjustFields` anchors to wherever the shared rows live, delete `visualVJOverlay` (not excuse it), and sweep CLAUDE.md's '14 presentation modifiers' / '2 der 14' (CLAUDE.md:47/:52) plus the prose in TapTargetFloor :195, VisualLookTruth 
- [high] [constraint] Every new toolbar item in the merged window (still · donut toggle · panel toggle) is a new shed rank: ChromeFit has exactly six fields and two guards pin the six and their order  (FloatingVisualLayout.swift:245)
  #985 refused a 7th button for exactly this budget; the merge cannot append — it needs a redesigned bar (e.g. one 'controls' disclosure that hosts still/donut/panel inside a sheet-free overlay, or a two-row fullscreen-only bar) with ChromeFit + ranking + Mirror list + `barWidth` re-derivation extended in one commit. Exits (resize/close) stay the last two, never shed (TheWayOut :150-160); every `sizeRaw =` write must p
- [high] [constraint] The window laws the merged surface must keep: one MetalBioView per file, hidden = Color.clear + inert, every sheet through heldWhileHidden, one TouchInstrumentView app-wide, and the donut branch has no video-capture source  (FieldSurvivesAHiddenPictureTests.swift:185)
  A donut branch inside `visualLayer` must sit on the SAME hidden/recording guard and needs an explicit rule for a running take (the cover simply hides REC in donut mode — carry that rule, or a take started in donut mode is the #319 empty take by a new route). The cover's `.sheet(item: $visualShare)` must become the window's existing `recordedClip` sheet or route through `heldWhileHidden`. The tone grid stays inside th
- [medium] [constraint] Freeze-law risk of hosting SpectralDonutView in the window is LOW — measured: it reads masterLevel in its OWN body (a leaf), and no guard scans FloatingVisualWindow for hot reads  (SpectralDonutView.swift:67)
  Resolves worker 2's UNMEASURED item: mounting the donut view in `visualLayer` keeps the 60 Hz meter read in a leaf. The window hosts no `Picker(` (grep = 0), so the 10.76.41/50 law is not at stake there — but if the VJ/adjust panel moves INTO the window, do not put `MusicColourRowView`'s MusicalFrame read or any recorder outcome read in the window's body; keep the leaf pattern (EchoelStudioView.swift:6398-6401 record
- [high] [constraint] Compacting the Field panel collides with six exact-shape pins, two of them founder-circled (preset strip directly above its fields; Energy/caption/disclosure outside the grids)  (SectionHeadingIsOneTreatmentTests.swift:258)
  'Kompakter' has three legal shapes: (i) fewer, denser grids WITHOUT sweeping Energy/caption in; (ii) extracting the rows into one shared View used by both hosts (re-points every `private func visualAdjustFields(spacing:` anchor — the counts are description, the outside-the-grid and adjacency rules are law); (iii) a disclosure that hides the fine-tune block by default (already exists: `showVisualFineTune`). Removing t
- [medium] [constraint] Saturation ≥ 1.0 is a founder decision (#578 'Bunter'), not a taste call; 1.05 is pinned as agreement in four places, the warm grades are pinned nowhere  (GlitterCannotBecomeAFlashTests.swift:99)
  'Physikalisch korrekt' must act on the hue ROTATION (finding 3) and the CIE→sRGB seam (SpectralSeamTwinTests :41-46 keeps `tRedMetalLiteral`/`tVioletMetalLiteral` interpolated), not on the saturation floor or the colour-truth gate. Dropping to 1.0 stays green but is a visible step back from #578 — hold for founder. The warm point (2026-06-26 'natural warm light') and 'Bunter' coexist by design; strip neither wholesal
- [medium] [design] The tone grid is a display-only CALayer aid, OFF by default, toggled only from the window toolbar and shed fourth on narrow cards; no Field-panel toggle exists  (StudioDefaultKeys.swift:320)
  Ask (b) is decided by two founder-only choices: grid ON by default (a re-anchor of TouchSurfaceStorageKeysTests' default pin, no migration) and where its toggle lives in the one control unit. The grid's cell tints already read `SpectralColor.displayComponents` — ask (a) and (b) share one colour source; a hue-rotated palette (finding 3) makes grid tint and field colour DISAGREE for the same note, which is precisely th
- [medium] [history] #575's conditional half ('wenn dann ins Visual Window übertragen') is registered and unbuilt; all four analysis views are parked on purpose — a compact menu must not re-mount them in the Field panel  (EchoelStudioView.swift:5129)
  If the merged window grows a control panel, the founder-approved home for a readout is THAT panel, not the Field panel — but only after he sees the layout. Räumlichkeit from the same 2026-08-13 ask is also unbuilt (953fa54b: 'Räumlichkeit is deliberately its own cycle'; SESSION_LOG hits = 0) — the founder-backed reason to touch the field functions if this rework goes there; do not report #578 as closed.
- [medium] [history] Recorded dead-ends that bind the merge: never append to the modal chain; never delete the cover before its features have a home  (HARNESS_LEDGER.md:24)
  C1's 'doorless' premise expired with #747; the feature-loss half survives and is the ordering rule for the merge. The chain row says the merged panel must not become a new `.sheet` — host it as an in-window overlay (opacity transition, Uncodixfy) exactly as the cover's `visualVJOverlay` is today (`if showVisualControls { visualVJOverlay.transition(.opacity) }`, :1537-1540). Net chain effect of the merge is −1 (cover)

RECOMMENDATION: Plan the merge in the direction history already fixed: the FLOATING WINDOW is the one visual thing (fullscreen at launch #580, never unmounted #311, the only play surface + grid), and the `.fullScreenCover($showVisual)` is retired AFTER its three unique features get a home inside the window — (1) a donut branch in `visualLayer` under the same hidden/recording guards with REC disabled in donut mode, (2) the VJ/adjust panel as an in-window opacity overlay built from a SHARED row view (so the Field panel and the window cannot drift, and the exact-count anchors re-point once), (3) the still shutter + donut toggle + panel toggle via a redesigned fullscreen bar (ChromeFit grows to nine fields with a documented ranking, never an appended seventh button). Ship as three Ralph slices in that order, each re-anchoring its guards in the same commit and sweeping CLAUDE.md's '14'/'2 der 14' when the cover goes (net chain −1, the first shrink since 10.76.34). Run the colour slice FIRST and separately: the launch-path hue rotation (Vapor 0.82) is the only measured 'physically incorrect' defect — the CIE chain, seam literals, colour-truth gate and warm point are all founder-decided and correct — and fix Hue/Saturation edits so they clear or memo the preset. Do not touch the saturation floor, do not re-mount analysis views, do not add a second TouchInstrumentView, and do not make a second `showVisual`-style door: one labelled fullscreen door on the window, period.

FOUNDER QUESTIONS RAISED:
  ? Fresh-install colour: soll Vapor als Start-Stimmung bleiben, aber OHNE Farbdrehung (Hue 0 = physikalische Tonfarbe), oder soll der Start ganz ohne Preset sein? Heute dreht jeder Neustart die Palette um 0,82 Umdrehungen zurück, auch nachdem du Hue 0 gesetzt hast.
  ? Sättigung: 1,05 kam aus deinem 'Bunter' (2026-08-13). 'Physikalisch korrekt' verlangt Hue 0, nicht Sättigung 1,0 — willst du 1,05 behalten, oder auf dem Gerät A/B sehen (1,05 vs 1,00)?
  ? Ton-Gitter: standardmäßig AN im Vollbild? Und wo sitzt der Schalter in der einen Steuer-Einheit — in der Leiste (kostet einen Shed-Rang) oder im Panel?
  ? Donuts (Spektrum-Ringe): behalten und ins Fenster übernehmen, oder mit dem Cover streichen? Beides ist ein Feature-Entscheid; Donuts können kein Video aufnehmen (Canvas, kein Metal).
  ? Räumlichkeit (dein Wort vom 2026-08-13, nie gebaut) — gehört das in diese Überarbeitung (Feldfunktionen anfassen) oder bleibt es ein eigener Zyklus?
  ? Messwert im Visual (#575 'wenn dann ins Visual Window'): soll das neue Panel im Fenster eine Anzeige tragen, und wenn ja welche eine — Hz/Note, Spektrum, Kohärenz? Layout erst nach Gerätesicht.
  ? Gilt 'Visual immer oben rechts' (2026-07-11, Überprüfung seit 2026-08-10 fällig) noch, jetzt wo das Fenster beim Start Vollbild ist?
  ? Gerät: alle Farb-Slices seit #578/#853 und die #747-Tür sind NEEDS-FOUNDER-VERIFY — eine Geräte-Session vor dem Merge, damit 'schön' nicht aus Zahlen geraten wird.

---

# PM DECISIONS + CONSTRAINTS FOR THE PLANNERS (2026-09-06, after the audit; Council convened)

Founder ask 1 (verbatim): "Hetzte die visual Sektion insgesamt überarbeiten und korrigieren. Die physikalisch korrekte Darstellung der Farbtöne, das Ton Gitter, das Menü kompakter. Du überprüfst alles per Ultradeepaudith und machst alles korrekt und schön. Insgesamt soll das User Erlebnis im fordergrund stehen kompakter und übersichtlicher gestalten mit einer Einheit zum steuern aktuell gibt es fullscreen Mode das soll aber alles zu einem Ding zusammen gefasst werden"
Founder ask 2 (verbatim): "Ultraplan Ultracode ultrausablitiy ultraaudiovisionbiofeedbackcomposition" — plan deeply, orchestrate, USABILITY FIRST, and treat the visual as part of ONE audio·vision·biofeedback·composition instrument (the visual reflects what the body and the composition do; it is not a separate app).
Standing founder frame: "KEINE Fragen an Founder außer bei echter Ambiguität" and "Du entscheidest". So the PM decides; each decision below is REVERSIBLE and flagged, not a blocker.

## DECIDED (plan from these; do not re-litigate)
D1 ONE SURFACE = FloatingVisualWindow. Its existing WindowSize.fullscreen is THE fullscreen. The .fullScreenCover(isPresented: $showVisual) is RETIRED after its unique features are ported into the window: donut branch (non-capturing leaf), StillShutterButton, tap-to-hide chrome, keep-awake, the VJ/adjust drawer. The Field panel's "Full screen" button becomes a write of visual.floating.size = .fullscreen. Chain 14 → 13. Never append a modal.
D2 ONE CONTROL DEFINITION. Look · Preset · Energy/Fine-tune · Colour rows exist ONCE (shared row builders) and are mounted in the Field panel and, in fullscreen, as an in-window opacity drawer. No 560/360 caps anywhere (the #1027 law: screen-filling, adaptive via ViewThatFits/AdaptiveCardGrid).
D3 FIRST LAUNCH = PHYSICAL COLOUR: hue 0, saturation 1.0. Vapor stays as a tappable preset (its hue/sat travel with the chip), it is no longer the fresh-install default and is not re-applied on every appear. StudioDefaultKeys.visualSaturation 1.05 → 1.0 and the four agreeing pins move with it. FLAGGED REVERSIBLE: the founder's 2026-08-13 "Bunter" — if he wants 1.05 back it is one constant.
D4 PHYSICAL AT THE PIXEL where it is a DEFECT, not taste: the 0.35 luminance floor becomes ratio-preserving (no per-channel clamp hue rotation); the per-channel affine "filmic" offset becomes hue-preserving (or luminance-only); echoelHue skipped at hueShift 0; parity claim Swift↔MSL in CISmoke. NOT touched (founder-quoted, taste): warm daylight tint, coherence lift, glitter/texture.
D5 TONE GRID: (a) the played-note cloud lands on the touched cell — key-relative, root left (SpectralColor.notePosition becomes key/grid aware); (b) flat keys spell with ♭ (NoteNaming key-aware); (c) a "Fretboard / Note grid" toggle gets a door in the Field panel and is NEVER shed in fullscreen; (d) labels hide below a pure width threshold, whole grid below the shed point; (e) default stays OFF (founder 2026-07-08 "einblenden") — FLAGGED REVERSIBLE; (f) grid tint uses the field's encoder so one pitch = one colour.
D6 DONUTS stay as a LOOK inside the window (Ship-Gate 4's second half), non-capturing, REC disabled in donut mode, inert rows hidden in donut mode. Donut colour language: switch to the same octave-fold as field+grid (one language) — FLAGGED REVERSIBLE.
D7 FULLSCREEN BAR: in fullscreen nothing sheds — the bar WRAPS (ViewThatFits row → two rows), so the labelled Studio exit, look slider, grid toggle, still, donut toggle all survive on 375 pt. The shed ladder stays for the small/medium/large cards. Weather-mixed picture everywhere (window, beamer) — one hue/saturation source.
D8 CAPTIONS: at most ONE caption per group header; the flash-safety sentence and the colour-truth sentence stay (law/claims) but the colour sentence is rewritten to be TRUE (purple-line interpolation, "default" only when no preset). Everything else moves into accessibilityHint or is deleted. Three horizontal chip strips → wrapping rows / Picker where the set is named and ≤5.
D9 LAWS THAT BIND EVERY SLICE: freeze law (no ~10 Hz read in a menu-hosting body/ancestor; the window is a leaf); one MetalBioView per file; hidden = Color.clear + inert; every sheet through heldWhileHidden; EchoelValueField for numeric params, Picker for named; Uncodixfy (radius ≤16, no glow, opacity-only); flash ≤3 Hz; guards re-anchored in the SAME commit (#456), never deleted when they are law (#364); ≤3 files per Ralph slice where possible — a bigger slice must say why; every slice names compile-verifiable vs device-only.
D10 NOT IN THIS EPIC: new looks (kaleidoscope/water), Räumlichkeit, analysis-view remount, beamer redesign beyond one colour source, a second TouchInstrumentView, any new sheet.

## OPEN (device-only, NEEDS-FOUNDER-VERIFY after the merge ships)
V1 floating REC → fullscreen: exactly one renderer, clean MP4. V2 fullscreen keeps the screen awake. V3 grid readability with the lift removed at small/medium. V4 the look of the merged fullscreen (bleed, bar wrap). V5 saturation 1.0 vs 1.05 A/B — one word from the founder flips it.
