build: 2026-06-26 v10.76.36 (Bisect: revert Studio to last launching state)
branch: claude/piano-roll-clip-view-wozlie
notesT: 10.76.34 and 10.76.35 both still black-screened, so the earlier hypotheses
  were wrong — stopping the guesswork and doing a decisive bisect. Every diag log
  stops at "init done" and no UI-branch breadcrumb ever fires → the crash is at the
  first render, before any screen. The only render-path change since the last
  CONFIRMED-launching build (10.76.21) is EchoelStudioView. So this build restores
  EchoelStudioView to exactly that known-good state.
  Trade-off: this session's studio additions are temporarily removed (planet-tone
  picker, Licenses button, Bio→Visual, the new visuals menu, prism look entry). The
  shader Cousto colours, scales, planetary tunings, rPPG fix and the self-healing net
  all remain. If this launches, those features get re-added ONE AT A TIME with a device
  check each; if it still black-screens, the cause is outside the studio view.
  Test: 10.76.36 → cold launch → does the Studio appear (no black screen)? Please also
  send the previous-session log from the recovery screen if Safe Mode ever shows — it
  contains the crash backtrace that names the faulting function.
