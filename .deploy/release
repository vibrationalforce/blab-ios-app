build: 2026-07-21D — Genre-Melodie-Autopilot aus (#77 Teil 2, Founder-Ohr-Feedback)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Founder direkt: "Neuer TestFlight bitte."

  Seit dem letzten Deploy (62abddc) auf dem Branch:
  · Task #77 Teil 2 (9dce29b): Founder-Ohr-Feedback "Die Genres Psytrance bis Rocksteady
    werden sehr stressig wegen den Melodien. Melodien sollen die Leute selbst machen." —
    Council (inline) erweiterte den genannten 17-Genre-Bereich auf alle 23 Genres für eine
    einheitliche Invariante: MusicStyle.harmonicProfile.leadDensity ist jetzt 0.0 überall.
    BioComposer generiert damit für KEIN Genre mehr eine automatische Lead-Melodie mehr —
    Bass/Harmony/Drums unverändert. leadPatchName (das warme Lead-Timbre) bleibt unverändert,
    d.h. wer selbst eine Melodie spielt (Touch/MPE/Piano Roll) bekommt weiterhin den
    genre-passenden Klang. code-reviewer fing vor dem Commit einen sonst gebrochenen Test
    (NoteRoleTests.swift, erwartete noch eine Lead-Rolle) — korrigiert. Zwei dadurch dauerhaft
    leere Emergent-Tests entfernt (nicht XCTSkip — die Prämisse ist dauerhaft falsch, nicht nur
    vorübergehend), ein neuer kanonischer Invarianten-Test ergänzt
    (MusicStyleTests.testNoGenreAutoGeneratesLeadNotes).
  · Entscheidung geloggt (decisions.csv + memory/decisions.md, a857eac): der zweite,
    größere Founder-Ask derselben Nachricht — "rhythmische Vielfalt bei allen Genres,
    die sich am Biofeedback orientieren soll" — ist bewusst NICHT in diesem Build. Er
    berührt BioComposer.swift's geteilte Beat-Archetyp-Funktionen (fourOnFloorBeat/
    backbeatBeat/offbeatBeat/halfTimeBeat, je über 3-6 Genres geteilt) und braucht einen
    eigenen PLAN + Council-Durchgang zuerst (gleiche Disziplin wie "Automation in der
    Spur"). Als Task #79 im Tracker angelegt.

  Bekannte offene Punkte (bewusst NICHT in diesem Build angefasst, device-gated/founder-gated/
  bewusst sequenziert, unverändert seit dem letzten Deploy): Automation-Präzisions-Editor-
  Retarget (S2, Gerät nötig), Clip-Zittern C3/C7 (warten auf eine echte Bildschirmaufnahme),
  AUv3 "-3000" (Portal/Registrierung), EchoelPublish/Weather-Synth/EEG-Quelle/Bio-Session-Brain
  (bewusster Ideen-Parkplatz), Leiste-Verteilung S2-S6 (wartet auf Founder-Redesign-Entscheidung),
  BodyVibe Kamera-Modulator (Info.plist-Datenschutztext braucht Founder-Freigabe), DAW#4
  Video-Spuren (kein sicherer nicht-geräte-gebundener Slice gefunden), rhythmische Vielfalt
  via Biofeedback (Task #79, neu — braucht eigenen Plan zuerst).

  Gates: Xcode Compile Check + Echoelmusic CI/CD Pipeline grün auf 9dce29b (verifiziert via
  actions_list vor diesem Deploy). Sources-Commit hatte einen Pflicht-Reviewer-Pass
  (code-reviewer, fing die NoteRoleTests-Lücke).
