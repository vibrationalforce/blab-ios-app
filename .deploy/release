build: 2026-06-26 v10.76.25 (In-app Licenses & Credits — legal compliance)
branch: claude/piano-roll-clip-view-wozlie
notesT: Builds on 10.76.24. Adds the in-app "Licenses & Credits" screen (Compose
  footer, next to Diagnostics) so the open-source attributions travel with the app:
  Atkinson Hyperlegible (SIL OFL 1.1 — Braille Institute; full OFL text already
  bundled in Resources/Fonts/OFL.txt), Hans Cousto's Cosmic Octave concept
  (colour-octave + planetary tones — creative tuning, NO health/chakra claims),
  procedurally-synthesised drums (original work), Apple frameworks, and Echoel's
  own MIT licence. Mirrors THIRD_PARTY_NOTICES.md. App Store hygiene; the font's
  OFL prefers the licence ship alongside the font.
  Launch-safe by construction: standalone NavigationStack + ScrollView + plain
  Text sections — NO Menu, NO ForEach, NO @Observable-binding (the patterns that
  black-screened); AnyView-erased like the sibling sheets and built lazily on
  presentation, so it cannot touch launch-time root metadata. Reviewed GO
  (ui-state-reviewer). One new @State Bool with a complete dismiss path.
  Test: 10.76.25 launches? Compose → scroll to footer → tap "Licenses & Credits"
  → the credits sheet opens (font OFL, Cousto, drums, Apple, Echoel MIT) → Done.
