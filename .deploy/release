build: 2026-06-22 v10.43.0 (Two-clock tempo entrainment + richer default unison)
branch: claude/piano-roll-clip-view-wozlie
notes: Phase 1+2 of the science-grounded composition/sound overhaul. (1) TWO-CLOCK TEMPO — in Flow mode the pulse follows the heart but is now pulled toward the resonance band (72 BPM = 6 breaths/min × 12) as HRV coherence rises, so the groove settles WITH the body instead of merely mirroring it (zero coherence → follows heart; full coherence → ~72 BPM). (2) RICHER SOUND — every generative lead/pad now gets a gentle default 2-voice / ~7¢ unison (previously NO genre patch used unison → thin single-voice tone, the top "rudimentary" cause). Explicit patch unison (incl. mono) is always honoured. Carries v10.42.0 (coherence-convergence servo: high coherence makes takes decisively sparser/settled) + v10.41.0 (real-FFT oscillating spectral rings, non-standard-tuning hero-path guard). Compile + CI tests green. All grounded in a peer-reviewed HRV-coherence brief; Solfeggio/"healing-frequency" pseudoscience explicitly excluded.
