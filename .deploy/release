build: 2026-07-14 v10.79.198 (Variationen-Audition — die Idea-Maze bekommt ihre Tür)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Enthält alles aus 197 (adaptives Home + rPPG-Puls-Fix) PLUS die neue
  Variationen-Audition.

  NEU — VARIATIONEN (Idea-Maze, jetzt erlebbar). Der bio-kuratierte Ideen-Komponist
  war gebaut + getestet, hatte aber keine Tür. Jetzt im Comp-Menü (Composition-
  Dropdown) ganz unten eine Karte "Variationen":
    · "Erkunden" tippen → 6 Varianten DESSELBEN Grooves werden gerankt, danach
      sortiert, wie nah jede an dem liegt, was dein Körper gerade will
      (realisierte Dichte vs. dein musicalState.busy — eine ehrliche Zahl, keine
      Gesundheitsaussage).
    · Eine Zeile antippen → sie spielt SOFORT. Skelett + Melodie-Seed der gewählten
      Idee werden exakt übernommen; dein LIVE-Körper färbt weiter Tempo/Dynamik.
      Die gewählte Idee ist markiert (Häkchen).
  Kein neuer Regler doppelt, keine neue Modal-Fläche — sitzt in der bestehenden
  Comp-Tür. Was du auditionierst, ist genau die Eingabe, die auch generate() nutzt
  (eine geteilte makeComposerInput-Funktion — kein doppelter Kompositions-Code).

  Sauber gebaut: ui-state-Reviewer PASS (0) · concurrency-Reviewer PASS (0).
  Render-Safety eingehalten: keine neue .sheet (Modal-Decke), die Maze-UI liest nur
  @State-Schnappschüsse (kein 10-Hz-Read im Menü-Host → kein Freeze).
  Gates grün: Xcode Compile Check + CI/CD Pipeline.

  NOCH OFFEN vom letzten Build (bitte am Gerät gegenchecken):
  rPPG-PULS-LOCK (aus 197). Dein 2301-Log: Finger drauf, Lauf 2 lockte NIE (pk=0,
  bpm=0). Fix: lineare Drift-Steigung wird jetzt vor der Periodizitäts-Analyse
  entfernt (RPPGConditioning.linearDetrend). Bitte NEUEN Log: lockt Lauf 2 jetzt?
  UND bleibt der Bewegungs-/Druck-Fall sauber (kein Fehl-Lock bei Wackeln)?

  KONVERGENZ-BACKLOG (offene Baustellen, eine pro Zyklus): Szene→OSC-Ausgabe
  (Council: Objekt-Index-Koexistenz mit Bio) · Immersive-Stage-Automation
  (Puck-Bewegungen aufnehmen/abspielen) · Audio-Loop-Import in die Spur.
