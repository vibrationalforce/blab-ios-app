build: 2026-06-20 v10.34.14 (Bio comes alive — one-tap pulse gateway)
branch: claude/piano-roll-clip-view-wozlie
includes: The bio-reactive heart of the app is now reachable in ONE tap from the element you actually look at. The live strip's right end is honest + alive in three states: "Read pulse" (no signal yet) starts the camera rPPG → your body drives the generative sound + visual in realtime; "Reading… / Cover camera" gives live lock feedback (pulsing heart, Reduce-Motion aware); a green source tag appears only when a REAL fresh signal is live (no synthetic/demo data, ever). Camera runs only on explicit tap (battery + privacy) and reuses the existing proven start path — no second code path. Fixes the "No signal dead-end" first impression: the body is now the front door, not a hidden step behind the Record button. Plus the Arrangement timeline (v10.34.13) and everything from v10.34.3–v10.34.13. Verified: ci.yml (build+test, all platforms) + xcode-compile-check (AUv3) green on 4b4ad44.
