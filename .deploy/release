build: 2026-07-17 v10.79.280 (Oktaver komplett + MPE-Tür + Triolen-Q + AUv3-Selbstdiagnose) [retry 2: Type-Check-Budget-Fix RegionBlockView]
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Antwort auf den Morgen-Test-Log (v279/2385) — danke, der Log war Gold wert.

  WAS DEIN LOG GEZEIGT HAT: Launch gesund, Puls-Lock exzellent (BPM 67-68,
  Konfidenz bis 0.97 — die Kette hält), Instrument spielt. ABER ownAUv3
  weiter false. Serverseitig verifiziert: die AUv3-Extension IST korrekt
  im Build 2385 eingebettet (Archiv-Check grün, Registrierungsstruktur
  korrekt). Das Problem liegt auf dem Gerät — dieser Build sagt dir WO:

  1) AUv3-SELBSTDIAGNOSE: Wenn die eigene AUv3 nach allen Scan-Retries
     fehlt, versucht die App sie EINMAL direkt zu instanziieren. Im Log
     steht dann eine von zwei Zeilen: "self-probe: INSTANTIATE OK"
     (= Registry liefert sie, nur die Liste ist kalt) oder "self-probe:
     FAILED …#-3000" (= iOS hat sie nicht registriert → iPhone NEU
     STARTEN, dann GarageBand öffnen — das ist der bekannte iOS-Weg).
     → Bitte: neuen Log nach einem Geräte-Neustart schicken.

  2) OKTAVER (dein Wunsch "transpose detune und Oktaver" — jetzt komplett):
     Im Spur-Editor neben Detune gibt es "Octave" (−1/0/+1): dopplet jede
     Note dieser Spur eine Oktave tiefer/höher (halbe Lautstärke, folgt
     Slides). Wird mit dem Song gespeichert.

  3) PIANO-ROLL: MPE pro Note (Bend/Slide/Pressure im Inspector, wirkt
     auf MIDI-Out an externe Synths) + Triolen-Quantize im Q-Menü
     (1/8T, 1/16T) + Menü friert beim Playback nicht mehr ein.

  4) AUDIO-CLIP-EDITOR: Warp-Charakter "Beats" ist jetzt HÖRBAR in der
     Vorschau (transient-fest, eigener WSOLA-Kern). Timeline spielt
     vorerst weiter Clean — ehrlich beschriftet.
