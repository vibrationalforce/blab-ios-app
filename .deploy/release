build: 2026-07-19 v10.79.300 (AUv3-HOST-FIX D — fail-on-stripped Entitlement-Beweis)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: SELBST-BERICHTENDER BUILD. Der Sandbox-Agent kann den CI-Job-Log nicht lesen
  (Org-Policy 403 auf den Actions-Log-Blob) — nur das RUN-ERGEBNIS (grün/rot) ist
  lesbar. Darum failt der Beweis-Schritt diesen Build jetzt HART, wenn inter-app-audio
  aus dem signierten App-Bundle gestrippt ist. Damit ist das Build-Ergebnis selbst die
  Antwort — du musst keinen weiteren evtl. toten Build testen:

  - RUN GRÜN  → inter-app-audio ist im signierten Binary → Entitlement wirkt →
    dann (und erst dann) testest du: Browse AUv3 → erscheinen Fremd-Plugins?
  - RUN ROT (am "Prove Inter-App-Audio"-Schritt) → Profil trägt die Capability nicht
    → du regenerierst im Portal das Distribution-Provisioning-Profil für
    com.echoelmusic.app (mit Inter-App Audio aktiv), dann re-archive ich. Ich sage
    dir das dann exakt.

  Kein App-Verhalten geändert — nur der CI-Beweis entscheidet jetzt über grün/rot.
