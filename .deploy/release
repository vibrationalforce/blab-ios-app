build: 2026-07-02 v10.79.27 (Visual-Design wirkt im Fenster · MP4 mit passendem Namen)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notesT: Founder: "Visual Design muss schon noch möglich sein. Session Recording für video und
  auch passender Name." Zwei Dinge:
  (1) Visual-Design wirkt jetzt im schwebenden Fenster. Das Fenster nutzte bisher nur den
  Default-Look; jetzt liest es die GETEILTEN Design-Keys (Look/Style + Blend + gewähltes Preset
  aus dem Visual-Panel) und zeigt sie an — was du im Visual-Panel einstellst, siehst du im Fenster.
  Das Visual-Panel bleibt (Design weiterhin möglich). (Die Fein-Regler Intensity/Detail/Motion/
  Spread sind noch view-lokal → das PRESET trägt diese Form; Live-Regler ins Fenster ist ein
  Folgeschritt.)
  (2) Die Video-Aufnahme (MP4) heißt jetzt beschreibend wie die WAV:
  `Echoel_2026-07-02_Dm_120bpm_A440_Dub-Techno.mp4` (Tonart · Tempo · Stimmung · Genre).
  Fällt sicher auf den Originalnamen zurück, falls das Umbenennen scheitert.
  TEST 10.79.27: Im Visual-Panel ein Preset/Look wählen → schwebendes Fenster zeigt ihn.
  Aufnahme (●) → Stop (⏹) → Teilen-Blatt: die .mp4 trägt Tonart/Tempo/Genre im Namen.
