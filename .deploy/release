build: 2026-07-11 v10.79.169 (Die Timeline SPIELT — funktionierende Spuren, Reorg P3)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notes: Der große Schritt: die TIMELINE spielt jetzt wirklich.

  NEU: "Play timeline"-Knopf im Timeline-Bereich:
  · Spielt die platzierten Regionen über den geteilten Transport ab —
    lädt pro Region ihren Clip in Piano-Roll + Pattern (genau der bewährte
    Lade-Pfad, den die Song-Sektionen/Clips schon nutzen), loopt sauber am
    Song-Ende (auf ganze Takte gerundet).
  · OPT-IN und GETRENNT von Generate+Play → das startende Instrument bleibt
    komplett unberührt (der neue Abspiel-Aufruf ist ein No-Op, solange du die
    Timeline nicht startest).

  Unter der Haube (alles getestet, alle Gates grün):
  · P2 — reiner Region-Scheduler (welche Region liegt unter dem Playhead, wann
    wird umgeschaltet), 16 Unit-Tests.
  · P3 — TimelineRegionPlayer (reiner Positions-Cursor mit Tests) + Verdrahtung
    an den Transport; render- + concurrency-audited (Swift-6-clean).

  WICHTIG zum Testen: erstmal EINZELN testen — Timeline ODER Generate+Play,
  noch nicht beides gleichzeitig (das Zusammenspiel koordiniere ich als
  Nächstes). NEEDS-FOUNDER-VERIFY: spielen + loopen die Regionen sauber?

  Reorg-Fahrplan läuft weiter (autonom, sicher): als Nächstes adaptives Layout
  horizontal+vertikal, Master-Stimmen aufs Hackbrett, Wetter-Panel.

  Frühere Builds warten noch auf dein Ohr/Auge: v162–v166, v167 (Groove „Eins"),
  v168 (Hackbrett).
