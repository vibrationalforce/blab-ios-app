build: 2026-07-14 v10.79.207 (Umbau Schritt 1: die Leiste-Auflösung beginnt — erst der sichere Unterbau)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Direkt auf deine Ansage — "die Leiste unten soll sich auflösen und woanders
  wiederfinden" + "die EchoelTools funktionieren generell noch nicht". Ich habe zuerst
  den Ist-Zustand sauber kartiert (gründlicher Lese-Agent), bevor ich an dem
  4299-Zeilen-Monolithen operiere, der den Start schon zweimal zerschossen hat.

  WAS DIE KARTE ZEIGTE:
   · Die "Leiste unten" = die Chip-Reihe in EchoelStudioView (Comp · Session · Sound ·
     Mix · FX · Mood · Synth · Video). Die soll hoch in den Header + in eine
     pro-Instrument-Fläche.
   · "Tools funktionieren nicht" heißt NICHT "kaputt/Platzhalter" — sie sind real
     verdrahtet, aber ihre Auslöser sind verwaist, seit das alte Tools-Gitter
     abgehängt wurde. Der lebende Weg zu Piano Roll / Patch / Automation / Audioclip /
     AUv3 ist heute die Tür am SPUR-KOPF in der Timeline.

  WARUM DIESER SCHRITT ZUERST (unsichtbar, aber tragend): EchoelStudioView hatte ~21
  Modal-Schichten (.sheet/.cover) — genau an der Metadaten-Klippe, die den
  Black-Screen-SIGSEGV auslöst. SECHS davon waren tote Doppel (nur vom abgehängten
  Gitter ausgelöst; der echte Weg ist die Spur-Kopf-Tür). Ich habe sie entfernt →
  Kette ~21→15. Das schafft den Sicherheits-Spielraum, den die SICHTBAREN Schritte
  brauchen (sonst reißt beim Umbau der Start).

  NÄCHSTE SCHRITTE (Plan steht: scratchpads/PLAN_DISSOLVE_BOTTOM_BAR):
   2. HEADER-UP — Tonart/Skala/Stimmung/Tempo + Session-Name/Ort wandern hoch in eine
      kompakte Kopfzeile; die Chips "Comp"/"Session" verschwinden. DAS siehst du dann.
   3. PRO-INSTRUMENT-ECHOELSYNTH — pro Spur ein Panel (Sound&Textur · Mix · FX;
      Genre · Variation · Mood; Weather), auf die schon vorhandenen Spur-Felder
      (patch/genreOverride/mood) gestützt.
   4. Restliche Chips auflösen, bis die Leiste weg ist.

  Diese v207 ist bewusst der sichere Unterbau — noch keine sichtbare Änderung, aber
  ohne ihn wäre der nächste (sichtbare) Schritt ein Start-Crash-Risiko.

  Reviewer: ui-state PASS (jede entfernte Ansicht bleibt über die Spur-Tür
  erreichbar — kein Verlust) + code-review PASS (keine offenen Referenzen, Klammer-
  /#if-Balance 47/47, kein -warnings-as-errors-Bruch). Gates: Xcode + CI/CD.
