build: 2026-07-09 v10.79.136 (Umbau Stufe 0: Arbeitsflächen-Umschalter — Arrange · Clips · Studio · Mix)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notesT: Der große Umbau beginnt (dein Beschluss: Arrange wird die Hauptansicht).

  NEU: Unter der Transport-Leiste gibt es jetzt einen Umschalter mit vier
  Arbeitsflächen:
  - ARRANGE — die Song-Timeline (heute noch die Abschnitts-Liste; das echte
    Beat-Grid mit WAV-Wellenform + Snap/stufenlos kommt in den nächsten Stufen)
  - CLIPS — die startbaren Pattern-Zellen (war fertig gebaut, aber unerreichbar)
  - STUDIO — das gewohnte Bio-Instrument (bleibt vorerst die Start-Ansicht)
  - MIX — die Kanal-Pegel (war fertig gebaut, aber unerreichbar) → deine
    "Levels im Schnellzugriff" sind damit einen Tipp entfernt

  Alles bleibt beim Umschalten am Leben (kein Neuladen, sofortiger Wechsel).
  Bio-Kern + Transport bleiben in JEDER Fläche oben stehen.

  → Bitte testen: die vier Flächen durchschalten — startet alles, reagiert
    alles, friert nichts ein (auch während Biofeedback läuft)?

  (Dein Syng-Pitch-Input — Audio-Input/Stimme, Vintage-FX, gute Grund-Pegel,
  tiefes Eingreifen statt Preset-Klickerei — ist in den Bauplan eingearbeitet;
  Audio-Input-Kanal kommt als eigene Stufe. Offen zum Gegentesten: v134
  Video-Capture mehrfach, v135 Knistern seltener?)
