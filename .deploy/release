build: 2026-06-22 v10.46.0 (Swinging color rings + room-in-room reverb core — multidimensional begins)
branch: claude/piano-roll-clip-view-wozlie
notes: First multidimensional slice. (1) VISUALS — the FFT color rings now SWING ("schwingen"): the whole ring system sways like a slow pendulum bound to the body — breath drives the horizontal azimuth, HRV the vertical lift — so the rings move where the spatial object is. Pendulum ~0.10 Hz / bob ~0.13 Hz (far under the 3 Hz flash ceiling), sway amplitude follows loudness (silent → still/centred), Reduce Motion → no sway. Uses the same bio→azimuth/elevation mapping as ADM-OSC so picture and space agree. (2) UNDER THE HOOD — a pure, unit-tested "Raum-im-Raum" convolution-reverb core (SpaceReverb: inner early-reflection room inside an outer diffuse tail, one bio-mappable blend); built + verified, not yet in the live render (wiring it lock-free + partitioned-FFT is the next audio-thread-reviewed, device-verified slice — the founder-approved live sound stays untouched). Plan: scratchpads/PLAN_MULTIDIMENSIONAL_SOUND_2026-06-22.md. iOS compile gate green.
