# Ultracode Design Briefs — pending build tasks (workflow wf_792707e5-72e, 2026-07-12)

Generated by the B11 ultracode workflow (18 agents). These are laws-aware implementation
plans for the remaining build tasks; each names its mandatory reviewer + device-gating.
Build order recommendation: B8 (offbeat, founder-diagnosed) → A5 → B6 → L2/L3 → B7.

## B8-offbeat — deterministic quarter/downbeat bass anchor when heartbeat/tresillo rhythmics are active
- **Summary:** Under a LOCKED trap (and dub) take the pad already re-articulates on a tresillo/dotted grid (BioComposer.heartbeatOnsets, only when busy>=0.5), but the bass does not: for a `sustained` HarmonicProfile, appendBass takes its `guard !sustained` path and emits ONE held root spanning the whole 8-step section (steps 0 and 8). Trap's kick is syncopated (0,6,10), so nothing lands on the quarter grid (4,12) -> the "off-grid" feel and phase-concentration C=0.04-0.07. Fix (pure logic, BioComposer.swift only): factor the heartbeat-active test into one shared pure helper `heartbeatActive(energy:secLen:)`, use it in BOTH heartbeatOnsets and a NEW branch in appendBass. When `sustained && heartbeatActive(energy: busy, secLen: len)`, appendBass emits a chord-ROOT pedal on the quarter grid (gap=4: secStart, secStart+4, ... < secEnd, each len<=gap, all pitch = key.degree(rootDegree, octave)) instead of the single held root; downstream shapeBarDynamics already weights s%4==0 so THE ONE leads. When inactive (calm body, busy<0.5) the original single-held-root code runs UNCHANGED and RNG-identical -> calm default fully preserved. Every extra note re-attacks the bass synth via PianoRollView.tick (mid-bar repeats are deliberate hits, ties only at the wrap), so NO render/PianoRoll/SubBassVoice edit is needed and no audio-thread rule is engaged. Gate the whole new branch behind a tunable `static let quarterAnchorBass = true` (mirrors dynamicDepth) for one-line revert. Deterministic (RNG-free grid, mirrors heartbeatOnsets), in-key, bar-tight, unit-testable; the musical result is a taste change requiring founder ear-verify on device.
- **Reviewer:** dsp-reviewer  |  **device-gated:** True  |  **est. commits:** 2
- **Files:** /home/user/Echoelmusic/Sources/Echoelmusic/Sequencer/BioComposer.swift, /home/user/Echoelmusic/Tests/EchoelmusicTests/BioComposerTests.swift
- **Laws engaged:** Determinism doctrine: SeededRNG only; RNG-free quarter grid mirrors heartbeatOnsets; calm/inactive path kept RNG-identical (bit-identical output); active path is new but fully deterministic; Protected DSP triad READ-ONLY: BioComposer is NOT in the triad; verify BioEventGraph/HilbertSensorMapper/BioSignalDeconvolver untouched; Audio-thread rules: NOT engaged for edits -- BioComposer is a pure off-thread function; verified the downstream bass/sub path (PianoRollView.tick, SubBassVoice) needs no render change (extra .bass notes re-attack via existing scheduler; ties only at wrap); Placebo rule: the new control moves REAL audio (bass synth re-attacks on the quarter grid) -- no fake articulation; Minimal-change/reversible: single-file logic change behind a tunable `quarterAnchorBass` flag for one-line revert
- **Steps:**
  1. Read Tests/EchoelmusicTests/BioComposerTests.swift to match its XCTest style and the existing heartbeatOnsets/appendBass-through-compose test patterns.
  2. RED: add failing tests in BioComposerTests. (a) `heartbeatActive` returns false for secLen<4 or energy<0.5, true otherwise. (b) appendBass-via-compose: trap-style sustained profile + high busy -> the `.bass`-role notes include startSteps {0,4,8,12} (quarter grid), all == key.degree(rootDegree) pitch class, all inside [0,16). (c) calm regression: same profile + busy<0.5 -> exactly one held `.bass` note per section (startSteps {0,8}, len 8) i.e. bit-identical to today. (d) determinism: same seed/input twice -> identical note arrays.
  3. In BioComposer.swift add pure helper `static func heartbeatActive(energy: Float, secLen: Int) -> Bool { secLen >= 4 && energy >= 0.5 }` and refactor heartbeatOnsets' guard (line ~963) to call it (pure refactor, behaviour unchanged); keep an existing-output test green to prove no regression.
  4. Add tunable `static let quarterAnchorBass = true` near dynamicDepth (line ~260) and a NEW branch at the top of appendBass' sustained path: if `quarterAnchorBass && sustained && heartbeatActive(energy: busy, secLen: len)`, loop s from secStart by gap=4 to <secEnd emitting Note(id: nextUUID(&rng), pitch: key.degree(rootDegree, octave), startStep: s, lengthSteps: max(1,min(4, secEnd-s)), velocity: hVel(velocity,&rng), role: .bass) and RETURN; otherwise fall through to the existing single-held-root code UNCHANGED (calm/inactive path stays RNG-identical).
  5. swift test --filter BioComposer -> GREEN. Add an objective phase-concentration gate test: BioComposer.compose(trap, studioLocked, lockedTempo 132, high heartRate/low HRV so busy>=0.5) -> collect `.bass`-role startSteps, assert the quarter set {0,4,8,12} is covered; and the same at rest (low HR, high coherence) -> only {0,8} held roots. This locks 'C rises' objectively.
  6. Confirm the protected triad (BioEventGraph/HilbertSensorMapper/BioSignalDeconvolver) is untouched and that SubBassVoice/PianoRollView render paths are NOT edited (grep diff).
  7. dsp-reviewer review of the compositional-logic change (mandatory). Convene the-council briefly: the fix reconciles the two standing founder asks -- 'am Herzschlag orientierte Rhythmen/Synkopen' (pad stays tresillo) AND 'die Eins zu erkennen' (bass anchors the quarter) -- flag as the intended synthesis, note the prior 'nicht statisch im Takt' rejection is respected because the anchor only fires when the body is genuinely active.
  8. Commit `fix: bass anchors the quarter/downbeat when heartbeat rhythmics are active (B8-offbeat)`; ship to TestFlight; founder A/B ear-verify on device (trap 132 lock, active body) -- device-gated, subjective.
- **Tests:**
  - heartbeatActive(energy:secLen:) boundary: false for secLen<4 and for energy<0.5; true otherwise
  - heartbeatOnsets output unchanged after refactor to use heartbeatActive (regression)
  - appendBass active path: sustained profile + busy>=0.5 -> .bass startSteps cover {0,4,8,12}, all root pitch class, all in [0,16)
  - appendBass calm path bit-identical: sustained + busy<0.5 -> exactly one held root per section {0,8} len 8 (RNG-identical to pre-change)
  - determinism: same seed+Input twice -> identical notes (bass + downstream)
  - objective phase-concentration gate: compose(trap, studioLocked 132, high-arousal bio) covers the quarter grid; at-rest bio does not
- **Risks:**
  - RNG-shift on the ACTIVE path changes downstream pad/lead notes for a given seed (calm path preserved); could alter a high-energy take the founder liked -> mitigated by the quarterAnchorBass tunable for instant revert and founder A/B verify
  - Over-articulation: a full quarter-grid bass under trap can read as four-on-floor and dilute trap's syncopated identity -> fallback: scope the anchor to downbeat+beat3 (steps 0,8) only, or to trap style only
  - Felt SubBassVoice will NOT re-pulse at a fixed root (follows lowest active pitch, re-gates on pitch change only) -- only the audible bass synth articulates; if the founder wants the FELT sub to pulse on the one, that is a separate render-path change (PianoRollView/SubBassVoice) requiring audio-thread-reviewer, out of scope here
  - Applying to ALL sustained profiles also changes dub ('schon sehr gut') when active -> low risk (root pedal under dub's 4-on-floor kick is idiomatic) but note the scope-to-trap option for a founder who wants dub untouched
  - Subjective musical result cannot be CI-verified -> requires device ear-verify; code + phase-concentration test are the objective gate

## B6/#19 — BLE-MIDI coupling dialog: host CABTMIDICentralViewController as content of the existing Routing (Patchbay) panel via slot-reuse, not a new sheet.
> **STATUS: BUILT 2026-07-13 (device-verify pending).** Panel-home = Routing (PatchbayView),
> per the brief's recommendation. New `Studio/BluetoothMIDIPairingView.swift` (VC bridge) +
> `bluetoothMIDISection` in PatchbayView (NavigationLink push, `!embedded`, `#if os(iOS) &&
> canImport(CoreAudioKit)`). Verified zero-touch: no new sheet, no EchoelStudioView edit, no
> MIDIInput edit (`.msgSetupChanged`→`connectAllSources` auto-connects), no Info.plist edit
> (Bluetooth strings present, lines 67/69), no project.yml edit (main target globs it, AUv3
> globs DSP/ only → no leak). ui-state-reviewer run. Device-gated: pair a real BLE-MIDI
> controller and confirm notes sound (simulator has no BLE-MIDI central).
- **Summary:** Add a "Bluetooth MIDI" section to PatchbayView (the existing "Routing" door, reached through the already-live showRouting sheet slot) that pushes Apple's CABTMIDICentralViewController via a SwiftUI NavigationLink inside PatchbayView's existing NavigationStack. A NavigationLink push is NOT a .sheet/.fullScreenCover, so the ~18-modal metadata chain on EchoelStudioView.body is untouched (slot-reuse law satisfied — we reuse showRouting, add zero new modals). CABTMIDICentralViewController is wrapped in a new UIViewControllerRepresentable (BluetoothMIDIPairingView) under strict platform guards. No render/DSP/bio code is touched: this is pure UI + CoreMIDI setup. Pairing a controller brings a new CoreMIDI source online, which the existing MIDIInput.handleMIDINotification(.msgSetupChanged) -> connectAllSources() auto-connects — so real MIDI notes reach the synth (placebo rule satisfied, zero MIDIInput changes). Info.plist already carries NSBluetoothAlwaysUsageDescription + NSBluetoothPeripheralUsageDescription (lines 67-70) — no plist edit. XcodeGen globs Sources/Echoelmusic (project.yml:65) and SwiftPM globs by directory, so the new Studio file compiles with no project.yml change; the AUv3 target only compiles DSP/ so it never sees the CoreAudioKit host UI. PATH DECISION FOR FOUNDER/COUNCIL: primary home = PatchbayView ("Routing") because it is the signal-routing door, already hosts a "Herzgurt (BLE)" bio-source sibling and MIDI edges; alternate home = AudioInputPickerView ("Audio input") if the founder reads "Input" literally. Same pattern applies to either — pick one, do not add to both.
- **Reviewer:** ui-state-reviewer  |  **device-gated:** True  |  **est. commits:** 1
- **Files:** /home/user/Echoelmusic/Sources/Echoelmusic/Studio/BluetoothMIDIPairingView.swift (NEW — UIViewControllerRepresentable wrapping CABTMIDICentralViewController), /home/user/Echoelmusic/Sources/Echoelmusic/Studio/PatchbayView.swift (EDIT — add a guarded 'Bluetooth MIDI' section with a NavigationLink to the bridge)
- **Laws engaged:** SwiftUI render-safety (a) modal-chain / slot-reuse: reuse the existing showRouting sheet slot; the BLE UI is a NavigationLink PUSH (not a .sheet/.fullScreenCover), so EchoelStudioView.body's ~18-modal metadata chain does not grow — no black-screen SIGSEGV risk.; SwiftUI render-safety (b) no 10 Hz @Observable read in a Picker-host ancestor: none introduced — PatchbayView reads only router.graph (event-rate, not the 10 Hz bio/rPPG/playhead snapshots); the new section adds no live-bio read.; SwiftUI render-safety (c) never two modals true at once: satisfied — no new modal boolean is added; the push lives inside the single showRouting sheet.; Placebo rule: the pairing control moves REAL audio — a paired controller becomes a CoreMIDI source that MIDIInput auto-connects, driving synth notes; not a fake knob.; Brand/health copy: section label/subtitle stay factual ('pair a wireless MIDI controller'), no medical/therapy/Solfeggio/healing wording.; Platform guards: new file + section guarded `#if canImport(UIKit) && canImport(CoreAudioKit) && os(iOS)` (CABTMIDICentralViewController is iOS-only CoreAudioKit).; DO-NOT (Info.plist / new targets / new top-level dirs): none triggered — Bluetooth usage strings already present, file lands in existing Studio/ dir, no new target.
- **Steps:**
  1. STEP 0 (Council-light gate): this is >1 file + user-facing copy + a new representable, but small and fully reversible. Convene the-council for ONE pass on the panel-home choice (Routing vs Input) and confirm 'proceed'. Reference precedent: AUv3PluginUIView.swift already bridges a UIViewController into SwiftUI the same way.
  2. STEP 1 (new file — the bridge): create Sources/Echoelmusic/Studio/BluetoothMIDIPairingView.swift. Guard the WHOLE file with `#if canImport(UIKit) && canImport(CoreAudioKit) && os(iOS)` (CABTMIDICentralViewController is iOS-only CoreAudioKit; on macOS it does not exist). Define `struct BluetoothMIDIPairingView: UIViewControllerRepresentable` whose `makeUIViewController` returns `CABTMIDICentralViewController()` and `updateUIViewController` is a no-op — mirror the shape of AUv3PluginUIView.swift. The VC is a UITableView of nearby BLE-MIDI peripherals and manages its own pairing lifecycle; do not add child-VC embedding logic. No @MainActor hop tricks needed (representable methods are already main-actor). Add a doc header in the AUv3PluginUIView.swift house style explaining it hosts Apple's built-in BLE-MIDI central and that paired devices auto-connect through MIDIInput's setup-changed path.
  3. STEP 2 (edit PatchbayView.swift — the section): inside `content` (the ScrollView VStack, e.g. after `lichtSection`), add a new `bluetoothMIDISection`. Guard it `#if os(iOS) && canImport(CoreAudioKit)`. It is a titled card (same visual grammar as lichtSection: EchoelTheme.fill background, 1px EchoelTheme.border, radius EchoelTheme.radius, font EchoelTheme.font) containing a single `NavigationLink { BluetoothMIDIPairingView().navigationTitle("Bluetooth MIDI") } label: { ... }`. The push lands inside PatchbayView's existing NavigationStack (present whenever embedded==false, i.e. the showRouting sheet path — the only live path today). Give the label an icon (e.g. 'pianokeys' or 'wave.3.right'), title 'Bluetooth MIDI', and a one-line honest subtitle: 'Pair a wireless MIDI keyboard or controller — it plays the synth directly.' Add .accessibilityLabel/.accessibilityHint.
  4. STEP 3 (embedded-path safety): PatchbayView.content is shared between the sheet path (has NavigationStack) and the dormant embedded==true workspace path (no NavigationStack). To avoid a dead NavigationLink if embedded is ever revived, either (a) only render bluetoothMIDISection when `!embedded`, or (b) leave it — a NavigationLink with no enclosing stack is inert, not a crash. Prefer (a) for honesty (no dead control). Confirm the showRouting sheet path still wraps in NavigationStack (PatchbayView.swift:29) so the push works.
  5. STEP 4 (verify zero-touch on the hot paths): confirm NO edit to EchoelStudioView.swift (showRouting slot already exists at :621, :2008-2012) — this is the slot-reuse proof. Confirm NO edit to MIDIInput.swift (the .msgSetupChanged auto-connect at :201-202 already handles the newly-paired source). Confirm NO Info.plist edit (Bluetooth keys present). Confirm NO new .sheet/.fullScreenCover anywhere.
  6. STEP 5 (brand + placebo pass): re-read the section copy — no medical/therapy/wellness/Solfeggio wording (trivially clean here). Confirm the control moves real audio: paired controller -> CoreMIDI source -> MIDIInput.connectAllSources -> onNoteOn -> synth. Not a fake knob.
  7. STEP 6 (compile gate — no local Swift build in this env): rely on CI. Push branch; ensure xcode-compile-check.yml (XcodeGen + xcodebuild, the only gate that exercises CoreAudioKit/UIKit iOS-only code) and ci.yml go green. Watch specifically for: CoreAudioKit import resolving only under the os(iOS) guard, and the new file NOT leaking into the AUv3 target.
  8. STEP 7 (ship): conventional commit `feat(midi): BLE-MIDI pairing dialog hosted in Routing panel via slot-reuse (showRouting, no new sheet)`. Update scratchpads/PLAN_DMMW_PROFI_LEVEL_2026-07-12.md B6 checkbox and SESSION_LOG.md. Mark device-gated verification pending.
  9. STEP 8 (device-gated verification — cannot be done in this env): on a real iPhone, open Master -> Routing -> Bluetooth MIDI, grant the Bluetooth prompt, pair a BLE-MIDI controller, and confirm (a) the device appears/connects in the CABTMIDI table and (b) pressing keys sounds the synth. Simulator has no BLE-MIDI/CoreAudioKit central — do not claim verified from sim.
- **Tests:**
  - No meaningful unit test: CABTMIDICentralViewController is system UI over live Bluetooth hardware and the representable holds ZERO branching logic — there is nothing pure to assert. Test-first (TDD) does not apply to this VC bridge; do not fabricate a snapshot/mock test for coverage theatre.
  - The real gates are the CI compile check (xcode-compile-check.yml — proves the CoreAudioKit/UIKit guards resolve on iOS and the file stays out of the AUv3 target) plus ci.yml.
  - Device-gated acceptance (manual, on real hardware): pair a BLE-MIDI controller via the new section and confirm notes drive the synth — this is the only trustworthy verification and it cannot run in this sandbox or the simulator.
- **Risks:**
  - Panel-home ambiguity: task says 'Input/Routing panel'. Brief picks Routing (PatchbayView); if the founder meant the literal 'Audio input' door (AudioInputPickerView), swap the section into that view (same NavigationLink pattern). Resolve in the Step-0 Council pass; do not wire both.
  - CABTMIDICentralViewController requires an enclosing UINavigationController to render its nav bar — SwiftUI's NavigationStack provides this on the showRouting (embedded==false) path only. If PatchbayView's dormant embedded==true workspace path is ever revived, the link is inert (Step 3 gates it behind !embedded).
  - Device-gated: cannot be verified in this environment or the simulator (no BLE-MIDI/CoreAudioKit central). Ship as NEEDS-FOUNDER-VERIFY; the founder must pair a real controller.
  - First on-open, iOS shows the Bluetooth permission prompt; if denied, CABTMIDICentralViewController shows an empty/limited list — acceptable system behaviour, but the section subtitle should not overpromise ('pair a controller', not 'connect instantly').
  - AUv3-target leakage: the new Studio file must NOT compile into EchoelmusicAUv3 (which only globs DSP/ + a few named Core files). Confirmed by current project.yml, but re-check after any target-source change.

## B7-spatial-s4
- **Summary:** S4 delivers a PURE, deterministic, unit-tested Feedback-Delay-Network room reverb core (EchoelFDNReverb) whose parameters are derived from RoomModel (from SpatialScene). It reuses the existing zero-alloc EchoelDelayLine ring buffers, mixes via an orthogonal (Hadamard) feedback matrix for provable stability, and sets per-line feedback gains from RoomModel.decayTime via Jot's RT60 formula. The core is NOT added to the live render graph in this slice (flag-neutral, Release bit-identical); the future insert/wiring is what the FeatureFlags.spatialEngine gate (default OFF) protects, and that wiring is a separate device-gated slice (S5). Test-first, Linux/CI-provable, no device needed. Do NOT create a new flag (reuse .spatialEngine), do NOT modify SpatialScene/RoomModel, FeatureFlags, or the protected Bio triad.
- **Reviewer:** audio-thread-reviewer (render-path zero-alloc/lock-free proof of processInPlace) — plus dsp-reviewer mandatory for the FDN maths (per SPATIAL_EXPANSION_AUDIT §2.3)  |  **device-gated:** None  |  **est. commits:** None
- **Files:** /home/user/Echoelmusic/Sources/Echoelmusic/DSP/EchoelFDNReverb.swift (NEW — the FDN core, next to EchoelSpaceReverb/EchoelDelayLine/EchoelReverb/BinauralPanner; no new top-level dir), /home/user/Echoelmusic/Tests/EchoelmusicTests/EchoelFDNReverbTests.swift (NEW — write FIRST, RED before GREEN), /home/user/Echoelmusic/Sources/Echoelmusic/DSP/EchoelDelayLine.swift (READ-ONLY reuse — power-of-two ring buffer, read(delaySamples:)/readAllpass, zero-alloc), /home/user/Echoelmusic/Sources/Echoelmusic/Core/SpatialScene.swift (READ-ONLY — RoomModel is the input struct; do NOT modify), /home/user/Echoelmusic/Sources/Echoelmusic/Core/FeatureFlags.swift (READ-ONLY — reuse existing .spatialEngine key at the FUTURE wiring site; add no new key in S4), /home/user/Echoelmusic/docs/SPATIAL_EXPANSION_AUDIT.md (reference — S4 is item 5 in the confirmed cycle order; §2.2 placement law, §2.3 reviewer law)
- **Steps:**
  1. STEP 0 — Council/gate check: this is a new audio-thread DSP block (>1 file, hard-to-reverse sound work) → per audit §2.3 dsp-reviewer + audio-thread-reviewer are mandatory before any wiring; S4 stays a pure core so it convenes lightly. Confirm CI green on branch first (Ralph Wiggum: no red build = start allowed).
  2. STEP 1 (TEST-FIRST, RED) — Write Tests/EchoelmusicTests/EchoelFDNReverbTests.swift guarded by #if canImport(Accelerate), @testable import Echoelmusic, mirroring EchoelSpaceReverbTests structure. Cover: (a) Params(from:RoomModel,sampleRate:) is deterministic — same RoomModel+SR+seed → identical delay lengths/gains; (b) RT60/energy-decay: feed a unit impulse, measure the sample index where cumulative-tail energy drops 60 dB, assert it matches RoomModel.decayTime within tolerance for two contrasting rooms (small dry vs large long); (c) STABILITY: over ~10×RT60 samples of sustained input at max decayTime (30 s) output stays finite and bounded (no NaN/Inf, |out| under a ceiling) — proves the orthogonal matrix + per-line gain<1 is lossless-bounded; (d) mix=0 → pure dry passthrough; (e) diffusion parameter monotonically increases early-echo density/smoothness (compare an early-window autocorrelation or zero-crossing count at diffusion 0 vs 1); (f) process(_:) (allocating) == processInPlace(_:) bit-identical for two identically-seeded instances. Run swift test --filter EchoelFDNReverb → confirm RED.
  3. STEP 2 (GREEN) — Implement Sources/Echoelmusic/DSP/EchoelFDNReverb.swift as `public final class EchoelFDNReverb: @unchecked Sendable` inside #if canImport(Accelerate). Plain value core (NOT @MainActor) so no main-actor property is read from the render loop. N=8 EchoelDelayLine instances allocated ONLY in init. Pre-allocate ALL scratch (input-into-lines vector, matrix-out vector, wet buffer) in init sized to maxBlock — the render loop indexes only.
  4. STEP 3 — RoomModel → FDN mapping as a pure nested struct `EchoelFDNReverb.Params`: delay lengths = 8 mutually-prime sample counts scaled from RoomModel.width/depth/height (bigger room → longer, sparser modes), chosen by a DETERMINISTIC prime search seeded from a default seed (e.g. 0xFD2 like EchoelSpaceReverb's constant) — NO Hasher, NO wall clock, NO RNG-per-call. Per-line feedback gain g_i = pow(10, -3 * L_i / (decayTime * sampleRate)) (Jot) so measured RT60 == RoomModel.decayTime. diffusion → input allpass-diffuser depth/coefficient (reuse EchoelDelayLine.readAllpass) and/or damping in the feedback path. Clamp everything (RoomModel already clamps its own fields; re-guard finiteness at the DSP edge).
  5. STEP 4 — Feedback matrix = normalized 8×8 Hadamard (constant, unrolled/precomputed; orthogonal → energy-preserving → unconditional stability given gains<1). Hot loop: read each line at its length, apply the Hadamard mix + per-line gain + optional HF damping (one-pole), write input+feedback back into each line, sum outputs to wet, blend dry/wet by mix. Zero allocation, no locks/Task/os_log/ObjC/file-I/O in process — only Float array index access, vDSP/scalar arithmetic, memcpy-style ops.
  6. STEP 5 — Public control surface mirroring EchoelSpaceReverb: `public private(set) var` for mix + current Params; control-rate `setRoom(_ room: RoomModel, sampleRate:)` and `setMix(_:)` that recompute gains/lengths and clamp (called from control thread, never the render block). Provide `process(_:) -> [Float]` (allocating, offline/test) AND `processInPlace(_ buffer: inout [Float])` (real-time-safe, pre-alloc scratch) that are bit-identical — exactly the EchoelSpaceReverb dual-path contract. reset() clears all lines.
  7. STEP 6 — Run swift test --filter EchoelFDNReverb → GREEN. Refactor while green. Confirm no force-unwrap, all divisions guarded (decayTime, sampleRate), all array access bounded.
  8. STEP 7 — Do NOT wire into AudioEngine/master graph in S4. Add a one-line doc comment marking the future insert point + that it will be gated by `FeatureFlags.spatialEngine` (default OFF) at the wiring call site (S5), so Release stays bit-identical now. Verify swift build with -warnings-as-errors.
  9. STEP 8 (MANDATORY REVIEW) — Invoke audio-thread-reviewer on processInPlace (zero-alloc/lock-free/no-ObjC/no-Task proof) AND dsp-reviewer on the FDN maths (Hadamard orthogonality, Jot gain, stability bound, determinism). Both must pass before merge. No SwiftUI touched → ui-state-reviewer N/A; no Bio/flash touched → bio-safety-reviewer N/A.
  10. STEP 9 (SHIP) — Commit `feat(spatial): FDN room reverb core driven by RoomModel — pure, deterministic, flag-neutral (S4)`. Update scratchpads/SESSION_LOG.md and tick task #16's S4. This slice is CI-provable and needs no TestFlight/device; the device gate belongs to the S5 wiring slice.
- **Tests:**
  - EchoelFDNReverbTests.testParamsDeterministicBySeedAndRoom — same RoomModel+SR+seed → identical lengths/gains; different room → different
  - EchoelFDNReverbTests.testEnergyDecayMatchesRoomDecayTime — measured 60 dB decay index ≈ RoomModel.decayTime (small vs large room)
  - EchoelFDNReverbTests.testStabilityBoundedOverLongInput — max decayTime, sustained input over ~10×RT60: output finite + bounded, no NaN/Inf (orthogonal-matrix lossless proof)
  - EchoelFDNReverbTests.testMixZeroIsPureDry — mix=0 → output == input
  - EchoelFDNReverbTests.testDiffusionIncreasesEchoDensity — diffusion 1 smoother/denser early field than diffusion 0
  - EchoelFDNReverbTests.testProcessInPlaceMatchesProcess — allocating vs real-time-safe path bit-identical (accuracy 1e-5)
  - EchoelFDNReverbTests.testAllOutputsFiniteForExtremeRoom — clamps + guards hold for degenerate RoomModel edges

## A5-bio-per-note
- **Summary:** Extend the already-shipped A4 bio-operator model (NoteOperators.bioBentChance) with A5 per-note EXPRESSION modulation: coherence + breath deterministically shape a note's velocity (and, model-only for now, micro-timing + brightness) each loop pass, seeded and UUID-folded so any take replays bit-identically. Everything is pure value-type math on NoteOperators; the only wiring is a scalar velocity multiply at the existing note-on in PianoRollModel.trigger() — no new views, no new sheet, no audio-thread work, no Note.swift change (operators already ride on Note). Timing and filter land as tested model outputs but are NOT wired: the trigger clock is step-quantized (sub-tick timing needs the W2 clock) and the synth voices have no per-note brightness input (needs a voice-API change). Default depth 0 = exact identity, so legacy clips and plain notes are untouched and byte-identical.
- **Reviewer:** audio-thread-reviewer  |  **device-gated:** True  |  **est. commits:** 3
- **Files:** /home/user/Echoelmusic/Sources/Echoelmusic/Sequencer/NoteOperators.swift, /home/user/Echoelmusic/Sources/Echoelmusic/Studio/PianoRollView.swift, /home/user/Echoelmusic/Tests/EchoelmusicTests/NoteOperatorsTests.swift
- **Laws engaged:** Determinism doctrine — SplitMix64 SeededRNG + UUID-fold (never Hasher) + decodeIfPresent migration; distinct salt de-correlates expression from the chance roll; conditional encode keeps chance-only operator JSON byte-identical.; Audio thread — velocity multiply is applied at note-on on the main-actor control plane (feeds the synth voice, not the render block); must confirm scalar-only, clamped (min(1, ...)), no allocation, and no @MainActor property read from any nonisolated render path.; SwiftUI render-safety — no new .sheet/.fullScreenCover (chain stays put), and the new coherence/breath reads live in trigger() (a control-plane tick), NOT in EchoelStudioView/WorkspaceView/any body or Picker ancestor; no expression UI added in this slice.; Protected DSP triad READ-ONLY — BioEventGraph / HilbertSensorMapper / BioSignalDeconvolver untouched.; Placebo rule — velocityDepth moves real audio (note-on amplitude); depth 0 is an honest off (identity), no fake knob.; Brand/health — no user-facing copy added; any future depth-editing UI must avoid medical/therapy claims and reuse an existing sheet slot (out of scope here).
- **Steps:**
  1. Model shape (NoteOperators.swift): add three expression-depth fields, all default 0 = transparent — velocityDepth: Double (-1...1), timingDepth: Double (-1...1), filterDepth: Double (0...1). Add clamped init params after the existing occurrence params. Add a NoteExpression value type (Equatable, Sendable) with velocityScale: Float = 1, timingOffsetTicks: Int = 0, brightness: Float? = nil (nil = do not touch filter).
  2. Pure evaluator (NoteOperators.swift): add func expression(for note: Note, loopIndex: Int, seed: UInt64, coherence: Double?, breath: Double?) -> NoteExpression. Mirror bioBentChance semantics exactly: coherence 0.5 = NEUTRAL, nil bio OR depth 0 => identity (scale 1 / offset 0 / brightness nil). Bio provides the smooth bend (coherence delta = clamp(c)-0.5; breath shaped by sin(breath*pi) like the existing trigger breathSwell) and a SMALL seeded per-(seed, fold(note.id), loopIndex) jitter via SeededRNG gives organic variation — both fully reproducible. Use a DISTINCT salt constant (not 0xD1B54A32D192ED03, which the chance roll uses) so expression is de-correlated from the play/silence dice. Clamp velocityScale, bound timingOffsetTicks to a small tick window, clamp brightness to 0...1.
  3. Transparency + Codable (NoteOperators.swift): extend isDefault to also require velocityDepth==0 && timingDepth==0 && filterDepth==0. Add the three keys to CodingKeys and decode via decodeIfPresent ?? 0. To preserve the determinism doctrine's byte-identity, give NoteOperators a CUSTOM encode(to:) that encodes the original five fields UNCHANGED (as the synthesized encoder did) and encodes each new expression field ONLY when non-zero — so a chance-only operator's JSON stays byte-identical to today, and old builds ignore the extra keys.
  4. Gate/expression helper (PianoRollView.swift): add nonisolated static func noteExpression(_ note:, loopPass:, seed:, coherence:, breath:) -> NoteExpression next to the existing operatorAllows, returning .init() (identity) when operators is nil/isDefault. Pure + nonisolated so it is unit-tested without a transport, exactly like operatorAllows.
  5. Wire VELOCITY only (PianoRollView.swift trigger()): operatorCoherence is already fetched at line 431; also read breath = bus?.usableBio().map { Double($0.breathPhase) } in the same control-plane tick (NOT a SwiftUI body). In the `for note in starting` loop, compute exp = Self.noteExpression(note, loopPass: operatorLoopPass, seed: Self.operatorSeed, coherence: operatorCoherence, breath: breath) and fold exp.velocityScale into the existing v = min(1, note.velocity * laneGain * exp.velocityScale). Keep the clamp. Leave timing + brightness UNWIRED with an explicit comment (timing => W2 sub-tick clock; brightness => voice noteOn has no per-note filter input yet).
  6. Run swift test --filter NoteOperators and the PianoRoll gate tests; confirm the new determinism, neutrality, de-correlation, clamp, and byte-identity tests are GREEN and that every pre-existing test (default transparency, bioBentChance, legacy JSON, operatorAllows gate) still passes unchanged.
  7. Commit: feat: A5 bio-per-note expression (velocity wired; timing/filter model-only). One change, reversible — depth 0 = identity, no schema break.
- **Tests:**
  - testExpression_depthZero_isIdentity — velocityScale 1, offset 0, brightness nil regardless of coherence/breath.
  - testExpression_nilBio_isIdentity — nil coherence AND nil breath => identity even with non-zero depths (mirrors bioBentChance nil-guard).
  - testExpression_neutralAtMidCoherence — coherence 0.5 with breath at a cycle end yields the un-bent velocityScale center (parity with testBioBentChance_neutralAtMidCoherence).
  - testExpression_highCoherenceLiftsVelocity_lowThins — velocityDepth>0: coherence 0.9 => scale>1, coherence 0.1 => scale<1; symmetric.
  - testExpression_isDeterministic_sameInputsSameOutput — fixed UUID + fixed loopIndex+seed => identical NoteExpression across repeated calls (process-stable, no Hasher).
  - testExpression_decorrelatedFromChanceRoll — distinct salt: expression jitter is not locked to the operatorAllows play/silence outcome across many loops.
  - testExpression_clampsAndGuardsBadInput — coherence 7 and .nan, breath out-of-range: velocityScale/brightness stay in range, offset bounded, no crash.
  - testExpression_breathShaping_humpNotSaw — breath phase 0 and 1 give the cycle-end value, phase 0.5 the peak (sin hump), matching the trigger's breathSwell convention.
  - testExpression_isDefaultIncludesExpressionDepths — NoteOperators with only velocityDepth set is NOT isDefault; all-zero stays isDefault.
  - testOperatorsJSON_chanceOnly_byteIdenticalAfterExpressionFields — encoding a chance-only operator produces JSON without any *Depth key (byte-identity preserved).
  - testOperators_expressionRoundTrip — operator with non-zero velocity/timing/filter depths round-trips equal; legacy operator JSON (no depth keys) decodes with depths 0.
  - testGate_noteExpression_plainNote_isIdentity — PianoRollModel.noteExpression on a nil/default-operator note returns identity for any loopPass.
  - testGate_noteExpression_scalesVelocityWithCoherence — with velocityDepth set, high coherence returns scale>1, low <1, nil coherence returns 1.
- **Risks:**
  - Second mandatory reviewer: bio-safety-reviewer (coherence + breath drive per-note behavior) — must confirm neutral-at-0.5, nil-bio identity, and clamping so a noisy/absent body can never spike or zero velocities unexpectedly.
  - Timing + filter are tested MODEL OUTPUTS but deliberately UNWIRED: timing needs the W2 sample-accurate sub-tick clock (trigger() is step-quantized), and per-note brightness needs a synth-voice noteOn API that carries filter — do not fake either; flag as two follow-up tasks so the plan is honest about what actually moves audio today (velocity only).
  - Byte-identity regression risk: NoteOperators currently uses the SYNTHESIZED encoder — adding stored fields WITHOUT a custom encode(to:) would change every operator-bearing clip's JSON. The custom conditional encode (step 3) is required, and the byte-identity test guards it.
  - Device-gated verification: unit tests prove determinism with injected coherence/breath, but the FELT 'pattern comes alive as the player settles' effect can only be confirmed on device with live rPPG/BLE bio — CI cannot validate the perceptual result.
  - Salt-collision risk: if the expression jitter reuses the chance roll's multiplier, velocity would track the play/silence dice (a whole gated chord would also swell/thin as one unit) — the distinct salt + the de-correlation test exist specifically to prevent this.

## L2L3-lighting — L2 fixture groups + L3 MA3-style bio-phaser for EchoelLux
- **Summary:** Add two PURE, socket-free, deterministic value-type models to Sources/Echoelmusic/Sync/, each shipped test-first as its own Ralph-Wiggum cycle, mirroring the existing BioSpaceMap/FlashGuard pattern (no Network import, no RNG, no wall-clock, dt passed in). L2 = LightFixtureGroup: composes a single DMX universe buffer from N fixtures each at its own start channel, all scaled by one grandMaster and cut together by blackout (extends the L1 masteredDimmer law from one fixture to a group). L3 = BioPhaser: an MA3-style phaser where each fixture in a group gets a fanned phase OFFSET across the group and the BASE phase is driven by the body — breath phase read directly from frame.breathPhase, or heart phase accumulated from heartRateBPM/60·dt (BioSampleFrame has no heart-phase field, so it must be integrated statefully in step, wrapped [0,1)). The phaser's per-fixture output feeds the dimmer, so it is the flash-safety hot path: the effective oscillation must be clamped ≤3 Hz via FlashGuard.safeFrequency AND every per-fixture per-tick luminance step slew-limited via FlashGuard.limitedLuminance, so even a 180 bpm heart rate can never strobe. Both models stay UNWIRED initially (like BioSpaceMap: "nothing consumes this yet") so Release is bit-identical; the optional sender/UI integration is a separate reversible follow-on cycle. Reference reads for the phase pattern: BioSpaceMap.step/slew (Core/BioSpaceMap.swift), EntrainmentEngine.phase (Bio/EntrainmentEngine.swift), and the sender kernels/masteredDimmer in ArtNetSender.swift + SACNSender.swift.
- **Reviewer:** bio-safety-reviewer  |  **device-gated:** True  |  **est. commits:** 2
- **Files:** Sources/Echoelmusic/Sync/LightFixtureGroup.swift (NEW — L2 pure model: LightFixture{startChannel,dimmerUnit,colour}, FixtureGroup{fixtures,grandMaster,blackout}, static compose(...) -> [UInt8] up to 512, Codable w/ decodeIfPresent), Sources/Echoelmusic/Sync/BioPhaser.swift (NEW — L3 pure model: BioPhaserSource{breath,heart} enum, BioPhaser struct with spread/count/per-fixture slew state, mutating step(frame:count:dt:)->[Float], static phaseOffsets(count:spread:) + value(bioPhase:offset:)), Tests/EchoelmusicTests/LightFixtureGroupTests.swift (NEW — no Network guard, mirrors BioSpaceMapTests style), Tests/EchoelmusicTests/BioPhaserTests.swift (NEW — flash-safety + determinism + wrap assertions), Sources/Echoelmusic/Sync/ArtNetSender.swift (EDIT — DEFERRED/optional cycle: opt-in group+phaser path in sendIfFresh; default = current single-fixture behaviour, reversible), Sources/Echoelmusic/Sync/SACNSender.swift (EDIT — DEFERRED/optional cycle: same opt-in path, shares L2/L3 model), Sources/Echoelmusic/Studio/PatchbayView.swift (EDIT — DEFERRED/optional: Licht section group/phaser controls via EchoelValueField; live phaser readout, if any, must be a leaf View)
- **Laws engaged:** Flash-safety ≤3 Hz / WCAG 2.3.1 (CENTRAL to L3: phaser drives dimmer; clamp advance rate via FlashGuard.safeFrequency + per-tick slew via FlashGuard.limitedLuminance; L2 reuses the flash-safe masteredDimmer + existing sender slew); Determinism doctrine (no RNG, no wall-clock — dt passed in; UUID-fold not Hasher for any stable fixture id; Codable migration via decodeIfPresent so legacy light-config JSON stays byte-identical; Release bit-identical while models stay un-wired); Protected DSP triad READ-ONLY (BioEventGraph/HilbertSensorMapper/BioSignalDeconvolver not touched — confirmed not referenced); Placebo rule (the future group/phaser controls must move real DMX output, not fake knobs); SwiftUI render-safety (b) — only if the deferred UI adds a live phaser/bio readout: confine to a leaf View, never read in PatchbayView.body or a .menu ancestor; and do not grow the EchoelStudioView .sheet chain; Brand/health — light-model naming stays technical (no wellness/esoteric/Solfeggio/healing copy)
- **Steps:**
  1. READ FIRST (ground the model): Core/BioSpaceMap.swift (the pure-struct+slew+step template, wrap-aware slewAngleWrapped, sanitized/NaN-proofing), Studio/FlashGuard.swift (limitedLuminance/safeFrequency/maxFlashHz=3), Sync/ArtNetSender.swift (masteredDimmer/applyDimmer/dmxChannels — the L1 law and byte/word encoding), Sync/SACNSender.swift (shared kernel reuse), Bio/EntrainmentEngine.swift phase() (frac-wrap phase helper). Confirm protected triad (BioEventGraph/HilbertSensorMapper/BioSignalDeconvolver) is NOT referenced — it must stay untouched.
  2. CYCLE 1 (L2, test-first): write LightFixtureGroupTests.swift RED — compose() places each fixture's channels at its startChannel; total buffer clamped to 512; grandMaster scales every fixture's dimmer (reuse/extend ArtNetSender.masteredDimmer semantics); blackout zeroes ALL dimmers while leaving colour bytes intact; empty group → safe zero buffer; out-of-range/overlapping start channels clamp not crash; Codable round-trip + legacy-JSON-without-new-field decodes to default (decodeIfPresent, byte-identical). Keep the model free of ArtNetSender.DMXResolution (operate on unit-Float dimmer + raw channel counts so no #if canImport(Network) is needed); let the sender do resolution encoding later. Implement minimal LightFixtureGroup.swift to GREEN. Commit feat: L2 EchoelLux fixture groups (pure model).
  3. CYCLE 2 (L3, test-first): write BioPhaserTests.swift RED — phaseOffsets(count:spread:) fans evenly (edge cases count=0 and count=1); value(bioPhase:offset:) = 0.5+0.5·sin(2π(phase+offset)) at boundaries; heart-phase accumulation wraps [0,1) across steps and equals breathPhase-passthrough when source=.breath; nil frame HOLDS last output (BioSpaceMap semantics, not snap-to-zero); NaN/inf frame fields sanitized; FLASH-SAFETY: with a 180 bpm frame stream the per-fixture dimmer delta per tick never exceeds FlashGuard.limitedLuminance's cap and the effective advance rate is clamped by FlashGuard.safeFrequency(≤3 Hz); determinism: identical (frames,dt) sequence → identical output arrays (no RNG, no Date()). Implement BioPhaser.swift to GREEN. Commit feat: L3 EchoelLux bio-phaser (pure model, flash-safe).
  4. VERIFY: swift test --filter LightFixtureGroup and --filter BioPhaser green; whole suite green; confirm Sync/ still has zero new external deps and Release path unchanged (senders not yet calling the new models = bit-identical output).
  5. DEFERRED CYCLE 3 (optional, reversible, only on founder/Council ask): wire an OPT-IN group+phaser mode into ArtNetSender/SACNSender.sendIfFresh (default off = current single-fixture behaviour); if adding Licht UI in PatchbayView use EchoelValueField (no raw Slider) and put any live phaser readout in its own leaf View (never read live bio/phaser output in PatchbayView.body or an ancestor of a .menu). Do NOT grow the EchoelStudioView .sheet chain. Placebo rule: the control must move real DMX, not a dummy. Device-verify against a real Art-Net/sACN rig.
- **Tests:**
  - LightFixtureGroupTests: compose places fixtures at start channels; buffer clamped ≤512; grandMaster scales all dimmers; blackout zeroes all dimmers + preserves colour; empty group safe; out-of-range/overlapping addresses clamp; Codable round-trip; legacy JSON (missing new key) → default via decodeIfPresent (byte-identical); deterministic same-in-same-out
  - BioPhaserTests: phaseOffsets even fan (count 0/1 edges); value = 0.5+0.5sin at offsets; heart phase wraps [0,1) over dt accumulation; breath source = frame.breathPhase passthrough; nil frame holds last; NaN/inf sanitized; FLASH-SAFETY per-tick luminance delta capped (FlashGuard.limitedLuminance) and effective Hz ≤3 (FlashGuard.safeFrequency) even at 180 bpm; deterministic across a fixed (frames,dt) sequence
  - Regression: full swift test suite green; ArtNetSenderTests/SACNSenderTests/BioSpaceMapTests/FlashGuardTests unchanged (senders un-wired ⇒ Release bit-identical)
- **Risks:**
  - Audio-thread laws are N/A but MUST be stated: these models run on the 30 Hz main-actor PollingLoop, NOT the audio render thread; step() allocates its output array (fine off-thread) — it must never be called from a DSP/render block
  - Heart phase does not exist in BioSampleFrame — it is DERIVED by integrating heartRateBPM/60·dt; a paused/stale bio stream must hold (nil-frame-holds-last) not reset, matching BioSpaceMap, or lights would jump
  - Flash-safety is the real hazard: unlike the current single-fixture slow-bio fade, an MA3 fan across many fixtures with a fast heart base could visually beat >3 Hz across the rig — the ≤3 Hz clamp + per-fixture slew must be proven by test, not assumed
  - Keep L2/L3 free of ArtNetSender.DMXResolution so they need no #if canImport(Network); resolution encoding stays in the sender (avoids coupling the pure model to a Network-guarded type)
  - Scope creep: do not auto-wire the senders/UI — un-wired ship keeps Release bit-identical and is the reversible minimal change; wiring is a Council-gated follow-on

---

## CORRECTION²  (re-verified 2026-07-13 — the first CORRECTION was itself WRONG)

**The 2026-07-12 note below claimed the fix belongs in `trapMelody`. That is FALSE.**
Ground truth, re-read at BioComposer.swift:411-428 and 420-421:
- **`trapMelody` is RETIRED** — "dubMelody/trapMelody remain defined below (unused,
  reversible)" (line 420-421). Trap no longer touches it.
- **Trap routes through `composeHarmonic` → `appendBass`** with `sustained: true`
  (the case `.dubTechno, .trap` at line 412-428 calls `composeHarmonic`; the section
  loop at line 1070-1087 calls `appendBass`). So the ORIGINAL brief (appendBass) was
  right; the b99c672 "trapMelody" correction was a misread and is reverted here.
- For trap's 2-chord profile `[0, 5]` → `composeHarmonic` builds 2 sections of len 8
  → `appendBass` (guard `!sustained`) emits ONE held root per section = bass at {0, 8}.
  Beats 2 (step 4) and 4 (step 12) carry no bass — the founder-diagnosed quarter gap.
- **Actual build (test-first, dsp-reviewer mandatory):**
  1. `static func heartbeatActive(energy:secLen:) -> Bool { secLen >= 4 && energy >= 0.5 }`;
     refactor `heartbeatOnsets` guard (line 963) to call it (pure, behaviour unchanged).
  2. `static let quarterAnchorBass = true` near `dynamicDepth` (line 260).
  3. Thread `quarterAnchor: Bool = false` through `composeHarmonic` → `appendBass`;
     set it `input.style == .trap` at the compose call site (line 422) — TRAP ONLY
     (dub/meditation stay false, held Flächen unchanged).
  4. In `appendBass`, BEFORE the existing guard: when `quarterAnchor && sustained &&
     Self.quarterAnchorBass && Self.heartbeatActive(energy: busy, secLen: len)`, emit a
     root PEDAL on the quarter grid (gap 4, all `rootDegree` = one pitch class, downbeat
     `s % 8 == 0` full velocity, off-quarters ×0.7) instead of one held root; else fall
     through UNCHANGED (calm trap + every other genre → RNG-identical, bit-identical).
  5. Scoping to trap keeps `testSustainedFlächeIsStillWhenCalm_AliveWhenAroused`
     (meditation, asserts aroused bass count == 1) GREEN.
  6. Tests via `compose(...)`: trap aroused (hr 130, coh 0.05) → `.bass` startSteps ⊇
     {0,4,8,12}; trap calm (hr 52, coh 0.95) → exactly {0,8} held len 8; determinism.
- **Taste caveat (device ear-verify):** full-emphasis quarter pedal vs downbeat-only is
  the founder's aesthetic call; `quarterAnchorBass = false` reverts in one line.

---

## CORRECTION (ground-truth check before build, 2026-07-12) — SUPERSEDED, KEPT FOR TRAIL

> ⚠️ This section is WRONG (it names `trapMelody`, which is retired). See CORRECTION²
> above. Retained only to show the misread was caught and corrected, not silently.

**B8-offbeat brief has a target error — verified against the real code:**
- **Trap does NOT use `appendBass`.** It uses `trapMelody` (BioComposer.swift:786),
  which emits its OWN 808 bass on steps **[0, 8]** (len 6, octave 2, line 793-796).
  `appendBass` (line 901) serves the *sustained* house/ambient profiles only.
- So the founder-diagnosed **Trap 132** offbeat feel must be fixed in **`trapMelody`**,
  not `appendBass`.
