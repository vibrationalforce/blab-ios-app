build: 2026-07-01 v10.77.5 (DAW reorg — Mix page + smoother visual pulse)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notesT: DAW-look reorg begins. NEW "Mix" page: the Channel Rack (per-track mute/solo/level/
  FX) is now a first-class workspace tab, so the bottom bar reads Arrange · Clips · Compose ·
  Mix — a familiar DAW layout, with biofeedback becoming one element rather than the whole
  app. (More cycles coming: a persistent transport bar, a Bio source lane, a browser, and a
  video page.)
  Also: the immersive visual PULSE is now smoothed — on a weak-signal pulse reading the HR
  could bounce and stutter the on-screen pulse; it now slew-limits + glides slowly so the
  picture breathes steadily. The measured heart rate stays honest (the pulse detector is
  unchanged); only the visual is smoothed.
  Everything from 10.77.4 stands (brainwave entrainment audio+visual; Oscilloscope + Fractal
  looks; Aurora/Lissajous/Depth; unified Tools; route hardening; projection hint).
  Test 10.77.5: tap the new "Mix" tab → mute/solo/level per track; open the fullscreen visual
  and confirm the pulse breathes smoothly (no stutter) even when the finger signal is weak.
