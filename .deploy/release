build: 2026-07-11 v10.79.170 (Adaptives Hackbrett — Querformat/iPad 2-spaltig, Reorg P4)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notes: Erster Schritt "adaptives Design horizontal + vertikal" (dein Auftrag).

  NEU: das Hackbrett (Channel Rack im Mix) passt sich der Ausrichtung an:
  · Querformat / iPad: die Channel-Strips liegen jetzt in 2 Spalten — nutzt
    die Breite statt einer sehr langen Einzelspalte.
  · Hochformat (Handy): unverändert eine Spalte → kein Risiko für den
    Normalfall.

  Layout-only, render-safe (Size-Class-Reads passieren nur bei Drehung, nie
  pro Frame; auf einen eigenen Leaf-View begrenzt → kein Ancestor-Churn).
  ui-state-Review: 0 Risiken. Beide CI-Gates grün.

  Reorg läuft weiter: als Nächstes die Master-Stimmen (Bass/Pad/Lead) als
  Strips aufs selbe Hackbrett, dann das Wetter-Panel (getrennte Klang+Bild-
  Parameter), dann adaptives Layout auch für Timeline + Panels.

  NEEDS-FOUNDER-VERIFY (Feel im Querformat / auf dem iPad).

  Zum Antesten warten: v167 (Groove „Eins"), v168 (Hackbrett), v169 (Timeline
  spielt — „Play timeline"), + v162–v166 (Warmth/Tempo/FX).
