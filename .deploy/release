build: 2026-07-03 v10.79.36 (Bar-Cycling — Komposition stellt sich je nach Loop-Größe um)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notesT: Der hörbare Teil deines Loop-Wunsches (nach der optischen Anzeige in 79.35): die
  Komposition spielt jetzt WIRKLICH so viele verschiedene Takte, wie die Loop-Größe sagt —
  nicht mehr denselben Takt N-mal.
  • generate() baut `loopBars` verschiedene, aber ZUSAMMENGEHÖRIGE Takte (gleicher Struktur-
    Seed → sie klingen wie EIN Stück; pro-Takt-Detail-Seed → jeder Takt ist anders). Die
    Piano-Roll zykliert einen Takt pro Loop und tauscht sie NAHTLOS am bestehenden Takt-
    Übergang (kein Schnitt, keine neue Uhr, kein stepCount-Umbau).
  • Audio bleibt IN SYNC mit der „Takt N/M"-Anzeige (bewiesen: bei Transport-Takt k klingt
    Takt k mod N). Live-Neugenerierung, Stop→Play und Loop-Größe-Wechsel während des Spielens
    halten die Phase (adversariale Logikprüfung: 1 Bug gefunden + gefixt — Phasen-Reset in den
    onStop-Hook verschoben; Loop-Größe-Picker regeneriert jetzt beim Spielen).
  • 1 Takt / leer = klassisches Verhalten (keine Regression). Drums bleiben im Generativ-Pfad
    stumm (unverändert).
  Compile-Check grün (5bdf18b). Das LOOP-TIMING kann nur dein Gerät final bestätigen.
  TEST 10.79.36: Generate starten → hörst du über die Loop-Länge mehrere verschiedene Takte,
  die sich zu einem Stück fügen und sauber loopen? Stimmt die „Takt N/M"-Anzeige mit dem
  gehörten Takt überein? Loop-Größe umstellen (2/4/8…) → ändert sich Länge + Inhalt sauber?
