build: 2026-07-15 v10.79.234 (Ein Play-Knopf: der Transport ▶ spielt jetzt die Arrangement-Timeline)
branch: claude/piano-roll-clip-view-wozlie-5kxnrl (tokenless deploy via .deploy/release bump)
notes: Abschluss aus deinem Video: "zu viele Play-Knöpfe — einer reicht."

  Bisher gab es DREI: Transport ▶ (spielte nur das Live-Pattern), den Puls-Knopf
  (Bio — schon in v233 auf den Puls-Pill gewandert) und "Play timeline" in der
  Timeline-Leiste (spielte die platzierten Clips/Regionen).

  JETZT nur noch EINER: der Transport ▶ oben.
  • Sind Regionen auf der Timeline arrangiert → er spielt das ARRANGEMENT (die
    platzierten Clips werden der Reihe nach abgespielt, wie es "Play timeline" tat).
  • Ist nichts arrangiert → er spielt den Live-Loop wie gehabt.
  • Stop stoppt sauber, was gerade läuft (Arrangement löst seine Regions-Clips + die
    Nebenspuren-Stimmen; der reine Loop stoppt einfach).
  Der separate "Play timeline"-Knopf in der Timeline-Leiste ist WEG.

  Technisch sauber: der Transport-Schritt wird ohnehin schon immer in die Timeline
  eingespeist (nur ein No-Op, solange nichts arrangiert läuft) — der ▶ engagiert also
  dieselbe, bereits getestete Abspiel-Maschine. Es ist EIN Player-Objekt, kein
  Doppelbau.

  SICHER: ui-state-reviewer CONFIRMED — die Timeline-Reads liegen NUR im Tap-Handler
  des Knopfs (nicht im body → Freeze-Regel gewahrt), Signaturen/Typen passen, es ist
  garantiert dieselbe TimelineRegionPlayer-Instanz wie im Schritt-Relay (sonst spielte
  nichts), keine hängenden Referenzen. Gates v232+v233 beide grün.

  DAMIT ist dein Video komplett: v232 Playhead-Griff größer · v233 Bio auf den Pill +
  ein Play-Knopf weniger · v234 der letzte doppelte Play weg. Aus drei Play-Knöpfen ist
  EINER geworden.

  Reihenfolge: v232 Playhead-Griff größer · v233 Bio auf den Pill · v234 ein Play-Knopf.
