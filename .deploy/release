build: 2026-06-21 v10.34.22 (Music drives the visuals — the DMMW differentiator goes live)
branch: claude/piano-roll-clip-view-wozlie
includes: The instrument now SHAPES the visual by what you play. The notes sounding right now are published as a live musical snapshot, mapped to colour the perceptually-honest way (OKLab hue circle, amplitude-weighted chord mix): a "Music → colour" readout in Compose ▸ Visual, and the immersive full-screen visual now colours from the loudest live note so it tracks the melody (not a fixed tonic). This builds on the new workstation home: it opens on the Arrangement/Clips timeline (Arrange · Clips · Compose surface switcher), the bio-compose instrument is one "Compose" tool, and clips are typed (Audio · MIDI · Video · Visual) with each cell showing its kind — non-playable lanes read honestly ("engine coming") until their engines ship. Old clip libraries load unchanged. Switching surfaces never interrupts the live audio. Verified: Xcode Compile Check + CI green.
