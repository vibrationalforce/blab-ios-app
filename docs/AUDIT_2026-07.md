# AUDIT 2026-07 — Great Cleanup & Restructure · Phase 0 Inventory

> **Status: DRAFT — read-only inventory. No files changed. Awaiting owner approval before any demolition (Phase 1).**
> Method: sandbox has no Swift toolchain — every structural change lands green only via CI + device. This audit is measurement, not surgery.
> Branch: `claude/piano-roll-clip-view-wozlie` · Deploy string: v10.79.7

---

## 0. Executive summary

The **code** is in good shape and largely clean of legacy debt. The **documentation, config, and repo periphery** are where the sprawl lives. The single biggest source of confusion is that **CLAUDE.md and the CLEANUP prompt describe a repo that no longer exists** — the "four pillars" (EchoelTools / EchoelWorks / EchoelSync / EchoelWell) were never built as modules, and the file counts are stale.

**Recommended demolition order (cheapest → highest value, one per cycle):**
1. Consolidate AI config → `.claude/` (delete `.agents/`, `.ai/`; kill the duplicated `remotion-best-practices` skill).
2. Reconcile the vision/reality drift in `CLAUDE.md` (stale counts, dead "4 pillars" model, stale Views/ deprecation list).
3. Prune the branch graveyard (`deploy-dryrun`, `dsym-probe`).
4. Eindampfen `scratchpads/` (59 files, ~40 `PLAN_*`) → an archive + a live index.
5. Relocate misplaced non-view logic out of `Studio/` (owner-run file moves, no `.pbxproj` edits by me).

**Net-line expectation:** strongly negative — most of the win is deleting duplicated config and archiving superseded plans, not touching `Sources/`.

---

## 1. Vision-vs-reality drift (⚠️ MUST reconcile, do not silently paper over)

The CLEANUP prompt references **CLAUDE.md v7.0** and **four pillars: EchoelTools / EchoelWorks / EchoelSync / EchoelWell + EngineBus**. The as-built repo is different:

| Prompt / old-vision claim | Reality on disk | Verdict |
|---|---|---|
| 4 pillars EchoelTools/Works/Sync/Well | **None exist as modules.** Actual dirs: `Audio Bio Core DSP Resources Sequencer Stream Studio Sync Tools Video Views`. Only `Tools/` and `Sync/` even share a name; `Works`/`Well` never existed. | **DEAD MODEL** — the pillar language is aspirational vocabulary, not architecture. Flag in CLAUDE.md; do not build the pillars to match a doc. |
| EngineBus as the coupling spine | **REAL** — `Core/EngineBus.swift` present, 34 files reference it. | ✅ Keep as the one true integration contract. |
| CLAUDE.md: "~133 Swift (Sources)" | **212 Swift files** in `Sources/` + 132 in `Tests/`. | Stale count. |
| CLAUDE.md: "133 Swift + 2 Metal" | **1 Metal file** (`Video/Shaders/ChromaKey.metal`). `MetalBioView` compiles its shader inline at runtime (not a `.metal` file). | Stale count. |
| CLAUDE.md Views/ = long DEPRECATED list (MomentCaptureView, MasterView, SoundscapeView, SessionGridView, …) | `Views/` now holds **only** `MetalBioView.swift` + `OnboardingView.swift`. The deprecated files are **already gone**. | Stale section — the audit already happened, the doc didn't catch up. |
| CLAUDE.md: "Studio surfaces = Arrange · Clips · Compose · Mix · Bio · Browse (6)" | Matches `WorkspaceView` (verified real). | ✅ Current. |

**Action:** the very first documentation cycle must rewrite the stale spans of CLAUDE.md (counts, pillar model, Views/ list). This is the "vision layer" the prompt worries about — but the fix is to make the doc match the good code, **not** to restructure the good code to match a dead doc.

---

## 2. Directory tree (depth 3, condensed)

```
Echoelmusic/
├── Sources/
│   ├── Echoelmusic/            212 .swift
│   │   ├── Studio/    56   ← LARGEST; mixes real views + misplaced logic (see §6)
│   │   ├── Core/      39   ← EngineBus, stores, ModulationEngine, PollingLoop
│   │   ├── DSP/       30   ← EchoelDDSP, Cellular, ModalBank, VDSPKit
│   │   ├── Sequencer/ 26   ← PatternEngine, BioComposer, Note, Clip, BeatPlayer
│   │   ├── Bio/       18   ← PROTECTED triad + publishers (HealthKit/rPPG/Polar)
│   │   ├── Audio/     13   ← AudioEngine, mixing, export
│   │   ├── Sync/      10   ← OSC, ADM-OSC, Art-Net, sACN, MIDI bus
│   │   ├── Video/      6   ← CameraCapture/Analyzer + 1 .metal
│   │   ├── Tools/      4   ← PolySynthVoice, BioReactiveSynthVoice, FXBioModulator
│   │   ├── Views/      2   ← MetalBioView, OnboardingView (was ~14, now trimmed)
│   │   ├── Stream/     1   ← BroadcastPublisher scaffold (HaishinKit not integrated)
│   │   └── Resources/  1
│   ├── EchoelmusicAUv3/     2   ← plugin (compiles DSP/ only)
│   ├── EchoelmusicWidgets/  1   ← thin
│   └── EchoelmusicWatch/    1   ← thin
├── Tests/                   132 .swift
├── docs/                    54 files (GitHub Pages site + docs/dev/*.md)
├── scratchpads/             59 files (SESSION_LOG + ~40 PLAN_* + audits)
├── memory/                  durable knowledge (decisions/vision/user/…)
├── .claude/  .agents/  .ai/  ← AI config — 3 dirs, overlapping (see §3)
├── .github/workflows/       14 workflows
├── 5 root .md + 7 root .sh + scripts/(9) + ci_scripts/(3) + fastlane/
├── decisions.csv (98 KB) · inspiration.csv (27 KB) · metrics.jsonl
```

---

## 3. AI-config chaos (highest-value, lowest-risk demolition)

Three overlapping config roots:

| Dir | Contents | Verdict |
|---|---|---|
| `.claude/` | skills (14), agents (11), rules, routines, commands, settings — **the canonical, actively-used root** | ✅ KEEP as sole root |
| `.agents/` | only `skills/remotion-best-practices/**` | ❌ **DUPLICATE** — same skill exists at `.claude/skills/remotion-best-practices`. Delete `.agents/`. |
| `.ai/` | only `WORKING_METHOD.md` (sandbox↔user collaboration table, dated "ship v10 by 2026-05-17" — past) | ⚠️ MERGE the still-true "no local toolchain / CI-only" reality into CLAUDE.md or a `.claude/` note, then delete `.ai/`. |

**Duplicated skill confirmed:** `remotion-best-practices` lives in BOTH `.agents/skills/` and `.claude/skills/`. One must go.

`.claude/skills/marketing/` is a large vendored MIT pack (Corey Haines, 45 skills) fronted by `echoel-marketing` — intentional, PIPELINE-only, keep.

---

## 4. Legacy / esoteric term sweep

**`Sources/` is CLEAN** — zero hits for BLAB / Vibrational Force / Echoela / EchoelMusic(wrong-case) / healing-frequency / chakra / Solfeggio in shipping code.

All repo-wide hits are **negative/guard context** (rules, review checklists, and vision docs saying *"never use these"*) — those are correct and must stay. Two historical-record mentions only:
- `scratchpads/SALVAGE_INVENTORY_REBUILD.md:3` — "formerly 'blab' — renamed, history preserved" (historical note, harmless).
- `scratchpads/DEEP_AUDIT_2026-06-03.md:13` — evolution timeline mentioning BLAB (historical, harmless).

**Verdict:** no legacy-term purge needed in code. Do NOT "clean" the guard-rules that mention the banned words — deleting them would remove the guardrails.

---

## 5. Branch graveyard

Remote branches:

| Branch | Verdict |
|---|---|
| `main` | ✅ default |
| `claude/piano-roll-clip-view-wozlie` | ✅ current dev branch |
| `deploy` | ⚠️ deploy mechanism branch — confirm still wired before touching |
| `deploy-dryrun` | ❌ likely stale — candidate for deletion (owner confirm) |
| `dsym-probe` | ❌ likely stale one-off probe — candidate for deletion (owner confirm) |

**Action:** owner-confirm + delete `deploy-dryrun`, `dsym-probe`. Leave `deploy` until its role in `testflight.yml` is re-verified.

---

## 6. Module reality vs. misplacement

`Studio/` (56 files) is doing too much — it holds genuine views **and** non-view logic that belongs in `Core/`, `DSP/`, or `Bio/`. Misplaced (non-`*View`) files currently under `Studio/`:

- **Bio/DSP logic:** `BioModulation.swift`, `BioVisualParams.swift`, `VocoderCore.swift`, `FeedbackGuard.swift`, `VoiceAnalyzer.swift`, `SpectrumAnalysis.swift`, `LoudnessTarget.swift`, `SpectralColor.swift`
- **Core/services:** `LearnLibrary.swift`, `FlashGuard.swift`, `LoudnessTarget.swift`, `MasterLoudnessGrid.swift`, `BioHaptics.swift`, `HapticController.swift`, `HapticEngine.swift`
- **Design system (legit shared UI, could form an `EchoelUI/` group):** `EchoelTheme.swift`, `EchoelValueField.swift`, `EchoelNumberPad.swift`, `EchoelPanel.swift`, `EchoelSheetPanel.swift`, `EchoelLogoMark.swift`

> ⚠️ **File moves = owner-run only.** I will NOT edit `.pbxproj`/`project.yml` target membership. Any relocation is delivered as an explicit move-list for you to apply (SwiftPM auto-globs `Sources/`, but the Xcode project + AUv3 target membership must be updated by you). This is a Phase 2 candidate, **not** urgent — nothing here is broken, it's just organization.

**"Built but app-unwired" cores confirmed present** (foundations, keep): `BioModulation`, `VocoderCore`, `LearnLibrary`, `CloudSync` (in `Core/`), `FeedbackGuard`, `BioVisualParams`. These are tested foundations, not dead code — do not delete.

**Confirmed already-removed** (CLAUDE.md is accurate here): `SoundscapeEngine`, `ClipEngine`, `WeatherProvider` — absent. ✅

---

## 7. Docs & scratchpads sprawl

- **`scratchpads/` = 59 files**, including **~40 `PLAN_*`/`STRATEGY_*`/`RESEARCH_*`/`AUDIT_*`**. Many superseded (e.g. `PLAN_DAW_REORG`, `PLAN_FOUNDATION_*`, `SALVAGE_INVENTORY*`, `DEEP_AUDIT_2026-06-03`). `SESSION_LOG.md` is the live index.
  - **Action (Phase 1):** create `scratchpads/archive/` and move superseded plans there; keep a short `PLAN_INDEX` of what's live. Do not delete history — archive it.
- **`docs/` = 54 files** — a real GitHub Pages site (`*.html`) + `docs/dev/*.md`. The `.html` site is a product surface, **leave it**. `docs/dev/` has some overlap (ROADMAP vs ARCHITECTURE_NORTH_STAR vs DMMW_ARCHITECTURE) — low priority.
- **Root `.md`:** CLAUDE.md, README, TODO, APPLE_INTEGRATION_NOTES, THIRD_PARTY_NOTICES — all legitimate, keep.

---

## 8. Scripts & data files

- **7 root `.sh`** (`build test debug deploy review check-decisions run_integration_tests`) + **`scripts/` (9)** + **`ci_scripts/` (3)**. Some root scripts may be stale given the CI-only reality (no local Swift). **Action (later cycle):** audit which root `.sh` are still invoked; the toolchain-dependent ones (`build.sh`, `test.sh`, `debug.sh`) are likely dead in the sandbox but may be used on the owner's Mac — confirm before removing.
- `decisions.csv` = **98 KB** and `inspiration.csv` = 27 KB — growing append-only logs. Not a cleanup target (durable memory), just noting size.

---

## 9. Targets & schemes (project.yml)

4 targets / 4 schemes: `Echoelmusic` (main), `EchoelmusicAUv3`, `EchoelmusicWidgets`, `EchoelmusicWatch`. Widgets + Watch are **1 Swift file each** — thin but real. Deferred bundles (`.clip`, `.notification-service`) are not present as targets. **No target changes proposed** — target/scheme structure is sound.

---

## 10. UNANTASTBAR — rules honored by this audit

1. **Protected DSP READ-ONLY** — `BioEventGraph`, `HilbertSensorMapper`, `BioSignalDeconvolver` inventoried in `Bio/`, never to be moved/renamed/touched.
2. **No `.pbxproj`/`project.yml` target edits by me** — file moves ship as owner instruction lists.
3. **Science-only language** — verified clean in code (§4).
4. **Tests green / build after each change / red = revert.**
5. **ONE structure step per cycle** (Ralph Wiggum) — no big-bang.
6. **No JUCE, no new deps, no force-unwraps in new code.**

---

## 11. Proposed Phase 1 sequence (one cycle each — awaiting approval)

| # | Cycle | Files touched | Risk | Net lines |
|---|---|---|---|---|
| 1 | Delete `.agents/` + dedup remotion skill | config only | none | −− |
| 2 | Merge `.ai/WORKING_METHOD.md` truth into `.claude/`, delete `.ai/` | 1 merge, 1 delete | none | − |
| 3 | Reconcile CLAUDE.md drift (§1: counts, pillars, Views/ list) | CLAUDE.md | none | ± |
| 4 | Archive superseded `scratchpads/PLAN_*` → `scratchpads/archive/` + index | docs only | none | ± (moves) |
| 5 | Prune stale remote branches (owner-confirm) | git only | low | n/a |
| — | (Phase 2, later) `Studio/` relocation move-list for owner | owner-run | medium | 0 (moves) |

**STOP.** No demolition begins until you approve this plan. Reply with which cycles to run (all / a subset / reorder), and I start with #1.
