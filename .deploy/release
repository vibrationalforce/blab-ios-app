build: 2026-07-01 v10.77.4 (Visual — Oscilloscope + Fractal looks, blendable)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notesT: NEW visual looks — Oscilloscope and Fractal, and you can MIX them.
  Open the fullscreen visual → Look → pick "Scope" (a glowing oscilloscope beam that traces
  a waveform driven by the sounding pitch) or "Fractal" (ridged fractal noise that drifts and
  breathes). Then use "Blend with" + Mix to overlay the two — an oscilloscope over a fractal
  field (exactly the "mix of oscilloscope and fractals" idea). Both animate slowly and stay
  under the 3 Hz flash-safety limit; colour still follows the heard tone.
  Reviewed for Metal shader correctness (clean). Everything from 10.77.3 stands (biofeedback-
  driven brainwave entrainment, audio + visual), plus 10.77.2 (Aurora/Lissajous/Depth looks,
  unified Tools menu, audio route hardening, AirPlay projection hint).
  Test 10.77.4: fullscreen visual → Look → Scope, then Fractal; then Blend Scope with Fractal
  and move Mix — confirm the oscilloscope beam rides over the fractal texture and neither
  flashes fast.
