build: v10.79.343 — Korrektheits-Batch (VideoMuxer-Sync · HealthKit-SDNN · Temp-Leak · AUv3 fullState + note-priority)
branch: claude/echoelmusic-neustart-auv3-6ri2ek (tokenless deploy via .deploy/release bump)
notes: |
  Superset von v342. Bündelt die fünf grünen Korrektheits-/Härtungs-Commits, die die
  Audit-MEDIUMs (#94–#99) vollständig abschließen. KEINE neue Oberfläche — die
  Kamera-Home-Instrument-Erfahrung ist unverändert. Alle Commits einzeln reviewer-CLEAN,
  beide echten Gates grün (Xcode Compile Check + CI/CD Pipeline auf c58e82c).

  NEU ggü. v342:
  - #96 VideoMuxer END-Alignment: Video + Audio enden beide bei Stop, wurden aber ab t=0
    gelegt → bei Aufnahmen länger als der Audio-Ring lief Video-KOPF über Audio-SCHWANZ
    (Desync) + Material hinter dem Audio-Fenster verworfen. Fix: die letzten min(v,a) s
    jedes Tracks (reiner VideoMuxAlignment-Helper, 6 Tests), exakte CMTime-Arithmetik.
  - #98c1 HealthKit-HRV ehrlich via SDNN: die App fabrizierte „RMSSD" aus gemittelter
    HR (physiologisch bedeutungslos, ruhige HR → falsche „tote HRV") und überschrieb das
    echte SDNN. Fabrikation entfernt; SDNN treibt jetzt hrvNormalized. Nur Watch/HealthKit-
    Pfad, nicht die Default-Kamera.
  - VisualRecorder .caf-Leak: das Temp-Mix-Audio wurde nach dem Mux nie gelöscht (Video-
    only + Mux-Fehler + Video-nil-Pfade). defer-Cleanup über alle Exit-Pfade.
  - #99a AUv3 fullState-Roundtrip: der Value-Provider las Param-Werte aus dem (bio-
    modulierten) Synth zurück → Preset/State roundtrippte nie. Provider gelöscht → Params
    fallen auf ihren Control-Wert zurück; Host-Readback + Save/Restore korrekt.
  - #99b AUv3 mono last-note-priority: Note-Off löste die Stimme unbedingt → Loslassen
    einer gehaltenen früheren Taste killte die aktuelle Note. Jetzt note-gated; MIDI-Panic
    (CC 120/123) über expliziten .panic-Case unbedingt force-release (kein #91-Regress).

  Geräte-Verify offen (Watch als Bio-Quelle für #98c1; AUv3-Discovery weiter device-
  gated per Reboot+Registrar-Warm-Test). Rausch-Triad unberührt, kein Heilungs-Claim.
