build: 2026-07-12 v10.79.174 (Puls-Knopf neben Play · Pulsmonitor im Header · Monitor in echten Visual-Farben · Visual-Diagnose)
branch: claude/piano-roll-clip-view-wozlie (deployed via deploy branch)
notes: Deine drei Screenshot-Anweisungen:
  · „Create from Within" ist weg — der Puls-Knopf (⌁) sitzt oben im Transport
    NEBEN Play und startet/stoppt das Instrument (grüner Ring = Körper live).
  · Der Pulsmonitor (Live-Kurve + BPM) sitzt oben zwischen Logo und Titel.
  · Die Monitor-Kachel rechts zeigt die ECHTEN Visual-Farben: sie färbt sich
    mit dem klingenden Akkord (dieselbe physikalische Ton→Farbe wie der
    Shader), schwillt mit dem Musik-Level, pulsiert mit dem Herzschlag.
  · Die Erklär-Zeile ist raus (kommt später als richtiges EchoelAI).

  PLUS: das Visual schreibt jetzt alle 5 s eine Diagnose-Zeile ins Log
  („visual: bio= mfNotes= level= …"). BITTE: kurz mit laufendem Visual
  spielen und das Log schicken — die Zeilen zeigen exakt, warum das Visual
  grau/statisch wurde.

  ui-state-Review: konform, 0 kritisch (+2 Härtungen). Alle Gates grün auf
  809d621. NEEDS-FOUNDER-VERIFY (Header-Layout, Puls-Knopf-Flow, Kachel).

  In Arbeit (mehrstündige DMMW-Sitzung, Meldung wenn fertig): die
  Produktions-Shell — Menüleiste + Dropdowns statt Karten-Stapel, Buttons
  auf den Spuren, Kammerton-reaktive Notenraster-Farben, Video-Fenster.
