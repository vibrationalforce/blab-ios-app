build: 2026-06-24 v10.76.19 (bisect step 1 — restore colours + camera diag on the launching 10.76.9 base)
branch: claude/piano-roll-clip-view-wozlie
notesT: 10.76.18 (exact 10.76.9 code) LAUNCHED — so the launch break is in the code added
  after 10.76.9, not the build/install. Now re-adding it in safe groups with a launch check
  each step (lambda). This step re-adds the SAFE, non-launch-path group on top of the
  launching base: the MetalBioView colour-cloud shaders (the natural warm "verschiedene
  Farbwolken" you wanted) + the camera rPPG diagnostic logging. None of these touch the
  launch path (shader compiles lazily with a fallback; camera logging runs only while
  measuring), so this should still launch — and you get the colours back.
  Next step isolates the actual regression to the Bio→Visual routing feature.
  Test: still launches? And open the immersive Visual (Well) — the colour clouds should be
  back. Tell me launches/black.
